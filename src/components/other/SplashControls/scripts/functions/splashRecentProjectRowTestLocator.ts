/**
 * Stable data-test-locator for a row in the splash recent-projects menu.
 */
export function splashRecentProjectRowTestLocator (index: number): string {
  return `splashPage-recentProject-${String(index)}`
}
