/**
 * Minimal Pinia markdown-document store shape for open-routing (documentToOpen / dialogUUID).
 */
export interface I_dialogMarkdownStoreLike {
  dialogUUID?: unknown
  documentToOpen?: unknown
}
