import type { ComputedRef, Ref } from 'vue'

/**
 * Vue Ref / ComputedRef aliases for level-1 functions/ factories.
 * Re-export real Vue types so composable returns match SFC script and template usage.
 */
export type { ComputedRef, Ref }
