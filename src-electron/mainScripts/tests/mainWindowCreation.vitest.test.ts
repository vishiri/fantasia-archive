import { test } from 'vitest'

/**
 * preventSecondaryAppInstance
 * We cannot test this function as it requires Electron to be running.
 */
test.skip('Test that app window does not start if another instance is already running')

/**
 * mainWindowCreation
 * We cannot test this function as it requires Electron to be running.
 */
test.skip('Test that the main window is created successfully')
