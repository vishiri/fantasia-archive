/**
 * Default delay (ms) component and E2E Playwright specs wait after launch before assertions.
 * Main-process extra-env snapshot exposes the same value to the renderer.
 */
export const FA_FRONTEND_RENDER_TIMER_MS = 3000
