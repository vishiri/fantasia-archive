# Registro delle modifiche
----------

## 2.4.14 - Versione bump

### Nuove funzionalità
- **Localizzazione**: gli errori di salvataggio di Action Manager, i fallback degli errori di apertura/creazione del progetto e gli avvisi di errore di **Importa/Esporta configurazione app** ora utilizzano file locali in ogni lingua supportata. **Impostazioni archivio Fantasia** **Registra il payload completo dell'attività** il testo della guida corrisponde all'inglese in tutte le lingue.
- **Impostazioni progetto**: **Salva senza chiudere** salva come **Salva impostazioni** ma lascia aperto il dialogo.

### Correzioni di bug e ottimizzazioni
- **Impostazioni progetto**: **Elimina mondo** nella riga dettaglio **Mondi** si allinea verticalmente con **Nome mondo** e **Colore mondo**.
- **Selettore lingua**: **Svenska** mostra ora la bandiera della Svezia invece di quella di El Salvador.

## 2.4.13 - Versione bump

### Nuove funzionalità
- **Lingue dell'interfaccia**: il selettore della lingua elenca **16** lingue dell'interfaccia (**English, US**, **Deutsch**, **Français**, **Italiano**, **Svenska**, **Suomi**, **Norsk**, **Português**, **Español**, **Ελληνικά**, **Українська**, **العربية**, **中文**, **日本語**, **हिन्दी** e **Русский**). Le etichette dei menu contrassegnano le impostazioni locali assistite dall'intelligenza artificiale con un suffisso localizzato **AI** (le lettere **AI** rimangono latine in ogni etichetta).
- **Traduzioni**: ogni lingua del selettore fornisce stringhe dell'interfaccia utente complete per menu, finestre di dialogo, finestre mobili, notifiche e markdown della guida in-app (**Guida alla ricerca avanzata**, **Cheat sheet per la ricerca avanzata**, **Suggerimenti, trucchi e curiosità** e il **Registro delle modifiche**), generati dal catalogo **Inglese, USA** corretto.
- **Layout arabo**: scegliendo **العربية** si applica il layout da destra a sinistra nell'app; il menu della lingua rimane da sinistra a destra con la direzione di lettura per riga in modo che gli endonimi vengano visualizzati in modo naturale.- I **programmi di installazione desktop** registrano il tipo **`.faproject`** per **Apri con** sui pacchetti **Windows**, **macOS** e **Linux**; facendo doppio clic su un file di progetto o scegliendo **Fantasia Archive** nel selettore di sistema si apre **`.faproject`** come sessione attiva e si aggiorna **Carica progetto recente** quando l'apertura ha esito positivo (lo sviluppo **`yarn quasar:dev:electron`** salta le aperture automatiche a meno che non si imposti **`FA_DEV_OPEN_FAPROJECT=1`**).
- **Schermata di benvenuto** e **Gestione progetto**: le azioni di benvenuto splash e le righe corrispondenti di **Gestione progetto** utilizzano **Crea nuovo progetto** e **Carica progetto esistente** (**en-US**, **Deutsch**, **Français**).- **Schermata di benvenuto**: quando **Project Management** ha un elenco **Carica progetto recente** salvato, **Splash** mostra un controllo di ripresa diviso: il segmento principale riporta **Riprendi ultimo progetto** quando nessun **`.faproject`** è attivo (apre il file più recente più recente) o **Riprendi progetto corrente** quando una sessione è già caricata (torna a quel file senza un avviso già attivo). La freccia elenca le stesse righe del progetto recente del menu (nome più percorso) ed espone **Sfoglia gli ultimi progetti** come nome accessibile e una descrizione comando **Quasar** alla sua destra. Senza recenti salvati, quel controllo rimane nascosto.
- **Impostazioni app**: **Nascondi suggerimento "Sfoglia ultimi progetti"** rimuove il suggerimento accanto a **Riprendi ultimo progetto** nella schermata di benvenuto; lo splash si aggiorna quando salvi l'interruttore senza riavviare l'app.- **Progetto personalizzato CSS**: con un **`.faproject`** attivo, **Impostazioni e strumenti** elenca **Progetto personalizzato CSS** accanto a **App personalizzata CSS**. La finestra mobile Monaco mantiene il testo del foglio di stile per progetto in SQLite con salvataggio automatico antirimbalzo, applica l'anteprima dal vivo tramite **`#faProjectUserCss`** dopo l'**App personalizzata CSS** a livello di app, mantiene la cornice mobile tra l'**App personalizzata CSS** e gli appunti di lavoro virtuale e si aggiorna quando carichi, cambi o importi progetti. La scorciatoia globale predefinita è **Ctrl+Shift+J** su ogni piattaforma desktop (stesso tasto Control letterale su macOS), cablata come di sola apertura: non chiude mai la finestra e una pressione ripetuta mentre l'editor rimane aperto lascia intatta la bozza in memoria.- **Project Noteboard**: **Project Management** ottiene **Attiva/disattiva Project Noteboard**, disabilitato finché non viene caricato un **`.faproject`**, scorciatoia predefinita **Ctrl+Alt+D** su ogni piattaforma desktop; **Chiudi** mostra la scorciatoia tra parentesi disattivata sotto l'etichetta come **App Noteboard**. Il testo e la geometria della finestra mobile persistono per progetto in SQLite **`project_data`** righe KV; il renderer mantiene il floater impilato sopra **App Noteboard** quando entrambi sono aperti mentre entrambi rimangono al di sotto del cromo modale (**6000+**).

### Correzioni di bug e ottimizzazioni
- **Copia in inglese**: i titoli e le descrizioni delle opzioni **Impostazioni app**, la guida all'importazione/esportazione, le etichette di associazione dei tasti e di monitoraggio delle azioni e il ribasso della guida sono stati corretti in **inglese, USA**; I prefissi **TODO** di staging sono stati rimossi dalle etichette di impostazione.
- **Schermata di benvenuto**: **`/`** e **`/home`** ora condividono una shell **MainLayout** (i menu della barra superiore, il selettore della lingua e i controlli della finestra rimangono montati). Solo la pagina centrale sfuma tra la schermata di benvenuto e l'area di lavoro; il cassetto sinistro scorre su **`/home`** con lo stesso tempismo. Quando una sessione **`.faproject`** diventa attiva (crea, carica, riprendi, selezioni di progetti recenti o apertura di un progetto dal sistema operativo), l'app passa a **`/home`** con una breve transizione in dissolvenza da qualsiasi percorso non correlato all'area di lavoro (benvenuto, **Errore/Non trovato** o pagine **MainLayout** future), ad eccezione del percorso di cablaggio **test dei componenti**.
- **Schermata di benvenuto**: il cassetto sinistro **Link essenziali** rimane completamente fuori dal percorso di benvenuto — nessuna guida riservata e nessun cassetto cromato su **`/`** — e scorre su **`/home`** quando l'area di lavoro si apre (stesso tempismo della dissolvenza incrociata della pagina) invece di apparire a tutta larghezza.- **Errore/Non trovato**: i percorsi sconosciuti ora utilizzano la stessa barra superiore **Layout principale** delle schermate di benvenuto e dell'area di lavoro (**Gestione progetti**, **Documenti e contenuti**, **Impostazioni e strumenti**, **Guida e informazioni**, oltre al selettore della lingua e ai controlli della finestra) senza il cassetto di sinistra.
- **Errore/Non trovato**: quando una sessione **`.faproject`** è già attiva, la pagina mostra **Riprendi progetto corrente** sopra **Torna alla schermata iniziale dell'app**; riapre il file attivo allo stesso modo del segmento primario di benvenuto **Riprendi progetto corrente**, senza il menu a discesa del progetto recente o un avviso di avviso già attivo.- **Caricamento progetto**: riapertura del progetto già attivo — da **Carica progetto esistente**, **Carica progetto recente**, doppio clic del sistema operativo o menu corrispondente o righe splash **Carica progetto esistente** — mostra un messaggio di avviso **avviso** nello stesso formato di riga **nome progetto** in caso di caricamento riuscito, non ricarica il database e passa all'area di lavoro quando ti trovavi nella schermata di benvenuto o nella pagina **Errore/Non trovato**. **Riprendi progetto corrente** in caso di **Errore/Non trovato** e il segmento principale di ripristino di benvenuto (**Riprendi progetto corrente** quando una sessione è attiva, **Riprendi ultimo progetto** quando non ce n'è) tornano all'area di lavoro silenziosamente senza alcun avviso.
- **Schermata di benvenuto**: il menu **Riprendi ultimo progetto** dei progetti recenti utilizza lo stesso sfondo della pagina in ardesia, bordo dorato, titoli di riga in oro brillante, percorsi disattivati, divisori di riga in oro, una tinta al passaggio del mouse chiara primaria-luminosa e la stessa ombra esterna dei menu della barra superiore invece del pannello del menu dell'app verde scuro predefinito.
- **Schermata di benvenuto**: la freccia a discesa divisa **Riprendi ultimo progetto** mostra nuovamente il relativo gallone; lo stile ereditato del testo maiuscolo aveva interrotto le legature delle icone dei materiali all'interno di quel segmento.- **Schermata di benvenuto**: **Crea nuovo progetto** nel percorso di benvenuto apre **Nuovo progetto** tramite lo stesso host di dialogo **AppControlMenus** del percorso dell'area di lavoro.
- **Impostazioni app**: **Nascondi collegamenti social nella schermata di benvenuto** nasconde il divisore dorato e le righe dei pulsanti **Contatto social** nella schermata di benvenuto; lo splash reagisce quando le impostazioni salvate cambiano in **Impostazioni app**.
- **Impostazioni app**: **Nascondi popup dei suggerimenti nella schermata iniziale** sopprime la notifica dei suggerimenti all'avvio **Lo sapevi?** sul percorso di benvenuto (leggi una volta all'avvio dell'app, non a ogni modifica delle impostazioni).
- **Impostazioni app**: **Salta schermata di benvenuto** apre l'ultimo **`.faproject`** recente all'avvio (o il file della sessione attiva quando ne è già caricato uno) e passa allo spazio di lavoro (**`/home`**, **MainLayout** condiviso con il cassetto sinistro) invece di rimanere nella schermata di benvenuto; lo splash reindirizza anche quando attivi l'impostazione mentre viene visualizzato il percorso di benvenuto. Se il file più recente non esiste più, l'app mostra un errore e rimane nella schermata di benvenuto invece di aprire il progetto recente successivo (anche dopo l'avvio già provato a saltare una volta); senza progetti recenti, viene ancora visualizzata la schermata di benvenuto.- **Gestione progetti**: eliminati **Salva progetto** e **Chiudi progetto** dal menu fino a quando tali azioni non avranno implementazioni desktop reali (copie **en-US**, **Deutsch**, **Français** aggiornate).
- **Project SQLite**: Main mantiene il percorso attivo **`.faproject`** accanto all'handle del database in modo che brevi desincronizzazione (ad esempio dopo la finestra inattiva) possano riaprire lo stesso file, riprovare l'operazione una volta e facoltativamente chiedere al renderer il percorso quando main ha perso entrambi. I ricaricamenti completi del renderer cancellano comunque la sessione sulle navigazioni dei documenti reali, ma la varianza dello stesso documento e del sottoframe non elimina più l'handle da solo.
- **Handshake di apertura del sistema operativo**: l'avvio Splash **`faProjectOsOpen`** ora attende brevemente **`faContentBridgeAPIs.faProjectOsOpen`** prima dell'handshake IPC **renderer-ready**, quindi si avvia con un percorso **`.faproject`** su Electron **argv** (associazioni di installazione o **FA_E2E_OS_OPEN** replay end-to-end) non è possibile perdere il cablaggio quando le superfici precaricate **`contextBridge`** seguono i primi tick di avvio.- **Progetto personalizzato CSS**: il salvataggio dall'editor mobile quando non è attiva alcuna sessione **`.faproject`** (ad esempio nella schermata di benvenuto o dopo che l'app ha cancellato l'handle del database) ora mostra un chiaro messaggio **Progetto personalizzato CSS** invece di considerare una scrittura SQLite saltata come riuscita e quindi emergere un errore di mancata corrispondenza.
- **Project Noteboard**: il caricamento di un altro **`.faproject`** mentre la finestra rimane aperta ora sposta e ridimensiona la cornice per adattarla alla geometria salvata del progetto anziché lasciare l'impronta del progetto precedente sullo schermo.
- **Note del progetto**: lo spostamento o il ridimensionamento della finestra non cancella più il testo graffiato sullo schermo quando viene letto un viaggio di andata e ritorno di salvataggio di solo frame SQLite prima che la scrittura del testo rimbalzato sia terminata; il renderer mantiene il valore live textarea quando unisce le riletture per le patch che omettono **testo**.
- **Project Noteboard**: il salvataggio del testo della nota o della geometria non attiva più un messaggio di errore quando **main** ha cancellato l'handle SQLite durante un ricaricamento del renderer (ad esempio **Control+F5** o l'aggiornamento del controllo ortografico della lingua): la scrittura differita si interrompe silenziosamente invece di implicare un salvataggio andata e ritorno fallito.- **Storybook**: l'area di lavoro nidificata **content bridge** simula ora stub **project noteboard** più **stile progetto** KV legge e scrive in modo che **WindowProjectNoteboard** e **WindowProjectStyling** **Autodoc** più anteprime su tela non mostrino più un errore spurio di **action manager** quando le finestre mobili salvano automaticamente geometria, testo di nota o bozze CSS. Una configurazione **TypeScript** con ambito **simulato** ripristina la tipizzazione del percorso **`app/`** per i manutentori senza estendere il controllo del tipo all'intero pacchetto di anteprima.
- **Storybook**: **AppControlMenus** La descrizione del componente Autodoc elenca le righe correnti di **Gestione progetti** in modo che corrispondano a **`_data/project`** dopo le modifiche al menu.
- **Test** — **`checkProjectStyling`** end-to-end rinomina **`.faproject`** le baseline delle apparecchiature e i titoli delle suite in modo che il flusso **Progetto personalizzato CSS** descriva l'anteprima live di Monaco, la persistenza di SQLite, la cancellazione del foglio di stile e l'isolamento per progetto invece delle etichette opache **alpha** / **beta**. Il cablaggio del componente **`WindowProjectStyling.playwright.test`** copre il cromo mobile, **Monaco** caricato a freddo, l'elenco dei temi del pannello della guida e **Chiudi senza salvare** lasciando vuoto il progetto memorizzato CSS.- **Test** — **`checkProjectManagementFlow`** end-to-end ora asserisce splash **Riprendi l'ultimo progetto** (**Le etichette a discesa MRU** corrispondono a **Progetto → Carica progetto recente**, ricarica tramite il segmento primario diviso dopo il ripristino della sessione di controllo ortografico, carica da un'altra riga recente).
- **Test** — **`checkProjectOsOpenArgv`** riproduce il pacchetto **electron.launch argv** con apertura a freddo con **`FA_E2E_OS_OPEN=1`** isolato dai normali lanci solo splash.

## 2.4.12 - Versione bump

### Nuove funzionalità
- **Menu della barra superiore** (**Inglese**): le quattro stringhe principali **`AppControlMenus`** **`titolo`** leggono **Gestione progetti**, **Documenti e contenuti**, **Impostazioni e strumenti** e **Guida e informazioni** (in sostituzione dei precedenti brevi **Progetto** / **Documenti** / **Strumenti e impostazioni** / **Guida e informazioni**, quindi testo provvisorio più lungo). Le righe del sottomenu utilizzano le maiuscole in stile titolo sulle parole principali (**en-US**) sotto tali titoli. I titoli principali **Deutsch** e **Français** corrispondono agli stessi quattro ruoli.- **Testuali a livello di app**: le etichette rivolte all'utente e la documentazione per la **noteboard** provvisoria (in stile **noteboard**, una parola), **Impostazioni app**, **App personalizzata CSS** e **Importa/Esporta configurazione app** ora utilizzano un vocabolario **app** coerente (identificatori interni, nomi di canali IPC e percorsi sono stati allineati). **Documenti e contenuti** elenca **Aggiunta rapida nuovo documento**, **Ricerca rapida documento esistente**, **Eliminazione di massa di documenti** e **Esporta progetto/documenti**. **Impostazioni e strumenti** elenca **Attiva/disattiva Blocco note app**, **App personalizzata CSS**, **Impostazioni associazione tasti**, **Impostazioni app**, quindi **Importa/esporta configurazione app** (sotto un separatore). Le voci ZIP **electron-store** e **.faconfig** utilizzano solo **`faAppNoteboard.json`** e **`faAppStyling.json`**.
- **App Noteboard**: **Chiudi** impila l'accordo **Attiva/disattiva App Noteboard** disattivato e tra parentesi sotto l'etichetta utilizzando lo stesso colore del suggerimento di combinazione di tasti e la stessa dimensione del tipo compatto delle linee di collegamento nelle righe del menu **Impostazioni e Strumenti**. I lanci di test dei componenti ridotti al minimo caricano le combinazioni di tasti persistenti quando il layout viene montato in modo che le anteprime del cablaggio risolvano il suggerimento come la normale shell.- **Schermata di benvenuto**: l'app si apre nel percorso predefinito con gli stessi menu principali, controllo della lingua e pulsanti finestra del resto dell'app; il riquadro di navigazione sinistro rimane nascosto solo su quella schermata (**`faMainLayoutHideDrawer`** route **meta**). Il corpo centrato corrisponde al classico **FA 1.0** (riga di benvenuto e titolo con grafica dell'ala specchiata, tre azioni primarie delineate in oro senza ancora alcun comportamento, un divisore, quindi l'attuale riga di pulsanti **Contatto sociale**). La riga di benvenuto, la coppia **img** di ala e le azioni si trovano in **`SplashControls`** (**`src/components/other/SplashControls/`**, token di layout in **`styles/_variables.scss`**); **`SplashPage`** mantiene la shell della pagina, il divisore e **`SocialContactButtons`**. Wing Art utilizza **`public/images/splash/splash_logo.png`** (allineato con la favicon **512**). **`index.html`** precarica il **PNG** con priorità di recupero elevata; ogni ala **img** usa **loading=eager** quindi la grafica è richiesta per la prima verniciatura. La pagina **Indice** dello scratch dello sviluppatore rimane in **`/home`** con la stessa shell incluso il drawer.- **Progetto attivo**: quando **`S_FaActiveProject`** non ha alcun progetto caricato, **Gestione progetto** disabilita **Salva progetto**, **Mostra dashboard progetto**, **Impostazioni progetto**, **Chiudi progetto** e **Unisci un altro progetto in quello corrente** (in **Strumenti di progetto avanzati**); **Documenti e contenuti** disabilita **Aggiunta rapida nuovo documento**, **Ricerca rapida documento esistente**, **Eliminazione di massa documenti** e **Esporta progetto/documenti**. **Impostazioni e strumenti** rimane completamente disponibile, incluso **Importa/esporta configurazione app** come ultima riga sotto **Impostazioni app**. **Nuovo progetto**, **Carica progetto**, **Strumenti di progetto avanzati** (riga principale), **Converti progetto legacy in FA 2.0** e **Attiva/disattiva Blocco note app** rimangono disponibili.- **Nuovo progetto** — schermata **Benvenuto** **Nuovo progetto** e **Gestione progetto → Nuovo progetto** aprono una finestra di dialogo delle impostazioni persistente, quindi creano un file **`.faproject`** SQLite tramite il processo principale (**Splash** e **Gestione progetto** collegati tramite **`faActionManager`**). L'intestazione mostra il nome del progetto attivo quando ne viene caricato uno. Quando la creazione ha esito positivo, **`faActionManager`** mostra un positivo **Notify**: **Nuovo progetto creato e pronto per partire!** con il nome del progetto sulla stessa riga (la finestra di dialogo non invia un secondo brindisi).
- **Carica progetto** — Schermata **Benvenuto** **Carica progetto** e **Gestione progetto → Carica progetto** aprono un selettore file del sistema operativo che per impostazione predefinita è **Download** (Playwright **E2E** utilizza **userData** isolati invece della finestra di dialogo nativa). La scelta di un **`.faproject`** lo apre nell'handle SQLite del processo principale e aggiorna **`S_FaActiveProject`** come **Nuovo progetto**. Un positivo **Notify** conferma il caricamento con **Progetto caricato e pronto per partire!** e il nome del progetto sulla stessa riga.- **Carica progetto recente** - **Gestione progetti** include **Carica progetto recente** (dopo **Carica progetto**) con un massimo di **10** file **`.faproject`** aperti o creati di recente. Le voci provengono dall'elenco salvato della shell del desktop; i file mancanti vengono eliminati automaticamente. La scelta di una riga apre quel percorso allo stesso modo di **Carica progetto** e lo sposta in cima all'elenco quando l'apertura ha esito positivo; si apre per percorso che non riesce rimuove quella riga dall'elenco tranne quando il progetto è già attivo.
- **Quasar** variabili brand pubbliche su **`:root`**, da **`--q-primary`** a **`--q-dark-page`**, vengono ricollegate ai nomi **Fantasia** **`--fa-color-…`** corrispondenti in **`src/css/fa-quasar-public-vars-bridge.scss`** (ultimo foglio di stile **app**), quindi **App personalizzata CSS** può guidare **`var(--q-*)`** dallo stesso set di token di colore Fantasia.- **SCSS** layout del tema: **`app.palette.scss`** (inclusi token di testo semantico in **COLORI APP**), proprietà personalizzate predefinite in **`fa-theme.scss`**, condiviso **`faSemanticText.scss`** ( **`fa-text-*`**, **`fa-icon-*`** e **`.text-white` / `.text-black`** mappati su **Fantasia** neutrali), dividere i token **`q-*`** in **`src/css/theme/quasar-components/`**, la funzione co-localizzata **`styles/_variables.scss`**, i globali **`htmlAdjustments.variables.scss`** e **`scrollbar.variables.scss`** e un sottile **`quasar.variables.scss`** botte. **`yarn audit:quasar-component-tokens`** aiuta a individuare le definizioni **`$q*`** mancanti.
- **La copia e le icone dell'interfaccia utente** utilizzano questi helper invece di quelli ad hoc **Quasar** Classi di ombreggiatura del testo del materiale in cui un ruolo dovrebbe seguire il tema di runtime. **Notify** i valori predefiniti non sono più impostati su **`textColor`**.- **App personalizzata CSS**: il menu Aiuto **?** (ad esempio sotto **Fantasia Archive elenco variabili**) elenca i nomi **`--fa-color-…`** con un piccolo campione di colore a sinistra di ciascun nome (il campione utilizza **`var(…)`** da **`:root`**, quindi corrisponde al tema e all'**App personalizzata CSS**). Quando sono presenti stili live (incluso **Vite** HMR in dev), i nomi provengono dal **DOM**; altrimenti l'elenco ritorna agli stessi nomi analizzati dal testo **`fa-theme.scss`** in bundle. Il renderer non dipende più dalle letture del file **Node** per quel pannello.
- **App personalizzata CSS** — **Aiuto per le combinazioni di tasti dell'editor** (**?** nella riga del titolo): un **`QMenu`** in stile Fantasia globale **QTooltip** sostituisce il vecchio suggerimento solo al passaggio del mouse. Si apre dopo lo stesso ritardo di **500 ms** al passaggio del mouse o con un clic sull'icona della Guida, rimane aperto per la lettura finché non si fa clic all'esterno del pannello, si preme **Esc** o si fa nuovamente clic sull'icona della Guida; **`no-focus`** impedisce a **Monaco** di sfocarsi all'apertura del menu.- **Barra superiore**: **Aggiunta rapida nuovo documento**, **Ricerca rapida documento esistente** ed **Eliminazione di massa documenti** spostati da **Impostazioni e strumenti** a **Documenti e contenuto** (stesse azioni ed etichette); **Documenti e contenuti** termina con **Esporta progetto/documenti**. **Gestione progetti** non elenca più **Esporta progetto/documenti** (la stessa azione si trova solo in **Documenti e contenuti**). **Importa/Esporta configurazione app** è l'ultima riga in **Impostazioni e strumenti**, sotto **Impostazioni app** e un separatore.
- **Menu della barra superiore**: pannelli di sottomenu nidificati (ad esempio **Gestione progetto → Carica progetto recente** e **Strumenti di progetto avanzati**) si aprono quando si punta alla riga principale, con un breve ritardo prima che si chiudano in modo che il puntatore possa spostarsi nel pannello mobile; facendo clic sulla riga si attiva o disattiva comunque il sottomenu in cui **Quasar** lo supporta insieme alla visibilità controllata.
- **Guida e informazioni**: **Informazioni su Fantasia Archive** appare sopra il **Log delle modifiche** (prima della **Licenza**).

### Correzioni di bug e ottimizzazioni
- **App personalizzata CSS**: **Quasar** sottolineato **`q-field`** scuro, copia di input, etichette e bordi evidenziati o di sola lettura ora utilizzano **`var(--fa-color-white)`** o **`color-mix`** invece di **`#fff` / RGBA** fissi. I dischi scuri **`q-toggle`** per il pollice inattivo, la pila di etichette interne e la tinta dell'icona del pollice nello stato attivo seguono **`--fa-color-white`** allo stesso modo.
- **Benvenuto/Informazioni sulle righe social**: le etichette e i riempimenti dei chip **Social** utilizzano solo i token della tavolozza **Sass** (nessun **`fa-v()`** o runtime **`--fa-color-*`** cablaggio nei file variabili dei componenti), quindi **App personalizzata CSS** non può retintare **Patreon**, **Ko-fi**, **GitHub**, **Discord**, **reddit**, **Sito web** o marchio **X**. I colori delle etichette **Discord**, **reddit** e **GitHub** sono fissati sulla radice **`q-btn`** con **`!important`** quindi le regole del testo scuro ereditate da **Quasar** non li sovrascrivono.- **App personalizzata CSS**: le sostituzioni per Fantasia **`--fa-color-dark`** (e i token del marchio corrispondenti su **body**) ora raggiungono **Quasar** **`.q-dark`** sfondi che contengono **`var(--q-dark)`**. Il bridge del foglio di stile ripete i collegamenti delle proprietà personalizzate **`--q-*`** su **body**, non solo su **`:root`**, perché le variabili CSS ereditate portano il loro valore calcolato; **a livello del corpo** **`--fa-color-*`** altrimenti le modifiche non riuscirebbero a ridipingere le superfici scure **Quasar**. **`.q-dark`**, **`body.body--dark`**, **`.q-layout__section--marginal`** e dark **`.q-list` / `.q-item`** chrome **usa** **`var(--fa-color-white)`** o **`color-mix`** contro **`--fa-color-white`** invece di valori fissi vicini al bianco, quindi **`--fa-color-white`** ripristina questi stack insieme ai riempimenti **`--fa-color-dark`** / **`--q-primary`**.- **Copia inglese**: **en-US** **prosa corrente** (corpi dei dialoghi, paragrafi, **`Notify`** righe di messaggi, narrazione del log delle modifiche) rimane in stile frase (prima parola chiusa, parole successive in minuscolo a meno che non siano acronimi o nomi di prodotti (**CSS**, **FA**, Fantasia Archive, **FA 1.0**, Electron, Monaco, SQLite, UI, **`macOS`**, ecc.), **Chrome** (righe di sottomenu **barra superiore**, azioni primarie **Splash** associate ai menu, **`dialogs.keybindSettings.commands.*`**, intestazioni di colonna **`Action Monitor`** più etichette **`globalFunctionality.faActionManager`** corrispondenti, didascalie dell'elenco di controllo accanto attiva/disattiva, le intestazioni principali **dialogo** e **finestra mobile** come **Fantasia Archive Impostazioni**) utilizzano le maiuscole in stile titolo sulle parole principali mentre le parole grammaticali brevi rimangono minuscole dove convenzionali (**unisci … in quella corrente**, **Suggerimenti, trucchi e curiosità**) Le intestazioni delle finestre di dialogo supportate da Markdown mantengono l'ortografia **Guida alla ricerca avanzata** e **Suggerimenti, trucchi e curiosità** Le quattro barre **barra superiore** **`titolo`** rimangono più ricche il titolo è maiuscolo (vedi **Nuove funzionalità** sopra). **Francese** rispecchia il titolo su righe comparabili (**Astuces, conseils & aneddotes**); **Tedesco** mantiene le maiuscole dei nomi ordinari e non è rewscritto meccanicamente.
- **Persistenza del desktop**: **electron-store** non rinomina più automaticamente i file JSON desktop legacy in **userData**; L'importazione di **.faconfig** accetta solo i nomi base **`faApp*.json`** correnti all'interno dell'archivio; le chiavi di override dell'associazione di tasti sconosciute (inclusi gli ID ritirati) vengono eliminate durante la pulizia invece di essere riscritte in nuovi ID.
- **Testo**: le etichette residue a livello di applicazione sono state normalizzate nel vocabolario **app** (ad esempio titoli **App personalizzata CSS** in **en-US** / **de** / **fr**, suggerimenti, curiosità, copia di convalida dell'archivio importato, errori del bridge noteboard e documenti dei contributori corrispondenti o titoli dei test).
- **I titoli delle finestre di dialogo e delle finestre mobili** utilizzano **Quasar** **text-h5** sulle intestazioni semantiche **`h5`** (**Informazioni su Fantasia Archive**, **Monitor azioni**, **Impostazioni associazione tasti**, **Fantasia Archive Impostazioni**, **Importa/esporta configurazione app**, **Nuovo progetto**, **App personalizzata CSS**, **App Noteboard**) in modo che le intestazioni corrispondano a un'unica scala compatta.- **App Noteboard**: l'area dell'editor utilizza lo stesso sfondo del pannello **`scuro-chiaro`** delle righe del menu **Impostazioni e Strumenti**; **Chiudi** è centrato nella riga di azioni inferiore. Il campo nota è una **textarea** nativa, quindi il contenuto scorre all'interno della finestra quando è piccola; il controllo ortografico nativo è disattivato, con i suggerimenti di **`correzione automatica`** / **`completamento automatico`** disattivati ​​e un attributo di disattivazione **grammaticale** per il testo iniziale. Il titolo **`h5`** utilizza lo stesso token di margine verticale di **App personalizzata CSS** in modo che entrambe le intestazioni della finestra mobile siano allineate.
- **Importa/Esporta configurazione app**: i pannelli dell'elenco di controllo **Esporta** e **Importa** sono ciascuno racchiusi come propri **Quasar** **QStep** figli della finestra di dialogo **QStepper**, quindi scegliendo **Esporta** o avanzando alla selezione di importazione viene visualizzato il contenuto corretto del passaggio anziché solo il primo pannello registrato con lo stepper. Il corpo dello stepper mantiene un'altezza minima e non utilizza più l'animazione della diapositiva orizzontale, quindi l'elenco di controllo rimane visibile nelle build **Electron** pacchettizzate in cui lo stile flessibile e di transizione precedente potrebbe lasciare l'area del contenuto compressa o ritagliata dopo aver cambiato passaggio.- **Importa/esporta configurazione app**: **Applica importazione** da un archivio **.faconfig** aggiorna l'**app personalizzata CSS** in **Monaco** mentre la finestra mobile rimane aperta in modo che l'editor corrisponda allo stile appena importato, lo stesso comportamento del **blocco note**** rimane allineato con il testo persistente quando rimane aperto.
- **Menu della barra superiore**: la prima riga di ogni menu o sottomenu aperto non mostra più la sottile linea divisoria sopra di essa; i divisori sottili continuano ad apparire tra righe consecutive e le righe di separatori complete si comportano come prima.
- Il menu **Impostazioni e strumenti** non elenca più **Attiva/disattiva albero gerarchico** come riga di menu.
- **Manutenzione**: rimossa una riesportazione di un'etichetta di accordo di combinazione di tasti inutilizzata, segnaposto commentati obsoleti alla fine del file di voce principale **Electron** dal primo avvio di SQLite e un menu TODO **Aiuto e informazioni** ridondante nei dati del menu (nessuna modifica ai menu visibili o alle scorciatoie).- **Monitor azione**: la copia di una riga di azione negli appunti mostra **Riga di azione copiata negli appunti** come titolo del toast (**Notify** **`message`**) con una riga di didascalia **Attività copiata -** e l'ID dell'azione registrata (**de** / **fr** utilizza lo stesso modello con le stringhe localizzate); quel brindisi positivo salta la riga mirrored **`[Notify]`** negli strumenti per sviluppatori (gli **errori di copia** omettono comunque il mirror duplicato accanto a **`console.error`**).
- **Strumenti per sviluppatori**: ogni toast **Notify** scrive anche una riga corrispondente nella console degli strumenti per sviluppatori (**console.error**, **console.warn**, **console.info** o **console.log**) dal toast **type** o **color**, contrassegnato con **`[Notify]`** più **message**, **caption**, **type**, **color** e **timeout** quando presenti, quindi puoi correlare i popup con l'output del log; i percorsi che già emettono la propria riga **console** per lo stesso evento saltano il mirror duplicato.
- **Strumenti per sviluppatori**: **Guida e informazioni → Attiva/disattiva strumenti per sviluppatori** e il percorso **openDevTools** di precaricamento richiedono al frontend Chromium DevTools in bundle il pannello **Console** dopo l'apertura quando esiste l'API interna, anziché fermarsi sulla scheda **Elementi** predefinita.- **Action manager**: le righe fallite **Notify** disattivano il mirroring automatico del toast-to-console quando il gestore emette già **console.error**, quindi le righe di errore rimangono singole (lo stesso hook si applica quando la copia della riga **Action Monitor** fallisce dopo la propria riga **console.error**).
- **Quasar palette** (**`app.palette.scss`**): gli slot del marchio **negativo**, **positivo** e **avviso** utilizzano valori esadecimali più profondi e meno evidenti; **info** utilizza un **#ffebb8** caldo fisso in modo che gli sfondi **Notify** con struttura colorata rimangano coerenti con il trattamento scuro su chiaro. Nel loro insieme, questi slot del marchio allineano la cromatura con mappatura **Material** con l'identità di Fantasia.
- **Carica progetto recente**: la riga **Gestione progetto** per **Carica progetto recente** corrisponde a **Strumenti di progetto avanzati** (stile freccia laterale grigia) e rimane disabilitata quando non è presente un elenco recente, invece di aprire un sottomenu vuoto; ogni voce recente mostra il nome del progetto con il percorso **`.faproject`** su una seconda riga nello stesso stile disattivato dei suggerimenti di scelta rapida del menu, senza icona finale.- **Carica progetto**: ogni **`.faproject`** ora memorizza un UUID persistente **progetto** in SQLite; tentando di caricare nuovamente lo stesso progetto mentre è già attivo viene visualizzato un singolo messaggio cancellato invece di riaprire il file e **Notify** utilizza uno stile **avviso** (non rosso) mentre la console e **Action Monitor** registrano ancora un'azione non riuscita.
- **Notify** — i messaggi di avviso di tipo **avviso** dipingono ignora e altre righe di azione **QBtn** vengono cromate nello stesso tono scuro del testo del messaggio in modo che le icone rimangano leggibili sullo sfondo chiaro **avviso** (i siti di chiamata spesso passano ancora il colore Quasar **bianco**, pensato per le barre di errore rosse).
- **Sessione desktop**: un ricaricamento completo di **WebContents** della finestra principale (ad esempio **Control+F5** o l'aggiornamento del controllo ortografico della lingua) elimina l'handle del database **`.faproject`** attivo del processo principale in modo che l'app corrisponda a un nuovo renderer con **nessun** progetto aperto finché non ne carichi di nuovo uno.- **Controllo della lingua**: l'icona di aggiornamento del controllo ortografico accanto al selettore della lingua utilizza nuovamente la classe **`fa-icon-spellcheck-refresh`**, quindi **`--fa-color-icon-spellcheck-refresh`** dipinge l'accento rosso; aveva fatto riferimento a un nome di utilità **`fa-color-…`** inesistente ed era tornato al tono predefinito dell'icona della luce.
- **Nuovo progetto**: successo **Notify** appare solo una volta (**`faActionManager`**); la finestra di dialogo del nuovo progetto non pubblica più un secondo brindisi con copia sovrapposta.
- **Test**: obiettivi di copertura end-to-end del **Nuovo progetto** **Gestione progetto → Nuovo progetto** senza fare clic erroneamente sulle azioni di benvenuto le cui etichette contengono anche **progetto** (**Playwright** utilizza un nome accessibile esatto per il pulsante del menu **Gestione progetto**).
- **Test** — Vitest **unit-components** ha ottenuto una copertura dei rami più ampia per finestre di dialogo, finestre mobili, menu nella barra superiore, **MainLayout**, bordi del selettore della lingua e **iniezione dell'app personalizzata CSS** tramite montaggi aggiuntivi più piccoli predicati **`script/*.ts`** testati direttamente dove modelli o tempistiche rendevano difficili da raggiungere i singoli rami.- **Test**: specifiche **Playwright** end-to-end **`checkAppNoteboard`** e **`checkAppFloatingWindows`** riguardano **noteboard** la persistenza del testo scratch tra riavvii e **app personalizzata mobile CSS** più **App Noteboard** persistenza della geometria, impilamento **z-index** e flussi di ripristino approssimativo della finestra; una specifica di cablaggio del componente **`WindowAppNoteboard`** copre la cromatura mobile e la persistenza del testo bridge in modo isolato.
- **Nuovo progetto**: il campo **Nuovo nome progetto** viene evidenziato quando si apre la finestra di dialogo in modo da poter iniziare a digitare senza ulteriori clic.- Finestra di dialogo **Nuovo progetto**: non più persistente, quindi Sfondo ed Esci la chiudono di nuovo come le altre finestre di dialogo. Le etichette utilizzano **Crea nuovo progetto** e **Nuovo nome progetto** (**en-US**, **de**, **fr**); la vecchia riga **nameHint** disattivata sotto il campo è scomparsa. Il campo del nome viene cancellato ogni volta che si apre il foglio; la scheda mantiene una spaziatura delle azioni più stretta e il campo utilizza la larghezza centrata e la **luminosità primaria** sulla shell di input a tema. Il nome del registro del componente modale è **`DialogNewProject`** (sostituisce **`DialogNewProjectSettings`**); **`faActionManager`** utilizza **`openNewProjectDialog`** e i percorsi **vue-i18n** utilizzano **`dialogs.newProject`**. L'etichetta di accessibilità **en-US** si allinea con **Nuovo progetto**.
- **Nuovo progetto**: il nome del file **`.faproject`** suggerito dalla finestra di dialogo **Salvataggio del sistema operativo** mantiene la spaziatura e le maiuscole quando rimangono al sicuro nelle regole comuni del filesystem invece di riscrivere la radice in lettere minuscole con trattino; i caratteri rifiutati dai percorsi vengono sostituiti da spazi o eliminati per compatibilità (**emoji**, caratteri di controllo). L'impostazione predefinita suggerita include ancora il suffisso **`.faproject`** insieme al filtro del tipo in modo che il percorso offerto rimanga esplicito su tutte le piattaforme.- **Nuovo progetto**: i percorsi **`.faproject`** sono accettati quando sono assoluti sul sistema operativo host o utilizzano un'unità **Windows** o root **UNC**, quindi la convalida corrisponde alle finestre di dialogo di salvataggio reali anche quando il main viene eseguito su nonWindows (ad esempio test automatizzati).
- **Nuovo progetto**: il campo del nome accetta al massimo **120** caratteri (**Quasar** **q-input** **maxlength** con un contatore di caratteri attivo). Lo stesso limite si applica alla convalida del processo principale, allo stem suggerito di **salvataggio del sistema operativo** e ai nomi archiviati per **Carica progetto recente**; se i dati persistenti del progetto recente non rispettano questa regola (ad esempio qualsiasi nome più lungo di **120** caratteri), la shell del desktop cancella l'elenco memorizzato finché non si aprono nuovamente i progetti correttamente.
- Il sottomenu **Documenti e contenuti** elenca **Aggiunta rapida nuovo documento**, **Ricerca rapida documento esistente**, **Eliminazione di massa documenti**, quindi **Esporta progetto/documenti** in **en-US**, con copie **de** / **fr** allineate; **Esporta progetto/documenti** spostato qui da **Gestione progetti** in precedenza; **Importa/Esporta configurazione app** si trova nella parte inferiore di **Impostazioni e strumenti**. Le etichette **`titolo`** del menu principale non vanno a capo, quindi le frasi lunghe rimangono su una riga.- **App personalizzata CSS** (**en-US**): l'intestazione del pannello di aiuto per l'elenco delle variabili del tema è di nuovo **Fantasia Archive elenco variabili** (che sostituisce l'etichetta abbreviata **FA elenco variabili**).
- **Testo semantico**: **enfasi del pericolo** (**`fa-text-danger-emphasis`**, palette **`$text-danger-emphasis`**) utilizza un rosso più brillante (**#ff4c4c**) per un contrasto più chiaro sulle cromature scure.
- **Impostazioni app**: la scheda **categoria** selezionata a sinistra utilizza nuovamente il colore brillante dell'etichetta **primario**. **fa-text-muted** ora si applica solo alle schede che non sono selezionate, quindi non combatte più **Quasar** **active-color** quando entrambe le utilità impostano **color** con **`!important`**.
- **Storybook**: Autodocs non mostra più il primo brano due volte quando un file definisce più piani; **DialogActionMonitor** include Autodoc come altre finestre di dialogo; I tooltip della tavolozza dei colori **Fondazione** omettono la riga extra **SCSS** per **`$info`**; **SocialContactButtons** elimina la storia ridondante di **CompositionContract** (linea di base visiva orfana rimossa).- **Storybook** istantanee visive: l'attesa di disponibilità dell'iframe ispeziona **`#storybook-root`** e **`#root`** separatamente per gli elementi secondari (insieme alla finestra di dialogo **Quasar** visibile del portaled o al markup del menu) in modo che **`querySelector`** su un **`#storybook-root, #root`** raggruppato selettore da solo non possa segnalare erroneamente un **`#root`** vuoto quando l'albero di anteprima si trova sotto **`#rootbook-story`**.
- **Aiuto → Registro modifiche**: se una riga di rilascio tentasse di mostrare il testo della regola **CSS** utilizzando le **parentesi graffe** delimitatori di blocco, **vue-i18n** potrebbe trattare quei caratteri come segnaposto del messaggio; il corpo della finestra di dialogo potrebbe non essere visualizzato. La riga interessata **2.4.12** è riformulata in un linguaggio semplice.
- **QTooltip**: i popup su Chrome scuro utilizzano **`$accent`** (**#f5f5f5**) e **`$dark`** (**#1b333e**), **13px** copia con peso **600**, **600px** larghezza massima, un **`z-index`** alto per l'impilamento della barra del titolo, più **`line-height` / `letter-spacing`** in **`quasarComponentsAdjustments`**, invece di **Quasar** dimensioni e colori dei tooltip standard.- **Lo sapevi?** (**info** **Notify**): l'azione di chiusura è **`color: dark`**; **`_qNotify`** utilizza il **primo piano scuro** su **`.q-notification.bg-info`**, e **`quasarComponentsAdjustments`** applica quel **`color`** con **`!important`** in modo da prevalere sulla classe **Quasar** **`text-white`** dello stesso nodo e sull'utility globale **`.text-white`** che utilizza anche **!important**, che altrimenti forzerebbe quasi bianco su sfondo chiaro **info**.
- **Schermata di benvenuto** e **Informazioni**** righe social: il chip **Reddit** utilizza solo l'immagine del marchio denominativo (nessuna etichetta di testo separata; **en-US**, **de**, **fr**). La copia di benvenuto utilizza il markup dell'intestazione e l'opacità dei sottotitoli disattivata. **SocialContactButtons** nidifica i wrapper **row aware-center** in modo che Patreon/Ko‑fi e i gruppi di collegamenti secondari rimangano centrati orizzontalmente sui layout ampi.
- **Informazioni su Fantasia Archive**: il titolo della finestra di dialogo utilizza un'intestazione di livello quattro in modo che la struttura del documento corrisponda alla lettura visiva del titolo.- **Schermata di benvenuto**: le azioni primarie utilizzano una spaziatura interna orizzontale più ampia e una larghezza di **adattamento del contenuto** in modo che ogni **pulsante** si adatti alla sua etichetta invece di allungare l'intera colonna delle azioni. **SplashPage** e **SplashControls** leggono il layout dai moduli **`styles/_variables.scss`** colocati **`@forward`ed** attraverso **`quasar.variables.scss`**, corrispondenti alle regole token semantiche **SCSS** utilizzate altrove in **`src/components/`**.
- La riga del titolo della **schermata di benvenuto** e il menu a discesa della **lingua** della barra superiore sopprimono il testo selezionabile durante i trascinamenti in modo che le etichette cromate non raccolgano evidenziazioni accidentali.
- **Dipendenze**: **dompurify** (inserito da **Monaco**) è bloccato su **3.4.2** con una voce **Yarn** **risoluzioni** in modo che lo stack dell'editor tenga traccia di una versione di sanificazione **3.x** con patch invece di un pin transitivo più vecchio.
- **Electron desktop**: il percorso **`BrowserWindow` `preload`** si risolve nell'artefatto **`preload/electron-preload.cjs`** del pacchetto Quasar in modo che le API **`contextBridge`** (**`window.faContentBridgeAPIs`**) vengano inizializzate in modo affidabile invece di saltare silenziosamente il precaricamento quando nomi di file o cartelle derivano dal bundle **`dev`** disposizione.- **Monitor azioni**: le righe della cronologia di **Caricamento progetto** riuscite mantengono vuoto il payload di accodamento ma allegano un'anteprima di chiusura con il percorso **`.faproject`** scelto e il nome del progetto caricato, in linea con il modo in cui **Nuovo progetto** presenta i dettagli dopo il completamento; quando l'apertura fallisce dopo aver selezionato un file, le righe non riuscite possono includere il messaggio più il percorso tentato quando la shell del desktop lo fornisce.
- **Electron desktop**: **`faRoutingEnv`** attende brevemente lo snapshot precaricato dell'**ambiente extra** prima di gestire i percorsi iniziali del cablaggio, evitando una corsa di avvio in cui il ponte si attacca dopo la prima verniciatura del renderer.
- **Playwright helpers**: il codice di cablaggio per il componente Electron e le suite **E2E** è suddiviso in **`helpers/playwrightHelpers_universal`**, **`helpers/playwrightHelpers_e2e`** e **`helpers/playwrightHelpers_component`**, con aiutanti di lancio e smontaggio condivisi in modo che i blocchi seriali **`beforeAll`** rimangano uniformi; **`README`** e le note del contributore elencano i percorsi di importazione aggiornati.

## 2.4.11 - Versione bump

### Nuove funzionalità
- **Barra superiore**: l'etichetta del menu **Strumenti** è **Strumenti e impostazioni** (e le stringhe **de** / **fr** corrispondenti) in modo che il nome corrisponda alle voci relative alle impostazioni all'interno del menu.
- **Parità locale**: **de** e **fr** ora seguono lo stesso albero chiavi **vue-i18n** di **en-US** (inclusi **errorNotFound**, **globalWindowButtons**, **fantasiaMascotImage**, **dialogs.markdownDocument**, **globalFunctionality.unsortedAppTexts** e bundle **documents**), quindi il chrome della finestra, la pagina di errore, l'intestazione dei suggerimenti e la mascotte il testo alternativo viene risolto senza chiavi errate o mancanti. **Impostazioni app** in **de** utilizza ancora il modulo stringa **en-US** finché non arriva un passaggio completo **de** per le impostazioni dell'app; **fr** mantiene il file parziale di **impostazioni dell'app** con **en-US** fallback per il resto. Le linee di avviso di **importazione/esportazione** sono state rese più restrittive in **en-US** con aggiornamenti **de**/**fr** corrispondenti.
- **Importa/esporta configurazione app**: i pannelli **Importa**/**Esporta**/elenco di controllo si scambiano con una diapositiva orizzontale (animazione stepper); l'intestazione del passaggio rimane nascosta in modo che il flusso assomigli ancora a una finestra di dialogo.- **Importa/esporta configurazione app**: la parte superiore della finestra di dialogo include un blocco localizzato **Nota** (guida all'esportazione, avviso di sovrascrittura e opzione di importazione selettiva) in **en-US**, **de** e **fr** prima del selettore **Importa**/**Esporta**.- **Strumenti → Importa/Esporta configurazione app** (**mdi-database-export-outline**), direttamente sotto **Mostra nota**: apre una finestra di dialogo non persistente **Importa/Esporta configurazione app** (clic esterni e **Escape** la chiudono) con **Importa** ed **Esporta** nel primo passaggio in due colonne con un separatore verticale, ciascun pulsante a tutta larghezza della sua metà con **8 px** riquadro orizzontale per la riga delle azioni, azioni **primarie** piatte (grassetto, etichette maiuscole), corrispondente alla classica finestra di dialogo **FA 1.0** **Inserisci immagine**. **Esporta** offre tre caselle di controllo preselezionate (**Impostazioni app**, **Abbinazioni di tasti app**, **App personalizzata CSS**), un controllo **Crea file di esportazione** (abilitato solo se è selezionata almeno una parte), quindi la normale finestra di dialogo **Salvataggio sistema operativo** per scrivere un **`.faconfig`** (ZIP con gli stessi nomi JSON **`electron-store`** dell'app live: **`faUserSettings.json`**, **`faKeybinds.json`**, **`faAppStyling.json`**). **Importa** utilizza la finestra di dialogo di apertura file solo per **`.faconfig`**; il processo principale applica un limite di **3 MB**, membri ammessi nella lista consentita, convalida **JSON** + **Zod** e gestione temporanea prima di confermare **Importazione selezionata**; l'applicazione sostituisce gli store scelti e il renderer ricarica le impostazioni dell'app, le combinazioni di tastie app **CSS** dal ponte. Il flusso registra le azioni **faActionManager** (**openImportExportAppConfigDialog**, **exportAppConfigPackage**, **exportAppConfigSaveResult**, **importAppConfigStageResult**, **importAppConfigApply**) per **Action monitor** con payload piccoli e sicuri per il monitoraggio (nessun corpo di file).- **Strumenti → App personalizzata CSS**: una nuova voce apre una finestra mobile **persistente** **App personalizzata CSS** (icona **mdi-lingual-css3**) con un editor **Monaco** caricato a freddo configurato per il supporto della lingua **CSS**. La finestra è **spostabile** (trascina la riga del titolo) e **ridimensionabile** (maniglie dei bordi e degli angoli); può essere chiuso solo con **Chiudi senza salvare** o **Salva modifiche** (**Esci** e i clic casuali all'esterno della finestra non lo chiudono). Il CSS salvato viene archiviato nel processo principale **Electron** tramite **electron-store** e applicato globalmente come un elemento **`<style id="faUserCss">`** inserito in **`document.head`** al caricamento dell'app e aggiornato ogni volta che si salvano le modifiche, in modo che le regole personalizzate vengano applicate su ogni percorso, layout e superficie incorporata. Il frame è una superficie in-renderer **`position: Fixed`** teletrasportata su **`document.body`**, centrata e dimensionata in base alle frazioni e ai margini del viewport, con **Vue** **Transizione** e l'animazione personalizzata **fa-floatingWindowPop** (opacità e scala; la scala minima non è mai **0**; **300 ms** come un'impostazione predefinita **QDialog**; non stock **q-transition--scale** **scale(0)**, che può lasciare una **trasformazione** degenerata in **Electron**). **Monaco** viene caricato solo quando window si apre (e solo il pacchetto lingua **CSS** e il lavoratore sono in bundle) quindi l'avvio iniziale dell'app non viene influenzato. **Strumenti → Impostazioni combinazione di tasti** elenca **Apri l'app personalizzata CSS** con una scorciatoia predefinita di **Ctrl + Alt + Shift + J** (**Cmd** sostituisce **Ctrl** su **macOS**); l'accordo può essere riassegnato o cancellato da lì. Sia l'apertura che il salvataggio passano attraverso il **gestore azioni** (**openAppStylingWindow** e **saveAppStyling**), quindi gli errori di salvataggio presentano lo stesso trattamento di notifica singola delle altre impostazioni. (Il pacchetto **[@quasar/quasar-ui-qwindow](https://github.com/quasarframework/quasar-ui-qwindow)** upstream ha come target solo **Quasar v1 / Vue 2**; Fantasia Archive fornisce invece un frame mobile in-app **Vue 3**.)- **Gestione azioni**: ogni azione significativa per l'utente (apertura di dialogo, salvataggio delle impostazioni, riduzione a icona/ridimensionamento/chiusura della finestra, cambio di lingua, attivazione/disattivazione degli strumenti di sviluppo, notifica dei suggerimenti di avvio e simili) ora scorre attraverso un singolo dispatcher con due tipi di esecuzione: **async** (attiva e dimentica) e **sync** (coda FIFO singola, soft cap **32**, **30 s** timeout per azione, deduplicazione con attivazione). Gli errori mostrano una singola notifica di **errore** con l'ID dell'azione e il messaggio di errore sottostante e gli stessi dettagli vengono visualizzati nella riga della console per sviluppatori accanto ad essa. I flussi di salvataggio per **Impostazioni associazione tasti** e **Impostazioni app** vengono instradati attraverso questa superficie, quindi le doppie notifiche precedenti su un salvataggio non riuscito scompaiono.- **Guida e informazioni → Monitoraggio azioni**: la voce sotto **Attiva/disattiva strumenti per sviluppatori** apre una finestra di dialogo **Monitoraggio azioni** con una tabella di azioni dalla sessione corrente dell'app: **ID azione**, **Ora di inizio**, **Ora di fine**, **Payload** (segno di spunta verde quando esiste il testo del payload del tempo di accodamento, trattino grigio quando vuoto), **Tipo** (**Sincronizzazione** o **Asincrono**) e **Stato** (spinner dell'ingranaggio durante l'esecuzione, segno di spunta verde in caso di successo, croce rossa in caso di errore, icona blu della clessidra mentre è in coda, ciascuna con un suggerimento al passaggio del mouse per il significato). Un'icona di aiuto spiega che un clic sinistro copia i dettagli della riga negli appunti. La griglia è un'**istantanea** all'apertura della finestra di dialogo, limitata a **500** righe per sessione, non scritta su disco. **Impostazioni combinazione tasti** elenca **openActionMonitor** in modo da poter assegnare una scorciatoia globale; non esiste ancora un accordo predefinito.- **Strumenti → Impostazioni associazione tasti** apre una finestra di dialogo **Impostazioni associazione tasti** (tabella delle azioni, scorciatoie predefinite e personalizzate, filtro, salvataggio nella memoria persistente nel processo principale **Electron**). **Aiuto e informazioni** non elenca più una voce separata del cheatsheet di associazione dei tasti; le scorciatoie sono gestite da **Strumenti**. Le scorciatoie globali rimangono attive durante la digitazione di input laddove l'associazione lo consente e l'invio si interrompe mentre questa finestra di dialogo o il relativo popup di acquisizione è aperto.
- **Lingua dell'interfaccia**: un controllo **lingua** nella barra in alto (accanto ai pulsanti della finestra) mostra la lingua corrente come **bandiera** in un controllo circolare e apre un breve menu per **Inglese, USA**, **Deutsch** e **Français**. La scelta viene memorizzata come **lingualCode** nelle **impostazioni utente** persistenti (**en-US**, **de** o **fr**, predefinito **en-US**) e si applica **vue-i18n** in tutta l'app. **Impostazioni app** non espone ancora questo campo.
- **Bandiere nazionali**: raggruppato un set completo di immagini di bandiere **SVG** in **`public/countryFlags/`** per il controllo della lingua e altre interfacce utente compatibili con le impostazioni locali.- **Tedesco** **barra superiore**: le voci di menu **Progetto**, **Documenti**, **Strumenti** e **Guida e informazioni** utilizzano i moduli locali **Deutsch** insieme a **Informazioni** e alla riga condivisa **social**, corrispondenti alla forma del menu **en-US**.
- **Guida alla ricerca avanzata**: scorciatoia predefinita **Ctrl + Alt + Shift + G** apre la guida ai markdown nella stessa finestra di dialogo di **Guida e informazioni → Guida alla ricerca avanzata**; **Strumenti → Impostazioni combinazione tasti** lo elenca come **Apri Guida alla ricerca avanzata**.
- **Suggerimenti per le scorciatoie del menu**: **Strumenti** e **Guida e informazioni** mostrano la scorciatoia globale in tempo reale accanto a **Impostazioni app**, **Impostazioni associazione tasti**, **Guida alla ricerca avanzata** e **Attiva/disattiva strumenti per sviluppatori**, utilizzando le stesse etichette di **Strumenti → Impostazioni associazione tasti**; la riga di suggerimento appare solo dopo il caricamento delle combinazioni di tasti da **Electron** e solo per quelle azioni.
- **Storybook**: le storie **AppControlSingleMenu** e **AppControlMenus** includono dispositivi **`keybindCommandId`** (livello superiore e sottomenu), un controllo della composizione che apre **Guida e informazioni** e prevede il markup dei suggerimenti di scelta rapida e una variante facoltativa **`embedDialogs`** in modo che gli host delle finestre di dialogo vengano montati come in produzione.- **End-to-end**: la specifica **`checkKeybinds`** Playwright copre il valore predefinito **Ctrl + Alt + Shift + G** e un accordo personalizzato persistente per **Apri guida alla ricerca avanzata** rispetto alla finestra di dialogo della guida al markdown, nello stesso flusso delle altre scorciatoie globali.
- **End-to-end**: la specifica **`checkAppStyling`** Playwright apre l'**app personalizzata CSS** da **Strumenti**, inserisce **CSS** in **Monaco**, salva in modo che **document.body** prenda **zoom** dal foglio di stile inserito, riapre la finestra di stile, cancella l'editor, salva **CSS** vuoto e afferma che **zoom** ritorna al valore predefinito.
- **Storybook**: **`externalFileLoader`** registra **`floatingWindows.appStyling`** per **en-US** in modo che **WindowAppStyling** **autodoc** e storie su tela risolvano titoli, copia di tooltip ed etichette di azioni invece delle chiavi **vue-i18n** non elaborate; la storia genera un esempio su più righe **CSS** quindi l'evidenziazione della sintassi **Monaco** è facile da individuare.- **Storybook**: **DialogImportExportAppConfig** canvas e **autodoc**; **`externalFileLoader`** registra **dialogs.importExportAppConfig**; il mock del bridge dei contenuti dell'area di lavoro aggiunge stub **faAppConfig** in modo che **prepareImport** possa avere successo nel canvas mentre **exportToFile** restituisce **canceled**, corrispondendo al flusso reale abbastanza per rivedere la finestra di dialogo senza **Electron**.
- **End-to-end**: **`checkImportExportAppConfigFlow`** esegue il flusso di configurazione dell'app di importazione/esportazione (esportazione, importazione a fasi, controlli **Action monitor** e relativi **Keybind**/effetti collaterali di stile dell'app) utilizzando percorsi fissi nel profilo isolato **Playwright**; **E2E** può saltare le finestre di dialogo dei file nativi per questi passaggi tramite una cucitura del processo principale che si applica solo quando **`TEST_ENV`** è **e2e**. Le stringhe di scelta rapida **Playwright** utilizzano **`helpers/playwrightHelpers_universal/faPlaywrightKeyboardChords.ts`** quindi le impostazioni predefinite seguono il modificatore **primary** dell'app e gli accordi che utilizzano la corrispondenza fisica dei tasti **Control** **macOS** e gli host **Windows** / **Linux**.
- **Test del componente**: **`DialogImportExportAppConfig.playwright.test`** copre Close-on-**Escape** per la finestra di dialogo di importazione/esportazione nel cablaggio **component**.

### Correzioni di bug e ottimizzazioni
- **Playwright** (componente, **E2E** e **Storybook** visual) utilizza il terminale **`list`** reporter: ogni test viene stampato sulla propria riga mentre l'esecuzione procede (report HTML invariati).
- **Menu principali delle app** (**AppControlSingleMenu**): dopo una riga **separatore** a larghezza intera nei dati del menu, il divisore sottile prima dell'elemento successivo non si sovrappone più al separatore completo (nessuna doppia regola orizzontale).
- **Importa/esporta la configurazione dell'app**: le finestre di dialogo di salvataggio e apertura del **sistema operativo** vengono impostate automaticamente sulla cartella **Download**, mentre l'esportazione suggerisce il nome file **faConfigExport.faconfig** in modo che il percorso offerto sia coerente quando un semplice nome file finirebbe altrimenti in una cartella inaspettata su alcuni sistemi.
- **App personalizzata CSS** finestra mobile: il frame **Transition** utilizza un'animazione **fa-floatingWindowPop** personalizzata (circa da **0,1** a **1** scala più opacità, stessi **300 ms** di **QDialog** predefinito e andamento standard) invece di **q-transition--scale** standard, che può lasciare la radice a una **trasformazione** degenerata in **Electron** e interruzione **Monaco** nei controlli di visibilità **Playwright**.- **App personalizzata CSS** finestra mobile: le maniglie di ridimensionamento dei bordi e degli angoli mostrano un gradiente morbido **primario** verso il bordo esterno solo mentre passi il mouse o trascini; le maniglie inattive rimangono invisibili. Il bagliore si dissolve in entrata e in uscita con una breve transizione di opacità. I colori degli handle sono token del tema nelle variabili **quasar**; le regole risiedono in **`_FaFloatingWindowFrameResizeHandles`** **`styles`** in modo che il file **Vue** rimanga entro i limiti di riga.
- **Finestre mobili**: il ridimensionamento del puntatore aggiorna la larghezza e l'altezza del riquadro al massimo una volta per riquadro di visualizzazione, eliminando il lavoro di layout ridondante per **Monaco** e il resto della superficie mentre trascini una maniglia.
- **App personalizzata CSS** finestra mobile: il testo del titolo non forza più **`margin: 0`**, quindi l'intestazione mantiene la spaziatura normale accanto alla riga di trascinamento e all'icona della guida.
- **App personalizzata CSS**: mentre l'editor mobile è aperto, le modifiche **Monaco** vengono applicate in tempo reale al foglio di stile globale **`#faUserCss`** in modo da poter visualizzare in anteprima le regole prima di salvare. **Chiudi senza salvare** ricarica **CSS** dal disco (stesso bridge letto all'avvio) in modo che l'app ritorni all'ultimo file persistente.- **App personalizzata CSS** finestra mobile: **Layout** non limita più la larghezza a **1400 px**. **CSS** utilizza **`max-width: stretch`** invece di **`100vw`** in modo che la cornice rimanga all'interno del layout visibile quando si applicano barre di scorrimento o inserti. **`<Transition>`** avvolge la radice del frame, non **Teleport** (quindi entra/esci da run su un singolo elemento e **Vue** non avvisa sui figli di transizione non-elemento); apri/chiudi usa **fa-floatingWindowPop** (vedi **Bugfix**). **_FaFloatingWindowBodyTeleport** invia il frame a **`document.body`** quindi **Storybook** e i test non dipendono da una destinazione di montaggio **`#q-app`**. La riga del titolo utilizza una flessibilità **altezza minima** in modo che la striscia di trascinamento mantenga un vero bersaglio da colpire. Le superfici mobili utilizzano una fascia **z-index** sotto la cromatura principale. Dopo la chiusura, **Monaco** elimina solo dopo la transizione della finestra di dialogo; uno smaltimento in sospeso viene annullato se si riapre o si chiude nuovamente prima del termine del ritardo.- Script **Playwright** **yarn**: **`scripts/playwrightWithArtifactTrim.mjs`** trasforma **`--component=bucket/ComponentFolder`** in **`src/components/...`** quando **Yarn** su **Windows** non espande i segnaposto di configurazione **npm** e rimuove **test-results/playwright-artifacts** dopo un'esecuzione quindi gli alberi allegati duplicati non vengono lasciati sul disco.
- **Cablaggio app personalizzata CSS** **directInput** (cablaggio e **Storybook**): il **`directInput`** osservatore prop viene eseguito **immediatamente** durante la configurazione, quindi **`WindowAppStyling`** si apre quando **`directInput`** è già **WindowAppStyling** quando gli argomenti si idratano.
- **End-to-end** **`checkAppStyling`**: i controlli di disponibilità e chiusura utilizzano il localizzatore **`windowAppStyling-frame`** invece di fare affidamento solo sulla visibilità del titolo dopo l'apertura della finestra.- **Electron protocollo renderer**: le build pacchettizzate ora caricano il renderer tramite uno schema personalizzato privilegiato **`app://`** registrato nel processo principale (**standard**, **secure**, **supportFetchAPI**, **stream**, **codeCache**, **bypassCSP**) invece di **`file://`**. Il gestore risolve la root del renderer accanto al bundle principale in esecuzione, rifiuta l'attraversamento del percorso e i segmenti **`..`** e ricade attraverso **`index.html`**. Questo è ciò che consente alla finestra **App personalizzata CSS** di avviare il suo lavoratore **Monaco** (**css.worker** caricato con le importazioni **Vite** **`?worker`**) in modo affidabile nell'app in pacchetto, poiché i lavoratori web non vengono inizializzati allo stesso modo in **`file://`**. Il flusso del server di sviluppo è invariato.- **Abbinazioni di tasti globali**: le impostazioni predefinite integrate su **Windows** e **Linux** sono ora **Ctrl + F12** per **Attiva/disattiva strumenti per sviluppatori**, **Ctrl + Alt + Shift + L** per **Apri impostazioni app**, **Ctrl + Alt + Shift + K** per **Apri impostazioni combinazione tasti**, **Ctrl + Alt + Shift + J** per **Apri app personalizzata CSS** e **Ctrl + Alt + Shift + G** per **Apri Guida alla ricerca avanzata** (su **macOS**, **Comando** sostituisce **Ctrl** dove l'accordo utilizza il modificatore primario; **Apri Guida alla ricerca avanzata** utilizza sempre **Ctrl**). Le sostituzioni salvate esistenti nell'archivio principale **Electron** rimangono invariate.
- Le finestre di dialogo **Documento Markdown** aperte con **directInput** (ad esempio **licenza** o **changeLog** dai menu) ora registrano la stessa **protezione dello stack delle finestre di dialogo Markdown** delle aperture indirizzate, quindi le **Impostazioni app** e le finestre di dialogo di altri componenti non possono impilarsi in primo piano finché non si chiude il foglio Markdown.- L'avvio dei **link esterni** collega un piccolo **`faExternalLinkMouseDispatch`** helper in modo che la logica dei clic e degli auxclick rimanga testabile nel **Node** Vitest (inserito **`getBrowserWindow`**) mentre la produzione legge ancora **`globalThis.window`**. Il percorso no-op del gestore viene eseguito quando manca **`faExternalLinksManager`** o i suoi metodi, quindi i rendering non **Electron** non generano eccezioni.
- **Combinazioni di tasti globali**: se il caricamento delle scorciatoie dal processo principale **Electron** fallisce, l'app mostra una notifica **Quasar** con un breve messaggio di **errore di caricamento** invece della sola registrazione; lo stesso messaggio appare se **Strumenti → Impostazioni combinazione tasti** non riesce ad aggiornarsi all'apertura.
- **Component test cablaggio**: il JSON **COMPONENT_PROPS** non valido da **`extraEnvVariables`** viene ignorato (**false**) invece di mandare in crash il gestore **IPC** del processo principale.
- **Storybook** regressione visiva: **`yarn test:storybook:visual`** ora fallisce quando l'iframe di una storia non mostra mai il contenuto renderizzato nella root Storybook (e nessuna finestra di dialogo o menu portaled visibile), a meno che la storia non venga disattivata con **`skip-visual-render-check`** o il suo **id** sia elencato in **`EXCLUDED_STORY_IDS`** nella specifica visiva Playwright.- **Documenti dei contributori**: **README**, **AGENTS.md**, **CONTRIBUTING**, **testing-terminal-isolation**, **storybook-stories**, **git-conventional-commits**, **fantasia-dev-setup**, **fantasia-testing** e **fantasia-changelog-en-us** rimangono allineati su **Yarn** **1.x** e CI **1.22.19**, **i18n** **componenti** bucket inclusi **dialoghi**, **testbatch:verify** livelli di copertura (incluso **i18n**), repo-root **helper** solo per codice di cablaggio, le stesse regole VRT, testo **Husky** corretto **`yarn lint:commit --edit`** e collegamenti relativi a repo-root nella funzionalità changelog.
- **Documenti del collaboratore**: **README**, **AGENTS.md**, **playwright-tests**, **testing-terminal-isolation**, **fantasia-testing** e **fantasia-keybinds** documentano le regole **`keyboard.press`** tra sistemi operativi per le combinazioni di tasti delle app e puntano a **`helpers/playwrightHelpers_universal/faPlaywrightKeyboardChords.ts`**.- **Flusso di lavoro del collaboratore**: **AGENTS.md** e cursore **regole** / **competenze** (**changelog-en-us**, **git-conventional-commits**, **testing-terminal-isolation**, **fantasia-changelog-en-us**, **fantasia-i18n**, **fantasia-testing**, **storybook-stories**) descrive la piegatura **`changeLog.md`** nello stesso batch di commit del lavoro sostanziale prima del **push**, utilizzando i commit del solo log delle modifiche solo per correzioni rare e mantenendo Git o le frasi di pulizia fuori dalle note di rilascio in-app.
- L'acquisizione di **Impostazioni associazione tasti** considera i tasti modificatori fisici (**ShiftLeft**, **ControlRight**, **AltLeft** e simili) come input incompleto invece di associarli a etichette modificatrici duplicate e accetta scorciatoie costituite da lettere, cifre, **F1–F24**, i quattro tasti freccia e un set fisso di tasti di punteggiatura di digitazione (virgola, parentesi, barra e simili) - non tasti solo tastierino numerico o la maggior parte degli altri tasti speciali. Queste linee guida utilizzano il campo **suggerimento** (stile informativo); **Scorciatoia duplicata** su **Imposta** viene ancora visualizzata come errore di campo.- Il popup di acquisizione **Impostazioni combinazione di tasti** si allinea con **legacy Fantasia Archive (FA 1)** perfezionamento dell'acquisizione: il nome **azione** come titolo, un segnaposto **Digita la combinazione di tasti**, una sottolineatura **sottile tratteggiata** sotto la linea dell'accordo (**rosso** solo per conflitti duplicati, tinta attenuata quando si applicano suggerimenti di **modificatore** o **tasto non supportato**) e **un pannello compatto direttamente sotto** la riga di accordi il cui **il testo si scambia** tra la guida **necessario modificatore**/**tasto non supportato** e il messaggio di **scorciatoia duplicata** a seconda di ciò che è appena accaduto (tono di **errore** e ruolo di **avviso** solo per il caso duplicato). Il pannello **svanisce e scorre** in quello slot invece di impilare due aree di messaggio separate. L'icona di **avviso** di conflitto rimane accanto alla riga dell'accordo quando è attivo un duplicato; la vecchia icona **info** autonoma per i suggerimenti è scomparsa. Il suggerimento **aiuto** elenca ancora i modelli di modifica e le chiavi accettate; **Cancella combinazione tasti** e **Imposta** rimangono sul pannello luminoso. Le pressioni basate sui soli modificatori fisici evitano comunque di dipingere il segnaposto in rosso; una sola pressione del modificatore dopo **è necessario un modificatore** o una guida tramite tasto non supportata può cancellare nuovamente il suggerimento.- **Impostazioni combinazione di tasti** cattura **Cancella combinazione di tasti** imposta l'override di funzionamento di quell'azione su **null** in modo che la colonna **Abbinazioni di tasti utente** della tabella principale corrisponda immediatamente di nuovo all'impostazione predefinita incorporata (**Aggiungi nuovo...** quando quell'impostazione predefinita è ciò che è attivo, stessa regola delle sostituzioni persistenti **null**), chiude il foglio di acquisizione e rimane disabilitato mentre la linea dell'accordo mostra solo il segnaposto vuoto (nessun testo di scelta rapida ancora).
- **Impostazioni di associazione tasti** cattura contrassegna una scorciatoia duplicata non appena l'accordo viene riconosciuto, utilizzando l'associazione **attiva** di ciascuna riga (predefinite più sostituzioni non salvate). Un'impostazione predefinita che non si applica più perché è stata ripristinata un'altra azione non blocca il riutilizzo. Il messaggio duplicato è più chiaro in **English**, **Deutsch** e **Français**; un tasto modificatore vagante dopo un conflitto non cancella più solo l'avviso. Mentre viene mostrato l'errore duplicato, la riga di cattura rimane sulla scorciatoia che era visibile quando hai aperto la cattura (o rimane vuota quando quella riga non aveva scorciatoia) invece di mostrare l'accordo rifiutato; **Set** con un accordo in conflitto fa lo stesso.- L'acquisizione di **Impostazioni combinazione di tasti** si apre con la scorciatoia **corrente** della riga già mostrata (predefinite e sostituzioni personalizzate), elimina il controllo ridondante **Usa predefinito** dal foglio di acquisizione e nidifica il popup di acquisizione all'interno della finestra di dialogo principale di associazione dei tasti. Il foglio principale **Impostazioni combinazione tasti** rimane **persistente**; puoi chiudere il foglio di acquisizione con un **clic esterno** o **Escape** senza chiudere la finestra di dialogo principale. L'**Escape** sul foglio di acquisizione viene gestito nel listener di acquisizione in modo che chiuda sempre quel foglio invece di essere trattato come un tasto di scelta rapida non supportato.
- La finestra di dialogo **Impostazioni combinazione di tasti** utilizza una larghezza della carta di **1200 px** su finestre ampie (si restringe comunque su finestre strette), intestazioni di tabella **Abbinazioni di tasti utente** e **Abbinazioni di tasti predefinite** e il segnaposto del filtro **Abbinazioni di tasti filtro…**. Il controllo **Comandi da tastiera utente** **Aggiungi nuovo…** utilizza il contorno **primario-luminoso** mentre la scorciatoia attiva corrisponde all'impostazione predefinita incorporata o non è ancora stato assegnato nulla, e il contorno **grigio-7** con l'accordo formattato solo quando una scorciatoia personalizzata differisce da quella predefinita.- **Impostazioni combinazioni di tasti** **Combinazioni di tasti predefinite** mostra un **trattino em** quando l'accordo predefinito integrato non può essere espanso per la piattaforma corrente, invece di un testo formattato fuorviante.
- **Impostazioni combinazione di tasti**: la tabella di scelta rapida utilizza lo spazio verticale tra il titolo della finestra di dialogo e la riga di azione del piè di pagina e la sua altezza di scorrimento si aggiorna quando ridimensioni la finestra.
- **Impostazioni delle combinazioni di tasti**: la tabella delle scorciatoie elenca solo le azioni rapide reali dall'elenco delle combinazioni di tasti; le righe di anteprima del riempitivo solo di sviluppo vengono rimosse.
- **Impostazioni associazione tasti** cattura la descrizione comando della guida (**inglese**): riepilogo più chiaro delle chiavi consentite e **la tua chiave**** testo segnaposto negli esempi di combinazioni di modificatori.- Il filtro **Impostazioni associazione tasti** senza corrispondenze mostra lo stesso trattamento **ErrorCard** della ricerca **Impostazioni app** (mascotte in modalità **lettura**, titolo e testo di supporto da **vue-i18n**) tramite lo slot **q-table** **no-data** invece di una tabella vuota. La barra inferiore della tabella è nascosta solo mentre esistono righe, perché **Quasar** dipinge **no-data** all'interno di quella barra e **hide-bottom** la salta completamente quando è sempre attiva. **Lo scorrimento virtuale** su quella tabella si disattiva mentre l'elenco di righe filtrate è vuoto, quindi **Quasar** monta comunque lo slot **no-data** invece di lasciare un corpo virtualizzato vuoto. La cancellazione del campo filtro non genera più errori quando **Quasar** imposta **v-model** su **null** invece di una stringa vuota. Il campo del filtro viene reimpostato vuoto ogni volta che la finestra di dialogo viene nascosta, anche dopo **Salva impostazioni** o **Chiudi senza salvare**.- **Electron controllo ortografico**: la lingua del controllo ortografico di Chromium tiene traccia dello stesso **lingualCode** persistente dell'interfaccia (**en-US**, **fr** o **de**) e l'etichetta del menu contestuale **Aggiungi al dizionario** corrisponde a tale scelta anziché alle impostazioni locali del sistema operativo. **Français** e **Deutsch** non ricadono più in **English** quando l'elenco disponibile di Chromium omette le esatte chiavi hunspell che cerchiamo per prime; abbiniamo i tag **fr-*** / **de-*** nell'ordine dell'elenco, quindi proviamo i codici brevi **fr** / **de** quando necessario. Dopo un cambio di lingua, la **sessione** principale disattiva il controllo ortografico, cancella l'elenco dei dizionari precedente, imposta la lingua risolta e riattiva il controllo ortografico. Il controllo **lingua** della barra del titolo può mostrare un'azione opzionale di **ricarica** dopo aver scelto una lingua di interfaccia diversa in modo da poter aggiornare tu stesso la finestra se il controllo ortografico in un campo già aperto segue ancora il dizionario precedente (Chromium a volte mantiene la vecchia associazione fino alla navigazione).
- Controllo della **lingua della barra superiore**: le stringhe **Deutsch** e **Français** per il suggerimento opzionale di **ricarica** del controllo ortografico (**aria-label** e tooltip multilinea) rispecchiano la dicitura **English** e le interruzioni di riga.- **Flag della barra della lingua** e icone della barra degli strumenti **social**: le risorse **public/** (ad esempio **countryFlags** SVG e **images/socialContactButtons**) ora si risolvono con le stesse regole **Vite** **BASE_URL** del resto del renderer, utilizzando una base **relativa** quando l'app viene eseguita da **file://** nel pacchetto **Electron**, in modo che flag e icone vengano caricati invece di interrompersi su root-absolute **/** URL.
- Finestra di dialogo **Informazioni su Fantasia Archive**: testo in **tedesco** per il titolo, la riga della versione, **Chiudi** e la riga del collegamento social (**Patreon**, **Ko-Fi**, **Sito Web**, **GitHub**, **Discord**, **Reddit** tooltip, **X**). **Francese** aveva già finestre di dialogo e stringhe di pulsanti; l'albero delle impostazioni locali ora registra anche **`socialContactButtons`** sotto la stessa chiave camelCase di **English**, quindi tali etichette si risolvono in **Français** invece di ricadere.
- **Menu Lingua**: dopo aver scelto una nuova lingua dell'interfaccia, il toast **impostazioni salvate** utilizza la **nuova** lingua invece di quella precedente.
- **Menu Lingua**: l'etichetta dell'opzione **en-US** è **Inglese, USA** (era **Inglese**) in ogni lingua dell'interfaccia.- **Menu principali dell'app** (**Progetto**, **Documenti**, **Strumenti**, **Guida e informazioni**): le etichette ora seguono la locale **vue-i18n** attiva quando si cambia lingua (sono state risolte una volta all'avvio e sono rimaste nella lingua iniziale). **AppControlSingleMenu** rilegge anche **titolo** e **elementi** quando l'elemento principale trasmette i dati del menu aggiornati.
- **Controlli finestra**: l'interruttore **massimizza**/**ripristina** della barra del titolo personalizzata salva i limiti della finestra prima di **massimizzare** e li riapplica dopo **unmaximize**, quindi il ripristino da uno stato ingrandito ritorna alla dimensione precedente su **Windows** con una finestra **Electron** senza cornice, dove **unmaximize** da solo potrebbe lasciare la finestra alle dimensioni massimizzate.
- **Electron** **preload** bridge: **Window** chrome, **DevTools**, la stringa **version** nel pacchetto e l'istantanea **extraEnvVariables** del cablaggio utilizzano **async** **IPC** tramite **`ipcMain.handle`** e **`ipcRenderer.invoke`** invece di bloccare i canali sincroni, quindi il **renderer** non si blocca su tali chiamate.- **Avvio dell'app e test dei componenti**: **`faRoutingEnv`** l'avvio carica l'istantanea del cablaggio, quindi **attende** il routing di avvio in modo che **`router.push`** termini prima del rendering della shell. **`ComponentTesting`** risolve reattivamente l'**SFC** testato dal percorso e avvia **COMPONENT_PROPS** da **`extraEnvVariables.getCachedSnapshot`** quando l'avvio ha già risolto **`getSnapshot`**; **`AppControlMenus`** legge **`TEST_ENV`** allo stesso modo per il primo disegno. **Precarica** **`extraEnvVariables`** aggiunge **`getCachedSnapshot`** dopo la prima risoluzione riuscita di **`getSnapshot`**. **ComponentTestingLayout** ora carica le **impostazioni utente** persistenti dal main sul montaggio in **Electron** e riapplica **vue-i18n** da **lingualCode**, corrispondente a **MainLayout** in modo che un ricaricamento dell'intera finestra durante l'esecuzione di componenti isolati mantenga la lingua dell'interfaccia scelta.
- **Pulsanti finestra globale**: i nomi accessibili sono disponibili solo su **`aria-label`**; **Quasar** i tooltip e l'attributo **`title`** sono stati rimossi dai pulsanti della barra del titolo.- **Impostazioni app** ricerca senza corrispondenze: lo stato vuoto **ErrorCard** mostra l'intestazione **Nessuna corrispondenza di ricerca** e una guida più lunga da **vue-i18n** come **dettagli** sotto la mascotte (**en-US** e **fr**). **ErrorCard** espone **dettagli** opzionali solo lì, con gli hook **data-test-locator** **errorCard-title** e **errorCard-details**; **Errore non trovato** mantiene solo il titolo e i **dettagli** su più righe.
- **Storybook** **`externalFileLoader`** registra fittizi **dialogs.markdownDocument** da **L_markdownDocument** in modo che le storie di **DialogMarkdownDocument** risolvano l'etichetta **Close** e le relative stringhe invece delle chiavi grezze.
- **Storybook** **`externalFileLoader`** registra mock **dialogs.actionMonitor** in modo che le storie di **DialogActionMonitor** risolvano titoli, intestazioni di colonna ed etichette di pulsanti anziché chiavi di traduzione non elaborate.
- **Storybook** visual capture chiama **page.waitForFunction** con **unfined** come parametro dell'argomento iniettato prima dell'oggetto opzioni in modo che Playwright applichi il timeout di rendering invece di attendere senza una scadenza interna.- **Storybook**: la configurazione del collaboratore **.storybook-workspace** disabilita l'elenco di controllo di onboarding della barra laterale, disattiva le notifiche **Novità** nelle opzioni **core** e **manager-head** nasconde il punto verde non letto sull'attrezzatura delle impostazioni per **yarn storybook:run** e build statiche.
- **Impostazioni app** Playwright gli aiutanti delle aspettative importano **dialogAppSettingsTreeOrdering** invece di **dialogAppSettingsTree** in modo che **Node** non carichi **app/i18n** markdown importazioni grezze durante la risoluzione del grafico del modulo specifiche.
- **FantasiaMascotImage** centra la grafica orizzontalmente quando la bitmap è più stretta del suo slot (**margin: auto** sulla parte interna **img**).- **Collegamenti esterni**: gli URL **http** e **https** vengono convalidati con un'analisi URL reale e una lista consentita dello schema **http** / **https** prima che **openExternal** venga eseguito nel main o che l'helper precaricato **checkIfExternal** venga eseguito nel renderer. Le destinazioni di loopback e non instradabili vengono bloccate utilizzando l'host analizzato (**localhost**, nomi host che terminano con **.localhost**, **127.0.0.0/8**, **::1**, loopback mappato IPv4, **0.0.0.0**), **localhost** con codifica percentuale viene ancora riconosciuto e i trucchi dello schema come **nothttps://** non passano più come **https**. I nomi host che contengono semplicemente le lettere **localhost** all'interno di un'etichetta (ad esempio **notlocalhost.example**) rimangono consentiti quando l'host analizzato non è loopback.
- **Impostazioni utente**: il processo principale **Electron** convalida ogni patch di salvataggio con **Zod** prima di unirsi a **electron-store**, accettando solo chiavi conosciute (**boolean** toggles e **lingualCode** come **en-US** / **de** / **fr**) e rifiutando campi sconosciuti o digitati in modo errato. L'archivio **S_FaUserSettings** Pinia mostra la notifica di errore di salvataggio esistente quando **setSettings** fallisce (inclusi errori di convalida o bridge) invece di registrare solo un rifiuto non gestito.- La versione fornita è ora **2.4.11** in **Informazioni** e metadati del pacchetto.
- **Documenti dei contributori**: **README**, **AGENTS.md**, regola **electron-preload** e **fantasia-electron-main** / **fantasia-electron-preload** descrivono le competenze **renderer sandbox** (**BrowserWindow** **webPreferences**), **Electron** sandbox ufficiale e collegamenti di isolamento del contesto, **main** **IPC** privilegiato (harness **extraEnvVariables**, collegamenti **http** / **https** esterni), **startApp** registrazione dei gestori **ipcMain** prima della prima finestra, una preferenza **async-first** per **invoke** / **handle** rispetto a **IPC** sincrono quando esiste un progetto asincrono ragionevole, quando convalidare i payload strutturati **IPC** con **Zod** in main rispetto a **typeof** più controlli di dominio e quali gestori rimangono solo scalari. Brevi commenti su **precarico elettronico** e **elettrone principale** fanno eco agli stessi guardrail.- **Flusso di lavoro del collaboratore**: file **Vitest**, specifiche dei componenti **Playwright** e storie **Storybook** ora si trovano nelle directory **`_tests/`** in **`src`**, **`src-electron`**, **`helpers`**, **`i18n`** e **`scripts/_tests`** accanto ai moduli di script estratti dove utilizzati (il precedente nome del segmento di cartella era **`test`**). **Vitest** la copertura esclude, **Storybook** i glob di scoperta delle storie e la guida del collaboratore (**README**, **AGENTS.md**, regole e competenze del cursore) seguono il nuovo percorso. **`e2e-tests`** e **`.storybook-workspace/visual-tests`** di livello superiore sono invariati.
- **Impostazioni app**: mentre il campo di ricerca dell'intestazione contiene testo, la tela oscurante copre nuovamente l'intera **scheda di dialogo**; il titolo, il corpo scorrevole, il campo di ricerca e le azioni del piè di pagina rimangono utilizzabili laddove il loro ordine di sovrapposizione li mantiene sopra l'overlay.
- L'implementazione di **Impostazioni app** è suddivisa in sottocomponenti **Vue** più piccoli e moduli **script** raggruppati in modo che la shell di dialogo rimanga entro i limiti di riga imposti senza modificare il layout visibile.- **Manutenzione dei contributori**: i contratti **TypeScript** condivisi per bridge, finestre di dialogo, dati di associazione di tasti, impostazioni utente e relativi helper sono raggruppati nel repository-root **`types/`** con un breve **JSDoc**, sostituendo molti file di tipo monouso e collocati **`*.types.ts`** accanto a **`src/`**, **`src-electron/`** e **`Storybook`** mock. **Impostazioni associazione tasti** e **Impostazioni app** il codice **`scripts/`** condiviso viene unito in meno moduli ciascuno per lo stesso comportamento di runtime e una navigazione più semplice rispettando i budget di linea **ESLint**. **README**, **AGENTS.md**, le regole del cursore e le abilità descrivono la preferenza di **meno moduli raggruppati logicamente** **`scripts/`** e **`src/scripts/`** finché i limiti di riga non forzano una divisione e il consolidamento di **`scripts/_tests/`** co-localizzati quando gli script di produzione si uniscono. Le directory **`tests/`** vuote rimaste dalla ridenominazione di **`_tests/`** sono state rimosse dall'albero.- **Strumenti del collaboratore**: **ESLint** applica limiti al numero di righe sulla produzione **Vue**, **TypeScript** e **JavaScript** (test, **Storybook**, configurazioni, **i18n**, moduli di solo tipo e percorsi simili rimangono esenti). **README**, **AGENTS.md** e le regole del cursore descrivono le maiuscole (**Vue** SFC **250** righe, **TypeScript** modulo **200** righe, funzione **100** righe) e quando suddividere in **`script/`** o sottocomponenti. I valori letterali oggetto restituiti nel codice di produzione devono elencare solo **associazioni precedenti** per i valori delle proprietà (nessuna funzione inline o logica ternaria nel valore letterale); vedere **`.cursor/rules/code-size-decomposition.mdc`**. Lo spostamento dell'ambito **CSS** in un file autonomo e la sua reimportazione rimane un'eccezione che necessita dell'approvazione esplicita del manutentore.- **Manutenzione del collaboratore**: le superfici di produzione **`return`** che utilizzano valori letterali oggetto (inclusi **action manager** modellazione del payload della coda, helper di associazione di tasti, costruttori di elementi **AppControlMenus**, **DialogActionMonitor** / **Impostazioni di associazione di tasti** / **Impostazioni app** helper di tabella, **Electron** **IPC** generatori di istantanee e **Playwright** **opzioni di `recordVideo`**) ora si associano **`const`** vengono prima i valori, quindi le proprietà dell'oggetto restituite rimangono identificatori o valori letterali semplici. Le implementazioni di **Action Manager** risiedono in **`src/scripts/actionManager/faActionDefinitionHandlers.ts`**, con **`faActionDefinitions.ts`** che contiene solo la **tabella di registro**; **AGENTS.md** e la regola/abilità cursore **fa-action-manager** descrivono la suddivisione.- Flusso di lavoro **GitHub Actions** manuale **Build App** (**.github/workflows/build.yml**): il lavoro **Windows** viene eseguito **yarn testbatch:ensure:nochange** end-to-end (stesso controllo di qualità delle esecuzioni della suite completa etichettate **PR**, più una build di produzione **Quasar** **Electron** build riutilizzata per **Playwright** e **Storybook** controlla), quindi carica il programma di installazione **Windows** senza una seconda build di pacchetto. **Linux** i lavori iniziano solo dopo che il gate ha esito positivo e vengono eseguiti come una matrice in modo che ogni gamba carichi il proprio set di artefatti: **AppImage**, pacchetto **Debian** e **RPM**. **macOS** esegue ancora una build di produzione e carica il file **.dmg**. **Storybook** le linee di base visive rimangono allineate con **win32**, quindi il gate completo rimane su **windows-latest**.
- **Documenti del collaboratore**: **README**, **AGENTS.md**, regola **vue-quasar** e competenza **fantasia-quasar-vue** documentano un layout a file singolo **Vue** per ogni file **.vue**: prima **template**, poi **script**, quindi **style** (inclusi gli SFC dell'area di lavoro **Storybook**). **Impostazioni app** I componenti helper sono stati riordinati nell'origine per corrispondere; il comportamento visibile è invariato.- **Manutenzione del collaboratore**: gli helper trasversali **Vue** per il corpo della finestra mobile **Teleport**, gli handle di ridimensionamento della finestra mobile e l'iniettore globale **`#faUserCss`** utilizzano la cartella **`_<PascalCase>`** e i nomi **`.vue`** (**`_FaFloatingWindowBodyTeleport`**, **`_FaFloatingWindowFrameResizeHandles`**, **`_FaUserCssInjector`**) in modo che vengano letti come infrastruttura accanto alle superfici **`Window*`** e **`Global*`**; **AGENTS.md** limita il prefisso a questi tre. Le note precedenti in questo registro che nominano quei pezzi ora usano la stessa ortografia. Il comportamento dell'app è invariato.- **Manutenzione del collaboratore**: gli helper del renderer **TypeScript** sono raggruppati in **`src/scripts/appInternals/`** (helper per l'applicazione locale, digitazione della lingua, percorsi delle risorse pubbliche, **Storybook** rilevamento canvas) e **`src/scripts/appGlobalManagementUI/`** (markdown e cablaggio dello stack di dialogo dei componenti, routing della mascotte del test dei componenti, attivazione/disattivazione degli strumenti di sviluppo, routing di avvio, notifiche curiosità) con nomi esportati più chiari rispetto ai precedenti punti di ingresso in stile **lease**. **Tastiera globale** pre-elaborazione e invio in tempo reale nei moduli **`src/scripts/keybinds/faKeybinds*`** che **`MainLayout`** compone tramite **`createFaKeybindKeydownHandler`**. Il modulo helper **precarica** dei collegamenti esterni è ora **`faExternalLinksManagementAPI`** (comportamento invariato; nome allineato con altre API di gestione).
- **Storybook** workspace **Vite** aggiunge **`server.fs.allow`** per la root del repository in modo che le **Impostazioni keybind** **`.vue`** file possano risolvere le importazioni di contratti **`types/`** fratelli durante build statiche e acquisizioni visive.- **Storybook** **regressione visiva statica** (**`yarn test:storybook:visual`**): **Vite** risolve **Pinia** e **Vue** dalla radice del repository in modo che l'area di lavoro nidificata non carichi una seconda copia **Pinia** che impediva il montaggio delle storie di dialogo nell'**iframe**. Il cablaggio **Playwright** serve la build statica su una porta dedicata e non riutilizza un server esistente lì, quindi una sessione locale **`yarn storybook:run`** sulla porta predefinita non può soddisfare le acquisizioni accidentali. Le acquisizioni non riuscite allegano comunque uno screenshot dell'intera pagina e un breve sondaggio **JSON** della root **Storybook** quando la navigazione, i controlli di rendering, i caratteri, le sovrapposizioni o il passaggio dello screenshot non riescono.
- **Manutenzione del collaboratore**: **Vitest** specifiche per **AppControlMenus**, **AppControlSingleMenu**, **GlobalLanguageSelector** (incluso il percorso di ritardo dell'aggiornamento del controllo ortografico), **GlobalWindowButtons**, **ErrorCard**, **Impostazioni app** blocchi di impostazione e schermate espanse delle impostazioni di **dialogo** che esercitano i rami del cablaggio della barra superiore, la selezione della lingua, i timer di Chrome delle finestre e **Quasar** percorsi di clic del menu in jsdom. Le specifiche della finestra di dialogo multi-stub utilizzano la stessa eccezione **ESLint** a livello di file già applicata ai test di cablaggio di protezione dello stack.- **Manutenzione del collaboratore**: maggiore copertura **Vitest** jsdom per **DialogActionMonitor** aggiornamento della finestra di dialogo, **DialogImportExportAppConfig** apre e importa staging, **DialogKeybindSettings** quando l'elenco delle definizioni di associazione di tasti è vuoto, **_FaFloatingWindowFrameResizeHandles** passaggio del mouse e ogni destinazione del puntatore di ridimensionamento, descrizione comando dell'editor **WindowAppStyling** contenuto più caricamento, sovrapposizioni di errore di caricamento e cablaggio del puntatore del frame, aggiornamento dello stile dell'app **MainLayout** quando il bridge **Electron** lo espone o meno, trigger del menu markdown per il test dei componenti **AppControlMenus**, righe del sottomenu **AppControlSingleMenu** senza gestori di clic, **GlobalLanguageSelector** quando il bridge delle impostazioni utente è assente e **_FaUserCssInjector** salto del testo di stile duplicato scrive.

## 2.4.10 - Impostazioni dell'app, ErrorCard, token del tema e strumenti del collaboratore

### Nuove funzionalità
- **Strumenti → Impostazioni app** apre la finestra di dialogo **Impostazioni app** (persistente fino a **Chiudi**; **Salva** è riservato per i collegamenti successivi).
- **Impostazioni app** elenca ogni valore booleano persistente come **categoria → sottocategoria → attiva/disattiva** righe con copia **en-US** / **fr** (titolo, descrizione, **nota** rossa opzionale). Le schede **Categoria** e **sottocategoria** seguono l'ordine chiave tranne **Accessibilità** (sempre penultima) e **Impostazioni sviluppatore** (sempre l'ultima); **commuta** all'interno di una sottocategoria ordinata per **titolo** tradotto (le legature di valore utilizzano il tasto di impostazione). **Mostra ID documento** si trova in **Impostazioni sviluppatore → Corpo del documento**. Una scheda **Accessibilità** di livello superiore raggruppa barre di scorrimento più ampie, divisori per il conteggio degli alberi, occultamento barrato e ombreggiatura del testo; il tema e gli elementi correlati rimangono in **Visualizzazioni e funzionalità a livello di app** senza nidificare il blocco lì.
- Aggiunte **impostazioni utente** persistenti nel processo principale **Electron** (**electron-store** nell'app **userData**), iniziando con una preferenza **tema** (**light** o **dark**, default **light**), esposta al renderer come **`window.faContentBridgeAPIs.faUserSettings`** (**get** / **set**).- Aggiunto l'archivio **`S_FaUserSettings`** Pinia per la gestione dello stato delle impostazioni utente nel renderer: carica le impostazioni complete impostate una volta all'avvio dell'app tramite il bridge IPC, accetta aggiornamenti basati su patch e mostra una notifica di successo o fallimento **Quasar** dopo ogni tentativo di salvataggio.
- **Impostazioni app** utilizza schede di **categoria** verticali (**`q-tabs`** / **`q-tab-panels`**) e una colonna principale **scorribile** (**`q-scrollarea`**): il **titolo**, la guida delle tabulazioni e **Chiudi** / **Salva** rimangono fissi mentre il contenuto scorre. Gli interruttori di sottocategoria utilizzano una griglia reattiva (**una**/**due**/**tre** colonne per punto di interruzione), posiziona l'**interruttore** sotto il titolo e mostra le descrizioni da un'icona di aiuto **tooltip**.
- **Impostazioni app** ricarica i valori di attivazione/disattivazione persistenti da **`S_FaUserSettings`** quando la finestra di dialogo si apre utilizzando una risoluzione protetta **Pinia** in modo che gli ambienti senza impostazioni utente attive memorizzino il salto dell'idratazione anziché l'attivazione. L'allineamento delle icone della Guida accanto ai titoli su più righe è stato spostato per un allineamento verticale più chiaro con la prima riga.- **ErrorCard** (**`src/components/elements/ErrorCard/`**): layout **q-card** riutilizzabile per stati di errore (**titolo**, **dettagli** multilinea opzionali, variante **FantasiaMascotImage**, **larghezza massima** configurabile); anteprima **Storybook** e **Vitest** collocate insieme. Il percorso **Errore non trovato** lo utilizza con la copia **`errorNotFound`** esistente invece di una scheda in linea e locale della pagina **SCSS**.

### Correzioni di bug e ottimizzazioni
- **Documenti del collaboratore**: **README**, **AGENTS.md**, **fantasia-testing**, **storybook-stories** e **testing-terminal-isolation** descrivono **Storybook** **VRT** **toHaveScreenshot** **maxDiffPixels** come un'immagine intera con budget di pixel diversi, perché **GitHub Actions** **windows-latest** può divergere leggermente dal **Windows** locale e quando aggiornare le linee di base anziché modificare il limite.
- **Storybook** regressione visiva: sollevate **Playwright** **toHaveScreenshot** **maxDiffPixels** per acquisizioni statiche **Storybook** in modo che **GitHub Actions** **windows-latest** superi **yarn test:storybook:visual** rispetto alle linee di base impegnate (caratteri e subpixel differiscono leggermente dalle tipiche esecuzioni locali **Windows**).
- **GitHub Actions** Il flusso di lavoro **OSV-Scanner** (**.github/workflows/osv-scanner.yml**) ora elenca **main** accanto a **master** su **push**, **pull_request** e **merge_group** in modo che le stesse scansioni vengano eseguite quando il ramo predefinito utilizza uno dei due nomi.- **Storybook** regressione visiva su **Windows**: aggiornati gli snapshot **Chromium** impegnati in **.storybook-workspace/visual-tests/** per **ErrorCard** (pagina di errore e ricerca impostazioni app vuote), lettura di **FantasiaMascotImage**, **MainLayout** con pagina **Index**, **ErrorNotFound** predefinito e **IndexPage** nel layout: allinea linee di base con anteprime attuali dopo la mascotte nativa **img** e gli hook di test del layout.
- **Electron** sicurezza: la **BrowserWindow** principale ora esegue il renderer **Chromium** con il **sandbox del sistema operativo** attivo (**`sandbox: true`**, **`nodeIntegration: false`**). I valori di cablaggio esposti come **`window.faContentBridgeAPIs.extraEnvVariables`** sono creati nel processo **main** e forniti tramite sincronizzazione **IPC**; l'apertura di collegamenti **http** o **https** esterni nel browser predefinito viene gestita in **main** (**`shell.openExternal`**) tramite **IPC** asincrono, quindi il precaricamento rimane entro i limiti **API** sicuri per sandbox. **Playwright** i percorsi di lancio utilizzano **`faPlaywrightElectronLaunchConstants`** in **`helpers/playwrightHelpers_universal/`** invece di importare moduli di solo precaricamento dal test runner di Node.- **Electron** IPC di sincronizzazione window-chrome: i gestori **processo principale** per **minimizzare**, **massimizzare**, **ripristina/attiva/disattiva** e **chiudi** ora impostano **`event.returnValue`** su ogni percorso. Senza di esso, **`ipcRenderer.sendSync`** non viene mai completato e il **renderer** rimane bloccato, il che ha bloccato l'intera interfaccia utente dopo la migrazione **@electron/remote**.
- Rimossa la dipendenza **@electron/remote**: chrome **window** frameless (**minimize**, **maximize**, **restore**, **close**, **maximized** state) e la stringa **app version**** mostrata in **Informazioni** ora utilizza l'**IPC sincrono del processo principale** dal precaricamento (**registerFaWindowControlIpc**, **registerFaAppDetailsIpc** in **mainScripts/ipcManagement/**), risolvendo la **BrowserWindow** di proprietà del mittente IPC in modo che il comportamento rimanga corretto quando i menu nativi rubano il focus del sistema operativo.
- Le interazioni **Window** chrome e **massimizza**/**ripristina** potrebbero sembrare leggermente più fluide nell'uso quotidiano dopo questa modifica: il **renderer** non si trova più dietro **@electron/remote** per tali azioni e la corretta gestione di **`event.returnValue`** impedisce a **sendSync** di bloccare il thread dell'interfaccia utente. La percezione esatta varia in base alla macchina e al carico di lavoro.- Aggiornato **axios** a **1.15.0** all'indirizzo **GHSA-3p68-rc4w-qgx5** (**CVE-2025-62718**): la corrispondenza **NO_PROXY** ora normalizza i nomi host (ad esempio **localhost** con un punto finale) e i valori letterali IPv6 tra parentesi in modo che le destinazioni di loopback elencate in **NO_PROXY** non vengano inviate tramite un proxy HTTP.
- **Impostazioni app** chiavi **booleane** persistenti in **electron-store** utilizzano identificatori interni corretti (**filtro relazione aggressivo**, opzione **collassa** albero, **dopo la selezione** chiusura con ricerca rapida).
- **Flusso di lavoro del collaboratore**: **README**, **AGENTS** e le regole e le competenze del cursore ora descrivono quando i manutentori possono saltare **`yarn testbatch:verify`** immediatamente prima di un commit che modifica solo il registro delle modifiche in-app per locale Markdown in **`i18n/*/documents/changeLog.md`**, a condizione che l'intero gate sia già passato alle ultime modifiche sostanziali e l'albero di lavoro non abbia altre modifiche in sospeso; in caso di dubbio, apri comunque il cancello.- **Documenti del collaboratore**: root **CONTRIBUTING.md** copre le etichette del fork **PR**, gli aggiornamenti locali **Storybook** **VRT** degli snapshot quando **CI** non può inviare le baseline ai fork, i limiti di sola lettura **`GITHUB_TOKEN`** sui flussi di lavoro del fork pubblico **`pull_request`** e le impostazioni delle **GitHub Actions** del manutentore per le approvazioni del fork; **README** aggiunge una sezione **Contributi**; il modello **pull request** collega **CONTRIBUTING.md**.
- **Repository** root **.gitignore** ora ignora **.playwright-mcp** (cartelle locali dell'area di lavoro dal server **Playwright MCP** utilizzate con **Cursor** e client simili; separate dall'output del test **Playwright** in **test-results**).
- **Playwright** suite seriali e allegati **WebM**: **`closeFaElectronAppWithRecordedVideoAttachments`** accetta un secondo **`TestInfo`** opzionale in modo che le registrazioni dello schermo si alleghino dove il rapporto **HTML** le elenca per i gruppi multi-test **`test.describe.serial`**; componente e le specifiche **E2E** utilizzano un lancio **Electron** per gruppo seriale con **`resetFaPlaywrightIsolatedUserData`** solo in **`test.beforeAll`** (**README** e **playwright-test** descrivono il modello). Vitest copre l'assistente **`drammaturgoElectronRecordVideo`** aggiornato.- La copertura del componente **Playwright** per **ErrorCard** esercita il controllo live (titolo, dettagli, **lettura** mascotte, larghezza massima della carta tramite **data-test-error-card-width**). Le indicazioni per i contributori (**AGENTS**, **playwright-tests**, **fantasia-testing**) preferiscono **data-test-layout-width**, **data-test-layout-height** e limiti di dominio per il dimensionamento dei contratti quando finestre strette ridurrebbero i risultati di **boundingBox**; utilizzare **boundingBox** per i pixel sottoposti a rendering solo quando il cablaggio garantisce spazio sufficiente.- **Storybook** **Components/foundation**: cataloghi in fase di progettazione in **`src/components/foundation/`** — **`FoundationColorPalette`** (**QUASAR COLORS - GENERAL** da **`quasar.variables.scss`** più il materiale predefinito **Quasar** **`bg-*`** stem) e **`FoundationTextList`** (**Quasar** tipografia: scala del tipo, pesi dei caratteri, helper di testo per **quasar.dev/style/typography**). Solo Canvas (**Documenti** disabilitati), **`skip-visual`** quindi **VRT** salta le pagine lunghe; pin globali della storia **Reimposta viewport** (iframe a larghezza intera). **`FoundationColorPalette`** root **SCSS** rimane trasparente, quindi la barra degli strumenti **Sfondi** (**carta** / **app scura**) dipinge la tela; le strisce di metadati dei campioni utilizzano lo sfondo scuro del catalogo da **quasar.variables.scss**.
- La barra degli strumenti del viewport **Storybook** aggiunge **Reimposta viewport** (iframe 100%) prima delle preimpostazioni phone-through-**4K** in **`viewportBreakpoints.ts`**; **`preview`** **`initialGlobals`** ora inizia con quella preimpostazione reattiva invece che con un frame del desktop fisso **1920px**.- **Storybook** regressione visiva su **Windows**: aggiornato lo screenshot di **Chromium** impegnato per **Components/dialogs/DialogAppSettings** predefinito in modo che corrisponda all'attuale **Reimposta viewport** predefinito e al frame **Storybook**.
- **Storybook** **Impostazioni app**: **`DialogAppSettings`** accetta **`directSettingsSnapshot`** opzionale (predefinito mirror **`FA_USER_SETTINGS_DEFAULTS`**) quindi **Documenti** e **Canvas** mostrano la categoria completa e attiva/disattiva la griglia senza **Electron**; un decoratore collocato unisce le stringhe **en-US** **`appOptions.*.title`** con i prefissi del trattino di staging **TODO** rimossi. Il mock dell'alias **Vite** per **`app/i18n/externalFileLoader`** implementa **`global.mergeLocaleMessage`** (unione profonda nei messaggi dello scenario) e **`global.te`**, corrispondendo a ciò che **`buildAppSettingsRenderTree`** e il decoratore si aspettano dalla produzione **vue-i18n**. Lo stub **Storybook** **`faContentBridgeAPIs`** espone **`faUserSettings`** con la stessa mappa predefinita, quindi l'idratazione **Pinia** funziona ancora quando una storia omette l'istantanea.- I percorsi del componente **Storybook** **`meta.title`** sono nidificati in **`Components`** per bucket del repository (**dialogs**, **elements**, **globals**, **other**) più il nome della cartella della funzione in modo che la barra laterale corrisponda a **`src/components/`**. Il selettore **`yarn test:components:list`** mostra le etichette in stile **`dialogs/DialogFoo`** e **`yarn test:components:single --component=`** prende lo stesso percorso relativo in **`src/components/`**. Aggiornate le **Storybook** linee di base visive impegnate e le voci **`EXCLUDED_STORY_IDS`** per la nuova forma **`id`** della storia; aumentato **`toHaveScreenshot`** **`maxDiffPixels`** a **220** nella suite visiva, quindi le esecuzioni di confronto tollerano un po' più di varianza di font o subpixel su finestre di dialogo di markdown dense senza mascherare grandi derive dell'interfaccia utente.- Catalogo temi **SCSS** (**`quasar.variables.scss`**): palette Quasar, globali, **`q-*` token di regolazione**, quindi sezioni per funzionalità; **`htmlAdjustments`**, **`scrollbar`**, **`quasarComponentsAdjustments`** e i blocchi **Vue** **`<style lang="scss">`** corrispondenti utilizzano variabili invece di valori letterali sparsi. Gli identificatori del tema **`$`** utilizzano solo segmenti separati da trattini (vedere **`project-scss.mdc`**). Rinominato il token di sfondo del codice **qMarkdown** in **` backgroundColor`** (è stato digitato in modo errato).
- **FantasiaMascotImage** esegue il rendering dell'arte della mascotte con un semplice **HTML** **img** (**object-fit: contiene**, **display: block**) invece di **Quasar** **q-img**, evitando il wrapper **q-img** e i salti del layout di caricamento che disturbavano i test mantenendo **width**, **height**, **alt**, **`max-width: 100%`** e **data-test** si aggancia all'immagine.
- **log delle modifiche** in-app e documenti dei contributori: riformulato un punto elenco **Playwright** e guida ampliata **vue-i18n** in modo che la prosa non venga analizzata come sintassi del segnaposto non valida (snippet API a forma di parentesi graffe e simili).- Ricerca **Impostazioni app**: **vue-i18n** segnaposto (**en-US** / **fr**). Con una query ritagliata non vuota, le schede verticali **categoria** rimangono visibili ma si attenuano dietro una breve dissolvenza **Animate.css** e non possono essere cliccate; la colonna di destra raggruppa le impostazioni **corrispondenti** in uno scorrimento (sottostringa senza distinzione tra maiuscole e minuscole su **titolo**, **tag** e descrizione comando **descrizione**; le righe non corrispondenti e le sezioni vuote vengono omesse; filtraggio in **`scripts/dialogAppSettingsSearch.ts`** con copertura Vitest), con le linee di sezione principali inserite come i separatori sotto ciascun blocco di sottocategoria. Quando non c'è corrispondenza, una carta centrata utilizza lo stesso bordo color crema **Error Not Found** e riempie come **`ErrorNotFound`**, con **FantasiaMascotImage** in modalità **lettura** e una stringa dedicata **searchNoResultsMessage**. Mentre la ricerca è attiva, la scheda senza risultati e la colonna dei risultati corrispondenti rimangono nel documento con la visibilità attivata (**v-show**) in modo che la mascotte di **lettura** possa caricarsi prima che venga mostrato lo stato vuoto. Il campo di ricerca mantiene una larghezza fissa quando **clearable** mostra la sua icona, rimane utilizzabile sopra il livello di oscuramento e la cancellazione non genera più errori quando il controllo imposta il modello su vuoto. Sottili sfumature ammorbidiscono la parte superiore ain fondo ai risultati in pila e lungo la striscia di ricerca mentre la ricerca è attiva; le regioni di scorrimento utilizzano un margine stabile della barra di scorrimento, quindi mostrare o nascondere una barra di scorrimento verticale non sposta lateralmente il layout. I nomi **SCSS** **BEM** per il wrapper di ricerca, i titoli adesivi e le azioni del piè di pagina corrispondono al modello, quindi si applicano le regole.
- **`rgbToHex`** e **`hexToRgb`** in **`src/scripts/_utilities/colorFormatConvertors.ts`**: **`rgbToHex`** ora mappa solo i primi tre canali numerici in modo che le stringhe in stile **rgba()** (ad esempio da **`getComputedStyle`**) non aggiungano un nibble alfabetico al risultato esadecimale, richiedono almeno tre corrispondenze prima della conversione e analizza il canale numeri interi con radice esplicita; **`hexToRgb`** ritaglia l'input ed elimina un **`#`** iniziale prima dell'analisi. La copertura colocalizzata **Vitest** esercita i percorsi aggiornati.
- **`toggleDevTools`** (**`src/scripts/appGlobalManagementUI/toggleDevTools.ts`**) catene opzionali attraverso il metodo **`toggleDevTools`** su **`window.faContentBridgeAPIs.faDevToolsControl`** in modo che un'implementazione mancante non generi un'eccezione quando il bridge è parzialmente stub (ad esempio nei test o **Storybook**).- Suite **Vitest** ampliate su **Electron** (**contentBridgeAPI**, **preload**, **mainScripts** inclusa la pulizia **userSettings** per snapshot persistenti nulli o parziali), **renderer** (**boot**, **stores**, **dialogs**, **app settings** scripts, **globals**, **pages**), test repo-root **i18n** e **aiutanti/aiutanti drammaturghi** in modo che le soglie di copertura a più livelli rimangano allineate con queste correzioni.
- **SocialContactButtons** Stile **pagina web**: **quasar.variables.scss** imposta lo sfondo del chip della pagina web su **$dark** invece di **$qCard- background** in modo che sia letto chiaramente sulla barra degli strumenti scura.
- **Impostazioni app**: i separatori di primo livello della riga di sottocategoria e della ricerca in pila utilizzano **`showNonLastSeparator`** e **`showNonLastTopCategorySeparator`** da **`scripts/dialogAppSettingsSearch.ts`** con copertura Vitest; il comportamento del layout corrisponde alla logica in linea precedente.- Spostato l'intero albero delle impostazioni locali **vue-i18n** da **`src/i18n/`** al repository-root **`i18n/`** (insieme a **`src/`**); le importazioni utilizzano **`app/i18n`** e **`app/i18n/...`**. **Vitest** **`unit-i18n`** (**`yarn test:coverage:i18n`**) applica la copertura **100%** sui file **`.ts`** con ambito **`i18n/`**; **README**, **AGENTS**, regole/competenze cursore, **Quasar** (**`@intlify/unplugin-vue-i18n`** include percorso) e gli alias **Storybook** sono stati aggiornati.
- I file dei messaggi locali **vue-i18n** utilizzano un prefisso del nome file **`L_`** (ad esempio **`dialogs/L_appSettings.ts`**) quindi **`T_`** rimane riservato per gli alias di tipo **TypeScript**; **Storybook** mock, **Electron** importazioni del controllo ortografico, test e documenti dei contributori utilizzano i nuovi percorsi.- Le impostazioni predefinite per la verifica locale sono più silenziose: la copertura **Vitest** utilizza il reporter terminale **agent**; Il componente **Playwright** e **E2E** utilizzano il reporter **list**; **Storybook** build statica e utilizzo smoke **quiet** flag CLI e più calmo **Vite** registrazione dell'anteprima di produzione; **Storybook** visual **Playwright** salta l'avanzamento della **console** per storia a meno che non sia impostato **FA_STORYBOOK_VISUAL_VERBOSE**; **`yarn testbatch:ensure:nochange`** e **`yarn testbatch:ensure:change`** invocano **`yarn quasar:build:electron:summarized`** in modo che il registro completo **Quasar** / **electron-builder** venga catturato in **test-results** quando una build fallisce. Le regole e le competenze **README**, **AGENTS** e Cursore descrivono gli script e i flag dettagliati di attivazione.- **Playwright** esegue (**`TEST_ENV`** **`components`** / **`e2e`**) utilizza **`%APPDATA%/<nome pacchetto>/playwright-user-data`** (ad es. **`fantasia-archive\playwright-user-data`**, non **`fantasia-archive-dev`**) quindi le **impostazioni utente** (**`electron-store`**) rimangono separate da **`quasar dev`** e profili pacchettizzati. Ogni componente e specifica **E2E** chiama **`resetFaPlaywrightIsolatedUserData()`** in **`test.beforeEach`** in modo che la cartella venga rimossa prima di ogni **`electron.launch`** e i test inizino dalle impostazioni persistenti predefinite. Il nome della cartella del profilo isolato è condiviso tramite **`src-electron/mainScripts/appIdentity/playwrightIsolatedUserDataDirName.ts`** (nessuna importazione **`electron`**) in modo che il ripristino lato nodo e Vitest rimangano al sicuro durante la raccolta dei test.- Risolto il problema con il percorso **Electron** **preload** del pacchetto in **`mainScripts/windowManagement/mainWindowCreation`**: il bundle del processo principale risolve **`import.meta.url`** nella directory principale condivisa del blocco (non nella cartella di origine **`windowManagement/`**). Risolvendo **preload** e **icon** da quella directory si carica nuovamente **`electron-preload`** correttamente, quindi **`window.faContentBridgeAPIs`** (incluso **`extraEnvVariables`** per **`TEST_ENV`** / **`COMPONENT_NAME`**) è disponibile; Il componente **Playwright** esegue il percorso verso **`/componentTesting/...`** invece di tornare alla pagina **Indice** predefinita.
- Suddivisione di **`src-electron/mainScripts/`** in cartelle di funzionalità: **`appIdentity`**, **`chromiumFixes`**, **`ipcManagement`** (**`register*Ipc`** gestori), **`nativeShell`**, **`userSettings`** e **`windowManagement`** — in modo che la registrazione **IPC**, le **impostazioni utente**, la creazione di finestre e le correzioni di **Chromium**-adiacenti abbiano percorsi di importazione stabili; Le importazioni **Vitest** e **Playwright**, **README**, **AGENTS** e le regole/abilità del cursore seguono il nuovo layout.- Documenti successivi dei contributori: **README** **Note sull'architettura** raggruppate con la risoluzione **`electron-preload`** in **`windowManagement/`**; **AGENTS** e **`electron-preload`** puntano **`faUserSettings`** **`electron-store`** a **`mainScripts/userSettings/`**; **`vitest-tests`** elenca ogni **`mainScripts/<area>/_tests/`** bucket; **`fantasia-electron-main`** mette in guardia contro un **`..`** extra quando si risolve il precarico da **`import.meta.url`** nelle build pacchettizzate.
- I moduli di cablaggio trasversali **Playwright** (**`playwrightElectronRecordVideo`**, **`playwrightUserDataReset`** e le relative suite Vitest) vivono in **`helpers/playwrightHelpers/`**; specs import **`app/helpers/playwrightHelpers/...`**. **Vitest** **`unit-helpers`** esegue **`helpers/**/*.vitest.test.ts`** tramite **`vitest/vitest.helpers.config.mts`**; Le configurazioni dell'area di lavoro condivisa Vitest risiedono sotto repo-root **`vitest/`** quindi il rigido cancello di copertura **`helpers/**/*.ts`** si applica solo ai pacchetti helper reali. I documenti dei contributori (**README**, **AGENTS**, regole/competenze cursore) descrivono **`helpers/`** per futuri pacchetti di cablaggio in modo che la root del repository rimanga per config, **`README`**, lockfile e **`scripts/`**.- I livelli di copertura **Vitest** sono documentati e cablati in **`vitest/`**: **100%** soglie v8 sui pacchetti **src-electron** e **helpers**; **100%** su tutti e quattro i parametri per i file **.ts** della voce repository-root **`i18n/`** in **unit-i18n** (**`specialCharactersFixer`**, registro **`index.ts`**, **`externalFileLoader.ts`** e moduli indice per locale **`en-US`**, **`fr`**, **`de`** — vedere **`vitest/vitest.i18n.config.mts`**); **100%** su tutti e quattro i parametri per il renderer **src** **.ts** in **unit-src-renderer** (**src/boot**, **src/scripts**, **src/stores** solo — **`specialCharactersFixer`** è stato rimosso con l'albero **`i18n/`**); **100%** su tutte e quattro le metriche per i file **.ts** uniti e colocalizzati in **src/components**, **src/layouts** e **src/pages** in **unit-components** (ogni area mantiene i propri include glob; **Storybook-only** **foundation/** i cataloghi rimangono esclusi da quella sezione). **Vue** Gli SFC non hanno soglie di guasto; i rapporti utilizzano filigrane del **60%**, quindi i totali deboli richiedono una revisione. **Impostazioni app** render-tree **.ts** crea foglie opzionali **nota** con un'assegnazione esplicita (non una diffusione di oggetti condizionale); i test coprono istantanee vuote e riutilizzano quando due impostazioni condividono un gattoegoria e sottocategoria; Vitest importa gli aiutanti di ordinamento direttamente da **dialogAppSettingsTree**. **README**, **AGENTS**, **.github/workflows/verify.yml** commenti, **eslint-typescript**, **vitest-tests**, **testing-terminal-isolation**, **git-conventional-commits**, **playwright-tests**, **vue-quasar** e competenze (**fantasia-testing**, **fantasia-dev-setup**, **fantasia-changelog-en-us**, **fantasia-electron-preload**, **fantasia-quasar-vue**, **git-conventional-commits**) descrivono la stessa politica.
- **Root** **vitest.config.mts** registra **cinque** progetti con **extends** in **vitest/** in modo che lo spazio di lavoro **root** rimanga il repository e i glob come **src-electron/** si risolvano correttamente. **package.json** **test:coverage:electron**, **test:coverage:helpers**, **test:coverage:i18n** e **test:coverage:src** caricano questi file; **tsconfig.json** esclude **vitest/vitest.components.config.mts** dal controllo del tipo dell'app. I precedenti frammenti **vitest.*.config.mts** / **vitest.setup.ts** a livello root sono stati spostati in **vitest/**.- **Vitest** **`unit-components`** ora include **`src/layouts/**/*.vitest.test.ts`** e **`src/pages/**/*.vitest.test.ts`** insieme a **`src/components/**`**, con una base di riferimento **1:1** **`tests/<Nome>.vitest.test.ts`** per ogni **`MainLayout`**, **`ComponentTestingLayout`**, **`IndexPage`**, **`ErrorNotFound`** e **`ComponentTesting`** SFC. **L'albero delle impostazioni dell'app** Vitest copre **`toSortedRecord`**, **`buildAppSettingsRenderTree`**, l'ordinamento delle categorie, la mappatura **`showDocumentID`** e la gestione delle impostazioni locali. **Electron** main Vitest aggiunge la copertura per **`suppressChromiumDevtoolsAutofillStderrNoise`**, **`registerFaDevToolsIpc`**, **`PLAYWRIGHT_ISOLATED_USER_DATA_DIR_NAME`**, **`fixAppName`** quando il nome del prodotto è vuoto, **mainWindowCreation** espanso e **spellChecker** comportamento e **modifiche**; **`mainWindowCreation`** espone **`assignAppWindowRefForTesting`** solo per test unitari.- **Renderer** ed espansioni **Vitest**: **boot** (**qmarkdown**, casi più ampi di **externalLinkManagement**), **negozi** modulo indice, **script** test di utilità (**colorFormatConvertors**, **mdListArrayConverter**), **AppControlMenus**; I pacchetti helper **Playwright** hanno ottenuto test unitari più approfonditi **`playwrightElectronRecordVideo`** e **`playwrightUserDataReset`**. **eslint.config.mjs** include **vitest/** nell'ambito lint; **yarn.lock** riflette gli aggiornamenti delle dipendenze da questo ciclo di lavoro.
- **Playwright** Electron **Gli allegati WebM** possono mostrare un **punto rosso sintetico del cursore** che segue i movimenti del puntatore tramite script (opzionale **`FA_PLAYWRIGHT_CURSOR_MARKER=0`** per disabilitare); **Impostazioni app** tentativi di specifiche del componente **Quasar** live **tooltip** passa il mouse con **`expect().toPass`**, **`locator.hover`** con l'opzione **`force`** abilitata e un ritardo di regolazione del pannello a schede leggermente più lungo per ridurre le instabilità in **Electron**.- **Playwright** **Impostazioni app** le specifiche del componente aggiungono test seriali per il campo **ricerca** dell'intestazione dopo il percorso della scheda **en-US**: una corrispondenza ciascuno tramite **titolo**, **tag** e impostazione delle sottostringhe **descrizione**, quindi la **ErrorCard** senza risultati con la mascotte **lettura**, copia del corpo senza risultati su **errorCard-details** e una larghezza massima della scheda **650** pixel tramite **larghezza-scheda-errore-test-dati**.
- **Impostazioni app** la descrizione comando della guida per il divisore di conteggio pronunciato ora mostra la barra verticale nella frase: **vue-i18n** tratta una semplice **pipe** come sintassi del formato messaggio, quindi la stringa en-US utilizza la barra rovesciata documentata in modo che il carattere venga visualizzato letteralmente.
- **Quasar** **ripple** è di nuovo attivo per impostazione predefinita; I controlli **finestra** (**GlobalWindowButtons**) e la barra degli strumenti **AppControlSingleMenu** **`q-btn`** disabilitano l'**ondulazione** in modo che Chrome e la barra dei menu rimangano visivamente stabili.
- **DialogMarkdownDocument** aumenta l'offset **max-height** del contenuto in modo che le finestre di dialogo alte lascino spazio per l'intestazione e il piè di pagina cromati durante lo scorrimento.- Componenti del renderer riorganizzati **Vue** nei bucket **`src/components/`** — **`dialogs/`** (SFC modali **`Dialog*`**), **`globals/`** (**`GlobalWindowButtons`**, **`AppControlMenus`**, **`AppControlSingleMenu`**), **`elements/`** (**`FantasiaMascotImage`**, **`SocialContactSingleButton`**) e **`altro/`** (**`SocialContactButtons`**); moduli locali con mirroring in **`i18n/*/components/<bucket>/...`**, importazioni aggiornate e mock **Storybook**, corrette importazioni di storie **`SocialContactSingleButton`** dopo lo spostamento, aggiornata **Storybook** linea di base VRT per **impostazioni app** e documenti dei contributori rivisti (**README**, **AGENTS**, regole/competenze cursore).
- Interruzione delle **impostazioni dell'app** **nota** stringhe opzionali dall'attivazione di **vue-i18n** avvisi di chiave mancante quando non esiste alcuna chiave **nota** per un'impostazione (risolvi le note solo dopo un controllo dell'esistenza della chiave).
- Aggiunto un passaggio di pulizia all'avvio per l'archivio persistente delle **impostazioni utente** in modo che le chiavi legacy o sconosciute che non esistono più in **`src-electron/mainScripts/userSettings/faUserSettingsDefaults.ts`** vengano rimosse e la configurazione ripulita venga salvata automaticamente.- Estratte tutte le stringhe locali **en-US** hardcoded da **`i18n/en-US/index.ts`** in moduli locali dedicati per funzione per componente, finestra di dialogo, pagina e funzione globale; normalizzato tutte le chiavi i18n di livello superiore e secondario in **camelCase** con la prima lettera minuscola nell'app.
- Allineato il mock di **`externalFileLoader`** di **Storybook** (**`.storybook-workspace/.storybook/mocks/externalFileLoader.ts`**) con l'albero dei messaggi **camelCase** (incluso **`globalFunctionality`**) e la copia cablata non markdown tramite gli stessi moduli locali utilizzati dall'app, quindi anteprime di componenti e layout e **`yarn locale test:storybook:visual`**: visualizza stringhe reali invece di percorsi chiave di traduzione non elaborati.
- Nomi dei canali **main ↔ preload** **IPC** centralizzati in **`src-electron/electron-ipc-bridge.ts`** (inclusi i canali di sincronizzazione **DevTools**) in modo che i gestori **Electron** e gli helper **preload** non possano andare alla deriva verso stringhe non corrispondenti; rimosso il vecchio modulo canale solo **DevTools**.- Registrazione documentata di **`electron-ipc-bridge.ts`**, **`mainScripts/ipcManagement/register*Ipc.ts`** e **IPC** durante **`startApp()`** (**`mainScripts/appManagement.ts`**) in **README**, **AGENTS** e cursore **`electron-preload`** / **`fantasia-electron-preload`** / Guida **`fantasia-electron-main`** / **`fantasia-sqlite-main`** in modo che contributori e agenti seguano un modello end-to-end.
- Aggiunto **`src/vue-i18n-shim.d.ts`** in modo che **vue-i18n** globali iniettati (**`$t`**, **`$d`**, **`$n`** e relativi **`$*`** helper) siano dichiarati su **`@vue/runtime-core`**, allineando **Vue** la digitazione del modello SFC con **`tsc`** e cancellando le informazioni spurie Diagnostica mancante-**`$t`** negli editor che risolvono **`ComponentCustomProperties`** attraverso il percorso runtime-core.
- Sostituita la dipendenza del processo principale **`sqlite3`** con **`better-sqlite3`** (binding sincroni **SQLite** per il livello del database pianificato; ancora un componente aggiuntivo nativo: ricostruzione dopo i bump **Electron**/**Nodo** come documentato in **README**). **AGENTS**, **`fantasia-sqlite-main`** e il commento dello stub **`electron-main`** ora si riferiscono a **`better-sqlite3`**.- Rinominati gli script **Yarn** relativi a Storybook in **`test:storybook:*`**: **`yarn test:storybook:visual`** (con **`:headed`**, **`:update`**, **`:update:headed`**) sostituisce **`yarn visual:storybook:*`**; **`yarn test:storybook:smoke`** sostituisce **`yarn storybook:smoke`**. **`.storybook-workspace`** le chiavi dello script corrispondono. Aggiornati **README**, **AGENTI**, regole/abilità cursore e note del piano.
- Dividere il gate dell'intero progetto: **`yarn testbatch:ensure:nochange`** esegue **`testbatch:verify`**, produzione **Electron** build, **Playwright** componente + **E2E**, **`yarn test:storybook:smoke`** e **`yarn test:storybook:visual`** (confronto istantanee). **`yarn testbatch:ensure:change`** è lo stesso attraverso il fumo, quindi **`yarn test:storybook:visual:update`** per l'aggiornamento intenzionale della linea di base. Rimosso **`yarn testbatch:ensure`**. Aggiornate le linee guida **README**, **AGENTS**, **testing-terminal-isolation**, **fantasia-testing**, **fantasia-dev-setup**, **fantasia-release-build** e **changelog-en-us**.- Rimosso il lavoro di regressione visiva **GitHub Actions** Storybook da **`.github/workflows/verify.yml`** e rinominato il flusso di lavoro in **Verify** (push/PR esegue solo **`yarn testbatch:verify`**; **installazione di `.storybook-workspace`** mantenuta per ESLint). Storybook VRT rimane **locale** o tramite **`yarn testbatch:ensure:nochange`**. Aggiornati **README**, **AGENTS**, **testing-terminal-isolation**, **fantasia-testing**, **fantasia-dev-setup** e **fantasia-changelog-en-us** (espressione omessa dall'elenco).- I flussi di lavoro **GitHub Actions** pull request **pr-full-suite-nochange** e **pr-full-suite-change** richiedono esattamente un'etichetta repository sul PR, **novisualchange** o **visualchange** (vedi **`.github/PULL_REQUEST_TEMPLATE.md`**). **novisualchange** esegue **`yarn testbatch:ensure:nochange`** su **Windows** quindi i nomi degli screenshot di **Storybook** rimangono allineati con le linee di base di **chromium-win32** impegnate; **visualchange** esegue **`yarn testbatch:ensure:change`** e può inviare file aggiornati in **`.storybook-workspace/visual-tests/`** quando il ramo PR risiede in questo repository (i fork PR eseguono comunque test ma non possono ricevere quel push). I PR che hanno entrambe le etichette o nessuno dei due non superano la **PR suite completa (assicurarsi che non venga apportata alcuna modifica)** fino a quando non vengono risolti; in questo caso la guardia **assicura il cambiamento** esce con successo, quindi lo stesso errore non produce due segni di spunta rossi identici. Un singolo commento del bot idempotente spiega la regola (i PR rimangono aperti in modo che sia possibile aggiungere un'etichetta dopo l'apertura). **`.github/scripts/pr-label-guard.sh`** centralizza il controllo delle etichette per entrambi i flussi di lavoro.- **Configurazione collaboratore**: **`commitlint`** e **`husky`** convalidano i messaggi **`git commit`** localmente (**`commitlint.config.mjs`**, **`.husky/commit-msg`**, **`yarn install`** esegue **`prepare`**). I tipi consentiti corrispondono a **AGENTS** (**feat**, **fix**, **test**, **chore**, **refactor**, **style**, **docs**); Le righe **`Unisci …`** e **`Ripristina …`** vengono ignorate. **README** e **AGENTI** descrivono gli hook; **`.cursor/rules/git-conventional-commits.mdc`** documenta la stessa politica.
- **GitHub Actions** **Il flusso di lavoro OSV-Scanner** (**`.github/workflows/osv-scanner.yml`**) ora chiama **`google/osv-scanner-action`** flussi di lavoro riutilizzabili su **v2.3.5**, quindi **`scan-pr`** non esegue più il pull di **`actions/upload-artifact`** v3 deprecato (GitHub non riusciva a eseguire tali esecuzioni su **Imposta lavoro**).
- Imposta **`actions/upload-artifact@v5`** nel flusso di lavoro manuale **Build** (**`.github/workflows/build.yml`**) per gli artefatti di rilascio **Linux**, **macOS** e **Windows**, insieme a **`actions/checkout@v5`** e **`actions/setup-node@v5`**, in modo che i caricamenti degli artefatti rimangano supportati **GitHub Azioni** tempi di esecuzione.- Aggiornamento di **Vue** a **`^3.5.32`** (root e **`.storybook-workspace`**), **`@types/node`** a **`^22.19.17`**, aggiornamento di **root** e **`.storybook-workspace`** file di lock con **`yarn upgrade`** e impostazione di Storybook VRT **`toHaveScreenshot`** **`maxDiffPixels: 150`** quindi le esecuzioni di confronto rimangono stabili tra le ricostruzioni. Aggiornato uno snapshot di Chrome **Win32** interessato dall'urto della toolchain.
- Rinominato l'helper dell'elenco puntato markdown in **`mdListArrayConverter`** (ortografia standard) e denominazione delle variabili del loop normalizzata in **`specialCharacterFixer`**; Le notifiche di **Suggerimenti, trucchi e curiosità** e l'escape dei documenti con riduzione di prezzo rimangono invariati nel comportamento.
- Testo **en-US** ottimizzato nella **Guida alla ricerca avanzata**, **Suggerimenti, trucchi e curiosità** e nella stringa del nome dell'app **en-US** per garantire grammatica, ortografia e chiarezza; corretto un articolo mancante nell'intestazione **README** Quasar CLI.- Riposizionato ogni file **`*.stories.ts`** sotto l'albero della funzione corrispondente **`tests/`** (**`src/components/**/tests`**, **`src/layouts/**/tests`**, **`src/pages/**/tests`**) e ha puntato **`.storybook-workspace/.storybook/main.ts`** story glob su quei percorsi in modo che **Storybook** si trovi accanto **Vitest** e **Playwright**; snapshot VRT **Storybook** aggiornati (incluse le anteprime predefinite di **DialogAppSettings** e **DialogMarkdownDocument**).
- Applicazione di valori letterali oggetto multilinea espansi a livello di progetto tramite **ESLint** (**`object-curly-newline`**, **`object-property-newline`**) per **JavaScript**, **TypeScript**, **Vue** e **JSON**, con **`yarn lint:eslint`** in esecuzione **`eslint .`** dalla root del repository.
- Estrazione di piccole interfacce file locali e alias di tipo dalle origini **Vue** e **TypeScript** in moduli **`<filename>.types.ts`** co-localizzati, ove applicabile (avvio del renderer, digitazioni Pinia, test **Electron**, mock **Storybook**) e registrazione della convenzione in **README**, **AGENTS** e Cursor regole/abilità.- Esteso **Stylelint** con **`stylelint-order`** in modo che le **CSS proprietà personalizzate** e le **dichiarazioni** regolari rimangano in ordine **alfabetico** (correzione automatica tramite **`yarn lint:stylelint:fix`**); ampliato **`yarn lint:stylelint`** per scansionare **`vue`**, **`css`**, **`scss`** e **`sass`** in **`src/`** e in **`.storybook-workspace/.storybook/`** (l'output di compilazione in **`storybook-static/`** rimane ignorato).
- **Electron Playwright** esegue allega registrazioni dello schermo **WebM** per test; aprire **`test-results/playwright-report/index.html`** dopo **`yarn test:components`** o **`yarn test:e2e`** (o gli script **`:single`**) per rivedere i video negli **Allegati** di ciascun test (file in **`test-results/playwright-report/data/`**). Ogni esecuzione Playwright **sostituisce** il report HTML; **`scripts/playwrightWithArtifactTrim.mjs`** rimuove **`test-results/playwright-artifacts`** dopo questi comandi di filo in modo che l'output di scratch non venga mantenuto tra le esecuzioni. Documentato nelle linee guida **README**, **AGENTS** e Cursor **fantasia-testing** / **playwright-test**; opzionale **`FA_PLAYWRIGHT_NO_VIDEO`** salta la registrazione.
- Rimosso un **HTML** **textarea** vagante dal modello di pagina **Indice**.

## 2.4.9 - Strumenti del collaboratore, finestre di dialogo Pinia e rafforzamento della CI

### Correzioni di bug e ottimizzazioni
- Rimossi **`yarn visual:storybook:test:local`** e **`yarn visual:storybook:update:local`** (e gli script **`.storybook-workspace`** **`storybook:visual:*:local`** corrispondenti). Per build + confronto con testa o aggiornamento di istantanee, utilizzare **`yarn test:storybook:visual:headed`** / **`yarn test:storybook:visual:update:headed`**; per eseguire Playwright solo quando **`storybook-static/`** è già corrente, utilizzare **`yarn --cwd .storybook-workspace test:storybook:visual`** o **`test:storybook:visual:headed`** / **`test:storybook:visual:update`** / **`:update:headed`**. Aggiornato **LEGGIMI**.
- Risolto il problema con il flusso di dialogo **Pinia 3**: **`S_DialogMarkdown`** e **`S_DialogComponent`** sono ora archivi di configurazione standard (chiama **`S_DialogMarkdown()`** / **`S_DialogComponent()`** nei siti di utilizzo). **`openDialogMarkdownDocument`**, **`DialogMarkdownDocument`** e **`DialogAboutFantasiaArchive`** utilizzano osservatori sorvegliati quando un negozio non è attivo (ad esempio **Storybook**). **`ComponentTesting`** gestisce gli instradamenti **`componentName`** mancanti in modo sicuro ed esegue il rendering del cablaggio solo dopo aver risolto un SFC corrispondente.- Semplificata la pagina di anteprima **Index** rimuovendo il blocco aggiuntivo **Fantasia Mascot** dal percorso predefinito **`IndexPage`** (mantiene il percorso come una shell leggera per collegamenti/test locali).
- Script dei contributori **Yarn** rinominati e raggruppati per maggiore chiarezza: controlli di qualità **`yarn testbatch:verify`** / **`yarn testbatch:ensure`**; lint **`yarn lint:eslint`**, **`yarn lint:typescript`**, **`yarn lint:stylelint`**; Quasar Electron **`yarn quasar:build:elettrone`** / **`yarn quasar:dev:elettrone`**; Playwright **`yarn test:components`** e con suffisso due punti **`test:components:*`**, **`test:e2e:*`**; Storybook dev **`yarn storybook:run`** e statico **`yarn storybook:build`** (precedentemente **`build-storybook`**). Rimossi **`yarn test:full`** obsoleti e duplicati **`yarn visual:storybook:ci`**; **GitHub Le azioni** chiamano direttamente **`yarn visual:storybook:test`**. Aggiornati **README**, **AGENTS**, regole/competenze del cursore, **`package.json`**, **`testRunner_*.mjs`** e flussi di lavoro.- Storybook **output statico** (**`.storybook-workspace/storybook-static/`**), **Playwright** configurazione di regressione visiva, **`visual-tests/`** (incluse le istantanee memorizzate), spazio di lavoro **`storybook:visual:*`** script e VRT devDependencies (**`http-server`**, **`@playwright/test`**) in **`.storybook-workspace/`**; root **`yarn visual:storybook:*`** delega con **`yarn --cwd .storybook-workspace`**. Aggiornati i percorsi **`.gitignore`**, **`tsconfig.json`** e **`lint:eslint`**.
- Imposta espliciti **`permessi`** (`contents: read`) con privilegi minimi espliciti sui flussi di lavoro **`verify`** e manuali **`build`** **GitHub Actions**; aggiunti **Revisione delle dipendenze**, **OSV Scanner** (flussi di lavoro riutilizzabili + pianificazione settimanale su **`master`**) e **Gitleaks** per l'igiene PR/push.

## 2.4.8 - Pinia 3 e Vue Router 5

### Correzioni di bug e ottimizzazioni
- Aggiornato **Sass** a **1.99.x** in **`^1.78.0`** in **root** e **`.storybook-workspace`** file di lock in modo che l'app desktop e **Storybook** utilizzino la stessa riga Dart Sass risolta per gli stili.
- Aggiornato **Pinia** a **v3** e **Vue Router** a **v5** per l'app desktop e **`.storybook-workspace`** (esplicito **`vue-router`** lì per **`StoryRouterShell`**); non è stato aggiunto **`@pinia/colada`** facoltativo. Aggiornati i file di lock **root** e **`.storybook-workspace`** con **`yarn upgrade`** all'interno degli intervalli di semver esistenti (**Storybook** rimane bloccato su **10.3.4** su quella riga).
- Aggiornato **`uuid`** a **v13** per gli UUID di markdown e di dialogo dei componenti (**`S_Dialog`**) e **`@quasar/cli`** a **v3** per la **`quasar`** CLI utilizzata con **`@quasar/app-vite`** v2.
- Aggiornato **Electron** a **41.x** (da **33.x**); **`electron-builder`** funziona ancora **`@electron/rebuild`** quindi **`sqlite3`** corrisponde al runtime in bundle. **`@electron/remote`** **2.1.x** e il noto avviso peer di installazione di produzione UnPackaged per **`electron`** sono rimasti invariati.- Importazioni di solo tipo standardizzate nella codebase TypeScript e Vue ("tipo di importazione") per allinearsi con linting rigoroso e mantenere i bundle di runtime privi di rumore di importazione di solo tipo.
- Aggiunto un nuovo comando "yarn Guarantee" per la convalida dell'intero progetto (`verify` + build di produzione + componente Playwright + Playwright E2E) e linee guida per i contributori allineate in **README**, **AGENTS** e regole/competenze del cursore per documentare chiaramente il flusso di lavoro Quick Gate e Full Gate.
- Ridotto il rumore di avviso Vue non utilizzabile nell'output del componente Vitest filtrando la risoluzione Quasar nota e gli avvisi di iniezione di solo test nella configurazione di test condivisa, mantenendo il segnale concentrato sugli errori che richiedono azioni.
- Risolta la risoluzione CI TypeScript per le importazioni SFC Vue sostituendo il riferimento shim `.quasar` generato in `src/env.d.ts` con una dichiarazione in-repo `declare module '*.vue'`, quindi `yarn verify` ha esito positivo sui corridori GitHub puliti senza richiedere file shim `.quasar` locali/generati.

## 2.4.7 - Allineamento Storybook 10 e Vite 8

### Correzioni di bug e ottimizzazioni
- Aggiornato **`.storybook-workspace`** a **Storybook 10** e **Vite 8** in modo che le anteprime dei componenti vengano eseguite sulla stessa linea di bundle principale dell'app desktop (**`@quasar/app-vite`**), aggiornato il file di blocco nidificato **Yarn** per installazioni riproducibili (incluse le installazioni bloccate **CI**) e root allineato **Storybook** / **`eslint-plugin-storybook`** versioni con quella famiglia di rilascio.
- File di lock **root** e **`.storybook-workspace`** aggiornati con **`yarn upgrade`** all'interno degli intervalli di semver esistenti (in particolare **Quasar**, **Vue**, **vue-i18n**, **Playwright**, **Sass** e strumenti correlati); mantenuto **`@types/node`** sulla riga di digitazione **22.x** per il motore **Node 22**. Aggiornati **README** e **AGENTS** in modo che i contributori vedano **Storybook 10** su **Vite 8** invece della vecchia dicitura split-Vite.
- Aggiunto **`debug-storybook.log*`** a **`.gitignore`** in modo che i log diagnostici Storybook locali non vengano rilevati come file non tracciati.

## 2.4.6 - Finestra cromata, Storybook, Stylelint e DevTools

### Correzioni di bug e ottimizzazioni
- Applicato **`getCurrentWindow()`** da **`@electron/remote`** a **window chrome** aiutanti bridge (**minimizza**, **massimizza**, **ridimensiona**, **chiudi**, **stato ingrandito**) in modo che i controlli rimangano legati alla finestra dell'app quando il focus del sistema operativo si sposta sui menu o su altre superfici.
- Allineato **`.storybook-workspace`** con l'app principale su **Vue 3.5**, **vue-i18n 11**, **Quasar 2.16+**, **Pinia 2.3+** e **@quasar/extras 1.17**; rimosso il **`package-lock.json`** vagante in modo che vengano utilizzati solo i file di lock **Yarn**.
- Aggiornati i file di lock **root** e **`.storybook-workspace`** con **`yarn upgrade`** all'interno degli intervalli di semver esistenti; aggiunti i componenti aggiuntivi root **Storybook** a **8.6.18** in modo che corrispondano a **`@storybook/vue3-vite`** e all'area di lavoro nidificata (in particolare i più recenti **Playwright**, **Sass** e altri strumenti compatibili).
- Migrazione di **`src/css/app.scss`** e **`globals/*.scss`** a Sass **`@use`** con importazioni esplicite di **`quasar.variables.scss`** in partial, sostituendo le catene **`@import`** deprecate per stili globali di proprietà dell'app prima di Dart Sass 3.- Vite di Storybook **`additionalData`** antepone **`quasar.variables.scss`** al progetto **`.vue` / `.scss`** (tramite URL **`file://`** in modo che i percorsi Windows con spazi vengano risolti). Per **`quasar/src/css/index.sass`** utilizza **`@import`** (non **`@use`**) prima delle importazioni di Quasar, lo stesso ordine efficace della trasformazione `sassVariables`** di **`@quasar/vite-plugin`, quindi **`!default`** le voci del tema si risolvono nella tavolozza e nelle variabili CSS del framework (**`--q-dark`**, **`--q-primary`** e token correlati) corrispondono all'app desktop; other **`node_modules`** Sass rimane intatto.
- Estesa la configurazione root di **Stylelint** con **`@stylistic/stylelint-config`** in modo che le regole stilistiche abbandonate dal core di **Stylelint 17** continuino ad applicare la familiare formattazione CSS/SCSS; aggiornati i consigli e le impostazioni **`.vscode`** in modo che l'estensione Stylelint possa utilizzare la configurazione del progetto, **correggere il salvataggio** e formattare i file **`.scss`** autonomi in modo coerente.- Controllo **DevTools** instradato (**toggle**, **open**, **close** e **status**) attraverso piccoli gestori **IPC del processo principale** invece di **`@electron/remote`** **`getCurrentWindow()`**, quindi **Aiuto → Attiva/disattiva strumenti per sviluppatori** rimane affidabile nelle build **Electron** pacchettizzate; il controllo **E2E** ora attende finché DevTools non termina l'associazione invece di essere affermato immediatamente dopo la chiusura del menu.
- Aggiornate **GitHub Actions** su **`verify`** e flussi di lavoro manuali **build** (**`actions/checkout`**, **`actions/setup-node`**, **`actions/upload-artifact`**) alle versioni principali attuali.
- Notato in **`quasar.config.ts`** che si prevede che il peer **`electron`** di **`@electron/remote`** venga visualizzato come non soddisfatto durante UnPackaged **`yarn install --production`**; la creazione della confezione riesce comunque perché l'app viene eseguita sul runtime Electron in bundle.- L'anteprima di Storybook registra il plugin **`Dark`** di Quasar, il plugin dell'app **`@quasar/quasar-ui-qmarkdown`** (dopo Quasar, ordine di avvio corrispondente), **Roboto (latin-ext)** e **`preview-body.html`** / **`Dark.set(true)`** in modo che **`body--dark`** rimanga allineato con l'app; Sass **`additionalData`** salta **`app.scss`** per evitare il doppio caricamento delle variabili. I documenti del componente **Dialog** e **top-menu** utilizzano di nuovo **`parameters.docs.story.inline: false`** in modo che Autodocs non monti ogni storia in un documento (ciascun **`q-dialog`** si apre automaticamente al momento del montaggio, che sovrappone le modalità quando **`inline: true`**); ogni iframe di documenti nidificati carica comunque lo stesso stack di anteprima CSS.

## 2.4.5 - Flusso di lavoro del collaboratore e CI

### Correzioni di bug e ottimizzazioni
- Aggiunto **`yarn verify`** per eseguire ESLint, TypeScript (`tsc`), Stylelint e unit test in un comando (denominato **`verify`** perché Yarn 1 riserva **`yarn check`** per la verifica delle dipendenze); l'ho documentato nel README e nella guida alla configurazione dello sviluppo, ho allineato le regole/competenze del cursore e l'ho eseguito nel flusso di lavoro manuale di creazione delle azioni GitHub prima della build di produzione Electron.
- Risolto il problema con l'helper combinato unit-then-Playwright in modo che esegua unit test e poi Playwright sui percorsi dei componenti e E2E (rimossa l'invocazione "concurrently" non funzionante ed eliminata la dipendenza **`concurrently`** inutilizzata).
- Cancellato il markdown del registro delle modifiche della locale francese in modo che le note di rilascio non vengano mantenute per ogni locale fino al ritorno delle traduzioni.
- Aggiornato **`@intlify/unplugin-vue-i18n`** alla v11 per la pipeline locale Quasar/Vite, **`uuid`** alla v11 (tipi TypeScript integrati; rimosso **`@types/uuid`**) e **`sqlite3`** alla v6 per la riga di dipendenza del processo principale Electron.
- Aggiunto un flusso di lavoro **GitHub Actions** che esegue **`yarn verify`** su **push** e **pull request** a **`master`** / **`main`** (inclusa l'installazione dello spazio di lavoro Storybook in modo che ESLint possa risolvere `.storybook-workspace/.storybook`).- File di lock **root** e **`.storybook-workspace`** aggiornati con **`yarn upgrade`** all'interno degli intervalli di semver esistenti (in particolare i più recenti **Playwright**, **Quasar**, **Vue**, **vue-i18n**, **Sass** e strumenti correlati dove gli intervalli sono consentiti).
- Gli aiutanti del bridge **DevTools** utilizzano **`getCurrentWindow()`** invece di **`BrowserWindow.getFocusedWindow()`** in modo che **toggle/status** rimanga legato alla finestra dell'app quando i menu o DevTools cambiano il focus del sistema operativo, stabilizzando il controllo DevTools **E2E**.

## 2.4.4 - Dipendenze e strumenti di sviluppo

### Correzioni di bug e ottimizzazioni
- Aggiornato il file di blocco delle dipendenze con `yarn upgrade` negli intervalli di semver esistenti in modo che le installazioni si risolvano con le ultime versioni compatibili (incluse Quasar, Vue, `vue-i18n`, Sass, Playwright e gli strumenti Vite inseriti da Quasar).
- Aggiornato `jsdom` alla v29 per l'ambiente DOM Vitest, `eslint-plugin-n` alla v17 e aggiornato `@types/node` sulla riga di digitazione del Nodo 22.
- Strumenti lint e tipi migrati alla configurazione flat **ESLint 9** (**neostandard**, **eslint-plugin-vue** v10, **typescript-eslint**), **TypeScript** 6 e **Stylelint** 17 con configurazioni condivisibili aggiornate; piccole modifiche al codice sorgente corrispondono a valori predefiniti più rigorosi (attributi di importazione, spaziatura dell'unione, protezione URL della finestra di sviluppo, QMarkdown CSS shim).
- **AGENTI**, regole del cursore e competenze allineati in modo che il flusso di lavoro standard di pre-commit e pre-changelog esegua sempre **ESLint**, **TypeScript (`tsc`)**, **Stylelint** e **unit test** in quest'ordine; il **quality gate** può essere eseguito come un comando terminale concatenato per efficienza, con comandi individuali riservati al debug degli errori. I controlli **Build** e **Playwright** prevedono un comando per terminale.

## 2.4.3 - Aggiornamenti aggiuntivi delle dipendenze e flusso di lavoro di test migliorato

### Correzioni di bug e ottimizzazioni
- Aggiornato `vitest` alla v4 e modificati i mock di test unitari del processo principale Electron in implementazioni compatibili con il costruttore in modo che i mock di `BrowserWindow`, `Menu` e `MenuItem` rimangano stabili nel runner più recente.
- Aggiornate dipendenze aggiuntive a basso rischio (`sqlite3`, `lodash-es`, `stylelint`, `stylelint-config-standard-scss` e `eslint-plugin-promise`) e riconvalidata la pipeline lint/test/build completa dopo gli aggiornamenti.
- Aggiunte indicazioni persistenti sull'agente/test per eseguire ciascun comando di convalida in un'invocazione terminale isolata per mantenere gli output dei test lunghi leggibili e più facili da controllare.
- Allineato il blocco del log delle modifiche "2.4.3" con la versione canonica di "package.json" e rafforzata la guida per i contributori in modo che le modifiche del log delle modifiche rileggano sempre prima "package.json" live, evitando deviazioni di sezione/versione.

## 2.4.2 - Aggiornamento delle dipendenze principali e convalida del flusso di lavoro Quasar/Vite

### Correzioni di bug e ottimizzazioni
- Aggiornato `vue-i18n` alla v11 e rimossa la soppressione legacy TypeScript nel bootstrap di i18n ora che le digitazioni attuali vengono compilate in modo pulito.
- Dipendenze dell'app principale e degli strumenti aggiornati (inclusi Quasar extra/qmarkdown, Axios, Pinia, Vue Router, Vitest e plug-in ESLint selezionati) per ridurre i rischi di manutenzione rimanendo nell'attuale architettura del progetto.
- Applicato un aggiornamento degli strumenti di follow-up per il flusso di lavoro Quasar/Vite (`@quasar/app-vite`, `@intlify/unplugin-vue-i18n`, `vite-plugin-checker`, `eslint-plugin-vue` e `@types/node`) con lint completo, controllo del tipo, unità, componente, E2E e convalida della build.

## 2.4.1 - Blocco delle policy del log delle modifiche e verifica della stabilità dei componenti

### Correzioni di bug e ottimizzazioni
- Risolto il problema con il rilevamento del test dei componenti Playwright per `DialogAboutFantasiaArchive` evitando le importazioni complete del caricatore i18n che inseriscono documenti di markdown nel caricamento del test lato nodo.
- Aggiornate le asserzioni dei pulsanti social della finestra di dialogo per confrontare le etichette visualizzate, corrispondere all'output corrente dell'interfaccia utente e mantenere stabili i test dei componenti.
- Guida del progetto bloccata per **non eseguire mai il bumping automatico delle versioni**; Le sezioni del registro delle modifiche ora seguono rigorosamente la versione esistente di "package.json" a meno che non venga esplicitamente richiesta una modifica manuale.

## 2.4.0 - Quasar CLI Vite v2 e toolchain Node 22

### Correzioni di bug e ottimizzazioni
- Aggiornata la toolchain dell'app desktop a `@quasar/app-vite` v2 (Vite 8), ESM `quasar.config.ts`, TypeScript 5.6 e feedback ESLint tramite `vite-plugin-checker` durante dev/build.
- Collaboratore standardizzato e CI Node.js su **22.22.0 o versioni successive** (richiesto dall'attuale Quasar CLI), azioni GitHub aggiornate `setup-node` e aspettative di ricostruzione del modulo nativo documentate per `sqlite3` dopo i bump di Electron/Node.
- Sostituita l'estensione dell'app QMarkdown con la registrazione diretta `@quasar/quasar-ui-qmarkdown`, importazioni locali `*.md?raw` native di Vite e gestione del modello Vue `isPreTag` in modo che le finestre di dialogo markdown rimangano stabili senza il plug-in Vite dell'estensione.
- Migrazione del bundle vue-i18n a `@intlify/unplugin-vue-i18n`, potenziato Electron e generatore di elettroni per l'output del processo principale ESM e allineato Sass con il sottoprogetto Storybook; Storybook rimane su Vite 6 finché l'intervallo peer supportato non include Vite 8. *(Sostituito in **2.4.7**: Storybook ora funziona su **Vite 8** con **`@quasar/app-vite`**.)*
- Aggiornato **`@electron/remote`** alla versione 2.1.x in modo che Electron 33 mantenga un precarico funzionante (evita le sonde delle funzionalità di Chromium rimosse che in precedenza interrompevano il caricamento del precarico e le API del bridge).- Risolto il problema con il routing **Playwright componente-testing**: `ComponentTesting` ora esegue il glob `../components/**/*.vue` da `src/pages`, quindi le esecuzioni di produzione Electron montano l'SFC richiesto invece di un cablaggio vuoto.
- Innalzato **`@typescript-eslint`** alla v8 per TypeScript 5.6, aggiunto **`yarn lint:types`** (progetto completo `tsc`) e guida ampliata AGENTS/Cursore per ESLint e porte typecheck (TSLint non viene utilizzato).
- Ridotte le linee stderr CDP **DevTools Autofill** rumorose in fase di sviluppo filtrando quei messaggi specifici nel processo principale Electron (solo mancate corrispondenze di protocollo innocue).
- Percorsi del codice del renderer rafforzati che chiamano **`window.faContentBridgeAPIs`** quando manca il bridge di precaricamento e allineato l'utilizzo di Vitest **`vi.stubEnv`** con Vitest 3 regole booleane per `DEV` / `PROD` / `SSR`.
- Configurazione **Quasar/electron-builder** ottimizzata per digitazioni e pacchetti più rigorosi: involucro PWA `workboxMode` (`GenerateSW`), blocco **`bex`** vuoto corrispondente ai tipi Quasar attuali, Linux **`desktop.entry`** per Electron-Builder 26, più collante del progetto TypeScript (`env.d.ts` Vue riferimento shim, gestione dell'aumento vue-i18n) in modo che `lint:types` rimanga affidabile.- Risolto il problema relativo alla deprecazione di **Dart Sass 2** in SCSS condiviso tramite `sass:color` nelle variabili Quasar e negli stili della barra di scorrimento (in sostituzione degli helper colore legacy).
- **Promemoria installazione:** il pacchetto **`electron`** necessita del download **postinstallazione**; saltare gli script del ciclo di vita (ad esempio `npm install --ignore-scripts`) può lasciare un file binario danneggiato finché non si reinstalla o si esegue `node node_modules/electron/install.js`.

## 2.3.2 - Storybook layout, pagine e strumenti per i contributori

### Nuove funzionalità
- Aggiunte anteprime Storybook solo su tela per i layout principali e le pagine rappresentative delle app utilizzando un router in memoria condiviso (`StoryRouterShell`), una singola tabella "STORYBOOK_APP_ROUTES" e storie `src/layouts/**` e `src/pages/**` co-localizzate.
- Collegata una trasformazione Vite solo Storybook che riscrive `import.meta.globEager` in `import.meta.glob` desideroso in modo che la pagina di test dei componenti venga caricata nella toolchain dell'area di lavoro Storybook.
- Esteso Storybook i18n simula in modo che la pagina non trovata mostri una copia reale invece delle chiavi di traduzione grezze; indice semplificato ed etichette di storie a componente singolo (eliminata l'anteprima ridondante del solo indice).
- Layout documentati e aspettative Storybook della pagina in "AGENTS.md" e regole/competenze del cursore, incluse anteprime solo su tela e ordinamento del registro delle modifiche insieme alle storie dei componenti.

### Correzioni di bug e ottimizzazioni
- ESLint puntato alla sorgente intenzionale e ai percorsi di configurazione invece che all'intero repository; aggiunto `yarn lint:style` con Stylelint per `src` Vue e SCSS più una configurazione Stylelint root.
- Abilitato TypeScript `skipLibCheck` ed esclusi gli artefatti di build, l'output statico Storybook, la configurazione dei componenti Vitest e collocato `*.stories.ts` dal progetto root `tsconfig` per controlli più puliti.
- Rimosso un blocco di stile con ambito vuoto da "SocialContactButtons" emerso dal linting dello stile.
- Copertura Vitest e Playwright aggiornata (gestore dei collegamenti esterni, immagine della mascotte, controlli delle finestre) per corrispondere al comportamento e ai selettori attuali.

## 2.3.1 - Icona del pacchetto Electron e copertura della favicon

### Correzioni di bug e ottimizzazioni
- Aggiunta la configurazione esplicita dell'icona Electron Builder per Windows, macOS e Linux, inclusi i metadati della voce desktop Linux e la configurazione della destinazione Linux.
- Collegate tutte le dimensioni delle favicon generate in `index.html` in modo che le dichiarazioni delle icone del renderer coprano l'intero set `public/icons/`.
- Aggiornata la dicitura del markdown del registro delle modifiche per evitare errori di analisi in fase di compilazione nella pipeline qmarkdown/Vite durante le build di produzione Electron.

## 2.3.0 - Storybook area di lavoro e rifinitura del desktop

### Nuove funzionalità
- Spostato Storybook in un pacchetto nidificato `.storybook-workspace` con la propria toolchain, rimosso l'albero radice `.storybook` e aggiornate le storie co-localizzate (menu, finestre di dialogo, mascotte, controlli delle finestre e pulsanti social) per componenti aggiuntivi stabili, risorse "pubbliche/" e flussi di lavoro di creazione.
- Ottimizzazione dei token variabili Quasar globali e delle regolazioni del componente condiviso SCSS per cromatura delle finestre, controlli e superfici di markdown/dialogo più coerenti.
- Aggiunte stringhe di etichette per le immagini della mascotte Fantasia per le versioni locali statunitensi e tedesche per supportare la denominazione accessibile.
- Storybook, Electron `file://` e Playwright documentati ricostruiscono le aspettative per i contributori in `AGENTS.md`, `README.md` e abilità/regole del cursore (inclusa una regola dedicata alle storie Storybook).

### Correzioni di bug e ottimizzazioni
- Risolto il problema con le icone dei pulsanti dei contatti social che non venivano caricate nelle build Electron in pacchetto risolvendo gli URL di immagini pubbliche con una base relativa quando l'URL di base dell'app è "/" o vuoto, invece degli URL relativi alla radice in file://.
- Aggiornata la copertura dei componenti Playwright per i controlli delle finestre in modo che corrispondano al markup corrente e alle etichette di accessibilità.

## 2.2.1 - Denominazione del tipo e scansione della coerenza

### Nuove funzionalità
- Aggiunta una guida esplicita alla governance dei test dei componenti tra AGENTS/regole/competenze: ogni componente `src/components/**` Vue mantiene una suite `*.vitest.test.ts` co-locata, con la parità trattata come presenza di copertura (percentuali di ramo/linea non esaustive).
- Politica di posizionamento standardizzata delle apparecchiature per la creazione futura di test: le apparecchiature Vitest e Playwright rimangono in linea nei rispettivi file di test, mentre `_data/` rimane di sola produzione.
- Refactoring delle dichiarazioni del menu condiviso e del tipo di finestra di dialogo/documento in nomi singolari e di raccolta più chiari, preservando le convenzioni del prefisso "I_" / "T_".
- Aggiunta una voce di menu riutilizzabile e blocchi costitutivi del tipo di sottomenu per mantenere più semplice la manutenzione della digitazione della configurazione del menu.
- Copertura Vitest lato renderer estesa in `src/` per script e transizioni di stato del negozio utilizzando mock deterministici.
- Aggiunto un inventario tracciato della strategia di test del renderer in `.cursor/plans/` per mappare i target `src/` TS/Vue tra le responsabilità di Vitest e Playwright.
- Suddividere Vitest in una configurazione Node/core e una configurazione di componenti jsdom con configurazione condivisa in modo che `yarn test:unit` copra boot, i18n, script, negozi e test del fumo co-localizzati per ogni `src/components/*.vue`.- Collocati `AppControlSingleMenu` e `SocialContactSingleButton` come componenti di primo livello (percorsi e importazioni aggiornati).
- Navigazione iniziale instradata tramite un helper dedicato "appStartupRouting" utilizzato da "App.vue".

### Correzioni di bug e ottimizzazioni
- Risolte le mancate corrispondenze di digitazione delle richiamate di trigger nei dati del menu di aiuto racchiudendo i gestori digitati in richiamate con zero argomenti compatibili con firme di trigger di menu generiche.
- Commenti e testo normalizzati soggetti a errori di battitura nei test dei componenti, nei test E2E e nei file sorgente selezionati per una denominazione e una grammatica coerenti.
- Aggiornati esempi di comandi di test README per corrispondere ai nomi degli script effettivi e ai modelli di utilizzo in "package.json".
- Separato l'output dell'artefatto Playwright dalla cartella del report HTML in "playwright.config.ts" per rimuovere gli avvisi di conflitto tra directory di output del reporter.
- Guida ai test riallineata per mantenere `yarn test:unit` come flusso di lavoro di base per i test unitari nei documenti di progetto e nei file di guida del cursore.
- Chiarite le indicazioni su AGENTS/regole/competenze in modo che Vitest tratti esplicitamente sia `src/` che `src-electron/` come superfici di test unitario di prima classe mantenendo Playwright come livello di integrazione/runtime.
- Regole dei dati di test chiarite: i dispositivi di test automatizzati rimangono in linea in ogni `*.vitest.test.ts` / `*.playwright.test.ts`, i payload di test dei componenti utilizzano `COMPONENT_PROPS` ove possibile e i dati del menu in modalità componente incorporati rimangono in linea in "AppControlMenus" per i trigger di dialogo.- Risolti i problemi ESLint nei mock del collegamento esterno di avvio Vitest (digitazione del listener `Event` e blocchi imbottiti).

## 2.2.0 - Test e strumenti dell'agente

### Nuove funzionalità
- Copertura Vitest estesa per i moduli bridge di precaricamento Electron e gli script del processo principale (controlli delle finestre, strumenti di sviluppo, collegamenti esterni, gestione delle app, controllo ortografico e creazione della finestra principale) utilizzando simulazioni deterministiche.
- Test del componente Playwright per DialogAboutFantasiaArchive (sostituisce il precedente file segnaposto TODO).
- Regola del cursore e abilità per i documenti del piano locale in `.cursor/plans/` (gitignored), oltre alla guida al registro delle modifiche per leggere i file del piano di corrispondenza della versione per il contesto di rilascio.

### Correzioni di bug e ottimizzazioni
- Sostituite le restanti annotazioni TypeScript "any" nelle dichiarazioni di tipo condiviso con "unknown" per mantenere la digitazione del trigger di menu e del componente Vue più rigorosa.
- Aggiunta una guida esplicita anti-`any` agli script Vue, TypeScript, Vitest e Playwright regole/abilità per mantenere il codice futuro fortemente tipizzato.

## 2.1.0 - Strumenti e sviluppo assistito dall'intelligenza artificiale

### Nuove funzionalità
- Regole e competenze del progetto cursore per Vue/Quasar, BEM e SCSS con ambito, Playwright e Vitest, precaricamento Electron, SCSS globale, commit Git convenzionali e manutenzione del registro delle modifiche en-US legata alla versione dell'app.
- AGENTS.md come punto di ingresso del progetto per lo sviluppo assistito dall'intelligenza artificiale.

##2.0.0 - La Grande Riscrittura

### Nuove funzionalità
- Un'intera pletora poiché questa è una riscrittura completa!

### Problemi noti
- Nessun problema alla data di rilascio.

### Correzioni di bug e ottimizzazioni
- Troppe ottimizzazioni da elencare poiché si tratta di una riscrittura.
- Nessun bug alla data di rilascio.
