# Endringslogg
----------

## 2.4.15

### Feilrettinger og optimaliseringer
- **Prosjektinnstillinger**: Skjermleserbrukere får merkede kontroller i hele dialogen. Filterfeltene **Verdener** og **Dokumentmaler**, deres nullstillingskontroller, filteret **Tilgjengelige dokumentmaler**, hjelpeikonene **Verdensfarge** og **Verdensfargepalett**, hvert palettfargevalg og hjelpeikonet **Verdensvedlegg** eksponerer nå tilgjengelige navn på hvert støttet grensesnittspråk.
- **Prosjektinnstillinger**: Den delte bekreftelsesknappen for sletting som brukes av **Verdener** og **Dokumentmaler**, eksponerer etiketten, bekreftelsesforespørselen og nedtellingen for hjelpeteknologi.

## 2.4.14 - Versjonsbump

### Nye funksjoner
- **Lokalisering**: Handlingsbehandler lagrer feil, prosjekt åpne/opprett feilreserver og **Importer/eksporter appkonfigurasjon** feiltoaster bruker nå lokalitetsfiler på alle støttede språk. **Fantasia Archive Settings** **Logg full aktivitetsnyttelast** hjelpetekst samsvarer med engelsk på alle språk.
- **Prosjektinnstillinger**: **Lagre uten å lukke** lagrer som **Lagre innstillinger**, men holder dialogen åpen.

### Feilrettinger og optimaliseringer
- **Prosjektinnstillinger**: **Slett verden** på **Verdener**-detaljraden er vertikalt på linje med **Verdensnavn** og **Verdensfarge**.
- **Språkvelger**: **Svenska** viser nå Sveriges flagg i stedet for El Salvadors.

## 2.4.13 - Versjonsbump

### Nye funksjoner
- **Grensesnittspråk**: Språkvelgeren viser **16** grensesnittspråk (**Engelsk, US**, **Deutsch**, **Français**, **Italiano**, **Svenska**, **Suomi**, **Norsk**, **Português**, **Español**, λκηλ, λκά **Українська**, **العربية**, **中文**, **日本語**, **हिन्दी** og **Русский**). Menyetiketter markerer AI-assisterte lokaliteter med et lokalisert **AI**-suffiks (bokstavene **AI** forblir latinske i hver etikett).
- **Oversettelser**: Hvert valgspråk sender komplette brukergrensesnittstrenger for menyer, dialogbokser, flytende vinduer, varslinger og hjelpeoppmerking i appen (**Advanced Search Guide**, **Advanced Search Cheat Sheet**, **Tips, Tricks & Trivia** og **Changelog**), generert fra den korrigerte **Engelsk, US.
- **Arabisk layout**: Å velge **العربية** gjelder oppsett fra høyre til venstre i hele appen; språkmenyen forblir venstre mot høyre med leseretning per rad, slik at endonymer vises naturlig.- **Desktop-installatører** registrerer typen **`.faproject`** for **Åpne med** på **Windows**, **macOS** og **Linux** pakker; dobbeltklikker du på en prosjektfil eller velger **Fantasia Archive** i systemvelgeren, åpnes det **`.faproject`** som den aktive økten og oppdaterer **Last inn nylig prosjekt** når åpningen lykkes (utvikling **`yarn quasar:dev:electron`** hopper over automatiske åpninger med mindre du angir **`FA__DEV_OPEN_FAPROSJEKT=1`**).
- **Velkomstskjerm** og **Prosjektledelse**: Velkomsthandlingene og matchende **Prosjektledelse**-rader bruker **Opprett nytt prosjekt** og **Last inn eksisterende prosjekt** (**no-US**, **Deutsch**, **Français**).- **Velkomstskjermen**: Når **Prosjektledelse** har en lagret **Last nylig prosjekt**-liste, viser **Splash** en delt CV-kontroll: hovedsegmentet leser **Resume Latest Project** når ingen **`.faproject`** er aktiv (åpner den nyeste siste filen) eller **Resume Current Project** når en økt allerede er lastet inn (og er allerede lastet inn). Pilen viser de samme nye prosjektradene som menyen (navn pluss bane) og viser **Bla gjennom siste prosjekter** som det tilgjengelige navnet og et **Quasar** verktøytips til høyre. Uten lagrede nylige nyheter forblir den kontrollen skjult.
- **Appinnstillinger**: **Skjul verktøytipset for "Bla gjennom siste prosjekter"** fjerner verktøytipset ved siden av **Fortsett siste prosjekt** på velkomstskjermen; spruten oppdateres når du lagrer bryteren uten å starte appen på nytt.- **Egendefinert prosjekt CSS**: Med et aktivt **`.faproject`**, viser **Innstillinger og verktøy** **Egendefinert prosjekt CSS** ved siden av **Egendefinert app CSS**. Det bevegelige Monaco-vinduet vedvarer stilarktekst per prosjekt til SQLite med avvist automatisk lagring, bruker direkte forhåndsvisning gjennom **`#faProjectUserCss`** etter hele appen **Tilpasset app CSS**, holder den flytende rammen mellom **Egendefinert app ** og __TK0 ikke når du går inn i scratch. laste, bytte eller importere prosjekter. Standard global snarvei er **Ctrl+Shift+J** på alle skrivebordsplattformer (samme bokstavelige kontrolltast på macOS), kablet som bare åpen: den lukker aldri vinduet, og et gjentatt trykk mens redigeringsprogrammet forblir åpent lar utkastet i minnet ditt være urørt.- **Prosjektnotatbord**: **Prosjektledelse** øker **Toggle Project Noteboard**, deaktivert til et **`.faproject`** er lastet, standard snarvei **Ctrl+Alt+D** på hver stasjonær plattform; **Lukk** viser den dempede snarveien i parentes under etiketten som **App Noteboard**. Tekst og bevegelig vindusgeometri vedvarer per prosjekt i SQLite **`prosjekt_data`** KV-rader; rendereren holder flyteren stablet over **App Noteboard** når begge er åpne mens begge holder seg under modal chrome (**6000+**).

### Feilrettinger og optimaliseringer
- **Engelsk kopi**: **Appinnstillinger** alternativtitler og beskrivelser, import/eksportveiledning, etiketter for nøkkelbinding og action-monitor, og hjelpeavmerking ble korrekturlest på **engelsk, US**; iscenesettelser **TODO**-prefikser ble fjernet fra innstillingsetiketter.
- **Velkomstskjerm**: **`/`** og **`/home`** deler nå ett **MainLayout**-skall (menyer i topplinjen, språkvelger og vinduskontroller forblir montert). Bare midtsiden går over mellom velkomstskjermen og arbeidsområdet; venstre skuff glir inn på **`/home`** med samme timing. Når en **`.faproject`**-økt blir aktiv – opprett, last inn, gjenoppta, valg av nylige prosjekter eller åpne et prosjekt fra operativsystemet – navigerer appen til **`/home`** med en kort uttoningsovergang fra en hvilken som helst rute utenom arbeidsområdet (velkommen, **Feil / Ikke funnet** eller fremtidige **Hovedoppsett**-sider) komponentruten, unntatt **testing av **
- **Velkomstskjerm**: Den venstre skuffen **Essential links** forblir helt utenfor velkomstruten — ingen reservert skinne og ingen skuff krom på **`/`** — og glir inn på **`/home`** når arbeidsområdet åpnes (samme tidspunkt som siden crossfade) i stedet for å sprette inn i full bredde.- **Feil / Ikke funnet**: Ukjente ruter bruker nå den samme **MainLayout** øverste linjen som velkomst- og arbeidsområdeskjermene (**Prosjektadministrasjon**, **Dokumenter og innhold**, **Innstillinger og verktøy**, **Hjelp og info**, pluss språkvelgeren og vinduskontrollene) uten venstre skuff.
- **Feil / Ikke funnet**: Når en **`.faproject`**-økt allerede er aktiv, viser siden **Resume Current Project** ovenfor **Return to app start screen**; den åpner den aktive filen på nytt på samme måte som velkomstens **Resume Current Project**-primærsegment, uten rullegardinmenyen for nylig prosjekt eller en allerede aktiv advarsel.- **Prosjektlasting**: Åpne prosjektet som allerede er aktivt - fra **Last eksisterende prosjekt**, **Last nylig prosjekt**, OS-dobbeltklikk, eller matchende meny eller splash **Last eksisterende prosjekt**-rader - viser en **advarsel**-toast i samme **prosjektnavn**-linjeformat som en vellykket lasting, laster ikke inn databasen på nytt, og velkomstskjermen når du ikke ble funnet på arbeidsområdet eller**Error** side. **Gjenoppta gjeldende prosjekt** på **Feil / Ikke funnet** og velkomst CV-primærsegmentet (**Gjenoppta gjeldende prosjekt** når en økt er aktiv, **Fortsett siste prosjekt** når ingen) går tilbake til arbeidsområdet stille uten advarsel.
- **Velkomstskjermen**: **Resume Latest Project**-menyen for nylig prosjekt bruker den samme tavlesidebakgrunnen, gullkanten, lyse gullradtitler, dempede baner, gullraddelere, en lett primær-lys svevefarge og samme skygge som topplinjemenyene i stedet for standardmenypanelet i mørk blågrønn app.
- **Velkomstskjermen**: Den delte **Resume Latest Project**-rullegardinpilen viser sin chevron igjen; arvet tekststil med store bokstaver hadde ødelagt Material Icons-ligaturer inne i det segmentet.- **Velkomstskjermen**: **Opprett nytt prosjekt** på velkomstruten åpnes **Nytt prosjekt** gjennom den samme **AppControlMenus**-dialogverten som arbeidsområderuten.
- **Appinnstillinger**: **Skjul sosiale lenker på velkomstskjermen** skjuler gulldeleren og **sosial kontakt**-knapprader på velkomstskjermen; spruten reagerer når lagrede innstillinger endres i **Appinnstillinger**.
- **Appinnstillinger**: **Skjul popup-vindu for tips på startskjermen** undertrykker oppstarten **Visste du?** tipsvarsling på velkomstruten (les én gang når appen starter, ikke ved hver endring av innstillingene).
- **App-innstillinger**: **Hopp over velkomstskjermen** åpner det siste siste **`.faproject`** ved lansering - eller den aktive øktfilen når en allerede er lastet inn - og navigerer til arbeidsområdet (**`/home`**, delt **MainLayout** med venstre skuff) i stedet for å bli på velkomstskjermen; spruten omdirigerer også når du slår på innstillingen mens velkomstruten vises. Hvis den nyeste filen ikke lenger eksisterer, viser appen en feil og forblir på velkomstskjermen i stedet for å åpne det neste siste prosjektet (inkludert etter oppstart allerede prøvd å hoppe over én gang); uten nyere prosjekter, vises velkomstskjermen fortsatt.- **Prosjektledelse**: Slippte **Lagre prosjekt** og **Lukk prosjekt** fra menyen til disse handlingene har reelle skrivebordsimplementeringer (**en-US**, **Deutsch**, **Français** kopier oppdatert).
- **Prosjekt SQLite**: Main beholder den aktive **`.faproject`**-banen ved siden av databasehåndtaket, slik at korte desynkroniseringer (for eksempel etter at vinduet er inaktive) kan åpne den samme filen på nytt, prøve operasjonen på nytt én gang, og eventuelt spørre gjengiveren om banen når hoveden mistet begge. Full gjeninnlasting fjerner fortsatt økten på ekte dokumentnavigering, men samme dokument- og underramme-churn slipper ikke lenger håndtaket alene.
- **OS-åpent håndtrykk**: Splash boot **`faProjectOsOpen`** venter nå kort på **`faContentBridgeAPIs.faProjectOsOpen`** før IPC-håndtrykket **renderer-klare**, så lanseres med en **`.faproject`**-bane på Electron **argv** (installasjonstilknytninger) **FA_E2E_OS_OPEN** ende-til-ende-replay) kan ikke gå glipp av ledninger når forhåndsinnlastede **`contextBridge`**-flater følger de første støvlene.- **Egendefinert prosjekt CSS**: Lagring fra den flytende editoren når ingen **`.faproject`**-økt er aktiv (for eksempel på velkomstskjermen eller etter at appen tømte databasehåndtaket) viser nå en tydelig **Egendefinert prosjekt CSS**-melding i stedet for å behandle en hoppet SQLite-skriving som vellykket og deretter vises en feiltilpasningsfeil.
- **Project Noteboard**: laster inn et nytt **`.faproject`** mens vinduet forblir åpent, flytter og endrer størrelsen på rammen for å matche prosjektets lagrede geometri i stedet for å etterlate det tidligere prosjektets fotavtrykk på skjermen.
- **Prosjektnotatbord**: flytting eller endring av størrelsen på vinduet sletter ikke lenger skrapelekst på skjermen når en lagringsreise med bare ramme leser SQLite før den avviste tekstskrivingen er ferdig; rendereren beholder verdien for levende tekstområde når du slår sammen tilbakelesninger for patcher som utelater **tekst**.
- **Prosjektnotatbord**: lagring av notattekst eller geometri utløser ikke lenger en feiltoast når **main** har tømt SQLite-håndtaket under en gjeninnlasting på nytt (for eksempel **Control+F5** eller oppdateringen av språkstavekontrollen): den utsatte skrivingen faller stille i stedet for å antyde en mislykket lagring tur-retur.- **Storybook**: Det nestede arbeidsområdet **innholdsbroen** mock now-stubber **prosjektnoteboard** pluss **prosjektstil** KV les og skriv så **WindowProjectNoteboard** og **WindowProjectStyling** **Autodocs** pluss forhåndsvisninger av lerret ikke lenger viser falske tekst-, **action-manager-vinduer eller autoe-flytende vinduer** CSS utkast. En **mock**-omfanget **TypeScript** konfigurasjon gjenoppretter **`app/`** baneskriving for vedlikeholdere uten å utvide typesjekken til hele forhåndsvisningspakken.
- **Storybook**: **AppControlMenus** Autodocs-komponentbeskrivelsen viser gjeldende **Prosjektledelse**-rader slik at de samsvarer med **`_data/prosjekt`** etter menyendringer.
- **Tester** — End-to-end **`checkProjectStyling`** gir nytt navn til **`.faproject`** basislinjer og suitetitler, slik at **Custom Project CSS** flyten beskriver Monaco live forhåndsvisning, SQLite utholdenhet, tømme stilarket, og i stedet for per-** en etikettisolasjon **a per-** Komponentsele **`WindowProjectStyling.playwright.test`** dekker flytende krom, kaldlastet **Monaco**, hjelpepanelets temaliste og **Lukk uten å lagre** slik at det lagrede prosjektet CSS er tomt.- **Tester** — **`checkProjectManagementFlow`** ende-til-ende hevder nå splash **Fortsett siste prosjekt** (**MRU-rullegardinetiketter** samsvarer med **Prosjekt → Last inn nylig prosjekt**, last inn på nytt gjennom det delte primærsegmentet etter tilbakestilling av stavekontrolløkten, last inn fra en annen nylig rad).
- **Tester** — **`checkProjectOsOpenArgv`** replays pakket **electron.launch argv** kald åpner med **`FA_E2E_OS_OPEN=1`** isolert fra vanlige splash-only lanseringer.

## 2.4.12 - Versjonsbump

### Nye funksjoner
- **Topplinjemenyer** (**Engelsk**): De fire primære **`AppControlMenus`** **`tittel`**-strengene lyder **Prosjektledelse**, **Dokumenter og innhold**, **Innstillinger og verktøy**, og **Hjelp og info** (erstatter tidligere korte **Prosjekt** / **Dokumenter** og **Innstillinger ** og **Im, deretter verktøy ordlyd). Undermenerader bruker store bokstaver i overskriftsstil på hovedordene (**en-US**) under disse titlene. Topptitlene **Deutsch** og **Français** matcher de samme fire rollene.- **Formulering på appnivå**: Brukervendte etiketter og dokumentasjon for ripe-**notatboard** (stilt **notatavle**, ett ord), **Appinnstillinger**, **Tilpasset app CSS** og **Importer/eksporter appkonfigurasjon** bruker nå konsistent **app**-ordforråd (interne kanalnavnidentifikatorer, og IP-kanalnavnidentifikatorer). **Dokumenter og innhold** viser **Hurtiglegg til nytt dokument**, **Hurtigsøk eksisterende dokument**, **Massesletting av dokumenter** og **Eksporter prosjekt/dokumenter**. **Innstillinger og verktøy** lister **Slå på appnoteboard**, **Tilpasset app CSS**, **Takebindingsinnstillinger**, **Appinnstillinger**, deretter **Importer/eksporter appkonfigurasjon** (under et skilletegn). **electron-store** og **.faconfig** ZIP-oppføringer bruker bare **`faAppNoteboard.json`** og **`faAppStyling.json`**.
- **App Noteboard**: **Close** stabler den dempede, parentesiserte **Veksle App Noteboard**-akkorden under etiketten ved å bruke den samme tastebind-hintfargen og kompakte typestørrelsen som snarveislinjer på **Innstillinger og verktøy** menyrader. Nedstrippet komponenttesting lanserer lastvedvarende tastebindinger når det oppsettet monteres, slik at forhåndsvisninger løser hintet som det vanlige skallet.- **Velkomstskjerm** — appen åpner til standardruten med samme toppmenyer, språkkontroll og vindusknapper som resten av appen; den venstre navigasjonsskuffen forblir skjult bare på den skjermen (**`faMainLayoutHideDrawer`** rute **meta**). Den sentrerte kroppen samsvarer med klassisk **FA 1.0** (velkomstlinje og tittel med speilet vingekunst, tre primærhandlinger med gull omriss uten atferd ennå, en skillelinje, deretter den gjeldende **Sosial kontakt**-knappraden). Velkomstlinjen, vinge **img**-paret og handlinger live i **`SplashControls`** (**`src/components/other/SplashControls/`**, layout-tokens i **`styles/_variables.scss`**); **`SplashPage`** beholder sideskallet, skillelinjen og **`SocialContactButtons`**. Vingekunst bruker **`public/images/splash/splash_logo.png`** (justert med **512**-favikonet). **`index.html`** forhåndslaster den **PNG** med høy henteprioritet; hver vinge **img** bruker **loading=eager** så kunsten er forespurt for første maling. Dev scratch **Index**-siden forblir på **`/home`** med det samme skallet inkludert skuffen.- **Aktivt prosjekt**: når **`S_FaActiveProject`** ikke har noe lastet prosjekt, deaktiverer **Prosjektledelse** **Lagre prosjekt**, **Vis prosjektdashbord**, **Prosjektinnstillinger**, **Lukk prosjekt** og **Slå et annet prosjekt inn i det gjeldende** (under **Avanserte prosjektverktøy**); **Dokumenter og innhold** deaktiverer **Quick-Add New Document**, **Quick-Search Existing Document**, **Massesletting av dokumenter** og **Eksporter prosjekt/dokumenter**. **Innstillinger og verktøy** forblir fullt tilgjengelig, inkludert **Importer/eksporter appkonfigurasjon** som siste rad under **Appinnstillinger**. **Nytt prosjekt**, **Last inn prosjekt**, **Avanserte prosjektverktøy** (overordnet rad), **Konverter eldre prosjekt til FA 2.0** og **Slå på appnotatbord** forblir tilgjengelige.- **Nytt prosjekt** — **Velkommen**-skjerm **Nytt prosjekt** og **Prosjektledelse → Nytt prosjekt** åpner en vedvarende innstillingsdialog, og lag deretter en **`.faproject`** SQLite-fil via hovedprosessen (**Splash** og **Prosjektledelse** koblet gjennom **`faActionManager`**). Overskriften viser det aktive prosjektnavnet når et er lastet inn. Når opprettelsen lykkes, viser **`faActionManager`** én positiv **Notify**: **Nytt prosjekt opprettet og klart til bruk!** med prosjektnavnet på samme linje (dialogen sender ikke en annen skål).
- **Last inn prosjekt** — **Velkommen**-skjerm **Last inn prosjekt** og **Prosjektledelse → Last inn prosjekt** åpner en OS-filvelger som er standard til **Nedlastinger** (Playwright **E2E** bruker isolerte **brukerdata** i stedet for den opprinnelige dialogboksen). Hvis du velger et **`.faproject`**, åpnes det i hovedprosessens SQLite-håndtak og oppdaterer **`S_FaActiveProject`** som **Nytt prosjekt**. Én positiv **Notify** bekrefter lasting med **Prosjekt lastet og klart til bruk!** og prosjektnavnet på samme linje.- **Last inn nylig prosjekt** — **Prosjektledelse** inkluderer **Last inn nylig prosjekt** (etter **Last inn prosjekt**) med opptil **10** nylig åpnede eller opprettede **`.faproject`**-filer. Oppføringer kommer fra skrivebordsskallets lagrede liste; manglende filer slettes automatisk. Hvis du velger en rad, åpnes den banen på samme måte som **Last inn prosjekt** og flytter den til toppen av listen når åpningen lykkes; åpnes med bane som mislykkes fjern den raden fra listen bortsett fra når prosjektet allerede er aktivt.
- **Quasar** offentlige merkevariabler på **`:root`**, fra **`--q-primary`** til **`--q-dark-page`**, er bundet på nytt til de samsvarende **Fantasia** **`--fa-color-…`** navnene i **`src/css/ic-quassar**-app. stilark) slik at **Custom App CSS** kan drive **`var(--q-*)`** fra samme Fantasia-fargetokensett.- **SCSS** temalayout: **`app.palette.scss`** (inkludert semantiske teksttokens under **APP COLORS**), standard egendefinerte egenskaper i **`fa-theme.scss`**, delt **`faSemanticText.scss`** ( **`fa-text-`***-i, **`fa-text-`***-i **`.text-white` / `.text-black`** kartlagt til **Fantasia** nøytrale), delt **`q-*`** tokens under **`src/css/theme/quasar-components/`**, samlokalisert funksjon **`styles/_variables.scss`**, globalscss.**`variables.s. **`scrollbar.variables.scss`**, og et slankt **`quasar.variables.scss`** fat. **`yarn audit:quasar-component-tokens`** hjelper med å fange opp manglende **`$q*`**-definisjoner.
- **UI-kopi og ikoner** bruker disse hjelperne i stedet for ad hoc **Quasar** Materialtekstfargeklasser der en rolle skal følge kjøretidstema. **Notify** standardverdier er ikke lenger satt **`textColor`**.- **Egendefinert app CSS** — **?** hjelpemenyen (for eksempel under **Fantasia Archive variabelliste**) viser **`--fa-color-…`** navn med en liten fargeprøve til venstre for hvert navn (prøven bruker **`var(…)`** fra **`:root`us-appen **, så den samsvarer med C__us-appen din ** og ** Når live-stiler er tilstede (inkludert **Vite** HMR i dev), kommer navn fra **DOM**; ellers faller listen tilbake til de samme navnene som er analysert fra den medfølgende **`fa-theme.scss`**-teksten. Gjengiveren er ikke lenger avhengig av **Node**-fillesing for det panelet.
- **Tilpasset app CSS** — **Editor-tastebind**-hjelp (**?** i tittelraden): en **`QMenu`** stilt som det globale Fantasia **QTooltip** erstatter det gamle verktøytipset som kun er på musen. Den åpnes etter samme **500 ms** sveveforsinkelse eller ved et klikk på hjelpeikonet, forblir åpen for lesing til du klikker utenfor panelet, trykker på **Escape**, eller klikker på hjelpeikonet igjen; **`no-focus`** forhindrer at **Monaco** blir uskarp når menyen åpnes.- **Topplinje**: **Hurtiglegg til nytt dokument**, **Hurtigsøk i eksisterende dokument** og **Massesletting av dokumenter** flyttet fra **Innstillinger og verktøy** til **Dokumenter og innhold** (samme handlinger og etiketter); **Dokumenter og innhold** avsluttes med **Eksporter prosjekt/dokumenter**. **Prosjektledelse** viser ikke lenger **Eksporter prosjekt/dokumenter** (samme handling lever kun under **Dokumenter og innhold**). **Importer/eksporter appkonfigurasjon** er den siste raden i **Innstillinger og verktøy**, under **Appinnstillinger** og en skilletegn.
- **Topplinjemenyer**: nestede undermenypaneler (for eksempel **Prosjektledelse → Last inn nylig prosjekt** og **Avanserte prosjektverktøy**) åpnes når du peker på den overordnede raden, med en kort forsinkelse før de lukkes slik at pekeren kan bevege seg inn i det flytende panelet; Hvis du klikker på raden, veksler du fortsatt undermenyen der **Quasar** støtter den sammen med kontrollert synlighet.
- **Hjelp og info**: **Om Fantasia Archive** vises ovenfor **Changelog** (før **Lisens**).

### Feilrettinger og optimaliseringer
- **Tilpasset app CSS**: **Quasar** mørk **`q-field`** understreker, inndatakopi, etiketter og fremhevede eller skrivebeskyttede kanter bruker nå **`var(--fa-color-white)`** eller **`color-mix`** mot den i stedet for faste **`#fff` / RGBA**. Mørke **`q-toggle`** inaktive tommelfingerskiver, indre etikettstabel og tommelikon på tilstanden følger **`--fa-farge-hvit`** på samme måte.
- **Velkommen / Om sosiale rader**: **Sosiale** brikkeetiketter og fyll bruker kun **Sass** palett-tokens (ingen **`fa-v()`** eller kjøretids-**`--fa-color-*`** ledninger i de variable komponentfilene), så **Custom App CSS** kan ikke gjeninnsette **__,T. **GitHub**, **Discord**, **reddit**, **Nettsted** eller **X** merkevarebygging. **Discord**, **reddit** og **GitHub** etikettfarger er festet på **`q-btn`** roten med **`!important`** så nedarvede **Quasar** regler for mørk tekst overstyrer dem ikke.- **Tilpasset app CSS**: Overstyringer for Fantasia **`--fa-color-dark`** (og matchende merkesymboler på **body**) når nå **Quasar** **`.q-dark`** bakgrunner med **`var(--q-dark)`**. Stilarkbroen gjentar **`--q-*`** egendefinerte egenskapsbindinger på **body**, ikke bare på **`:root`**, fordi arvede CSS variabler har sin beregnede verdi; **kroppsnivå** **`--fa-farge-*`** redigeringer ville ellers mislykkes i å male mørke **Quasar** overflater på nytt. **`.q-dark`**, **`body.body--dark`**, **`.q-layout__section--marginal`** og mørk **`.q-list` / `.q-item`** krom **bruk** **`var(--fa-farge-hvit)`** eller **`farge-miks`-** mot **`nær-hvit-`** i stedet for **`nær-hvit-` **`--fa-farge-hvit`** gjenoppretter disse stablene sammen med **`--fa-farge-mørk`** / **`--q-primær`** fyll.- **Engelsk kopi**: **en-US** **løpende prosa** (dialogtekster, avsnitt, **`Notify`** meldingslinjer, endringsloggfortelling) forblir setningsstil (første ord avkortet, små bokstaver senere, med mindre akronymer eller produktnavn (**CSS**, **CSS**, **__**TK2__, **__**TK2__, **__**TK2__, **__**TK0, Electron, Monaco, SQLite, UI, **`macOS`** osv. **`globalFunctionality.faActionManager`**-etiketter, sjekklistetekster ved siden av brytere, hoved-**dialog** og **flytende vindu**-overskrifter som **Fantasia Archive-innstillinger**) bruker store bokstaver i overskriftsstil på hovedord, mens korte grammatiske ord forblir små bokstaver der konvensjonelle (**flettes en**, **tilbake) dialogoverskrifter behold **Avansert søkeveiledning** og **Tips, triks og trivia** stavemåter. De fire **topplinjene** **`tittel`**-linjene forblir rikere med små bokstaver (se **Nye funksjoner** ovenfor **Fransk** speiler overskriftene på sammenlignbare rader (**Astuces, conseils & anecdotes **).ritten mekanisk.
- **Desktop persistens**: **electron-store** gir ikke lenger automatisk nytt navn til eldre desktop JSON-filer under **userData**; **.faconfig** import aksepterer bare de gjeldende **`faApp*.json`** basenavnene inne i arkivet; ukjente nøkkelbinding-overstyringsnøkler (inkludert pensjonerte IDer) blir droppet ved opprydding i stedet for å skrives om til nye IDer.
- ** Ordlyd**: Resterende etiketter på applikasjonsnivå ble normalisert til **app**-vokabular (for eksempel **Custom App CSS**-titler på **en-US** / **de** / **fr**, tips-trivia, importarkivvalideringskopier, noteboard-brofeil og samsvarende testbidragsdokumenter).
- **Dialog- og flytende vindu-titler** bruker **Quasar** **text-h5** på semantiske **`h5`** overskrifter (**Om Fantasia Archive**, **Action Monitor**, **Keybinding Settings**, **Fantasia Archive Innstillinger**, **Importer / Eksporter **C **C-appkonfigurasjon**,__ **App Noteboard**) slik at overskrifter matcher en enkelt kompakt skala.- **App Noteboard**: redigeringsområdet bruker den samme panelbakgrunnen **`mørk-lysere`** som **Innstillinger og verktøy** meny rader; **Lukk** er sentrert i den nederste handlingsraden. Notatfeltet er et naturlig **tekstområde**, så innholdet ruller i vinduet når det er lite; Innebygd stavekontroll er slått av, med **`autokorrektur`** / **`autofullfør`** hint av og et **Grammarly** opt-out-attributt for scratch-tekst. **`h5`**-tittelen bruker samme vertikale margin-token som **Custom App CSS**, slik at begge overskriftene i flytende vindu er på linje.
- **Importer/eksporter appkonfigurasjon**: **Eksport** og **Import** sjekklistepaneler er hver pakket inn som sine egne **Quasar** **QStep** underordnede av dialogen **QStepper**, så hvis du velger **Eksporter** eller går videre til importvalg, vises riktig trinninnhold i stedet for bare det første panelet som registreres med trinn-per-panelet. Stepperkroppen holder en minimumshøyde og bruker ikke lenger den horisontale lysbildeanimasjonen, slik at sjekklisten forblir synlig i pakkede **Electron**-bygg der den forrige flex- og overgangsstilen kunne gjøre innholdsområdet kollapset eller klippet etter trinnbytte.- **Importer/eksporter appkonfigurasjon**: **Bruk import** fra et **.faconfig**-arkiv oppdaterer **Tilpasset app CSS** i **Monaco** mens det flytende vinduet forblir åpent slik at redigeringsprogrammet samsvarer med nyimportert stil, samme oppførsel som ripeteksten **når den forblir åpen.
- **Topplinjemenyer**: den første raden i hver åpne meny eller undermeny viser ikke lenger den tynne skillelinjen over den; tynne skillelinjer vises fortsatt mellom påfølgende rader og fulle skillerader oppfører seg som før.
- Menyen **Innstillinger og verktøy** viser ikke lenger **Veksle hierarkisk tre** som en menyrad.
- **Vedlikehold**: Fjernet en ubrukt reeksport av keybind-akkordetiketter, foreldede kommenterte plassholdere på slutten av **Electron** hovedinnføringsfilen fra tidlig SQLite-oppføring, og en overflødig **Hjelp og info**-meny TODO i menydata (ingen endring av synlige menyer eller snarveier).- **Action Monitor**: kopiering av en handlingsrad til utklippstavlen viser **Handlingsrad kopiert til utklippstavlen** som toasttittel (**Notify** **`melding`**) med en tekstlinje **Aktivitet kopiert -** og den registrerte handlings-ID (**de** / **fr** bruker samme mønster med lokaliserte strenger); at positive toast hopper over den speilvendte **`[Notify]`**-raden i utviklerverktøy (**kopifeil** utelater fortsatt det dupliserte speilet ved siden av **`console.error`**).
- **Utviklerverktøy**: hver **Notify** toast skriver også en samsvarende linje til utviklerverktøykonsollen (**console.error**, **console.warn**, **console.info** eller **console.log**) fra toast **type** eller **farge**, merket med **`[Notify]`**, pluss **tekst, **tekst, **tekst,**,**,** **timeout** når tilstede, slik at du kan korrelere popup-vinduer med loggutdata; baner som allerede sender ut sin egen **konsoll**-linje for samme hendelse, hopper over det dupliserte speilet.
- **Utviklerverktøy**: **Hjelp og informasjon → Slå av/på utviklerverktøy** og forhåndsinnlastningsbanen **openDevTools** spør den medfølgende Chromium DevTools-grensesnittet om **Console**-panelet etter å ha åpnet når det interne API-et eksisterer, i stedet for å stoppe på standard **Elementer**-fanen.- **Action manager**: feil **Notify** rader velger bort det automatiske toast-to-console-speilet når behandleren allerede sender ut **console.error**, så feillinjer forblir enkeltstående (samme krok gjelder der **Action Monitor** radkopiering mislykkes etter sin egen **console.error**-rad).
- **Quasar palett** (**`app.palette.scss`**): **negative**, **positive** og **advarsel** merkevarespor bruker dypere, mindre skarpe hex-verdier; **info** bruker en fast varm **#ffebb8**, så rammefarget **Notify** bakgrunn forblir sammenhengende med mørk-på-lys-behandling. Samlet setter disse merkevaresporene **Material**-tilordnet krom på linje med Fantasia-identiteten.
- **Last inn nylig prosjekt**: **Prosjektledelse**-raden for **Last inn nylige prosjekt** samsvarer med **Avanserte prosjektverktøy** (grå sidepilstiling) og forblir deaktivert når det ikke er noen nylig liste, i stedet for å åpne en tom undermeny; hver nylige oppføring viser prosjektnavnet med **`.faproject`**-banen på en annen linje i samme dempede stil som meny-snarvei-tips, uten etterfølgende ikon.- **Last inn prosjekt**: hvert **`.faproject`** lagrer nå et vedvarende **prosjekt** UUID i SQLite; prøver å laste det samme prosjektet på nytt mens det allerede er aktivt viser en enkelt klar melding i stedet for å åpne filen på nytt, og **Notify** bruker en **advarsel** stil (ikke rød) mens konsollen og **Action Monitor** fortsatt registrerer en mislykket handling.
- **Notify** — **advarsel**-type toasts maling avvisning og annen action-rad **QBtn** krom i samme mørke tone som meldingsteksten slik at ikoner forblir lesbare på den lyse **advarsel** bakgrunnen (anropsnettsteder passerer ofte fortsatt Quasar farge **hvit**, ment for røde feilfelter).
- **Skrivebordsøkt** — et fullstendig hovedvindu **WebContents** gjenopplasting (for eksempel **Control+F5** eller språkstavekontrollens oppdatering) dropper hovedprosessens aktive **`.faproject`** databasehåndtak slik at appen matcher en fersk gjengiver med **ingen** prosjekt åpent før du laster inn en igjen.- **Språkkontroll**: stavekontrollikonet ved siden av språkvelgeren bruker klassen **`fa-icon-spellcheck-refresh`** igjen så **`--fa-color-icon-spellcheck-refresh`** maler den røde aksenten; den hadde referert til et ikke-eksisterende **`fa-color-…`**-verktøynavn og falt tilbake til standard lysikontone.
- **Nytt prosjekt** — suksess **Notify** vises bare én gang (**`faActionManager`**); dialogboksen for nytt prosjekt legger ikke lenger ut en ny skål med overlappende kopi.
- **Tester** — ende-til-ende **Dekningsmål for nytt prosjekt** **Prosjektledelse → Nytt prosjekt** uten feilklikking på velkomsthandlinger hvis etiketter også inneholder **prosjekt** (**Playwright** bruker et eksakt tilgjengelig navn for menyknappen **Prosjektledelse**).
- **Tester** — Vitest **enhetskomponenter** fikk bredere grendekning for dialoger, flytende vinduer, topplinjemenyer, **Hovedoppsett**, språkvelgerkanter og **Tilpasset app CSS** injeksjon via ekstra monteringer pluss små **`scripts/maler eller timets` laget direkte** å slå.- **Tester** — ende-til-ende **Playwright** spesifikasjoner **`checkAppNoteboard`** og **`checkAppFloatingWindows`** omslag **notatboard** ripetekstvedvarende på tvers av omstarter og flytende **Egendefinert app CSS** pluss **App Noteboard** **geometrisk resistens, **stackz-indeks, **stacks-indeks, **s. en **`WindowAppNoteboard`** komponentselespesifikasjon dekker flytende krom og brotekst-utholdenhet isolert.
- **Nytt prosjekt** — feltet **Nytt prosjektnavn** er fokusert når dialogboksen åpnes slik at du kan begynne å skrive uten et ekstra klikk.- **Nytt prosjekt** dialogboks: ikke vedvarende lenger, så bakteppe og Escape lukker den igjen som andre dialoger. Etiketter bruker **Opprett nytt prosjekt** og **Nytt prosjektnavn** (**en-US**, **de**, **fr**); den gamle dempede **navnHint**-linjen under feltet er borte. Navnefeltet tømmes hver gang arket åpnes. kortet holder tettere handlingsavstand og feltet bruker sentrert bredde og **primært lys** på det tematiske inndataskallet. Modalkomponentens registernavn er **`DialogNewProject`** (erstatter **`DialogNewProjectSettings`**); **`faActionManager`** bruker **`openNewProjectDialog`**, og **vue-i18n**-baner bruker **`dialogs.newProject`**. **no-US** tilgjengelighetsetikett stemmer overens med **Nytt prosjekt**.
- **Nytt prosjekt** — **OS-lagre**-dialogboksens foreslåtte **`.faproject`** filnavn beholder mellomrom og store bokstaver når det forblir trygt på tvers av vanlige filsystemregler i stedet for å omskrive stammen til små bindestreker; tegn som stier avviser, erstattes av mellomrom eller droppes for kompatibilitet (**emoji**, kontrolltegn). Den foreslåtte standarden inkluderer fortsatt **`.faproject`**-suffikset sammen med typefilteret slik at den tilbudte banen forblir eksplisitt på tvers av plattformer.- **Nytt prosjekt** — **`.faproject`**-baner aksepteres når de er absolutte på verts-OS eller bruker en **Windows**-stasjon eller **UNC**-rot, så validering samsvarer med ekte lagringsdialoger selv når hovedkjører på ikke-Windows (for eksempel automatisert testing).
- **Nytt prosjekt** — navnefeltet aksepterer maksimalt **120** tegn (**Quasar** **q-input** **maxlength** med en levende tegnteller). Den samme grensen gjelder for hovedprosessvalidering, den foreslåtte **OS Save** stammen og navn som er lagret for **Last nylig prosjekt**; hvis vedvarende data fra nylige prosjekter ikke oppfyller denne regelen (for eksempel et navn som er lengre enn **120** tegn), fjerner skrivebordsskallet den lagrede listen til du åpner prosjekter på nytt.
- **Dokumenter og innhold** undermenylister **Quick-Add New Document**, **Quick-Search Existing Document**, **Mass Delete Documents**, deretter **Export Project / Documents** in **en-US**, with justed **de** / **fr** kopier; **Eksporter prosjekt / dokumenter** flyttet hit fra **Prosjektledelse** tidligere; **Importer/eksporter appkonfigurasjon** vises nederst i **Innstillinger og verktøy**. Toppmeny **`tittel`** etiketter forblir ikke-innpakning så lange fraser forblir på én rad.- **Tilpasset app CSS** (**no-US**): hjelpepanelets overskrift for temavariabellisten er **Fantasia Archive variabelliste** igjen (erstatter den forkortede **FA variabelliste**-etiketten).
- **Semantisk tekst**: **danger emphasis** (**`fa-text-danger-emphasis`**, palett **`$text-danger-emphasis`**) bruker en lysere rød (**#ff4c4c**) for klarere kontrast på mørk krom.
- **Appinnstillinger**: den valgte **kategorien**-fanen til venstre bruker den lyse **primære** etikettfargen igjen. **fa-tekst-dempet** gjelder nå bare for faner som ikke er valgt, så den bekjemper ikke lenger **Quasar** **aktiv farge** når begge verktøyene setter **farge** med **`!viktig`**.
- **Storybook**: Autodocs viser ikke lenger den første historien to ganger når en fil definerer flere historier; **DialogActionMonitor** inkluderer Autodocs som andre dialogbokser; **Foundation** verktøytips for fargepalett utelater den ekstra **SCSS**-linjen for **`$info`**; **SocialContactButtons** sletter den overflødige **CompositionContract**-historien (foreldreløs visuell grunnlinje fjernet).- **Storybook** visuelle øyeblikksbilder: iframe-beredskapsventen inspiserer **`#storybook-root`** og **`#root`** separat for elementbarn (sammen med synlig portal **Quasar** dialog eller menyoppmerking) så **`querySelector`** på en gruppert **root-`#storybook kan ikke velge en tom **root-`#storybook alene **`#root`** når forhåndsvisningstreet lever under **`#storybook-root`**.
- **Hjelp → Endringslogg**: hvis en utgivelseslinje forsøkte å vise **CSS** regeltekst ved hjelp av **krøllete klammeparenteser** blokkavgrensninger, kunne **vue-i18n** behandle disse tegnene som meldingsplassholdere; Det kan hende at dialogboksen ikke gjengis. Den berørte **2.4.12**-linjen er omformulert på vanlig språk.
- **QTooltip**: popup-vinduer på mørk krom bruker **`$accent`** (**#f5f5f5**) og **`$dark`** (**#1b333e**), **13px** kopi med vekt **600**, **600px** maks bredde, en høy **linje for **`z-`-strek for **`z-` `bokstavavstand`** i **`quasarComponentsAdjustments`**, i stedet for **Quasar** lagerverktøytipsstørrelser og farger.- **Visste du?** (**info** **Notify**): avvisningshandlingen er **`farge: mørk`**; **`_qNotify`** bruker **mørk** forgrunn på **`.q-notification.bg-info`**, og **`quasarComponentsAdjustments`** bruker den **`fargen`** med **`!important`** slik at den vinner over den samme nodens **Quasar** **``tekst-white. bruker **!viktig**, som ellers ville tvinge nesten hvitt på en lys **info**-bakgrunn.
- **Velkomstskjerm** og **Om** sosiale rader: **Reddit**-brikken bruker kun ordmerkebildet (ingen egen tekstetikett; **en-US**, **de**, **fr**). Velkomstkopi bruker overskriftsmarkering og dempet undertekstopasitet. **SocialContactButtons** hekker **row justify-center**-omslag slik at Patreon/Ko-fi og sekundære lenkegrupper forblir horisontalt sentrert på brede layouter.
- **Om Fantasia Archive**: dialogtittelen bruker en nivå-fire overskrift slik at dokumentomrisset samsvarer med hvordan tittelen leses visuelt.- **Velkomstskjerm** — primære handlinger bruker bredere horisontal polstring og **tilpasningsinnhold**-bredde, slik at hver **knapp** tilpasser seg etiketten i stedet for å strekke hele handlingskolonnen. **SplashPage** og **SplashControls** leser layout fra samlokaliserte **`styles/_variables.scss`**-moduler **`@forward`ed** til **`quasar.variables.scss`**, matchende semantiske **SCSS** tokenregler brukt andre steder under **`s/`rc.**/
- **Velkomstskjermen** tittelraden og rullegardinmenyen **språk** på øverste linjen undertrykker valgbar tekst under drag slik at krometiketter ikke fanger opp tilfeldig fremheving.
- **Dependencies**: **dompurify** (trukket inn av **Monaco**) er festet til **3.4.2** med en **Yarn** **resolutions**-oppføring slik at redigeringsprogrammet sporer en lappet **3.x** sanitiseringsutgivelse i stedet for en eldre transitiv pin.
- **Electron desktop**: **`BrowserWindow` `preload`**-banen løser seg til Quasars pakkede **`preload/electron-preload.cjs`** artefakt, så **`contextBridge`** APIer (**`window.faContentBridgeAPIsIsbly hopper over filnavnet på nytt) i stedet for å starte filen på nytt i stedet for å starte filen på nytt fra **`dev`**-pakkeoppsettet.- **Action Monitor**: vellykket **Load prosjekt** historierader holder køens nyttelast tom, men legg ved en avsluttende forhåndsvisning med den valgte **`.faproject`** banen og lastet prosjektnavn, på linje med hvordan **Nytt prosjekt** overflater detaljer etter fullføring; når åpning mislykkes etter at du har valgt en fil, kan mislykkede rader inkludere meldingen pluss den forsøkte banen når skrivebordsskallet leverer den.
- **Electron desktop**: **`faRoutingEnv`** venter kort på forhåndslastet **ekstra miljø** øyeblikksbilde før du styrer innledende seleruter, og unngår et oppstartsløp hvor broen fester seg etter første maling.
- **Playwright hjelpere**: Utnyttelseskoden for Electron komponent og **E2E** suiter er delt inn under **`helpers/playwrightHelpers_universal`**, **`helpers/playwrightHelpers_e2e`** og **`helpers/playwrightHelpers_component so`se`r**, with helpers/playwrightHelpers_e2e`**, og **`helpers/playwrightHelpers_component so` forbli uniform; **`README`** og bidragsyternotater viser de oppdaterte importbanene.

## 2.4.11 - Versjonsbump

### Nye funksjoner
- **Topplinje**: menyetiketten **Verktøy** er **Verktøy og innstillinger** (og samsvarende **de** / **fr**-strenger) slik at navnet samsvarer med innstillingsrelaterte oppføringer i menyen.
- **Lokal paritet**: **de** og **fr** følger nå det samme **vue-i18n** nøkkeltreet som **en-US** (inkludert **errorNotFound**, **globalWindowButtons**, **fantasiaMascotImage**, **dialogs.markdownDocument** **globalFunctionality.unsortedAppTexts** og medfølgende **dokumenter**), så vinduskrom, feilsiden, tipsoverskriften og maskot-alt-teksten løses uten feil eller manglende nøkler. **Appinnstillinger** i **de** bruker fortsatt strengmodulen **en-US** inntil en full **de** app-innstillingspass lander; **fr** beholder sin delvise **appinnstillinger**-fil med **en-US** reserve for resten. **Import / eksport** varslingslinjer ble strammet inn i **en-US** med samsvarende **de** / **fr**-oppdateringer.
- **Importer / eksporter app-konfigurasjon** — **Import** / **Eksport** / sjekkliste-panelene bytter med et horisontalt lysbilde (stepper-animasjon); trinnoverskriften forblir skjult slik at flyten fortsatt ser ut som én dialogboks.- **Importer/eksporter appkonfigurasjon** — øverst i dialogboksen inneholder en lokalisert **Vennligst**-blokk (eksport-først-veiledning, overskrivingsadvarsel og selektiv import-alternativ) i **en-US**, **de** og **fr** før **Import** / **Export**-velgeren.- **Verktøy → Importer/eksporter appkonfigurasjon** (**mdi-database-export-outline**), direkte under **Vis noteboard**: åpner en ikke-vedvarende **Importer/eksporter appkonfigurasjon**-dialog (utvendige klikk og **Escape** lukk den) med **Import** og **Eksport** på det første trinnet ** med dens hele loddrettsbredde i to kolonner px** horisontalt innfelt for handlingsraden, flate **primære** handlinger (fet, store etiketter), som samsvarer med den klassiske **FA 1.0** **Sett inn bilde**-dialogen. **Eksport** tilbyr tre forhåndsavmerkede avmerkingsbokser (**Appinnstillinger**, **App-nøkkelbindinger**, **Tilpasset app CSS**), en **Opprett eksportfil**-kontroll (aktivert bare hvis minst én del er valgt), og deretter den vanlige **OS-lagre**-dialogen for å skrive en **`.faconfig`** (ZIP-nummer med samme Jlectron-navnet-**`e-app-navnet-**` **`faUserSettings.json`**, **`faKeybinds.json`**, **`faAppStyling.json`**). **Import** bruker dialogboksen for åpen fil kun for **`.faconfig`**; hovedprosessen håndhever en **3 MB**-tak, godkjente medlemmer, **JSON** + **Zod**-validering og iscenesettelse før du bekrefter **Importer valgt**; å bruke erstatter de valgte butikkene og rendereren laster inn appinnstillinger på nytt, keybinds, og app **CSS** fra broen. Flyten registrerer **faActionManager**-handlinger (**openImportExportAppConfigDialog**, **exportAppConfigPackage**, **exportAppConfigSaveResult**, **importAppConfigStageResult**, **importAppConfigApply**) for **Action-monitoren** med små, monitor-safe-filer.- **Verktøy → Egendefinert app CSS**: en ny oppføring åpner en **vedvarende** **Tilpasset app CSS** flytende vindu (ikon **mdi-language-css3**) med en kaldlastet **Monaco** editor konfigurert for **CSS** språkstøtte. Vinduet er **bevegelig** (dra i tittelraden) og **kan endre størrelse** (kant- og hjørnehåndtak); den kan kun lukkes med **Lukk uten å lagre** eller **Lagre endringer** (**Escape** og tilfeldige klikk utenfor vinduet forkaster det ikke). Lagret CSS lagres i **Electron** hovedprosessen via **electron-store** og brukes globalt som et **`<style id="faUserCss">`**-element som injiseres i **`document.head`** ved appinnlasting og oppdateres når du lagrer endringer, så tilpassede regler gjelder på tvers av alle ruter, layouter og innebygde overflater. Rammen er en in-renderer **`posisjon: fast`** overflate teleportert til **`document.body`**, sentrert og dimensjonert fra viewport-brøker og marginer, med **Vue** **Transition** og den egendefinerte **fa-floatingWindowPop**-animasjonen (opasitet og skala; minimumsskala er aldri som **00m**T** **0 m**s; stock **q-transition--scale** **scale(0)**, som kan etterlate en degenerert **transform** i **Electron**). **Monaco** lastes bare når window åpnes (og bare **CSS** språkpakken og arbeideren er samlet) så den første oppstarten av appen påvirkes ikke. **Verktøy → Tastebindingsinnstillinger** lister **Åpne tilpasset app CSS** med en standard snarvei på **Ctrl + Alt + Shift + J** (**Cmd** erstatter **Ctrl** på **macOS**); akkorden kan tilordnes på nytt eller slettes derfra. Både åpning og lagring flyter gjennom **handlingsbehandlingen** (**openAppStylingWindow** og **saveAppStyling**), så lagringsfeil får samme enkeltvarslingsbehandling som andre innstillingeroverflater. (Oppstrøms **[@quasar/quasar-ui-qwindow](https://github.com/quasarframework/quasar-ui-qwindow)** pakkemål **Quasar v1 / Vue 2** bare; Fantasia Archive sender en **Vuee 3.** i appen i stedet.)- **Handlingsbehandling**: hver brukerbetydende handling (åpne dialogboks, lagre innstillinger, minimer / endre størrelse / lukk vindu, språkbryter, veksel av utviklerverktøy, varsling om oppstartstips og lignende) flyter nå gjennom en enkelt koordinator med to utførelsestyper - **asynkronisering** (fire-and-glem) og **synkronisering** (enkelt ** ** FIFO , 3 ** FIFO s tidsavbrudd per handling, opt-in deduplisering). Feil viser et enkelt **feil**-varsel med handlings-ID og den underliggende feilmeldingen, og de samme detaljene lander i utviklerkonsollraden ved siden av den. Lagre flyter for **Keybinding-innstillinger** og **Appinnstillinger**-ruter gjennom denne overflaten, så de tidligere doble varslene om en mislykket lagring er borte.- **Hjelp og info → Handlingsmonitor**: oppføringen under **Slå på utviklerverktøy** åpner en **Handlingsmonitor**-dialog med en tabell over handlinger fra gjeldende appøkt: **Handling**-id, **Starttid**, **Sluttid**, **Nyttelast** (grønn hake når nyttelasttekst i køtid er tom**), **Grå synkronisering **Async**), og **Status** (girspinner mens du løper, grønn hake på suksess, rødt kryss ved feil, blått sand-timerikon mens du står i kø, hver med et verktøytips for pekeren for betydningen). Et hjelpeikon forklarer at et venstreklikk kopierer raddetaljer til utklippstavlen. Rutenettet er et **øyeblikksbilde** når dialogen åpnes, begrenset til **500** rader per økt, ikke skrevet til disk. **Keybinding-innstillinger** viser **openActionMonitor** slik at du kan tilordne en global snarvei; det er fortsatt ingen standardakkord.- **Verktøy → Innstillinger for nøkkelbinding** åpner en dialogboks for **Nøkkelbindingsinnstillinger** (tabell over handlinger, standard og egendefinerte snarveier, filtrer, lagre til vedvarende lagring i **Electron** hovedprosessen). **Hjelp og info** viser ikke lenger en separat oppføring for nøkkelbind-jukseark; snarveier administreres fra **Verktøy**. Globale snarveier forblir aktive mens du skriver inn inndata der bindingen tillater det, og sender pauser mens denne dialogboksen eller popup-vinduet er åpen.
- **Grensesnittspråk**: en **språk**-kontroll i topplinjen (ved siden av vindusknappene) viser gjeldende lokalitet som et **flagg** i en sirkulær kontroll og åpner en kort meny for **Engelsk, US**, **Deutsch** og **Français**. Valget lagres som **språkkode** i vedvarende **brukerinnstillinger** (**en-US**, **de** eller **fr**, standard **en-US**) og gjelder **vue-i18n** på tvers av appen. **Appinnstillinger** viser ikke dette feltet ennå.
- **Landsflagg**: samlet et komplett sett med **SVG**-flaggbilder under **`public/countryFlags/`** for språkkontrollen og andre lokalitetsbestemte brukergrensesnitt.- **Tysk** **topplinje**: **Prosjekt**, **Dokumenter**, **Verktøy** og **Hjelp og info** menyoppføringer bruker **Deutsch** lokalitetsmoduler ved siden av **Om** og den delte **sosiale**-raden, som samsvarer med **en-US** menyformen.
- **Avansert søkeveiledning**: standard snarvei **Ctrl + Alt + Shift + G** åpner nedmerkingsguiden i samme dialog som **Hjelp og info → Avansert søkeveiledning**; **Verktøy → Tastebindingsinnstillinger** viser det som **Åpne Advanced Search Guide**.
- **Tips for menysnarveier**: **Verktøy** og **Hjelp og info** viser den direkte globale snarveien ved siden av **Appinnstillinger**, **Takebindingsinnstillinger**, **Avansert søkeveiledning** og **Slå på utviklerverktøy**, ved å bruke de samme etikettene som **Verktøy → Tastebindingsinnstillinger**; hintlinjen vises bare etter at tastebindinger er lastet inn fra **Electron** og bare for disse handlingene.
- **Storybook**: **AppControlSingleMenu** og **AppControlMenus** historier inkluderer **`keybindCommandId`** inventar (toppnivå og undermeny), en komposisjonssjekk som åpner **Hjelp og info** og forventer oppmerking av snarveihint, og en valgfri **`embed like so production dialogs` variant.- **Ende-til-ende**: **`checkKeybinds`** Playwright-spesifikasjonen dekker standard **Ctrl + Alt + Shift + G** og en vedvarende tilpasset akkord for **Open Advanced Search Guide** mot markdown-guide-dialogen, i samme flyt som andre globale snarveier.
- **Ende-til-ende**: **`checkAppStyling`** Playwright-spesifikasjonen åpner **Tilpasset app CSS** fra **Verktøy**, legger inn **CSS** i **Monaco**, lagrer så **document.body** plukker opp **zoom** fra vinduet, tømmer stilarket, tømmer stilarket på nytt **CSS**, og hevder at **zoom** går tilbake til standard.
- **Storybook**: **`externalFileLoader`** registrerer **`floatingWindows.appStyling`** for **en-US** så **WindowAppStyling** **autodocs** og lerretshistorier løser titler, verktøytipskopi og handlingsetiketter i stedet for rå **i-i;18**vue historien starter flerlinjers eksempel **CSS** så **Monaco** syntaksutheving er lett å oppdage.- **Storybook**: **DialogImportExportAppConfig** lerret og **autodocs**; **`externalFileLoader`** registrerer **dialogs.importExportAppConfig**; arbeidsområdet innholdsbroen mock legger til **faAppConfig** stubber slik at **prepareImport** kan lykkes i lerretet mens **exportToFile** returnerer **cancelled**, og matcher den reelle flyten nok til å se gjennom dialogen uten **Electron**.
- **Ende-til-ende**: **`checkImportExportAppConfigFlow`** kjører import/eksport app-konfigurasjonsflyten (eksport, trinnvis import, **Action Monitor**-sjekker og relaterte **Keybinding** / app-styling-bivirkninger) ved å bruke faste baner i den isolerte **Playwright**-profilen; **E2E** kan hoppe over opprinnelige fildialoger for disse trinnene via en hovedprosesssøm som bare gjelder når **`TEST_ENV`** er **e2e**. **Playwright** snarveisstrenger bruker **`helpers/playwrightHelpers_universal/faPlaywrightKeyboardChords.ts`** så standardverdier som følger appens **primære** modifikator og akkorder som bruker den fysiske **Control**-tastematchen **macOS__** og **Windows**s host**s.
- **Komponenttest**: **`DialogImportExportAppConfig.playwright.test`** dekker close-on-**Escape** for import-/eksportdialogen i **komponent**-selen.

### Feilrettinger og optimaliseringer
- **Playwright** (komponent, **E2E** og **Storybook** visual) bruker terminalens **`liste`**-reporter: hver test skrives ut på sin egen linje mens kjøringen skrider frem (HTML-rapporter uendret).
- **Topp-appmenyer** (**AppControlSingleMenu**): etter en **separator**-rad i full bredde i menydataene, stables ikke lenger den tynne skillelinjen før neste element på hele skillelinjen (ingen doble horisontale regler).
- **Importer/eksporter appkonfigurasjon** — **OS** lagre og åpne dialogboksene er standard i **Nedlastinger**-mappen din, og eksport foreslår filnavnet **faConfigExport.faconfig** slik at den tilbudte banen er konsistent når et bart filnavn ellers ville havnet i en uventet mappe på enkelte systemer.
- **Tilpasset app CSS** flytende vindu — rammen **Transition** bruker en tilpasset **fa-floatingWindowPop**-animasjon (omtrent **0.1** til **1** skala pluss opasitet, samme **300 ms** som standard **QDialog** og standard **q5__** og standard **q)-overgang i stedet for en rotovergang. degenerere **transform** i **Electron** og bryte **Monaco** i **Playwright** synlighetskontroller.- **Tilpasset app CSS** flytende vindu — endre størrelse på kant- og hjørnehåndtak viser en myk **primær** gradient mot ytterkanten bare mens du svever eller drar; inaktive håndtak forblir usynlige. Gløden toner inn og ut med en kort opasitetsovergang. Håndtaksfarger er temasymboler i **quasar**-variabler; reglene lever i **`_FaFloatingWindowFrameResizeHandles`** **`stiler`** slik at **Vue**-filen holder seg innenfor linjegrensene.
- **Flytende vinduer**: pekerens størrelse oppdaterer rammebredde og høyde maksimalt én gang per skjermramme, noe som reduserer overflødig layoutarbeid for **Monaco** og resten av overflaten mens du drar i et håndtak.
- **Tilpasset app CSS** flytende vindu — tittelteksten tvinger ikke lenger **`margin: 0`**, så overskriften beholder normal avstand ved siden av draraden og hjelpeikonet.
- **Tilpasset app CSS**: mens den flytende editoren er åpen, gjelder **Monaco** endringer live for det globale **`#faUserCss`** stilarket slik at du kan forhåndsvise regler før du lagrer. **Lukk uten å lagre** laster inn **CSS** på nytt fra disk (samme bro lest som ved oppstart) slik at appen går tilbake til den siste filen som ble bevart.- **Tilpasset app CSS** flytende vindu — **Layout** begrenser ikke lenger bredden ved **1400 px**. **CSS** bruker **`max-width: stretch`** i stedet for **`100vw`** slik at rammen forblir innenfor det synlige oppsettet når rullefelt eller innsettinger gjelder. **`<Transition>`** omslutter rammeroten, ikke **Teleport** (så enter/la ut kjøring på et enkelt element og **Vue** advarer ikke om ikke-element overgangsbarn); åpen/lukk bruk **fa-floatingWindowPop** (se **Feilrettinger**). **_FaFloatingWindowBodyTeleport** sender rammen til **`document.body`** så **Storybook** og tester er ikke avhengig av et **`#q-app`** monteringsmål. Tittelraden bruker en flex **min-høyde** slik at dragstripen holder et ekte treffmål. Flytende overflater bruker et **z-indeks**-bånd under hovedkrom. Etter avslutning avhendes **Monaco** bare etter dialogovergangen; en ventende avhending kanselleres hvis du åpner igjen eller lukker igjen før den forsinkelsen er over.- **Playwright** **garn** scripts: **`scripts/playwrightWithArtifactTrim.mjs`** gjør **`--component=bucket/ComponentFolder`** til **`src/components/...`** når **Garn** på **Windowsnpms** ikke utvides ** og konfig **s plassholder **testresultater/dramatiker-artefakter** etter en kjøring slik at dupliserte vedleggstrær ikke blir igjen på disken.
- **Tilpasset app CSS** **directInput** ledninger (ledningsnett og **Storybook**): **`directInput`** prop-overvåkeren kjører **umiddelbart** ved oppsett, så **`WindowAppStyling`** åpnes når **`directInput`** allerede er **WindowAppStyling**.
- **Ende-til-ende** **`checkAppStyling`**: beredskaps- og lukkekontroller bruker **`windowAppStyling-frame`**-lokatoren i stedet for å stole på tittelsynlighet alene etter at vinduet åpnes.- **Electron gjengivelsesprotokoll**: Pakkede bygg laster nå gjengiveren gjennom et tilpasset privilegert **`app://`**-skjema registrert i hovedprosessen (**standard**, **secure**, **supportFetchAPI**, **stream**, **codeCache**, **bypassCSP**) i stedet for **.`file://`** Behandleren løser gjengivelsesroten ved siden av den løpende hovedbunten, nekter banegjennomgang og **`..`**-segmenter, og faller tilbake gjennom **`index.html`**. Dette er hva som lar vinduet **Tilpasset app CSS** spinne opp **Monaco**-arbeideren (**css.worker** lastet med **Vite** **`?worker`**-importer) pålitelig i den pakkede appen, siden nettarbeidere ikke initialiserer på samme måte under **`file://`**. Utviklerserverflyten er uendret.- **Globale nøkkelbindinger**: innebygde standardverdier på **Windows** og **Linux** er nå **Ctrl + F12** for **Toggle utviklerverktøy**, **Ctrl + Alt + Shift + L** for **Åpne appinnstillinger** for **K__T5__, **__TK5**, **__TK5** **Åpne innstillinger for nøkkelbinding**, **Ctrl + Alt + Shift + J** for **Åpne egendefinert app CSS**, og **Ctrl + Alt + Shift + G** for **Åpne Advanced Search Guide** (på **macOS**K 5) modifier; **Åpne Advanced Search Guide** bruker alltid **Ctrl**). Eksisterende lagrede overstyringer i hovedbutikken **Electron** er uendret.
- **Markdown-dokument**-dialoger åpnet med **directInput** (for eksempel **lisens** eller **changeLog** fra menyene) registrerer nå den samme **markdown-dialogen stabelvakt** som rutet åpnes, så **Appinnstillinger** og andre komponentdialoger kan ikke stables på toppen før du lukker markdown-arket.- **Eksterne lenker** kobler til en liten **`faExternalLinkMouseDispatch`**-hjelper slik at klikk- og auxclick-logikken forblir testbar i **Node** Vitest (injisert **`getBrowserWindow`**) mens produksjonen fortsatt leser **`globalThis.window`**. Behandleren no-op banen kjører når **`faExternalLinksManager`** eller dens metoder mangler, slik at ikke-**Electron** gjengivelser ikke kaster.
- **Globale nøkkelbindinger**: hvis lasting av snarveier fra **Electron** hovedprosessen mislykkes, viser appen et **Quasar** varsel med en kort **lastefeil**-melding i stedet for bare å logge; den samme meldingen vises hvis **Verktøy → Tastebindingsinnstillinger** ikke kan oppdateres ved åpning.
- **Komponenttestsele**: ugyldig **COMPONENT_PROPS** JSON fra **`extraEnvVariables`** ignoreres (**false**) i stedet for å krasje hovedprosessens **IPC**-behandler.
- **Storybook** visuell regresjon: **`garntest:storybook:visual`** mislykkes nå når en histories iframe aldri viser gjengitt innhold under Storybook-roten (og ingen synlig portaldialog eller meny), med mindre den historien velger seg bort med **`skip-visual-render-check`** eller dens **ED**Stor-ID-liste visuell Playwright spes.- **Bidragsyter-dokumenter**: **README**, **AGENTS.md**, **CONTRIBUTING**, **testing-terminal-isolation**, **storybook-stories**, **git-conventional-commits**, **fantasia-dev-setup**, **fantasia-testing-ensia-**, og ** stay** **Garn** **1.x** og CI **1.22.19**, **i18n** **komponenter** bøtter inkludert **dialoger**, **testbatch:verify** dekningsnivåer (inkludert **i18n**), repo-root **hjelpere** kun for selekode **, korriger **yRT lin **, **yRT lin regler** --edit`** ordlyd, og repo-root-relative lenker i endringsloggferdigheten.
- **Bidragsyter-dokumenter**: **README**, **AGENTS.md**, **playwright-tests**, **testing-terminal-isolation**, **fantasia-testing** og **fantasia-keybindings** dokument cross-OS **`keyboard.press`** regler for app-tastebindinger og pek på **`helpers/playwrightHelpers_universal/faPlaywrightKeyboardChords.ts`**.- **Bidragsyter arbeidsflyt**: **AGENTS.md** og markør **regler** / **ferdigheter** (**changelog-en-us**, **git-conventional-commits**, **testing-terminal-isolation**, **fantasia-changelog-en-us**, **fantasia-i18n-us**,booking**stories**,booking** beskriv folding av **`changeLog.md`** til samme commit-batch som innholdsarbeid før **push**, bruk av changelog-only commits for sjeldne rettelser, og hold Git- eller housekeeping-fraser ute av utgivelsesnotater i appen.
- **Takebindingsinnstillinger**-fangst behandler fysiske modifikasjonstaster (**ShiftLeft**, **ControlRight**, **AltLeft** og lignende) som ufullstendige inndata i stedet for å pare dem med dupliserte modifikasjonsetiketter, og aksepterer snarveier bygget fra bokstaver, sifre, **F1–F24-tastene, tu-tastene og pu-tastene, taster (komma, parentes, skråstrek og lignende) – ikke taster som bare inneholder numeriske tastaturer eller de fleste andre spesialtaster. Disse veiledningslinjene bruker feltet **hint** (informasjonsstil); **duplisert snarvei** på **Set** vises fortsatt som en feltfeil.- **Takebindingsinnstillinger** popup-vindu for fangst er på linje med **legacy Fantasia Archive (FA 1)** capture polish: **handlingsnavnet som tittel, en **Skriv inn nøkkelbindet** plassholder, en **tynt stiplet** understreking under akkordlinjen (**rød** kun for dupliserte**tynt-konflikter **modifikator-tone-konflikter eller **dempet tynt gjelder), og **ett kompakt panel rett under** akkordraden hvis **tekst bytter** mellom **behovsmodifikatoren** / **ikke-støttet nøkkel**-veiledning og **dupliserte snarveismeldingen** avhengig av hva som nettopp skjedde (**feiltone og **varsel**-rolle kun for dupliserte kasus). Panelet **blekner og glir** i det sporet i stedet for å stable to separate meldingsområder. Konfliktikonet **varsel** blir ved siden av akkordraden når en duplikat er aktiv; det gamle frittstående **info**-ikonet for hint er borte. **hjelp**-verktøytipset viser fortsatt modifikasjonsmønstre og aksepterte nøkler; **Tøm tastebinding** og **Set** forblir på lyspanelet. Trykker med kun fysisk modifikasjon unngår fortsatt å male plassholderen rød; et enkelt modifikatortrykk etter **trenger modifikator** eller ikke-støttet tastveiledning kan fjerne hintet igjen.- **Keybinding settings** capture **Clear keybinding** setter den handlingens arbeidsoverstyring til **null** slik at hovedtabellens **User keybindings**-kolonne umiddelbart samsvarer med den innebygde standarden igjen (**Legg til ny...** når den standarden er det som er aktiv, samme regel som vedvarende **null** overstyrer), lukker linjen for opptak uten plassering, og viser bare linjen for tom plassering. snarveitekst ennå).
- **Tastebindingsinnstillinger** fanger opp en duplikatsnarvei så snart akkorden gjenkjennes, ved å bruke hver rads **aktive** binding (standarder pluss ulagrede overstyringer). En standard som ikke lenger gjelder fordi en annen handling ble gjenopprettet, blokkerer ikke gjenbruk. Den dupliserte meldingen er tydeligere på **engelsk**, **Deutsch** og **Français**; en bortkommen modifikasjonsnøkkel etter en konflikt fjerner ikke lenger advarselen alene. Mens den dupliserte feilen vises, forblir fangstlinjen på snarveien som var synlig da du åpnet opptak (eller forblir tom når den raden ikke hadde noen snarvei) i stedet for å vise den avviste akkorden; **Sett** med en motstridende akkord gjør det samme.- **Tastebindingsinnstillinger**-opptak åpnes med radens **gjeldende**-snarvei allerede vist (standarder og egendefinerte overstyringer), dropper den overflødige **Bruk standard**-kontrollen fra opptaksarket, og legger inn fangst-popup-vinduet i hovedtastebindingsdialogen. Hovedarket for **Nøkkelbindingsinnstillinger** forblir **vedvarende**; du kan lukke opptaksarket med et **utvendig klikk** eller **Escape** uten å lukke den overordnede dialogboksen. **Escape** på opptaksarket håndteres i opptakslytteren, slik at det alltid avviser det arket i stedet for å bli behandlet som en snarveitast som ikke støttes.
- Dialogboksen **Takebindingsinnstillinger** bruker en **1200 px** kortbredde på brede visningsporter (den krymper fortsatt på smale vinduer), tabelloverskrifter **Brukernøkkelbinding** og **Standard nøkkelbindinger**, og filterplassholderen **Filter nøkkelbindinger...**. **Brukertastebindingene** **Legg til ny...**-kontrollen bruker **primær-lys**-omriss mens den aktive snarveien samsvarer med den innebygde standarden eller ingenting er tilordnet ennå, og **grå-7**-kontur med den formaterte akkorden kun når en egendefinert snarvei er forskjellig fra den standarden.- **Keybinding-innstillinger** **Standard keybindings**-kolonnen viser en **em-strek** når den innebygde standardakkorden ikke kan utvides for gjeldende plattform, i stedet for misvisende formatert tekst.
- **Innstillinger for nøkkelbinding**: snarveitabellen bruker det vertikale rommet mellom dialogtittelen og handlingsraden i bunnteksten, og rullehøyden oppdateres når du endrer størrelsen på vinduet.
- **Tastebindingsinnstillinger**: snarveistabellen viser bare ekte snarveihandlinger fra tastebindingslisten; Forhåndsvisningsrader for kun utvikling av fyllstoff fjernes.
- **Takebindingsinnstillinger** verktøytips for fangsthjelp (**engelsk**): klarere oppsummering av tillatte nøkler og **din nøkkel** plassholderordlyd i eksempler på modifikatorkombinasjoner.- **Keybinding-innstillinger**-filter uten treff viser samme **Feilkort**-behandling som **Appinnstillinger**-søk (maskot i **lesemodus, tittel og støttetekst fra **vue-i18n**) via **q-table** **no-data**-sporet i stedet for en tom tabell. Tabellens nederste linje er bare skjult mens rader eksisterer, fordi **Quasar** maler **ingen data** inne i den linjen og **skjul-bunn** hopper over den helt når den alltid er på. **Virtuell rulling** på den tabellen slås av mens den filtrerte radlisten er tom, så **Quasar** monterer fortsatt **no-data**-sporet i stedet for å etterlate en tom virtualisert kropp. Sletting av filterfeltet kaster ikke lenger når **Quasar** setter **v-modell** til **null** i stedet for en tom streng. Filterfeltet tilbakestilles til tomt hver gang dialogen skjules, inkludert etter **Lagre innstillinger** eller **Lukk uten å lagre**.- **Electron stavekontroll**: Chromiums stavekontrollspråk sporer den samme vedvarende **språkkoden** som grensesnittet (**en-US**, **fr** eller **de**), og **Legg til i ordbok** kontekstmenyetiketten samsvarer med dette valget i stedet for operativsystemets lokalitet. **Français** og **Deutsch** faller ikke lenger tilbake til **engelsk** når Chromiums tilgjengelige liste utelater de nøyaktige hunspell-nøklene vi først ser etter; vi matcher **fr-*** / **de-***-taggene i listerekkefølge, og prøv deretter de korte **fr** / **de**-kodene ved behov. Etter en språkendring slår hoved-**økten** stavekontrollen av, sletter den forrige ordboklisten, angir det løste språket og slår stavekontrollen på igjen. Tittellinjen **språk**-kontrollen kan vise en valgfri **reload**-handling etter at du har valgt et annet grensesnittspråk, slik at du kan oppdatere vinduet selv hvis stavekontrollen i et allerede åpent felt fortsatt følger den forrige ordboken (Chromium beholder noen ganger den gamle bindingen til en navigering).
- Kontroll av **Språk på øverste bjelke**: **Deutsch** og **Français** strenger for valgfri stavekontroll **reload** hint (**aria-etikett** og flerlinjers verktøytips) gjenspeiler **engelsk** ordlyden og linjeskiftene.- **Språklinjeflagg** og **sosiale** verktøylinjeikoner: **offentlige/** eiendeler (for eksempel **countryFlags** SVG-er og **images/socialContactButtons**) løses nå med de samme **Vite** **BASE_URL**-reglene som resten av gjengiveren, ved å bruke en **tilhørende**-pakke fra **filen**. **Electron**, så flagg og ikoner lastes i stedet for å bryte på rot-absolutt **/**-URL-er.
- **Om Fantasia Archive** dialog: **Tysk** kopi for tittelen, versjonslinjen, **Lukk** og raden for sosiale koblinger (**Patreon**, **Ko-Fi**, **Nettsted**, **GitHub**, **Discord**, **Reddit**X** verktøytips, **Reddit**X**). **Fransk** hadde allerede dialog- og knappestrenger; lokalitetstreet registrerer nå også **`socialContactButtons`** under den samme camelCase-nøkkelen som **engelsk**, slik at disse etikettene løses i **Français** i stedet for å falle tilbake.
- **Språkmeny**: etter å ha valgt et nytt grensesnittspråk, bruker **innstillinger lagret** toast den **nye** lokaliteten i stedet for den forrige.
- **Språkmeny**: **en-US** alternativetiketten er **Engelsk, US** (var **Engelsk**) i alle grensesnittlokaliteter.- **Toppmenyer i appen** (**Prosjekt**, **Dokumenter**, **Verktøy**, **Hjelp og info**): Etiketter følger nå den aktive **vue-i18n**-lokaliteten når du endrer språk (de ble løst en gang ved oppstart og holdt seg på det opprinnelige språket). **AppControlSingleMenu** leser også **tittel** og **elementer** på nytt når forelderen sender oppdaterte menydata.
- **Vindukontroller**: den egendefinerte tittellinjen **maksimer** / **gjenopprett**-bryteren lagrer vindusgrensene før **maksimer** og bruker dem på nytt etter **unmaksimer**, slik at gjenoppretting fra en maksimert tilstand går tilbake til forrige størrelse på **Windows** med et rammeløst **Electron**-vinduet ved ** alene.
- **Electron** **preload**-bro: **Window** krom, **DevTools**, den pakkede **versjon**-strengen og selen **extraEnvVariables** øyeblikksbilde bruker **async** **IPC** via **`ipcMain.handle`** og **`ipcMain.handle`** og **`ipcMain.handle`** og **`ipcMain.handle`** og **`ipcvoc-synchronous. **renderer** stopper ikke ved disse samtalene.- **Appoppstart og komponenttester**: **`faRoutingEnv`** oppstart laster selens øyeblikksbilde, og **venter på** oppstartsruting slik at **`router.push`** fullføres før skallet gjengis. **`ComponentTesting`** løser den testede **SFC** fra ruten reaktivt og setter **COMPONENT_PROPS** fra **`extraEnvVariables.getCachedSnapshot`** når oppstart allerede har løst **`getSnapshot`**; **`AppControlMenus`** leser **`TEST_ENV`** på samme måte for den første malingen. **Preload** **`extraEnvVariables`** legger til **`getCachedSnapshot`** etter den første vellykkede **`getSnapshot`**-løsningen. **ComponentTestingLayout** laster nå vedvarende **brukerinnstillinger** fra main på mount i **Electron** og bruker **vue-i18n** på nytt fra **languageCode**, matchende **MainLayout**, slik at en full vindu-innlasting under isolerte komponentkjøringer beholder det valgte grensesnittspråket.
- **Globale vindusknapper**: tilgjengelige navn lever kun på **`aria-label`**; **Quasar** verktøytips og **`title`**-attributtet ble fjernet fra disse tittellinjeknappene.- **Appinnstillinger** søk uten treff: tomt tilstand **ErrorCard** viser overskriften **Ingen søketreff** og lengre veiledning fra **vue-i18n** som **detaljer** under maskoten (**en-US** og **fr**). **ErrorCard** viser valgfrie **detaljer** bare der, med **data-test-locator** kroker **errorCard-title** og **errorCard-detaljer**; **Feil ikke funnet** beholder kun tittel og flere linjer **detaljer**.
- **Storybook** **`externalFileLoader`** mock registers **dialogs.markdownDocument** fra **L_markdownDocument** så **DialogMarkdownDocument** historier løser **Close**-etiketten og relaterte strenger i stedet for rånøkler.
- **Storybook** **`externalFileLoader`** mock registers **dialogs.actionMonitor** så **DialogActionMonitor** historier løser titler, kolonneoverskrifter og knappeetiketter i stedet for rå oversettelsesnøkler.
- **Storybook** visual capture kaller **page.waitForFunction** med **undefined** som injisert-argument-parameteren før options-objektet, så Playwright bruker gjengivelsestidsavbruddet i stedet for å vente uten en indre tidsfrist.- **Storybook** — bidragsyter **.storybook-workspace** config deaktiverer sjekklisten for introduksjon av sidefeltet, slår av **Hva er nytt**-varsler i **kjerne**-alternativer, og **lederhode** skjuler den grønne uleste prikken på innstillingsutstyret for **garnhistoriebok:løp** og statiske bygg.
- **Appinnstillinger** Playwright forventningshjelpere importerer **dialogAppSettingsTreeOrdering** i stedet for **dialogAppSettingsTree** så **Node** laster ikke inn **app/i18n** markdown-råimporter når spesifikasjonsmodulgrafen løses.
- **FantasiaMascotImage** sentrerer kunst horisontalt når punktgrafikken er smalere enn sporet (**margin: auto** på den indre **img**).- **Eksterne lenker**: **http** og **https** URL-er valideres med en ekte URL-parse og en **http** / **https** skjemagodkjenningsliste før **openExternal** kjører i main eller forhåndsinnlastingen **checkIfExternal**-hjelperen kjører i rendereren. Loopback og ikke-ruterbare mål blokkeres ved å bruke den analyserte verten (**localhost**, vertsnavn som slutter på **.localhost**, **127.0.0.0/8**, **::1**, IPv4-tilordnet loopback, **0.0.0.0**), prosentkodet **localhost** er fremdeles ikke lenger gjenkjent som https://sd, og ** ikke lenger er passsd. **https**. Vertsnavn som bare inneholder bokstavene til **localhost** inne i en etikett (for eksempel **notlocalhost.example**) forblir tillatt når den analyserte verten ikke er loopback.
- **Brukerinnstillinger**: **Electron** hovedprosessen validerer hver lagringsoppdatering med **Zod** før den slås sammen i **electron-store**, og aksepterer kun kjente nøkler (**boolean** veksler og **languageCode** som **en-US** / **de** / **fr**) og avviser ukjente felter eller feil. **S_FaUserSettings** Pinia-lageret viser den eksisterende lagringsfeilvarslingen når **setSettings** mislykkes (inkludert validerings- eller brofeil) i stedet for bare å logge et ubehandlet avslag.- Sendt versjon er nå **2.4.11** i **Om** og pakkemetadata.
– **Bidragsyterdokumenter**: **README**, **AGENTS.md**, **electron-preload**-regel og **fantasia-electron-main** / **fantasia-electron-preload** ferdigheter beskriver **renderer-sandbox** (**BrowserWindow** **webPreferencesTK**), offisielle ____0 kontekstkoblinger, sandbox og __0 lenker. **hoved** **IPC** (seler **extraEnvVariables**, eksterne **http** / **https**-lenker), **startApp** registrerer **ipcMain**-behandlere før det første vinduet, en **asynkron-først**-preferanse for **påkalling** / **håndtak** fremfor synkron **IPC** når det finnes rimelig **IPC-design**-struktur, synkronisert med IP-utforming**. **Zod** i hoved versus **type** pluss domenesjekker, og hvilke behandlere som forblir skalarbaserte. Korte kommentarer om **elektron-forspenning** og **elektron-hoved** gjengir de samme rekkverkene.- **Bidragsyter arbeidsflyt**: samlokaliserte **Vitest** filer, **Playwright** komponentspesifikasjoner og **Storybook** historier ligger nå under **`_tests/`** kataloger på tvers av **`src`**, **`src-electron`**, **`helpers`** n`**'s, **'s to next to utpakkede skriptmoduler der de ble brukt (det tidligere mappesegmentnavnet var **`tester`**). **Vitest** dekning utelukker, **Storybook** historieoppdagelsesglober og bidragsyterveiledning (**README**, **AGENTS.md**, markørregler og ferdigheter) følger den nye banen. Toppnivå **`e2e-tester`** og **`.storybook-workspace/visual-tests`** er uendret.
- **Appinnstillinger**: mens overskriftssøkefeltet har tekst, dekker den dimmede scrim igjen hele **dialogkortet**; tittel, rullbar brødtekst, søkefelt og bunntekst-handlinger forblir brukbare der stablingsrekkefølgen holder dem over overlegget.
- **Appinnstillinger**-implementering er delt inn i mindre **Vue**-underkomponenter og samlokaliserte **skript**-moduler slik at dialogboksen holder seg innenfor de påtvungne linjegrensene uten å endre den synlige layouten.- **Bidragsytervedlikehold**: delte **TypeScript** kontrakter for broer, dialogbokser, nøkkelbindingsdata, brukerinnstillinger og relaterte hjelpere er gruppert i repository-root **`typer/`** med kort **JSDoc**, som erstatter mange enkeltformålstypefiler og samlokalisert **`*.types.ts**``** ved siden av **````** **`src-electron/`**, og **`Storybook`** håner. **Takebindingsinnstillinger** og **Appinnstillinger** samlokaliserte **`scripts/`**-kode er slått sammen til færre moduler hver for samme kjøretidsatferd og enklere navigering samtidig som **ESLint** linjebudsjetter respekteres. **README**, **AGENTS.md**, markørregler og ferdigheter beskriver å foretrekke **færre logisk grupperte** **`scripts/`**- og **`src/scripts/`**-moduler inntil linjegrenser fremtvinger en splittelse, og konsolidering av samlokaliserte **`scripts/_tests/`** når produksjonsskript smelter sammen. Tomme gamle **`tests/`**-kataloger som er igjen fra **`_tests/`**-omnavnet ble fjernet fra treet.- **Bidragsyterverktøy**: **ESLint** håndhever grenser for antall linjer på produksjon **Vue**, **TypeScript** og **JavaScript** (tester, **Storybook**, konfigurasjoner, **i18n**, typemoduler og lignende baner forblir unntatt). **README**, **AGENTS.md** og Cursor-reglene beskriver caps—**Vue** SFC **250** linjer, **TypeScript** modul **200** linjer, funksjon **100** linjer—og når de skal splittes i **`scripts/`** eller underkomponenter. Returnerte objektliteraler i produksjonskoden må kun vise **tidligere bindinger** for egenskapsverdier (ingen innebygde funksjoner eller ternær logikk i bokstaven); se **`.cursor/rules/code-size-decomposition.mdc`**. Flytting av scoped **CSS** til en frittstående fil og reimportering av den forblir et unntak som krever eksplisitt vedlikeholdsgodkjenning.- **Bidragsytervedlikehold**: produksjons **`retur`** overflater som bruker objektliteraler (inkludert **action manager** kønyttelastforming, keybind-akkordhjelpere, **AppControlMenus** elementbyggere, **DialogActionMonitor** / **Keybind-innstillinger IP** / **Appinnstillinger** tabellhjelpere, **snapshot-hjelpere, **C0__**-bilder **Playwright** **`recordVideo`**-alternativer) binder nå **`const`**-verdier først slik at returnerte objektegenskaper forblir enkle identifikatorer eller bokstaver. **Action Manager**-implementeringer lever i **`src/scripts/actionManager/faActionDefinitionHandlers.ts`**, med **`faActionDefinitions.ts`** som kun holder **registertabellen**; **AGENTS.md** og **fa-action-manager** Markørregel/ferdighet beskriver delingen.- **GitHub Actions** manual **Build App** arbeidsflyt (**.github/workflows/build.yml**): **Windows** jobben kjører **yarn testbatch:ensure:nochange** ende til ende (samme kvalitetsport som merket **PR** full-suite-kjøringer, pluss én produksjon ****TK4__0** gjenbrukt for **Quasar0** **Playwright** og **Storybook** sjekker), laster deretter opp **Windows** installasjonsprogrammet uten en ny pakkeløsning. **Linux**-jobber starter først etter at porten lykkes og kjører som en matrise slik at hvert etappe laster opp sitt eget artefaktsett: **AppImage**, **Debian**-pakken og **RPM**. **macOS** kjører fortsatt én produksjonsbygging og laster opp **.dmg**. **Storybook** visuelle grunnlinjer forblir **win32**-justert, slik at hele porten forblir på **Windows-nyeste**.
- **Bidragsyterdokumenter**: **README**, **AGENTS.md**, **vue-quasar**-regel og **fantasia-quasar-vue** ferdighetsdokument ett **Vue** enkeltfiloppsett for hver **.vue**-fil: **mal** først, deretter **scriptarbeid**, deretter **2-stil**s**. **Appinnstillinger** hjelpekomponenter ble omorganisert i kilden for å matche; synlig oppførsel er uendret.- **Bidragsytervedlikehold**: kryssfunksjonene **Vue**-hjelperne for flytende vindu-kropp **Teleport**, håndtak for størrelse på flytende vindu, og den globale **`#faUserCss`**-injektorbruken **`_<PascalCase>`**-mappen og **`.vue`f`**-navnene (**`.vue`ind`**) **`_FaFloatingWindowFrameResizeHandles`**, **`_FaUserCssInjector`**) slik at de leser som infrastruktur ved siden av **`Window*`** og **`Global*`** overflater; **AGENTS.md** begrenser prefikset til disse tre. Tidligere notater i denne loggen som navngir disse brikkene, bruker nå samme stavemåte. Appens oppførsel er uendret.- **Bidragsytervedlikehold**: renderer **TypeScript** hjelpere er omgruppert under **`src/scripts/appInternals/`** (lokale applikasjonshjelpere, språkskriving, offentlige aktivabaner, **Storybook** lerretsgjenkjenning) og **`src/scripts/appGlobalManagement UI/`component, component UI/`component, maskot-ruting, dev-tools-veksling, oppstartsruting, trivia-varsler) med klarere eksporterte navn enn tidligere inngangspunkter i **lease**-stil. **Globalt tastatur** forhåndsbehandling og sending live i **`src/scripts/keybinds/faKeybinds*`** moduler som **`MainLayout`** komponerer gjennom **`createFaKeybindKeydownHandler`**. Hjelpemodulen **preload** eksterne lenker er nå **`faExternalLinksManagementAPI`** (atferd uendret; navnet stemmer overens med andre administrasjons-APIer).
- **Storybook** arbeidsområde **Vite** legger til **`server.fs.allow`** for depotroten slik at **Keybinding-innstillinger** **`.vue`**-filer kan løse søsken-**`typer/`**-kontraktimporter under statiske bygg og visuelle opptak.- **Storybook** statisk **visuell regresjon** (**`garntest:storybook:visual`**): **Vite** løser **Pinia** og **Vue** fra depotroten slik at det nestede arbeidsområdet ikke laster en ekstra **Pinia** kopi som forhindret mounting av dialogbokshistoriene i ** iframe. **Playwright**-selen betjener den statiske bygningen på en dedikert port og gjenbruker ikke en eksisterende server der, så en lokal **`yarn storybook:run`**-økt på standardporten kan ikke tilfredsstille fangst ved et uhell. Mislykkede opptak legger fortsatt ved et helsides skjermbilde og en kort **JSON**-probe av **Storybook**-roten når navigering, gjengivelsessjekker, fonter, overlegg eller skjermbildetrinnet mislykkes.
- **Bidragsytervedlikehold**: **Vitest** spesifikasjoner for **AppControlMenus**, **AppControlSingleMenu**, **GlobalLanguageSelector** (inkludert stavekontrollens oppdateringsforsinkelsesbane), **GlobalWindowButtons**, **Feilkort** og utvidede skjerminnstillinger, innstillinger for apper og trening, ** top-bar sele grener, språkvalg, vindu krom tidtakere, og **Quasar** meny klikkbaner i jsdom. Spesifikasjoner for multi-stub-dialoger bruker det samme **ESLint**-unntaket på filnivå som allerede er brukt på tester for stabelbeskyttelse.- **Bidragsytervedlikehold**: mer **Vitest** jsdom-dekning for **DialogActionMonitor**-dialogen viser oppdatering, **DialogImportExportAppConfig** åpner og importerer staging, **DialogKeybindSettings** når nøkkelbindingsdefinisjonslisten er tom, **_FaFloatingOverHindow resmeles point, **WindowAppStyling** editor hjelpeverktøytipsinnhold pluss last- og lastfeiloverlegg og rammepekerledninger, **MainLayout** appstyling oppdateres når **Electron**-broen avslører det eller ikke, **AppControlMenus** komponenttesting av nedmerkingsmenyutløser, **SomeControl**-undermeny-klikk. **GlobalLanguageSelector** når brukerinnstillingsbroen er fraværende, og **_FaUserCssInjector** hopper over duplikatstiltekstskrivinger.

## 2.4.10 – Appinnstillinger, ErrorCard, tematokens og bidragsyterverktøy

### Nye funksjoner
- **Verktøy → Appinnstillinger** åpner dialogboksen **Appinnstillinger** (vedvarende til **Lukk**; **Lagre** er reservert for senere kabling).
- **Appinnstillinger** viser hver vedvarende boolsk som **kategori → underkategori → veksle** rader med **en-US** / **fr** kopi (tittel, beskrivelse, valgfri rød **merknad**). **Kategori** og **underkategori**-fanene følger nøkkelrekkefølge bortsett fra **Tilgjengelighet** (alltid nest sist) og **Utviklerinnstillinger** (alltid sist); **veksler** innenfor en underkategori sorterer etter oversatt **tittel** (bindinger bruker innstillingstasten). **Vis dokument-ID-er** er under **Utviklerinnstillinger → Dokumenttekst**. En **Tilgjengelighet**-fane på toppnivå grupperer bredere rullefelt, skillelinjer for tretellinger, gjennomstreking og tekstskygge; tema og relaterte elementer forblir under **Visual- og app-omfattende funksjonalitet** uten å legge blokken der.
- Lagt til vedvarende **brukerinnstillinger** i **Electron** hovedprosessen (**electron-store** under app **userData**), som starter med en **tema**-preferanse (**lys** eller **mørk**, standard **lys**), eksponert for gjengivelsen som **`window.faContentBridgeAPIs.**Settings**s.- Lagt til **`S_FaUserSettings`** Pinia-butikk for administrasjon av brukerinnstillingstilstand i rendereren: laster inn hele innstillingene en gang ved appstart via IPC-broen, godtar patchbaserte oppdateringer og viser en **Quasar** melding om suksess eller feil etter hvert lagringsforsøk.
- **Appinnstillinger** bruker vertikale **kategori**-faner (**`q-tabs`** / **`q-tab-panels`**) og en **rullbar** hovedkolonne (**`q-scrollarea`**): **tittelen**, tabulatorlinjen og **Close** / **Lagre** forblir fast mens innholdet ruller. Underkategorivekslinger bruker et responsivt rutenett (**én** / **to** / **tre** kolonner etter bruddpunkt), plasser **veksleren** under tittelen og vis beskrivelser fra et hjelpeikon **verktøytips**.
- **Appinnstillinger** laster inn vedvarende bytteverdier fra **`S_FaUserSettings`** når dialogboksen åpnes ved hjelp av en bevoktet **Pinia** løser slik at miljøer uten en aktiv brukerinnstillinger lagrer hopper over hydrering i stedet for å kaste. Hjelpeikonjustering ved siden av titler med flere linjer skyves for tydeligere vertikal justering med den første linjen.- **ErrorCard** (**`src/components/elements/ErrorCard/`**): gjenbrukbart **q-card**-oppsett for feiltilstander (**tittel**, valgfri multi-line **detaljer**, **FantasiaMascotImage** variant, konfigurerbar **max-width**); colocated **Storybook** forhåndsvisning og **Vitest**. Ruten **Feil ikke funnet** bruker den med den eksisterende **`errorNotFound`** kopien i stedet for et innebygd kort og sidelokale **SCSS**.

### Feilrettinger og optimaliseringer
- **Bidragsyterdokumenter**: **README**, **AGENTS.md**, **fantasia-testing**, **storybook-historier** og **testing-terminal-isolation** beskriver **Storybook** **VRT** **toHaveScreenshot** **maxDiffPixels** som er forskjellig **hvorfor handlingsbudsjettet ____el, 0 **Windows-nyeste** kan avvike litt fra lokale **Windows**, og når du skal oppdatere grunnlinjene versus justere hetten.
- **Storybook** visuell regresjon: hevet **Playwright** **toHaveScreenshot** **maxDiffPixels** for statiske **Storybook** fanger så **GitHub Actions** **Windows-nyeste** består **garntest:storybook:visuelle grunnlinjer** mot different fonts **Windows** kjører).
- **GitHub Actions** **OSV-Scanner** arbeidsflyt (**.github/workflows/osv-scanner.yml**) viser nå **main** ved siden av **master** på **push**, **pull_request** og **merge_group**, slik at de samme skanningene kjører når standardgrenen bruker begge navnene- **Storybook** visuell regresjon på **Windows**: oppdatert forpliktet **Chromium** øyeblikksbilder under **.storybook-workspace/visual-tests/** for **ErrorCard** (feilside og appinnstillinger er tom), **FantasiaMascotImage** lesing, **Ingen **Innlegg for side standard, og **IndexPage** i layout – justerer grunnlinjene med gjeldende forhåndsvisninger etter native **img**-maskot- og layouttestkroker.
- **Electron** sikkerhet: hoved-**BrowserWindow** kjører nå **Chromium**-gjengivelsen med **OS-sandbox** på (**`sandbox: true`**, **`nodeIntegration: false`**). Seleverdier eksponert som **`window.faContentBridgeAPIs.extraEnvVariables`** er bygget i **hovedprosessen og leveres over sync **IPC**; åpning av eksterne **http**- eller **https**-koblinger i standardnettleseren håndteres i **main** (**`shell.openExternal`**) via asynkron **IPC** slik at forhåndsinnlastingen holder seg innenfor sandkassesikre **API**-grenser. **Playwright** lanseringsbaner bruker **`faPlaywrightElectronLaunchConstants`** i **`helpers/playwrightHelpers_universal/`** i stedet for å importere forhåndslastede moduler fra Node-testløperen.- **Electron** vindus-krom-synkroniserings-IPC: **hovedprosess**-behandlere for **minimer**, **maksimer**, **gjenopprettingsveksling** og **lukk** setter nå **`event.returnValue`** på hver bane. Uten den fullføres aldri **`ipcRenderer.sendSync`** og **rendereren** forblir blokkert, noe som fryste hele brukergrensesnittet etter **@electron/remote**-migreringen.
- Fjernet avhengigheten **@electron/remote**: rammeløst **vindu** krom (**minimer**, **maksimer**, **gjenopprett**, **lukk**, **maksimert**-tilstand) og den pakkede **appversjon**-strengen vist i **Om** bruk nå synkron **hovedprosessindlasting (**Connroller, Ipc-register) **registerFaAppDetailsIpc** i **mainScripts/ipcManagement/**), løser det eierende **BrowserWindow** fra IPC-avsenderen slik at oppførselen forblir korrekt når opprinnelige menyer stjeler OS-fokus.
- **Vindu** krom og **maksimer** / **gjenoppretting**-interaksjoner kan føles litt jevnere ved daglig bruk etter denne endringen: **rendereren** sitter ikke lenger bak **@electron/remote** for disse handlingene, og korrekt **`event.returnValue`**-håndtering hindrer **sendSync** i å stoppe grensesnitttråden. Nøyaktig oppfatning varierer etter maskin og arbeidsmengde.- Bumpet **aksios** til **1.15.0** til adresse **GHSA-3p68-rc4w-qgx5** (**CVE-2025-62718**): **NO_PROXY**-samsvar normaliserer nå vertsnavn (for eksempel **localhost** med en etterfølgende prikk i sløyfe) og 6-liters i parentes **NO_PROXY** sendes ikke gjennom en HTTP-proxy.
- **Appinnstillinger** vedvarte **boolske** nøkler i **elektronbutikk** bruker korrigerte interne identifikatorer (**aggressiv** relasjonsfilter, **kollaps** trealternativ, **etter valg** hurtigsøk-lukking).
- **Bidragsyter arbeidsflyt**: **README**, **AGENTER**, og markørregler og ferdigheter beskriver nå når vedlikeholdere kan hoppe over **`yarn testbatch:verify`** rett før en commit som endrer kun per-lokale endringslogg i appen. andre ventende endringer; hvis du er i tvil, kjør porten uansett.- **Bidragsyterdokumenter**: root **CONTRIBUTING.md** dekker gaffel **PR** etiketter, lokale **Storybook** **VRT** øyeblikksbildeoppdateringer når **CI** ikke kan sende grunnlinjer til gafler, skrivebeskyttede **`GITHUB_TOKEN`** begrensninger for offentlige gaffel **`pull_k **s** og`**er for handlingsflyt**s** og`**er innstillinger godkjenninger; **README** legger til en **Bidragende**-seksjon; **pull request**-mallenkene **CONTRIBUTING.md**.
- **Repository** root **.gitignore** ignorerer nå **.playwright-mcp** (arbeidsområde-lokale mapper fra **Playwright MCP**-serveren brukt med **Cursor** og lignende klienter; atskilt fra **Playwright** testutdata under **testresultater**).
- **Playwright** serieserier og **WebM** vedlegg: **`closeFaElectronAppWithRecordedVideoAttachments`** godtar en valgfri andre **`TestInfo`** så skjermopptak legges ved der **HTML**-rapporten viser dem for multi-test **`test.describe.serial`** grupper; komponent og **E2E** spesifikasjoner bruker én **Electron** lansering per seriegruppe med **`resetFaPlaywrightIsolatedUserData`** kun i **`test.beforeAll`** (**README** og **playwright-tests** beskriver mønsteret). Vitest dekker den oppdaterte **`playwrightElectronRecordVideo`**-hjelperen.- **Playwright** komponentdekning for **ErrorCard** trener live-selen (tittel, detaljer, **lesing** maskot, kortets maksbredde via **data-test-error-card-width**). Bidragsyterveiledning (**AGENTER**, **playwright-tests**, **fantasia-testing**) foretrekker **data-test-layout-width**, **data-test-layout-height** og domenetak for størrelseskontrakter når smale vinduer vil krympe **boundingBox**-resultater; bruk **boundingBox** for gjengitte piksler bare når selen garanterer nok plass.- **Storybook** **Komponenter/fundament**: designtidskataloger under **`src/components/foundation/`** — **`FoundationColorPalette`** (**QUASAR COLORS - GENERELT** fra **`quasar.variables.scss`** pluss standard **`**`bg-materiale**) og*__**`bg**s**) **`FoundationTextList`** (**Quasar** typografi: typeskala, skriftvekter, teksthjelpere per **quasar.dev/style/typography**). Kun lerret (**Docs** deaktivert), **`hopp over-visual`** så **VRT** hopper over lange sider; story globals pin **Tilbakestill viewport** (iframe i full bredde). **`FoundationColorPalette`** rot **SCSS** forblir gjennomsiktig slik at **Backgrounds**-verktøylinjen (**papir** / **mørk app**) maler lerretet; swatch-metadatastrimler bruker den mørke katalogbakgrunnen fra **quasar.variables.scss**.
- **Storybook** viewport-verktøylinjen legger til **Tilbakestill viewport** (100 % iframe) foran telefon-gjennom-**4K** forhåndsinnstillinger i **`viewportBreakpoints.ts`**; **`preview`** **`initialGlobals`** starter nå på den responsive forhåndsinnstillingen i stedet for en fast **1920px** skrivebordsramme.- **Storybook** visuell regresjon på **Windows**: oppdatert det forpliktede **Chromium**-skjermbildet for **Components/dialogs/DialogAppSettings** standard slik at det samsvarer med gjeldende **Reset viewport** standard og **Storybook** ramme.
- **Storybook** **Appinnstillinger**: **`DialogAppSettings`** godtar valgfrie **`directSettingsSnapshot`** (standardspeil **`FA_USER_SETTINGS_DEFAULTS`**) så **Docs** og **Canvas** viser hele kategorien **T og veksle KG uten __**; en samlokalisert dekoratør slår sammen **en-US** **`appOptions.*.title`** strenger med iscenesettelse **TODO** strekprefikser fjernet. **Vite** alias mock for **`app/i18n/externalFileLoader`** implementerer **`global.mergeLocaleMessage`** (dyp flette inn i scenariomeldinger) og **`global.te`**, som samsvarer med hva **`buildAppSettingsRenderTree`** forventer av produksjonen ** og dekoratoren. **Storybook** **`faContentBridgeAPIs`** stubben viser **`faUserSettings`** med det samme standardkartet slik at **Pinia** hydrering fortsatt fungerer når en historie utelater øyeblikksbildet.- **Storybook** komponent **`meta.title`** baner legges under **`Components`** etter repo-bøtte (**dialoger**, **elementer**, **globals**, **other**) pluss funksjonsmappenavnet slik at sidefeltet samsvarer med **`src/components/`**. **`yarn test:components:list`**-velgeren viser **`dialogs/DialogFoo`**-stiletiketter, og **`yarn test:components:single --component=`** tar den samme relative banen under **`src/components/`**. Oppdatert forpliktet **Storybook** visuelle grunnlinjer og **`EXCLUDED_STORY_IDS`** oppføringer for den nye historien **`id`**-formen; hevet **`toHaveScreenshot`** **`maxDiffPixels`** til **220** i den visuelle suiten, så sammenligningskjøringer tolererer litt mer font- eller subpikselavvik på tette markdown-dialoger uten å maskere stor UI-drift.- **SCSS** temakatalog (**`quasar.variables.scss`**): Quasar palett, globaler, **`q-*` justeringssymboler**, deretter per-funksjon seksjoner; **`htmlAdjustments`**, **`scrollbar`**, **`quasarComponentsAdjustments`** og matchende **Vue** **`<style lang="scss">`** blokker bruker variabler i stedet for spredte bokstaver. Tema **`$`** identifikatorer bruker bare bindestrek-separerte segmenter (se **`project-scss.mdc`**). Omdøpte **qMarkdown**-kodebakgrunnstokenet til **`backgroundColor`** (ble skrevet feil).
- **FantasiaMascotImage** gjengir maskotkunst med en vanlig **HTML** **img** (**object-fit: contain**, **display: block**) i stedet for **Quasar** **q-img**, og unngår **q-img** wrapper og lasteoppsett som forstyrret **høyden****, mens, ** **`max-width: 100%`**, og **data-test** hekter på bildet.
- **endringslogg** og bidragsyterdokumenter i appen: omformulert ett **Playwright**-punkt og utvidet **vue-i18n**-veiledning, slik at prosa ikke analyseres som ugyldig plassholdersyntaks (krøllete klammeparenteser-formede API-snutter og lignende).- **Appinnstillinger** søk: **vue-i18n** plassholder (**en-US** / **fr**). Med et ikke-tomt trimmet søk forblir de vertikale **kategori**-fanene synlige, men dempes bak en kort **Animate.css**-toning og kan ikke klikkes. høyre kolonne stabler **matchende**-innstillinger i én rulling (uavhengig av store og små bokstaver i understreng på **tittel**, **tags** og verktøytips **beskrivelse**; ikke-samsvarende rader og tomme seksjoner er utelatt; filtrering i **`scripts/dialogAppSettingsSearch.ts`** med Vitest-separasjonslinjedekning i hver hovedkategoriseksjon under sett. Når ingenting stemmer, bruker et sentrert kort den samme kremkanten **Feil ikke funnet**-stil og fyll ut som **`ErrorNotFound`**, med **FantasiaMascotImage** i **lesemodus og en dedikert **searchNoResultsMessage**-streng. Mens søket er aktivt, forblir kortet uten resultater og kolonnen med samsvarende resultater i dokumentet med synlighet vekslet (**v-show**) slik at **lese**-maskoten kan lastes inn før den tomme tilstanden vises. Søkefeltet beholder en fast bredde når **clearable** viser sitt ikon, forblir brukbart over det dunkle laget, og sletting kaster ikke lenger når kontrollen setter modellen til tom. Subtile blekninger myker toppen and nederst i de stablede resultatene og langs søkestripen mens søket er aktivt; rulleområder bruker en stabil rullefelt, slik at det å vise eller skjule en vertikal rullefelt ikke skyver oppsettet sidelengs. **SCSS** **BEM**-navn for søkeomslaget, selvklebende titler og bunnteksthandlinger samsvarer med malen, så regler gjelder.
- **`rgbToHex`** og **`hexToRgb`** i **`src/scripts/_utilities/colorFormatConvertors.ts`**: **`rgbToHex`** kartlegger nå bare de tre første numeriske kanalene, så strenger i **rgba()**-stil (for eksempel fra **Står ikke til en **-slutt) hex-resultat, krever minst tre treff før konvertering, og analyserer kanalheltall med en eksplisitt radiks; **`hexToRgb`** trimmer inndataene og fjerner et ledende **`#`** før parsing. Samlokalisert **Vitest** dekning utøver de oppdaterte banene.
- **`toggleDevTools`** (**`src/scripts/appGlobalManagementUI/toggleDevTools.ts`**) valgfrie kjeder gjennom **`toggleDevTools`**-metoden på **`window.faContentBridgeAPIs.faDevToolsControlbridge`** har for eksempel ikke en delvis manglende test (eksempelvis mangler testen) eller **Storybook**).- Utvidede **Vitest** suiter på tvers av **Electron** (**contentBridgeAPIs**, **preload**, **mainScripts** inkludert **userSettings** opprydding for ugyldige eller delvis vedvarende øyeblikksbilder), **renderer** (**boot**, **stores**, **dialoger**globals, **s, scripts**globals, **s **sider**), reporoot **i18n**-tester og **hjelpere/dramatikerhjelpere**, slik at lagdelte dekningsterskler forblir på linje med disse rettelsene.
- **SocialContactButtons** **webside**-stil: **quasar.variables.scss** setter nettsidebrikkebakgrunnen til **$mørk** i stedet for **$qCard-bakgrunn** slik at den leses tydelig på den mørke verktøylinjen.
- **Appinnstillinger**: underkategori-rad og stablet-søk toppnivåseparatorer bruker **`showNonLastSeparator`** og **`showNonLastTopCategorySeparator`** fra **`scripts/dialogAppSettingsSearch.ts`** med Vitest-dekning; layoutoppførsel samsvarer med den forrige innebygde logikken.- Flyttet hele **vue-i18n** lokalitetstreet fra **`src/i18n/`** til repo-root **`i18n/`** (ved siden av **`src/`**); importer bruker **`app/i18n`** og **`app/i18n/...`**. **Vitest** **`unit-i18n`** (**`yarn test:coverage:i18n`**) håndhever **100 %** dekning på **`i18n/`** oppføring **`.ts`** filer med omfang; **README**, **AGENTER**, markørregler/ferdigheter, **Quasar** (**`@intlify/unplugin-vue-i18n`** inkluderer bane), og **Storybook** aliaser ble oppdatert.
- **vue-i18n** lokale meldingsfiler bruker et **`L_`** filnavnprefiks (for eksempel **`dialogs/L_appSettings.ts`**) slik at **`T_`** forblir reservert for **TypeScript** typealiaser; **Storybook** spotter, **Electron** stavekontrollimporter, tester og bidragsyterdokumenter bruker de nye banene.- Lokale verifiseringsstandarder er roligere: **Vitest** dekning bruker **agent** terminalreporter; **Playwright** komponent og **E2E** bruker **liste**-reporteren; **Storybook** statisk konstruksjon og røykbruk **stille** CLI-flagg og roligere **Vite** logging av produksjonsforhåndsvisning; **Storybook** visuell **Playwright** hopper over per historie **konsoll** fremdrift med mindre **FA_STORYBOOK_VISUAL_VERBOSE** er satt; **`yarn testbatch:ensure:nochange`** og **`yarn testbatch:ensure:change`** påkaller **`yarn quasar:build:electron:summarized`** slik at hele **Quasar** / **electron-builder**-loggen blir fanget under **når et testresultat mislykkes.** **README**, **AGENTER** og markørregler og ferdigheter beskriver skriptene og detaljerte flagg.- **Playwright** kjører (**`TEST_ENV`** **`components`** / **`e2e`**) bruk **`%APPDATA%/<pakkenavn>/playwright-user-data`** (f.eks. **`fantasia-archive\playwright-user-data`**sia-**) **`fantasia-archive\playwright-user-data`**sia-**) (**`electron-store`**) holder seg atskilt fra **`quasar dev`** og pakkede profiler. Hver komponent og **E2E**-spesifikasjon kaller **`resetFaPlaywrightIsolatedUserData()`** i **`test.beforeEach`** slik at mappen fjernes før hver **`electron.launch`** og tester starter fra standard bevarte innstillinger. Navnet på den isolerte profilmappen deles gjennom **`src-electron/mainScripts/appIdentity/playwrightIsolatedUserDataDirName.ts`** (ingen **`electron`**-import) så tilbakestilling på nodesiden og Vitest forblir trygge under testinnsamlingen.- Fast pakket **Electron** **preload** bane i **`mainScripts/windowManagement/mainWindowCreation`**: hovedprosesspakken løser **`import.meta.url`** til den delte hovedkatalogen (ikke kildemappen **`windowManagement/`**). Løsning av **preload** og **ikon** fra den katalogen laster igjen **`elektron-preload`** riktig slik at **`window.faContentBridgeAPIs`** (inkludert **`extraEnvVariables`** for **`TEST_ENV`** / **`COMPONENT_NAME`**) er tilgjengelig komponent **Playwright** kjører rute til **`/componentTesting/...`** i stedet for å falle tilbake til standard **Indeks**-siden.
- Del **`src-electron/mainScripts/`** inn i funksjonsmapper - **`appIdentity`**, **`chromiumFixes`**, **`ipcManagement`** (**`register*Ipc`**-behandlere), **`nativeShell`**, **`userSettings`er`**, **`userSettings`er`**, og **`user-registrering**, og **`user` innstillinger**, vindusoppretting og **Chromium**-tilstøtende rettelser har stabile importveier; **Vitest** og **Playwright** importer, **README**, **AGENTER** og markørregler/ferdigheter følger den nye layouten.- Oppfølgingsbidragsdokumenter: **README** **Arkitektur**-notater samlet **`elektron-preload`** oppløsning under **`windowManagement/`**; **AGENTS** og **`electron-preload`** peker **`faUserSettings`** **`electron-store`** til **`mainScripts/userSettings/`**; **`vitest-tests`** viser hver **`mainScripts/<area>/_tests/`** bøtte; **`fantasia-electron-main`** advarer mot en ekstra **`..`** når forhåndsbelastning fra **`import.meta.url`** løses i pakkede bygg.
- Tverrgående **Playwright** selemoduler (**`playwrightElectronRecordVideo`**, **`playwrightUserDataReset`** og deres Vitest suiter) live under **`helpers/playwrightHelpers/`**; spesifikasjoner importerer **`app/helpers/playwrightHelpers/...`**. **Vitest** **`unit-helpers`** kjører **`helpers/**/*.vitest.test.ts`** via **`vitest/vitest.helpers.config.mts`**; delte Vitest arbeidsområdekonfigurasjoner live under reporoot **`vitest/`** så den strenge **`helpers/**/*.ts`** dekningsporten gjelder kun for ekte hjelpepakker. Bidragsyterdokumenter (**README**, **AGENTER**, markørregler/ferdigheter) beskriver **`hjelpere/`** for fremtidige selepakker, slik at depotroten forblir for config, **`README`**, låsefiler og **`scripts/`**.- **Vitest** dekningsnivåer er dokumentert og kablet i **`vitest/`**: **100%** v8-terskler på **src-electron** og **hjelpere**-pakker; **100 %** på alle fire beregningene for scoped reporoot **`i18n/`** oppføring **.ts**-filer i **unit-i18n** (**`specialCharactersFixer`**, register **`index.ts`**, **`externalFileLoader.en-locale`** og`per-locale`**,** **`fr`**, **`de`** indeksmoduler — se **`vitest/vitest.i18n.config.mts`**); **100 %** på alle fire beregningene for **src**-renderer **.ts** i **unit-src-renderer** (**src/boot**, **src/scripts**, **src/stores** bare — **`specialCharactersFixer`** flyttet ut med /**`i18n); **100 %** på alle fire beregningene for sammenslåtte samlokaliserte **.ts**-filer under **src/components**, **src/layouts** og **src/pages** i **unit-components** (hvert område beholder sine egne inkluder globs; **Storybook-bare ekskluderer/** **foundation). **Vue** SFC-er har ingen sviktende terskel; rapporter bruker **60 %** vannmerker, så svake totaler ber om gjennomgang. **Appinnstillinger** render-tree **.ts** bygger valgfrie **notat**-blader med en eksplisitt tilordning (ikke en betinget objektspredning); tester dekker tomme øyeblikksbilder og gjenbruk når to innstillinger deler en kattegori og underkategori; Vitest importerer sorteringshjelpere fra **dialogAppSettingsTree** direkte. **README**, **AGENTER**, **.github/workflows/verify.yml** kommentarer, **eslint-typescript**, **vitest-tests**, **testing-terminal-isolation**, **git-conventional-commits**, **playwright-tests**, **vue-a-quaser **fantasia-dev-setup**, **fantasia-changelog-en-us**, **fantasia-electron-preload**, **fantasia-quasar-vue**, **git-conventional-commits**) beskriver de samme retningslinjene.
- **Root** **vitest.config.mts** registrerer **fem** prosjekter med **utvider** til **vitest/** slik at arbeidsområdet **root** forblir depotet og globs som **src-electron/** løser seg riktig. **package.json** **test:coverage:electron**, **test:coverage:helpers**, **test:coverage:i18n** og **test:coverage:src** laster inn disse filene; **tsconfig.json** ekskluderer **vitest/vitest.components.config.mts** fra apptypesjekken. Tidligere **vitest.*.config.mts** / **vitest.setup.ts**-fragmenter på rotnivå flyttet til **vitest/**.- **Vitest** **`unit-components`** inkluderer nå **`src/layouts/**/*.vitest.test.ts`** og **`src/pages/**/*.vitest.test.ts`** ved siden av **`src/components/**`**, med en **`vitest.test.ts**-linje for`test.**s.** . hver **`MainLayout`**, **`ComponentTestingLayout`**, **`IndexPage`**, **`ErrorNotFound`** og **`ComponentTesting`** SFC. **Appinnstillinger** treet Vitest dekker **`toSortedRecord`**, **`buildAppSettingsRenderTree`**, kategoribestilling, **`showDocumentID`**-kartlegging og administrasjon av lokale innstillinger. **Electron** main Vitest legger til dekning for **`suppressChromiumDevtoolsAutofillStderrNoise`**, **`registerFaDevToolsIpc`**, **`PLAYWRIGHT_ISOLATED_USER_DATA_DIR_NAME`**, **`fixAppName`**, **`fixAppName`** når produktnavnet er tom og utvides **stavekontroll**-oppførsel og **justeringer**; **`mainWindowCreation`** avslører **`assignAppWindowRefForTesting`** kun for enhetstester.- **Renderer** og sele **Vitest** utvidelser: **boot** (**qmarkdown**, bredere **externalLinkManagement** tilfeller), **butikker** indeksmodul, **scripts** verktøytester (**colorFormatConvertors**, **mdListArrayConverter**), **AppControlMenus**; **Playwright** hjelpepakker fikk dypere **`playwrightElectronRecordVideo`** og **`playwrightUserDataReset`** enhetstester. **eslint.config.mjs** inkluderer **vitest/** i lo-omfanget; **yarn.lock** gjenspeiler avhengighetsoppdateringer fra denne arbeidssyklusen.
- **Playwright** Electron **WebM**-vedlegg kan vise en **syntetisk rød markørprikk** som følger skriptede pekerbevegelser (valgfritt **`FA_PLAYWRIGHT_CURSOR_MARKER=0`** for å deaktivere); **Appinnstillinger** komponentspesifikasjonsforsøk **Quasar** live **verktøytips** sveve med **`expect().toPass`**, **`locator.hover`** med **`force`**-alternativet aktivert, og en litt lengre tabulator-panelavregningsforsinkelse for å kutte flak under **Electron**.- **Playwright** **Appinnstillinger** komponentspesifikasjoner legger til serielle tester for overskriften **søk**-feltet etter **en-US**-fanegangen: ett treff hver via **tittel**, **tags** og innstilling av **beskrivelse**-understrenger, deretter resultatløse **Feilkort** med **lesing-no-resulter, **resulter**, **result. og en **650** pikselkort maks bredde via **data-test-error-card-width**.
- **Appinnstillinger** hjelpeverktøytips for den uttalte telledeleren viser nå den vertikale streken i setningen: **vue-i18n** behandler et blankt **pipe** som meldingsformatsyntaks, så en-US-strengen bruker den dokumenterte omvendte skråstreken, slik at tegnet gjengis bokstavelig.
- **Quasar** **rippel** er på som standard igjen; **vinduskontroller (**GlobalWindowButtons**) og **AppControlSingleMenu**-verktøylinjen **`q-btn`** deaktiverer **rippel** slik at krom og menystripen forblir visuelt stødige.
- **DialogMarkdownDocument** øker innholdet **maks-høyde**-forskyvning slik at høye dialoger fortsatt gir rom for topptekst og bunntekst når du ruller.- Omorganisert gjengiver **Vue** komponenter under **`src/components/`** buckets — **`dialogs/`** (modal **`Dialog*`** SFCs), **`globals/`** (**`GlobalWindowButtons`**, **`AppControlMenus`**),`SappControlMenus`,` **`elements/`** (**`FantasiaMascotImage`**, **`SocialContactSingleButton`**), og **`other/`** (**`SocialContactButtons`**); speilvendte lokalitetsmoduler under **`i18n/*/components/<bucket>/...`**, oppdaterte importer og **Storybook** hån, fikset **`SocialContactSingleButton`** historieimport etter flyttingen, oppdatert **Storybook** VRT-grunnlinje for **App-innstillinger RE**S, og reviderte **S, AG- og ADME-er Markørregler/ferdigheter).
- Stoppet valgfrie **appinnstillinger** **notat**-strenger fra å utløse **vue-i18n** advarsler om manglende nøkkel når det ikke finnes noen **note**-nøkkel for en innstilling (løs merknader kun etter en nøkkeleksistenskontroll).
- Lagt til et oppstartsoppryddingspass for de vedvarende **brukerinnstillingene**-lagre slik at eldre eller ukjente nøkler som ikke lenger eksisterer i **`src-electron/mainScripts/userSettings/faUserSettingsDefaults.ts`** fjernes og den rensede konfigurasjonen blir automatisk lagret.- Ekstraherte alle hardkodede **en-US**-lokalitetsstrenger fra **`i18n/en-US/index.ts`** til dedikerte lokalitetsmoduler per funksjon per komponent, dialogboks, side og global funksjon; normaliserte alle i18n-nøkler på toppnivå og undernivå til **camelCase** med en liten første bokstav på tvers av appen.
- Justert **Storybook**s **`externalFileLoader`** mock (**`.storybook-workspace/.storybook/mocks/externalFileLoader.ts`**) med det **camelCase**-meldingstreet (inkludert **`globalFunctionality`**) og kopier komponenten via appen som ikke er koblet til det samme lokale merket, og nedover komponenten via com-merket. layout-forhåndsvisninger – og lokal **`garntest:storybook:visual`** – gjengi ekte strenger i stedet for rå oversettelsesnøkkelbaner.
- Sentralisert **hoved ↔ forhåndsinnlasting** **IPC** kanalnavn i **`src-electron/electron-ipc-bridge.ts`** (inkludert **DevTools** synkroniseringskanaler) slik at **Electron** behandlere og **preload** hjelpere ikke kan gå over til strenger som ikke samsvarer; fjernet den eldre **DevTools**-bare kanalmodulen.- Dokumentert **`electron-ipc-bridge.ts`**, **`mainScripts/ipcManagement/register*Ipc.ts`** og **IPC**-registrering under **`startApp()`** (**`mainScripts/appManagement.ts`**) i **README**, **AGENTS**`` og Curron-sor **e **`fantasia-electron-preload`** / **`fantasia-electron-main`** / **`fantasia-sqlite-main`** veiledning slik at bidragsytere og agenter følger ett ende-til-ende-mønster.
- Lagt til **`src/vue-i18n-shim.d.ts`** slik at **vue-i18n** injiserte globaler (**`$t`**, **`$d`**, **`$n`** og relaterte **`$*`** hjelpere) er deklarert på **`@vue/Kuntime-core **Typing **__` **`tsc`** og fjerning av falske mangler-**`$t`**-diagnostikk i editorer som løser **`ComponentCustomProperties`** gjennom runtime-core-banen.
- Erstattet hovedprosessens **`sqlite3`** avhengighet med **`better-sqlite3`** (synkrone **SQLite** bindinger for det planlagte databaselaget; fortsatt et innebygd tillegg – gjenoppbygget etter **Electron**/**Node** bump som dokumentert i **README**). **AGENTER**, **`fantasia-sqlite-main`** og **`electron-main`** stubbekommentaren refererer nå til **`better-sqlite3`**.- Omdøpt Storybook-relaterte **Garn**-skript under **`test:storybook:*`**: **`yarn test:storybook:visual`** (med **`:headed`**, **`:update`**, **`:update:headed`**) erstatter **`yarn visual:***; **`garntest:historiebok:røyk`** erstatter **`garnhistoriebok:røyk`**. **`.storybook-workspace`** skriptnøkler samsvarer. Oppdatert **README**, **AGENTER**, markørregler/ferdigheter og plannotater.
- Del opp hele prosjektporten: **`yarn testbatch:ensure:nochange`** kjører **`testbatch:verify`**, produksjon **Electron** build, **Playwright** komponent + **E2E**, **`yarn test:storybook:smoke`** og **`storyarn:visual.n. **`garntestbatch:ensure:change`** er det samme gjennom røyk, deretter **`garntest:storybook:visual:update`** for tilsiktet grunnlinjeoppdatering. Fjernet **`garn testbatch:ensure`**. Oppdatert **README**, **AGENTER**, **testing-terminal-isolation**, **fantasia-testing**, **fantasia-dev-setup**, **fantasia-release-build** og **changelog-en-us** veiledning.- Fjernet **GitHub Actions** Storybook visuell-regresjonsjobben fra **`.github/workflows/verify.yml`** og endret navn på arbeidsflyten til **Verify** (push/PR kjører kun **`yarn testbatch:verify`**; **`.storybook-workspace`** installert for ESL`int). Storybook VRT forblir **lokal** eller via **`yarn testbatch:ensure:nochange`**. Oppdatert **README**, **AGENTER**, **testing-terminal-isolation**, **fantasia-testing**, **fantasia-dev-setup** og **fantasia-changelog-en-us** (utelat-liste-tekst).- **GitHub Actions** arbeidsflyter for pull-forespørsel **pr-full-suite-nochange** og **pr-full-suite-change** krever nøyaktig én depotetikett på PR, **novisualchange** eller **visualchange** (se **`.github/PULL_REQUEST_TEMPLATE.md`**). **novisualchange** kjører **`yarn testbatch:ensure:nochange`** på **Windows** så **Storybook** skjermbildenavn forblir på linje med forpliktede **chromium-win32** baselines; **visualchange** kjører **`yarn testbatch:ensure:change`** og kan pushe oppdaterte filer under **`.storybook-workspace/visual-tests/`** når PR-grenen bor i dette depotet (fork PR-er kjører fortsatt tester, men kan ikke motta det push). PR-er som har begge etikettene eller ingen av dem mislykkes i **PR full suite (sørg for nochange)** guard inntil løst; **sikre endring**-vakten går ut av suksess i så fall, så den samme feilen gir ikke to identiske røde sjekker. En enkelt idempotent bot-kommentar forklarer regelen (PR-er forblir åpne slik at en etikett kan legges til etter åpning). **`.github/scripts/pr-label-guard.sh`** sentraliserer etikettsjekken for begge arbeidsflytene.- **Contributor-oppsett**: **`commitlint`** og **`husky`** validerer **`git commit`**-meldinger lokalt (**`commitlint.config.mjs`**, **`.husky/commit-msg`**, **`yarn install`** kjører **`prepare`**). Tillatte typer samsvarer med **AGENTER** (**feat**, **fix**, **test**, **chore**, **refactor**, **style**, **docs**); **`Slå sammen …`** og **`Tilbakestill …`** linjer ignoreres. **README** og **AGENTER** beskriver kroker; **`.cursor/rules/git-conventional-commits.mdc`** dokumenterer den samme policyen.
- **GitHub Actions** **OSV-Scanner** arbeidsflyt (**`.github/workflows/osv-scanner.yml`**) kaller nå **`google/osv-scanner-action`** gjenbrukbare arbeidsflyter ved **v2.3.5** så **`scan-pr`** trekker ikke lenger v**-`artaction ut**-`opp` (GitHub mislyktes i disse kjøringene på **Konfigurer jobb**).
- Angi **`actions/upload-artifact@v5`** i den manuelle **Build**-arbeidsflyten (**`.github/workflows/build.yml`**) for **Linux**, **macOS** og **Windows** utgivelsesartefakter, sammen med **`actions/checkout@' artefaktopplastinger forblir på støttede **GitHub Actions** kjøretider.- Bumped **Vue** til **`^3.5.32`** (root og **`.storybook-workspace`**), **`@types/node`** til **`^22.19.17`**, oppdaterte **root** og **`.storybook-workspace`** og låste filer med **T__`0 og V-oppgradering med **T__` **`toHaveScreenshot`** **`maxDiffPixels: 150`** så sammenligningskjøringer forblir stabile på tvers av gjenoppbygginger. Oppdatert ett **Win32**-krom-øyeblikksbilde påvirket av verktøykjeden.
- Omdøpt markdown bullet-list-hjelperen til **`mdListArrayConverter`** (standard stavemåte) og normalisert sløyfevariabelnavning i **`specialCharacterFixer`**; **Tips, triks og trivia**-varsler og escape-dokumenter er uendret i oppførsel.
- Polert **en-US** kopi i **Advanced Search Guide**, **Tips, triks og trivia** og **en-US** appnavnstrengen for grammatikk, stavemåte og klarhet; fikset en manglende artikkel i **README** Quasar CLI-overskriften.- Flyttet hver **`*.stories.ts`**-fil under samsvarsfunksjonen **`tests/`**-treet (**`src/components/**/tests`**, **`src/layouts/**/tests`**, **`src/pages/**/tests`**) og pekte **`.storybook-arbeidet i glosboken. stier slik at **Storybook** ligger ved siden av **Vitest** og **Playwright**; oppdaterte **Storybook** VRT-øyeblikksbilder (inkludert **DialogAppSettings** standard og **DialogMarkdownDocument** forhåndsvisninger).
– Håndhevede utvidede flerlinjede objektliteraler for hele prosjektet via **ESLint** (**`object-curly-newline`**, **`object-property-newline`**) for **JavaScript**, **TypeScript**, **Vue** og **JSON**, med **`tråden`es` fra repetisjonen **o.
- Ekstraherte små fillokale grensesnitt og typealiaser fra **Vue** og **TypeScript** kilder til samlokaliserte **`<filnavn>.types.ts`** moduler der det er aktuelt (renderer-oppstart, Pinia-skrivinger, **Electron**-tester, **Storybook**-konvensjonene** og registrerte mocks) **AGENTER** og markørregler/ferdigheter.- Utvidet **Stylelint** med **`stylelint-order`** slik at **CSS egendefinerte egenskaper** og vanlige **deklarasjoner** forblir i **alfabetisk** rekkefølge (autofix via **`yarn lint:stylelint:fix`**); utvidet **`garn lint:stylelint`** for å skanne **`vue`**, **`css`**, **`scss`** og **`sass`** under **`src/`** og under **`.storybook-workspace/.storybook/`** (bygg utdata under **`storybook** forblir statisk/`
- **Electron Playwright** kjører vedlegg **WebM** skjermopptak per test; åpne **`test-results/playwright-report/index.html`** etter **`yarn test:components`** eller **`yarn test:e2e`** (eller **`:single`**-skriptene) for å se gjennom videoer under hver tests **vedlegg** (filer under **`test-right-**reports)/w. Hver Playwright-kjøring **erstatter** den HTML-rapporten; **`scripts/playwrightWithArtifactTrim.mjs`** fjerner **`test-results/playwright-artifacts`** etter disse garnkommandoene slik at scratch output ikke beholdes mellom kjøringene. Dokumentert i **README**, **AGENTER** og Cursor **fantasia-testing** / **dramatiker-tester** veiledning; valgfri **`FA_PLAYWRIGHT_NO_VIDEO`** hopper over opptaket.
- Fjernet et bortkommen **HTML** **tekstområde** fra sidemalen **Indeks**.

## 2.4.9 - Bidragsyterverktøy, Pinia-dialoger og CI-herding

### Feilrettinger og optimaliseringer
- Fjernet **`yarn visual:storybook:test:local`** og **`yarn visual:storybook:update:local`** (og matchende **`.storybook-workspace`** **`storybook:visual:*:local`**-skript). For build + headed compare eller snapshot refresh, bruk **`yarn test:storybook:visual:headed`** / **`yarn test:storybook:visual:update:headed`**; for å kjøre Playwright kun når **`storybook-static/`** allerede er gjeldende, bruk **`yarn --cwd .storybook-workspace test:storybook:visual`** eller **`test:storybook:visual:headed`** / **`test:storybook:visual:update`** / **`:update` Oppdatert **README**.
- Fast **Pinia 3** dialogflyt: **`S_DialogMarkdown`** og **`S_DialogComponent`** er nå standard oppsettbutikker (ring **`S_DialogMarkdown()`** / **`S_DialogComponent()`** på brukssteder). **`openDialogMarkdownDocument`**, **`DialogMarkdownDocument`** og **`DialogAboutFantasiaArchive`** bruker bevoktede overvåkere når en butikk ikke er aktiv (f.eks. **Storybook**). **`ComponentTesting`** håndterer manglende **`componentName`**-ruter trygt og gjengir selen først etter at en samsvarende SFC er løst.- Forenklet **Index** forhåndsvisningssiden ved å fjerne den ekstra **Fantasia Mascot**-blokken fra standard **`IndexPage`**-ruten (beholder ruten som et lett skall for lokale lenker/testing).
- Omdøpt og gruppert **Garn** bidragsyter skript for klarhet: kvalitetsporter **`yarn testbatch:verify`** / **`yarn testbatch:ensure`**; lo **`garn lint:eslint`**, **`garn lint:typescript`**, **`garn lo:stylelint`**; Quasar Electron **`yarn quasar:build:electron`** / **`yarn quasar:dev:electron`**; Playwright **`garntest:komponenter`** og kolon-suffiks **`test:komponenter:*`**, **`test:e2e:**`**; Storybook dev **`yarn storybook:run`** og statisk **`yarn storybook:build`** (tidligere **`build-storybook`**). Fjernet foreldet **`garntest:full`** og duplikat **`yarn visual:storybook:ci`**; **GitHub Actions** ring **`yarn visual:storybook:test`** direkte. Oppdatert **README**, **AGENTER**, markørregler/ferdigheter, **`package.json`**, **`testRunner_*.mjs`** og arbeidsflyter.- Samlokalisert Storybook **statisk utdata** (**`.storybook-workspace/storybook-static/`**), **Playwright** visuell-regresjonskonfigurasjon, **`visual-tests/`** (inkludert forpliktede øyeblikksbilder), arbeidsområde **`storybook:visual:*`encies** scripts,`-dependens,`-** **`@playwright/test`**) under **`.storybook-workspace/`**; root **`yarn visual:storybook:*`** delegater med **`yarn --cwd .storybook-workspace`**. Oppdaterte **`.gitignore`**-, **`tsconfig.json`**- og **`lint:eslint`**-baner.
- Angi eksplisitte minsterettigheter **`tillatelser`** (`innhold: les`) på **`verify`** og manuelle **`build`** **GitHub Actions** arbeidsflyter; lagt til **Dependency Review**, **OSV Scanner** (gjenbrukbare arbeidsflyter + ukeplan på **`master`**), og **Gitleaks** for PR/push-hygiene.

## 2.4.8 - Pinia 3 og Vue ruter 5

### Feilrettinger og optimaliseringer
- Oppdatert **Sass** til **1.99.x** under **`^1.78.0`** i **root** og **`.storybook-workspace`** låsefiler slik at skrivebordsappen og **Storybook** bruker den samme oppløste Dart Sass-linjen for stiler.
- Oppgradert **Pinia** til **v3** og **Vue ruter** til **v5** for skrivebordsappen og **`.storybook-workspace`** (eksplisitt **`vue-router`** der for **`StoryRouterShell`**); la ikke til valgfri **`@pinia/colada`**. Oppdatert **root** og **`.storybook-workspace`** låsefiler med **`yarn upgrade`** innenfor eksisterende semver-områder (**Storybook** forblir festet til **10.3.4** på den linjen).
- Oppgradert **`uuid`** til **v13** for UUID-er for markdown og komponentdialog (**`S_Dialog`**) og **`@quasar/cli`** til **v3** for **`quasar`** CLI brukt med **`@quasar/app-vite`** v2.
- Oppgradert **Electron** til **41.x** (fra **33.x**); **`electron-builder`** kjører fortsatt **`@electron/rebuild`** så **`sqlite3`** matcher den medfølgende kjøretiden. **`@electron/remote`** **2.1.x** og den kjente uemballerte produksjonsinstallasjonsadvarselen for **`electron`** er uendret.- Standardisert type-import på tvers av TypeScript og Vue kodebase ('importtype') for å tilpasse seg streng linting og holde kjøretidsbunter fri for kun type importstøy.
- Lagt til en ny `yarn secure`-kommando for full-prosjektvalidering (`verify` + produksjonsbygg + Playwright-komponent + Playwright E2E) og justert veiledning for bidragsytere i **README**, **AGENTS** og markørregler/ferdigheter for å dokumentere hurtigport kontra full gate arbeidsflyt tydelig.
- Redusert ikke-handlingsbar Vue-advarselsstøy i komponent Vitest-utgang ved å filtrere kjente Quasar-oppløsning og test-bare injeksjonsvarsler i delt testoppsett, og holde signalet fokusert på handlingsbare feil.
- Fikset CI TypeScript-oppløsning for Vue SFC-importer ved å erstatte den genererte `.quasar` shim-referansen i `src/env.d.ts` med en in-repo `declare module '*.vue'`-erklæring, slik at `yarn verify` lykkes på rene løpere/__TK0-ringer uten re.__ filer.

## 2.4.7 - Storybook 10 og Vite 8 justering

### Feilrettinger og optimaliseringer
- Oppgradert **`.storybook-workspace`** til **Storybook 10** og **Vite 8** slik at komponentforhåndsvisninger kjører på samme hovedpakkelinje som skrivebordsappen (**`@quasar/app-vite`**), oppdatert den nestede **Yarn**-låsefilen for reproduserbare installasjoner (inkludert fryste installasjoner **K__**) / **`eslint-plugin-storybook`** versjoner med den utgivelsesfamilien.
- Oppdatert **root** og **`.storybook-workspace`** låsefiler med **`yarn upgrade`** innenfor eksisterende semver-områder (spesielt **Quasar**, **Vue**, **vue-i18n**, **Playwright**, **Sass** og relatert verktøy); beholdt **`@types/node`** på **22.x**-tastingslinjen for **Node 22**-motoren. Oppdatert **README** og **AGENTER** slik at bidragsytere ser **Storybook 10** på **Vite 8** i stedet for den gamle splitt-Vite-teksten.
- Lagt til **`debug-storybook.log*`** til **`.gitignore`** slik at lokale Storybook diagnostiske logger ikke plukkes opp som usporede filer.

## 2.4.6 - Vinduskrom, Storybook, Stylelint og DevTools

### Feilrettinger og optimaliseringer
- Brukt **`getCurrentWindow()`** fra **`@electron/remote`** til **window chrome** brohjelpere (**minimer**, **maksimer**, **endre størrelse**, **lukk**, **maksimert tilstand**) slik at kontrollene forblir knyttet til appvinduet når OS-fokus flyttes til menyer eller andre overflater.
- Justert **`.storybook-workspace`** med hovedappen på **Vue 3.5**, **vue-i18n 11**, **Quasar 2.16+**, **Pinia 2.3+** og **@quasar/extras 1.17**; fjernet stray **`package-lock.json`** der så bare **Yarn** lockfiler brukes.
- Oppdatert **root** og **`.storybook-workspace`** låsefiler med **`yarn upgrade`** innenfor eksisterende semver-områder; festet root **Storybook** tillegg til **8.6.18** slik at de samsvarer med **`@storybook/vue3-vite`** og det nestede arbeidsområdet (spesielt nyere **Playwright**, **Sass** og annet kompatibelt verktøy).
- Migrerte **`src/css/app.scss`** og **`globals/*.scss`** til Sass **`@use`** med eksplisitt **`quasar.variables.scss`**-import i partiell, og erstattet utdaterte **`@import`**-kjeder for 3 app-eide globale stiler.- Storybooks Vite **`additionalData`** legger foran **`quasar.variables.scss`** for prosjekt **`.vue` / `.scss`** (via **`file://`** URL-er slik at Windows-baner med mellomrom løses). For **`quasar/src/css/index.sass`** bruker den **`@import`** (ikke **`@use`**) foran Quasars egne importer – den samme effektive rekkefølgen som **`@quasar/vite-plugin`s `sassVariables`** transformerer – så **` har defaulted them til **`! CSS variabler (**`--q-dark`**, **`--q-primary`** og relaterte tokens) samsvarer med skrivebordsappen; andre **`node_modules`** Sass forblir urørt.
- Utvidet rot-**Stylelint**-oppsettet med **`@stylistic/stylelint-config`** slik at stilistiske regler droppet fra **Stylelint 17**s kjerne fortsetter å påtvinge kjent CSS/SCSS-formatering; oppdaterte **`.vscode`** anbefalinger og innstillinger slik at Stylelint-utvidelsen kan bruke prosjektkonfigurasjonen, **fiks ved lagring** og formatere frittstående **`.scss`**-filer konsekvent.- Rutet **DevTools** kontroll (**toggle**, **open**, **close** og **status**) gjennom små **hovedprosess IPC**-behandlere i stedet for **`@electron/remote`** **`getCurrentWindow()`**, så **Hjelp → Toggle utviklerverktøy** forblir pålitelig i K0d____**** forblir pålitelig; **E2E**-kontrollen venter nå til DevTools er ferdig med å feste i stedet for å hevde umiddelbart etter at menyen lukkes.
- Oppdatert **GitHub Actions** på **`verify`** og manuelle **build** arbeidsflyter (**`actions/checkout`**, **`actions/setup-node`**, **`actions/upload-artifact`**) til gjeldende hovedversjoner.
- Notert i **`quasar.config.ts`** at **`@electron/remote`**s **`electron`** peer forventes å vises som uoppfylt under UnPackaged **`yarn install --production`**; pakking lykkes fortsatt fordi appen kjører på den medfølgende Electron kjøretiden.- Storybook forhåndsvisning registrerer Quasars **`Dark`** plugin, **`@quasar/quasar-ui-qmarkdown`** app-plugin (etter Quasar, matchende oppstartsrekkefølge), **Roboto (latin-ext)** og **`preview-body.D`ark)`** / **so` **`body--dark`** forblir på linje med appen; Sass **`additionalData`** hopper over **`app.scss`** for å unngå dobbeltlasting av variabler. **Dialog** og **toppmeny**-komponenten Dokumenter bruker **`parameters.docs.story.inline: false`** igjen slik at Autodocs ikke monterer hver historie i ett dokument (hver **`q-dialog`** åpnes automatisk ved montering, som stablet modaler når **`inline: true`**); hver nestede Dokument-iframe laster fortsatt den samme forhåndsvisningsstabelen CSS.

## 2.4.5 - Bidragsyter arbeidsflyt og CI

### Feilrettinger og optimaliseringer
- Lagt til **`yarn verify`** for å kjøre ESLint, TypeScript (`tsc`), Stylelint og enhetstester i én kommando (kalt **`verify`** fordi Yarn 1 reserverer **`yarn check`** for avhengighetsverifisering); dokumenterte det i README- og dev-oppsettveiledningen, justerte markørregler/ferdigheter, og kjørte det i manualen GitHub Handlinger bygger arbeidsflyt før produksjonen Electron build.
- Rettet den kombinerte enhet-da-Playwright-hjelperen slik at den kjører enhetstester og deretter Playwright over komponent- og E2E-baner (fjernet den ødelagte "samtidige"-påkallelsen og droppet den ubrukte **`samtidig`**-avhengigheten).
- Tømte den franske endringsloggen, slik at utgivelsesnotater ikke opprettholdes per lokalitet før oversettelser kommer tilbake.
- Oppgradert **`@intlify/unplugin-vue-i18n`** til v11 for Quasar/Vite locale pipeline, **`uuid`** til v11 (innebygde TypeScript-typer; fjernet **`@types/uuid`**), og **`sqlite3__/Vite for hovedlinjen __-prosessen ** til v11
- Lagt til en **GitHub Actions** arbeidsflyt som kjører **`yarn verify`** på **push** og **pull request** til **`master`** / **`main`** (inkludert installasjon av Storybook arbeidsområdet slik at ESLint kan løse `.storybook-workspace/.storybook`).- Oppdatert **root** og **`.storybook-workspace`** låsefiler med **`garnoppgradering`** innenfor eksisterende semver-områder (spesielt nyere **Playwright**, **Quasar**, **Vue**, **vue-i18n**, **Sass** og relaterte verktøy).
- Laget **DevTools** brohjelpere til å bruke **`getCurrentWindow()`** i stedet for **`BrowserWindow.getFocusedWindow()`** så **bytte / status** forblir knyttet til appvinduet når menyer eller DevTools endrer OS-fokus, og stabiliserer DevTools-sjekken.**E2

## 2.4.4 - Avhengigheter og utviklingsverktøy

### Feilrettinger og optimaliseringer
- Oppdatert avhengighetslåsefilen med "garnoppgradering" under de eksisterende semver-områdene, slik at installasjoner løses til de siste kompatible utgivelsene (inkludert Quasar, Vue, "vue-i18n", Sass, Playwright og Vite-verktøyet trukket inn av Quasar).
- Oppgradert `jsdom` til v29 for Vitest DOM-miljøet, `eslint-plugin-n` til v17, og oppdatert `@types/node` på Node 22-skrivingslinjen.
- Migrerte verktøy for lo og typer til **ESLint 9** flat config (**neostandard**, **eslint-plugin-vue** v10, **typescript-eslint**), **TypeScript** 6 og **Stylelint** 17 med oppdaterte delbare konfigurasjoner; små kildejusteringer samsvarer med strengere standardinnstillinger (importattributter, fagforeningsavstand, URL-beskyttelse for utviklervindu, QMarkdown CSS shim).
- Justert **AGENTER**, markørregler og ferdigheter slik at standard arbeidsflyt for forhåndsbekreftelse og før endringslogg alltid kjører **ESLint**, **TypeScript (`tsc`)**, **Stylelint** og **enhetstester** i den rekkefølgen; **kvalitetsporten** kan kjøres som én lenket terminalkommando for effektivitet, med individuelle kommandoer reservert for feilsøkingsfeil. **Bygg** og **Playwright** sjekker forblir én kommando per terminal.

## 2.4.3 - Ytterligere avhengighetsoppdateringer og forbedret testarbeidsflyt

### Feilrettinger og optimaliseringer
- Oppgradert "vitest" til v4 og justert Electron hovedprosessenhetstest-modeller til konstruktørkompatible implementeringer, slik at "BrowserWindow", "Menu" og "MenuItem" forblir stabil under den nyere løperen.
- Oppdaterte ytterligere lavrisiko-avhengigheter (`sqlite3`, `lodash-es`, `stylelint`, `stylelint-config-standard-scss` og `eslint-plugin-promise`) og revaliderte hele lint/test/build pipeline etter oppdateringene.
- Lagt til vedvarende agent/testveiledning for å kjøre hver valideringskommando i en isolert terminalpåkalling for å holde lange testutganger lesbare og enklere å revidere.
- Justerte endringsloggblokken `2.4.3` med den kanoniske `package.json`-versjonen og styrket veiledning for bidragsytere, slik at endringslogg-redigeringer alltid les live `package.json` på nytt først, og unngår seksjons-/versjonsdrift.

## 2.4.2 – Kjerneavhengighetsoppdatering og Quasar/Vite arbeidsflytvalidering

### Feilrettinger og optimaliseringer
- Oppgradert `vue-i18n` til v11 og fjernet eldre TypeScript-undertrykkelse i i18n bootstrapping nå som gjeldende skriving kompileres rent.
- Oppdatert kjerneapp- og verktøyavhengigheter (inkludert Quasar ekstramateriale/qmarkdown, Axios, Pinia, Vue ruter, Vitest og utvalgte ESLint-plugins) for å redusere vedlikeholdsrisikoen mens man holder seg innenfor gjeldende prosjektarkitektur.
- Brukte en oppfølgingsverktøyoppdatering for Quasar/Vite-arbeidsflyten (`@quasar/app-vite`, `@intlify/unplugin-vue-i18n`, `vite-plugin-checker`, `eslint-plugin-vue` og `@types/node`) med full lint, E, component build, validation, com build.

## 2.4.1 - Endringsloggpolicylås og komponentteststabilitet

### Feilrettinger og optimaliseringer
- Fikset Playwright komponenttestoppdagelse for `DialogAboutFantasiaArchive` ved å unngå full i18n-lasterimport som trekker nedmarkeringsdokumenter inn i testinnlasting på nodesiden.
- Oppdaterte dialogens sosiale knapppåstander for å sammenligne gjengitte etiketter, matche gjeldende UI-utgang og holde komponenttestene stabile.
- Låst prosjektveiledning til **aldri auto-bump-versjoner**; endringsloggseksjoner følger nå strengt den eksisterende `package.json`-versjonen med mindre en manuell endring er eksplisitt forespurt.

## 2.4.0 - Quasar CLI Vite v2 og Node 22 verktøykjede

### Feilrettinger og optimaliseringer
- Oppgraderte verktøykjeden for skrivebordsappen til `@quasar/app-vite` v2 (Vite 8), ESM `quasar.config.ts`, TypeScript 5.6 og ESLint-tilbakemelding via `vite-plugin-checker` under utvikling/bygg.
- Standardisert bidragsyter og CI Node.js på **22.22.0 eller nyere** (påkrevd av gjeldende Quasar CLI), oppdaterte GitHub Actions `setup-node` og dokumenterte native-modul rebuild forventninger for `sqlite3` etter Electron./Node bumps.
- Erstattet QMarkdown-apputvidelsen med direkte `@quasar/quasar-ui-qmarkdown`-registrering, Vite-native `*.md?raw`-lokaleimporter og Vue mal `isPreTag`-håndtering slik at markdown-dialoger forblir stabile uten utvidelsens Vite-plugin.
- Migrerte vue-i18n-bunting til `@intlify/unplugin-vue-i18n`, bumpet Electron og elektronbygger for ESM-hovedprosessutgang, og justerte Sass med Storybook-delprosjektet; Storybook forblir på Vite 6 til den støttede peer-serien inkluderer Vite 8. *(Erstattet i **2.4.7**: Storybook kjører nå på **Vite 8** med **`@quasar/app-vite`**.)*
- Bumpet **`@electron/remote`** til 2.1.x slik at Electron 33 beholder en fungerende forhåndsbelastning (unngår fjernede Chromium-funksjonsprober som tidligere brøt forhåndsbelastningen og bro-API-ene).- Fikset **Playwright komponent-testing** ruting: `ComponentTesting` globs nå `../components/**/*.vue` fra `src/pages`, så produksjon Electron kjører montere den forespurte SFC i stedet for en tom sele.
- Hevet **`@typescript-eslint`** til v8 for TypeScript 5.6, lagt til **`yarn lint:types`** (fullprosjekt `tsc`), og utvidet AGENTS/Markørveiledning for ESLint og typecheck-porter (TSLint brukes ikke).
- Redusert støyende **DevTools Autofyll** CDP stderr linjer under utvikling ved å filtrere de spesifikke meldingene i Electron hovedprosessen (kun ufarlige protokollfeil).
- Herdede gjengivelseskodebaner som kaller **`window.faContentBridgeAPIs`** når forhåndsbelastningsbroen mangler, og justert Vitest **`vi.stubEnv`** bruk med Vitest 3 boolske regler for `DEV` / `PROD` / `SSR`.
- Innstilt **Quasar/elektronbygger**-konfigurasjon for strengere skriving og innpakning: PWA `workboxMode`-hus (`GenerateSW`), tom **`bex`** blokk som samsvarer med strøm Quasar-typer, Linux **`desktop.entry`** for elektron-TK1-bygger, pluss __-prosjekter (`env.d.ts` Vue shim referanse, vue-i18n augmentation håndtering) så `lint:types` forblir pålitelig.- Adressert **Dart Sass 2** avskrivningsstøy i delt SCSS via `sass:color` i Quasar variabler og rullefeltstiler (erstatter eldre fargehjelpere).
- **Installasjonspåminnelse:** **`electron`**-pakken trenger **postinstallering** nedlasting; hopper over livssyklusskript (for eksempel `npm install --ignore-scripts`) kan etterlate en ødelagt binær til du installerer på nytt eller kjører `node node_modules/electron/install.js`.

## 2.3.2 - Storybook layouter, sider og bidragsyterverktøy

### Nye funksjoner
- Lagt til Storybook-forhåndsvisninger kun for lerret for hovedoppsett og representative appsider ved hjelp av en delt ruter i minnet (`StoryRouterShell`), en enkelt `STORYBOOK_APP_ROUTES`-tabell og samlokaliserte `src/layouts/**`- og `src/pages/**`-historier.
- Kablet en Storybook-bare Vite-transformasjon som omskriver `import.meta.globEager` til ivrig `import.meta.glob` slik at siden for komponenttesting lastes inn under verktøykjeden for Storybook-arbeidsområde.
- Utvidet Storybook i18n spotter slik at siden som ikke ble funnet viser ekte kopi i stedet for rå oversettelsesnøkler; forenklet indeks- og enkeltkomponent-historieetiketter (droppet den overflødige forhåndsvisningen av kun indeks).
- Dokumentert layout og side Storybook forventninger i `AGENTS.md` og markørregler/ferdigheter, inkludert forhåndsvisninger kun på lerret og bestilling av endringslogg sammen med komponenthistorier.

### Feilrettinger og optimaliseringer
- Spiss rot ESLint ved tilsiktede kilde- og konfigurasjonsbaner i stedet for hele depotet; lagt til `yarn lint:style` med Stylelint for `src` Vue og SCSS pluss en rot Stylelint-konfigurasjon.
- Aktiverte TypeScript `skipLibCheck` og ekskluderte byggeartefakter, Storybook statisk utgang, Vitest-komponentkonfigurasjonen og samlokaliserte `*.stories.ts` fra root-`tsconfig`-prosjektet for renere kontroller.
- Fjernet en tom scoped style-blokk fra `SocialContactButtons` dukket opp av stillinting.
- Oppdatert Vitest og Playwright dekning (ekstern lenkebehandling, maskotbilde, vinduskontroller) for å matche gjeldende oppførsel og velgere.

## 2.3.1 - Electron emballasjeikon og favorittikondekning

### Feilrettinger og optimaliseringer
- Lagt til eksplisitt Electron Builder-ikonkonfigurasjon for Windows, macOS og Linux, inkludert Linux metadata for skrivebordsoppføring og Linux måloppsett.
- Koblet alle genererte favicon-størrelser i `index.html` slik at gjengivelsesikondeklarasjoner dekker hele `public/icons/`-settet.
- Oppdatert endringslogg-markdown-ordlyd for å unngå analysefeil i byggetiden i qmarkdown/Vite-rørledningen under Electron-produksjonsbygg.

## 2.3.0 - Storybook arbeidsområde og skrivebordspolering

### Nye funksjoner
- Flyttet Storybook inn i en nestet `.storybook-workspace`-pakke med sin egen verktøykjede, fjernet rot-`.storybook`-treet, og oppdaterte colocated historier (menyer, dialogbokser, maskot, vinduskontroller og sosiale knapper) for stabile tillegg, `public/`-ressurser og forfatterarbeidsflyter.
- Justerte globale Quasar variable tokens og delte komponent SCSS justeringer for mer konsistent vinduskrom, kontroller og markdown/dialogoverflater.
- Lagt til Fantasia maskot bildeetikettstrenger for en-US og tyske lokaliteter for å støtte tilgjengelig navngivning.
- Dokumentert Storybook, Electron `file://`, og Playwright gjenoppbygger forventninger for bidragsytere i `AGENTS.md`, `README.md` og Cursor ferdigheter/regler (inkludert en dedikert Storybook historieregel).

### Feilrettinger og optimaliseringer
- Faste ikoner for sosiale kontaktknapper som ikke lastes inn i pakkede Electron-bygg ved å løse offentlige bilde-URL-er med en relativ base når app-base-URLen er "/" eller tom, i stedet for rot-relative URL-er under file://.
- Oppdatert Playwright komponentdekning for vinduskontroller for å matche gjeldende markering og tilgjengelighetsetiketter.

## 2.2.1 - Typenavn og konsistenssveip

### Nye funksjoner
- Lagt til eksplisitt komponentteststyringsveiledning på tvers av AGENTER/regler/ferdigheter: hver `src/components/**` Vue-komponent beholder en samlokalisert `*.vitest.test.ts`-pakke, med paritet behandlet som dekning tilstedeværelse (ikke uttømmende gren-/linjeprosent).
- Standardiserte retningslinjer for fixturplassering for fremtidig testutforming: Vitest- og Playwright-fixturer forblir inne i sine respektive testfiler, mens `_data/` forblir kun for produksjon.
- Omarbeidet delte meny- og dialog-/dokumenttypedeklarasjoner til klarere entalls- og samlingsnavngivning samtidig som prefikskonvensjonene for `I_` / `T_` bevares.
- Lagt til gjenbrukbare menyelementer og undermenytype byggeblokker for å holde menykonfigurasjonsskriving enklere å vedlikeholde.
- Utvidet Vitest-dekning på renderersiden under `src/` for skript og lagringstilstandsoverganger ved bruk av deterministiske mocks.
- Lagt til en sporet gjengivelsesteststrategi-inventar under `.cursor/plans/` for å kartlegge `src/` TS/Vue-mål på tvers av Vitest- og Playwright-ansvar.
- Del Vitest inn i en node/kjernekonfigurasjon og en jsdom-komponentkonfigurasjon med delt oppsett slik at `yarn test:unit` dekker boot, i18n, scripts, stores og colocated smoke tests for hver `src/components/*.vue`.- Samlokalisert 'AppControlSingleMenu' og 'SocialContactSingleButton' som komponenter på toppnivå (baner og importer oppdatert).
- Rutet innledende navigasjon gjennom en dedikert 'appStartupRouting'-hjelper konsumert fra 'App.vue'.

### Feilrettinger og optimaliseringer
- Fikset trigger callback-skrivingsfeil i hjelpemenydata ved å pakke inn maskinskrevne behandlere i null-argument callbacks som er kompatible med generiske menyutløsersignaturer.
- Normaliserte kommentarer og ordlyd som er utsatt for skrivefeil på tvers av komponenttester, E2E-tester og utvalgte kildefiler for konsistent navngivning og grammatikk.
- Oppdaterte README-testkommandoer for å matche faktiske skriptnavn og bruksmønstre i `package.json`.
- Separert Playwright artefaktutgang fra HTML-rapportmappen i `playwright.config.ts` for å fjerne advarsler om sammenstøt fra reporterutdatakatalogen.
- Justert testveiledning på nytt for å beholde "garntest:enhet" som grunnlinjearbeidsflyt for enhetstest i prosjektdokumenter og markørveiledningsfiler.
- Tydelige AGENTER/regler/ferdighetsveiledning slik at Vitest eksplisitt behandler både `src/` og `src-electron/` som førsteklasses enhetstesting-overflater samtidig som Playwright beholder som integrasjons-/runtime-laget.
- Tydelige testdataregler: automatiserte testarmaturer forblir inne i hver `*.vitest.test.ts` / `*.playwright.test.ts`, nyttelaster for komponenttesting bruker `COMPONENT_PROPS` der det er mulig, og innebygde komponentmodusmenydata forblir inne i `AppControl-triggermenus` for dialogbokser.- Løste ESLint-funn i boot external-link Vitest mocks («Event»-lytterskriving og polstrede blokker).

## 2.2.0 - Testing og agentverktøy

### Nye funksjoner
- Utvidet Vitest-dekning for Electron forhåndsinnlastede bromoduler og hovedprosessskript (vinduskontroller, devtools, eksterne lenker, appadministrasjon, stavekontroll og oppretting av hovedvindu) ved å bruke deterministiske hån.
- Playwright komponenttester for DialogAboutFantasiaArchive (erstatter den forrige TODO plassholderfilen).
- Markørregel og ferdighet for lokale plandokumenter i `.cursor/plans/` (gitignored), pluss veiledning for endringslogg for å lese versjonssamsvarende planfiler for utgivelseskontekst.

### Feilrettinger og optimaliseringer
- Erstattet gjenværende TypeScript `annoteringer` i delte typedeklarasjoner med `ukjent` for å holde menyutløser og Vue-komponentskriving strengere.
- Lagt til eksplisitt anti-'noen' veiledning til Vue, TypeScript skript, Vitest og Playwright regler/ferdigheter for å holde fremtidig kode sterkt skrevet.

## 2.1.0 – Verktøy og AI-assistert utvikling

### Nye funksjoner
- Markørprosjektregler og ferdigheter for Vue/Quasar, BEM og scoped SCSS, Playwright og Vitest, Electron forhåndsinnlasting, globale SCSS, konvensjonelle Git-forpliktelser og en-US changelog-vedlikehold knyttet til appversjonen.
- AGENTS.md som prosjektets inngangspunkt for AI-assistert utvikling.

## 2.0.0 - The Big Rewrite

### Nye funksjoner
– En hel mengde siden dette er en full omskriving!

### Kjente problemer
- Ingen problemer på utgivelsesdatoen.

### Feilrettinger og optimaliseringer
- For mange optimaliseringer å liste siden dette er en omskriving.
- Ingen feil på utgivelsesdatoen.
