# Ändringslogg
----------

## 2.4.13 - Version bump

### Nya funktioner
- **Gränssnittsspråk**: Språkväljaren listar **16** gränssnittsspråk (**engelska, US**, **Deutsch**, **Français**, **Italiano**, **Svenska**, **Suomi**, **Norsk**, **Português**, **Español**, λκηλ, λκηλ **Українська**, **العربية**, **中文**, **日本語**, **हिन्दी** och **Русский**). Menyetiketter markerar AI-assisterade språk med ett lokaliserat **AI**-suffix (bokstäverna **AI** förblir latinska i varje etikett).
- **Översättningar**: Varje väljarspråk levererar fullständiga gränssnittssträngar för menyer, dialogrutor, flytande fönster, aviseringar och hjälp i appen (**Advanced Search Guide**, **Advanced Search Cheat Sheet**, **Tips, Tricks & Trivia** och **Changelog**), genererade från den korrigerade **engelska, USA.
- **Arabisk layout**: Att välja **العربية** tillämpar layout från höger till vänster i hela appen; språkmenyn förblir från vänster till höger med läsriktning per rad så att endonymer visas naturligt.- **Skrivbordsinstallatörer** registrerar typen **`.faproject`** för **Öppna med** på paketen **Windows**, **macOS** och **Linux**; genom att dubbelklicka på en projektfil eller välja **Fantasia Archive** i systemväljaren öppnas det **`.faproject`** som den aktiva sessionen och uppdaterar **Ladda senaste projekt** när öppningen lyckas (utveckling **`yarn quasar:dev:electron`** hoppar över automatiska öppningar om du inte ställer in **`FA_DEV_OPEN_FAPROJECT=1`**).
- **Välkomstskärm** och **Projekthantering**: Välkomståtgärderna och matchande **Projekthantering**-rader använder **Skapa nytt projekt** och **Ladda in befintligt projekt** (**en-US**, **Deutsch**, **Français**).- **Välkomstskärm**: När **Project Management** har en sparad **Ladd senaste projekt**-lista visar **Splash** en delad återupptagningskontroll: huvudsegmentet läser **Återuppta senaste projekt** när inget **`.faproject`** är aktivt (öppnar den senaste senaste filen) eller **Återuppta aktuellt projekt** när en session redan har laddats upp (återuppta den aktuella filen). Pilen visar samma rader för senaste projekt som menyn (namn plus sökväg) och visar **Bläddra i senaste projekt** som dess tillgängliga namn och ett **Quasar** verktygstips till höger. Utan sparade senaste nyheter förblir den kontrollen dold.
- **Appinställningar**: **Dölj verktygstipset för "Bläddra i senaste projekt"** tar bort verktygstipset bredvid **Återuppta senaste projekt** på välkomstskärmen; splash uppdateras när du sparar växeln utan att starta om appen.- **Custom Project CSS**: Med ett aktivt **`.faproject`** listar **Settings & Tools** **Custom Project CSS** bredvid **Custom App CSS**. Det rörliga Monaco fönstret kvarstår per projekt stilmallstext i SQLite med avstuderad autosave, tillämpar liveförhandsgranskning genom **`#faProjectUserCss`** efter app-omfattande **Custom App CSS**, behåller den flytande ramen mellan **Custom App ** och __TK0 refres** och __TK0 inte när hon ladda, byta eller importera projekt. Den globala genvägen som standard är **Ctrl+Shift+J** på alla skrivbordsplattformar (samma bokstavliga Ctrl-tangent på macOS), kopplad som endast öppen: den stänger aldrig fönstret och en upprepad tryckning medan redigeraren förblir öppen lämnar ditt utkast i minnet orört.- **Project Noteboard**: **Project Management** vinner **Toggle Project Noteboard**, inaktiverad tills ett **`.faproject`** laddas, standardgenväg **Ctrl+Alt+D** på varje skrivbordsplattform; **Stäng** visar den avstängda parentesgenvägen under etiketten som **App Noteboard**. Text och geometri för rörligt fönster kvarstår per projekt i SQLite **`project_data`** KV-rader; renderaren håller den flytande över **App Noteboard** när båda är öppna medan båda håller sig under modal chrome (**6000+**).

### Bugfixar och optimeringar
- **Engelsk kopia**: **Titlar och beskrivningar av alternativen för **Appinställningar**, import/exportvägledning, nyckelbindnings- och åtgärdsövervakningsetiketter och hjälpmarkering korrekturlästes på **engelska, USA**; iscensättning **TODO**-prefix togs bort från inställningsetiketter.
- **Välkomstskärm**: **`/`** och **`/home`** delar nu ett **MainLayout**-skal (menyer i övre fältet, språkväljare och fönsterkontroller förblir monterade). Endast mittsidan övertonar mellan välkomstskärmen och arbetsytan; den vänstra lådan glider in på **`/home`** med samma timing. När en **`.faproject`**-session blir aktiv – skapa, ladda, återuppta, val av nyligen genomförda projekt eller öppna ett projekt från operativsystemet – navigerar appen till **`/home`** med en kort toningsövergång från valfri rutt utanför arbetsytan (välkommen, **Fel/hittades ej** eller framtida **MainLayout**-sidor).
- **Välkomstskärm**: Den vänstra lådan **Essential links** förblir helt utanför välkomstvägen — ingen reserverad skena och ingen låda krom på **`/`** — och glider in på **`/home`** när arbetsytan öppnas (samma tidpunkt som sidans övertoning) istället för att dyka in i full bredd.- **Fel / Hittade inte**: Okända rutter använder nu samma **MainLayout** översta fältet som välkomst- och arbetsytans skärmar (**Projekthantering**, **Dokument & Innehåll**, **Inställningar & Verktyg**, **Hjälp & Info**, plus språkväljaren och fönsterkontrollerna) utan den vänstra lådan.
- **Fel / Hittade inte**: När en **`.faproject`**-session redan är aktiv visar sidan **Återuppta aktuellt projekt** ovan **Återgå till appstartskärmen**; den öppnar den aktiva filen igen på samma sätt som det primära segmentet **Återuppta aktuellt projekt**, utan rullgardinsmenyn för senaste projekt eller en redan aktiv varningsskål.- **Projektet laddar**: Återöppning av projektet som redan är aktivt — från **Ladda in befintligt projekt**, **Ladda senaste projekt**, OS dubbelklick eller matchande meny eller splash **Ladda befintliga projekt** rader — visar en **varning** toast i samma **projektnamn** radformat som en lyckad laddning, laddar inte om databasen, och när du inte hittats på arbetsytan eller ** navigerar inte till /Error sida. **Återuppta aktuellt projekt** på **Fel / Hittade inte** och det primära segmentet för välkommen återuppta (**Återuppta aktuellt projekt** när en session är aktiv, **Återuppta senaste projekt** när inget) återvänder tyst till arbetsytan utan varning.
- **Välkomstskärm**: Menyn **Återuppta senaste projekt** senaste projekt använder samma griffelsidabakgrund, guldkant, titlar i ljusa guldrader, dämpade banor, guldradavdelare, en lätt primär-ljus svävningston och samma skugga som menyer i toppfältet istället för standardmenyn för mörkgrönt appmeny.
- **Välkomstskärm**: Den delade **Resume Latest Project**-rullgardinsmenyn visar dess chevron igen; den ärvda versaltextstilen hade brutit Material Icons-ligaturer inuti det segmentet.- **Välkomstskärm**: **Skapa nytt projekt** på välkomstrutten öppnas **Nytt projekt** via samma **AppControlMenus**-dialogvärd som arbetsytans väg.
- **Appinställningar**: **Dölj sociala länkar på välkomstskärmen** döljer guldavdelaren och **Social kontakt**-knapprader på välkomstskärmen; stänket reagerar när sparade inställningar ändras i **Appinställningar**.
- **Appinställningar**: **Dölj tipspopup på startskärmen** undertrycker uppstarten **Visste du?** tipsmeddelande på välkomstrutten (läs en gång när appen startar, inte vid varje ändring av inställningarna).
- **Appinställningar**: **Hoppa över välkomstskärm** öppnar det senaste senaste **`.faproject`** vid lansering — eller den aktiva sessionsfilen när en redan är laddad — och navigerar till arbetsytan (**`/home`**, delad **MainLayout** med den vänstra lådan) istället för att stanna på välkomstskärmen; stänket omdirigerar också när du aktiverar inställningen medan välkomstrutten visas. Om den senaste senaste filen inte längre finns, visar appen ett fel och stannar kvar på välkomstskärmen istället för att öppna nästa senaste projekt (inklusive efter uppstart redan försökt hoppa över en gång); utan nya projekt visas välkomstskärmen fortfarande.- **Projekthantering**: Släppte **Spara projekt** och **Stäng projekt** från menyn tills dessa åtgärder har riktiga skrivbordsimplementeringar (**en-US**, **Deutsch**, **Français** kopior uppdaterade).
- **Projekt SQLite**: Main behåller den aktiva **`.faproject`**-sökvägen bredvid databashandtaget så att korta avsynkroniseringar (till exempel efter att fönstret har varit inaktiva) kan öppna samma fil igen, försök igen operationen en gång och valfritt be renderaren om sökvägen när huvudet förlorade båda. Fullständig omladdning av renderare rensar fortfarande sessionen på verkliga dokumentnavigering, men samma dokument och subframe churn tappar inte längre handtaget ensam.
- **Open handshake**: Splash boot **`faProjectOsOpen`** väntar nu kort på **`faContentBridgeAPIs.faProjectOsOpen`** innan IPC-handskakningen **renderer-ready** så startar med en **`.faproject`** sökväg på Electron **argv** (installatörsassociationer) **FA_E2E_OS_OPEN** repris från slut till ände) kan inte missa ledningar när förladdade **`contextBridge`** ytor följer efter de första starttickarna.- **Anpassat projekt CSS**: Sparar från den flytande redigeraren när ingen **`.faproject`**-session är aktiv (till exempel på välkomstskärmen eller efter att appen har rensat databashandtaget) visar nu ett tydligt **Anpassat projekt CSS**-meddelande istället för att behandla en överhoppad SQLite-skrivning som lyckad och sedan dyka upp ett felmatchningsfel.
- **Project Noteboard**: laddar ett annat **`.faproject`** medan fönstret förblir öppet flyttar nu och ändrar storlek på ramen för att matcha det projektets sparade geometri istället för att lämna det tidigare projektets fotavtryck på skärmen.
- **Project Noteboard**: Flytta eller ändra storlek på fönstret rensar inte längre skraptext på skärmen när en frame-enbart spara tur och retur läser SQLite innan den avstudsade textskrivningen är klar; renderaren behåller värdet för levande textområde när återläsningar slås samman för patchar som utelämnar **text**.
- **Project Noteboard**: att spara anteckningstext eller geometri utlöser inte längre ett feltoast när **main** har rensat SQLite-handtaget under en omladdning av renderaren (till exempel **Control+F5** eller språkstavningskontrollens uppdatering): den fördröjda skrivningen sjunker tyst istället för att antyda en misslyckad spara tur och retur.- **Storybook**: Den kapslade arbetsytan **innehållsbryggan** låtsas nu stubbar **projektanteckningstavla** plus **projektstil** KV läser och skriv så **WindowProjectNoteboard** och **WindowProjectStyling** **Autodocs** plus förhandsgranskningar av arbetsytan inte längre visar falska **avvikande text-, åtgärdshanterarfönster eller autoe-actionhanterarfönster. CSS utkast. En **mock**-omfattad **TypeScript**-konfiguration återställer **`app/`** sökvägsskrivning för underhållare utan att utöka typkontrollen till hela förhandsgranskningspaketet.
- **Storybook**: **AppControlMenus** Autodocs komponentbeskrivning listar de aktuella **Project Management**-raderna så att de matchar **`_data/project`** efter menyändringar.
- **Test** — End-to-end **`checkProjectStyling`** byter namn på **`.faproject`** fixturbaslinjer och svittitlar så att **Custom Project CSS**-flödet beskriver Monaco liveförhandsgranskning, SQLite ihärdighet, rensa stilmallen och istället för per ** en etikettsisolering **a ** en etikett **. Komponentsele **`WindowProjectStyling.playwright.test`** täcker flytande krom, kallladdade **Monaco**, hjälppanelens temalista och **Stäng utan att spara** lämnar det lagrade projektet CSS tomt.- **Tester** — **`checkProjectManagementFlow`** från början till slut hävdar nu splash **Återuppta senaste projekt** (**MRU-rullgardinsetiketter** matchar **Projekt → Ladda senaste projekt**, ladda om genom det delade primära segmentet efter återställningen av stavningskontrollsessionen, ladda från en annan senaste rad).
- **Tester** — **`checkProjectOsOpenArgv`** återuppspelar paketerat **electron.launch argv** kallöppnar med **`FA_E2E_OS_OPEN=1`** isolerad från vanliga splash-only lanseringar.

## 2.4.12 - Version bump

### Nya funktioner
- **Menyerna i det övre fältet** (**engelska**): De fyra primära **`AppControlMenus`** **`titel`** strängarna läser **Projekthantering**, **Dokument & Innehåll**, **Inställningar & Verktyg** och **Hjälp & Info** (ersätter tidigare korta **Projekt** / **Dokument** & **Inställningar för dokument** & **Im, sedan /verktyg formulering). Undermenyrader använder versaler i rubrikstil på huvudord (**en-US**) under dessa titlar. **Deutsch** och **Français** topptitlar matchar samma fyra roller.- **Formulering på appnivå**: Användarvända etiketter och dokumentation för scratch **anteckningsbordet** (formaterad **anteckningstavla**, ett ord), **Appinställningar**, **Anpassad app CSS** och **Importera/exportera appkonfiguration** använder nu konsekvent **app**-ordförråd (interna kanalnamnsidentifierare, och IP-kanalnamnsidentifierare). **Dokument och innehåll** listar **Snabblägg till nytt dokument**, **Snabbsökning av befintliga dokument**, **Massradera dokument** och **Exportera projekt/dokument**. **Inställningar och verktyg** listor **Växla App Noteboard**, **Anpassad app CSS**, **Keybind Settings**, **App Settings**, sedan **Importera/exportera appkonfiguration** (under en avgränsare). **electron-store** och **.faconfig** ZIP-poster använder endast **`faAppNoteboard.json`** och **`faAppStyling.json`**.
- **App-anteckningsbok**: **Stäng** staplar det dämpade **Växla App-anteckningsbord**-ackordet i parentes under etiketten med samma färg och kompakta typstorlek som genvägslinjer på menyraderna **Inställningar och Verktyg**. Avskalad komponenttestning startar belastningsbeständiga tangentbindningar när den layouten monteras så att förhandsgranskningar löser tipset som det vanliga skalet.- **Välkomstskärm** — appen öppnas till standardrutten med samma toppmenyer, språkkontroll och fönsterknappar som resten av appen; den vänstra navigeringslådan förblir dold endast på den skärmen (**`faMainLayoutHideDrawer`** rutt **meta**). Den centrerade kroppen matchar klassiska **FA 1.0** (välkomstrad och titel med speglad vingkonst, tre guldkonturerade primära handlingar utan något beteende ännu, en avdelare, sedan den aktuella raden **Social kontakt**-knapp). Välkomstraden, wing **img** paret och åtgärder live i **`SplashControls`** (**`src/components/other/SplashControls/`**, layouttokens i **`styles/_variables.scss`**); **`SplashPage`** behåller sidskalet, avdelaren och **`SocialContactButtons`**. Wing art använder **`public/images/splash/splash_logo.png`** (i linje med **512** favicon). **`index.html`** förladdar den **PNG** med hög hämtningsprioritet; varje vinge **img** använder **loading=eager** så konsten efterfrågas för första målningen. Dev scratch **Index**-sidan finns kvar på **`/home`** med samma skal inklusive lådan.- **Aktivt projekt**: när **`S_FaActiveProject`** inte har något laddat projekt, inaktiverar **Projekthantering** **Spara projekt**, **Visa projektinstrumentpanel**, **Projektinställningar**, **Stäng projekt** och **Slå samman ett annat projekt till det aktuella** (under **Avancerade projektverktyg**); **Dokument och innehåll** inaktiverar **Snabblägg till nytt dokument**, **Snabbsökning i befintliga dokument**, **Massradera dokument** och **Exportera projekt/dokument**. **Inställningar och verktyg** förblir fullt tillgängliga, inklusive **Importera/exportera appkonfiguration** som sista raden nedanför **Appinställningar**. **Nytt projekt**, **Ladda in projekt**, **Avancerade projektverktyg** (överordnad rad), **Konvertera äldre projekt till FA 2.0** och **Växla App Noteboard** förblir tillgängliga.- **Nytt projekt** — **Välkomstskärmen** **Nytt projekt** och **Projekthantering → Nytt projekt** öppnar en beständig inställningsdialogruta, skapa sedan en **`.faproject`** SQLite-fil via huvudprocessen (**Splash** och **Project Management** kopplade via **`faActionManager`**). Rubriken visar det aktiva projektnamnet när ett är laddat. När skapandet lyckas visar **`faActionManager`** en positiv **Notify**: **Nytt projekt skapat och klart att köra!** med projektnamnet på samma rad (dialogrutan skickar inte en andra skål).
- **Ladda projekt** — **Välkommen**-skärmen **Ladda projekt** och **Projekthantering → Ladda projekt** öppnar en OS-filväljare som är standard till **Nedladdningar** (Playwright **E2E** använder isolerade **användardata** istället för den inbyggda dialogrutan). Om du väljer ett **`.faproject`** öppnas det i huvudprocessens SQLite-handtag och uppdaterar **`S_FaActiveProject`** som **Nytt projekt**. En positiv **Notify** bekräftar laddningen med **Projektet laddat och klart att köra!** och projektnamnet på samma rad.- **Ladda in senaste projekt** — **Projekthantering** inkluderar **Ladda senaste projekt** (efter **Ladda projekt**) med upp till **10** nyligen öppnade eller skapade **`.faproject`**-filer. Poster kommer från skrivbordsskalets sparade lista; saknade filer tas bort automatiskt. Att välja en rad öppnar den vägen på samma sätt som **Ladda projekt** och flyttar den till toppen av listan när öppningen lyckas; öppnas med sökväg som misslyckas ta bort den raden från listan förutom när projektet redan är aktivt.
- **Quasar** offentliga varumärkesvariabler på **`:root`**, från **`--q-primary`** till **`--q-dark-page`**, binds tillbaka till de matchande **Fantasia** **`--fa-color-…`**-namnen i **`src/css/facsvars`-app. stylesheet) så att **Custom App CSS** kan köra **`var(--q-*)`** från samma Fantasia-färgtokenuppsättning.- **SCSS** temalayout: **`app.palette.scss`** (inklusive semantiska texttokens under **APP COLOURS**), anpassade standardegenskaper i **`fa-theme.scss`**, delad **`faSemanticText.scss`** ( **`fa-text-`fa-**, **`fa-text-`***-i, **`fa-text-`***-i **`.text-white` / `.text-black`** mappad till **Fantasia** neutrals), split **`q-*`** tokens under **`src/css/theme/quasar-components/`**, samlokaliserad funktion **`styles/_variables.scss`**, globalscss.**`variables.sjustments andsjustments **`scrollbar.variables.scss`** och ett smalt **`quasar.variables.scss`** fat. **`yarn audit:quasar-component-tokens`** hjälper till att fånga upp saknade **`$q*`** definitioner.
- **UI-kopia och ikoner** använder dessa hjälpredor istället för ad hoc **Quasar** Materialtextnyansklasser där en roll ska följa runtime-tema. **Notify** standardinställningarna är inte längre inställda **`textColor`**.- **Anpassad app CSS** — **?** hjälpmenyn (till exempel under **Fantasia Archive variabellista**) listar **`--fa-color-…`** namn med en liten färgruta till vänster om varje namn (provet använder **`var(…)`** från **`:root`us-appen **, så det matchar din C__`tom-app ** och **T_us. När livestilar finns (inklusive **Vite** HMR i dev), kommer namnen från **DOM**; annars faller listan tillbaka till samma namn som analyserats från den medföljande **`fa-theme.scss`**-texten. Renderaren är inte längre beroende av **Node**-filläsningar för den panelen.
- **Custom App CSS** — **Editor keybinds** hjälp (**?** i titelraden): en **`QMenu`** utformad som den globala Fantasia **QTooltip** ersätter det gamla hovra-bara verktygstipset. Den öppnas efter samma **500 ms** hovringsfördröjning eller vid ett klick på hjälpikonen, förblir öppen för läsning tills du klickar utanför panelen, trycker på **Escape** eller klickar på hjälpikonen igen; **`no-focus`** gör att **Monaco** inte blir suddiga när menyn öppnas.- **Översta fältet**: **Snabblägg till nytt dokument**, **Snabbsökning i befintliga dokument** och **Massradera dokument** flyttade från **Inställningar och verktyg** till **Dokument och innehåll** (samma åtgärder och etiketter); **Dokument och innehåll** avslutas med **Exportera projekt/dokument**. **Projekthantering** listar inte längre **Exportera projekt/dokument** (samma åtgärd lever endast under **Dokument och innehåll**). **Importera/exportera appkonfiguration** är den sista raden i **Inställningar och verktyg**, nedanför **Appinställningar** och en separator.
- **Menyer i övre fältet**: kapslade undermenypaneler (till exempel **Projekthantering → Ladda senaste projekt** och **Avancerade projektverktyg**) öppnas när du pekar på den överordnade raden, med en kort fördröjning innan de stänger så att pekaren kan flytta in i den flytande panelen; Om du klickar på raden växlar fortfarande undermenyn där **Quasar** stöder den tillsammans med kontrollerad synlighet.
- **Hjälp och info**: **Om Fantasia Archive** visas ovan **Changelog** (före **Licens**).

### Bugfixar och optimeringar
- **Anpassad app CSS**: **Quasar** mörka **`q-fält`** understrykning, inmatningskopia, etiketter och framstående markerade eller skrivskyddade kanter använder nu **`var(--fa-color-white)`** eller **`color-mix`** mot det istället för fast **`#fff` / RGBA**. Mörk **`q-toggle`** lediga tumskivor, inre etikettstapel och on-state tumikonton följer **`--fa-color-white`** på samma sätt.
- **Välkommen / Om sociala rader**: **Sociala** chipetiketter och fyllningar använder endast **Sass** paletttokens (inga **`fa-v()`** eller runtime **`--fa-color-*`** ledningar i dessa komponentvariabelfiler), så **Custom App CSS** kan inte återintegrera **__,T. **GitHub**, **Discord**, **reddit**, **Webbplats** eller **X** varumärke. **Discord**, **reddit** och **GitHub** etikettfärger är fästa på **`q-btn`** roten med **`!important`** så ärvda **Quasar** regler för mörk text åsidosätter dem inte.- **Anpassad app CSS**: Åsidosättanden för Fantasia **`--fa-color-dark`** (och matchande varumärkestokens på **body**) når nu **Quasar** **`.q-dark`** bakgrunder som läser **`var(--q-dark)`**. Stilmallsbryggan upprepar **`--q-*`** anpassade egenskapsbindningar på **body**, inte bara på **`:root`**, eftersom ärvda CSS-variabler bär sitt beräknade värde; **kroppsnivå** **`--fa-color-*`** redigeringar skulle annars misslyckas med att måla om mörka **Quasar** ytor. **`.q-dark`**, **`body.body--dark`**, **`.q-layout__section--marginal`** och mörk **`.q-list` / `.q-item`** krom **använd** **`var(--fa-color-white)`** eller **`color-mix`-fas i stället för **`white-ite`** mot **`white-ite` **`--fa-color-white`** färgar om dessa staplar tillsammans med **`--fa-color-dark`** / **`--q-primary`** fyllningar.- **Engelsk kopia**: **en-US** **löpande prosa** (dialogtexter, stycken, **`Notify`** meddelanderader, ändringsloggberättelse) förblir meningsliknande (första ordet begränsat, små bokstäver senare om inte akronymer eller produktnamn (**CSS**, **CSS**, **__**TK2__, **__**TK2__, **__**TK2__, **__**TK2__, **__**TK2__) Electron, Monaco, SQLite, UI, **`macOS`**, etc. **Chrome** (**översta fältet** undermenyrader, **Splash** primära åtgärder inskrivna till menyer, **`dialogs.keybindSettings.commands.*`**`, **`Actions-matchning. **`globalFunctionality.faActionManager`** etiketter, checklista bildtexter bredvid växlarna, huvudrubriker **dialog** och **svävande fönster** rubriker som **Fantasia Archive Settings**) använder rubrikliknande versaler på huvudord medan korta grammatiska ord förblir gemener där konventionella (**Tips en**, **Tryck en **, **Tricker nedåt). dialogrubriker behåll **Avancerad sökguide** och **Tips, tricks & trivia** stavning De fyra **översta fälten** **`titel`** förblir rikare med bokstäver (se **Nya funktioner** ovan **Franska** speglar rubriker på jämförbara rader (**Astuces, conseils & anecdotes **).ritten mekaniskt.
- **Desktop persistens**: **electron-store** byter inte längre automatiskt namn på äldre JSON-filer för skrivbordet under **userData**; **.faconfig** import accepterar endast de aktuella **`faApp*.json`** basnamnen i arkivet; okända nyckelbindningsnycklar (inklusive pensionerade ID) släpps vid rensning istället för att skrivas om till nya ID.
- **Ordning**: Resterande etiketter på applikationsnivå normaliserades till **app**-ordförråd (till exempel **Anpassad app CSS**-titlar i **en-US** / **de** / **fr**, tipstrivia, importarkivvalideringskopia, anteckningsbrosfel och matchande testbidragsbidragsdokument).
- **Dialog- och flytande fönstertitlar** använder **Quasar** **text-h5** på semantiska **`h5`** rubriker (**Om Fantasia Archive**, **Action Monitor**, **Keybind Settings**, **Fantasia Archive Settings**, **Importera/exportera **C-appkonfiguration**, __ **App Noteboard**) så headers matchar en enda kompakt skala.- **Appanteckningstavla**: redigeringsområdet använder samma **`mörk-ljusare`** panelbakgrund som **Inställningar & Verktyg** menyrader; **Stäng** är centrerad i den nedre åtgärdsraden. Anteckningsfältet är ett inbyggt **textområde** så innehållet rullas in i fönstret när det är litet; Inbyggd stavningskontroll är avstängd, med **`autocorrect`** / **`autocomplete`** tips av och ett **Grammarly** opt-out-attribut för skraptext. **`h5`**-titeln använder samma vertikala marginal-token som **Custom App CSS** så att båda rubrikerna för flytande fönster är i linje.
- **Import/Export App Configuration**: **Export** och **Import** checklistpaneler är var och en insvept som sina egna **Quasar** **QStep** underordnade av dialogen **QStepper**, så att välja **Exportera** eller gå vidare till importval visar det korrekta steginnehållet istället för att bara den första panelen registreras med steg per panel. Stepperkroppen håller en minimihöjd och använder inte längre den horisontella bildanimeringen så checklistan förblir synlig i paketerade **Electron**-byggen där den tidigare flex- och övergångsstilen kunde lämna innehållsområdet kollapsat eller klippt efter stegbyte.- **Importera/exportera appkonfiguration**: **Använd import** från ett **.faconfig**-arkiv uppdateras **Anpassad app CSS** i **Monaco** medan det flytande fönstret förblir öppet så att redigeraren matchar nyimporterad stil, samma beteende som den repade **anteckningsplattan** förblir öppen.
- **Menyerna överst**: den första raden i varje öppen meny eller undermeny visar inte längre den tunna avgränsningslinjen ovanför den; tunna avdelare visas fortfarande mellan på varandra följande rader och hela avgränsningsrader fungerar som tidigare.
- Menyn **Inställningar och verktyg** visar inte längre **Växla hierarkiskt träd** som en menyrad.
- **Underhåll**: Tog bort en oanvänd återexport av tangentbindningsackordetiketter, inaktuella platshållare i slutet av **Electron** huvudinmatningsfilen från tidig SQLite-upptagning, och en överflödig **Hjälp & Info**-meny TODO i menydata (ingen förändring av synliga menyer eller genvägar).- **Action Monitor**: kopiering av en åtgärdsrad till urklipp visar **Åtgärdsrad kopierad till urklipp** som toasttitel (**Notify** **`meddelande`**) med en textrad **Aktivitet kopierad -** och det inspelade åtgärds-id (**de** / **fr** använder samma mönster med lokaliserade strängar); att positiv toast hoppar över den speglade **`[Notify]`**-raden i utvecklarverktyg (**kopieringsfel** utelämnar fortfarande dubblettspegeln bredvid **`console.error`**).
- **Utvecklarverktyg**: varje **Notify** toast skriver också en matchande rad till konsolen för utvecklarverktygen (**console.error**, **console.warn**, **console.info** eller **console.log**) från toast **typ** eller **färg**, taggade med **`[Notify]`** plus **text, **text, **text,**,**,** **timeout** när det finns, så att du kan korrelera popup-fönster med loggutdata; Sökvägar som redan avger sin egen **konsol**-linje för samma händelse hoppar över dubblettspegeln.
- **Utvecklarverktyg**: **Hjälp och info → Växla utvecklarverktyg** och förladdningssökvägen **öppnaDevTools** fråga den medföljande Chromium DevTools-gränssnittet efter **Konsol**-panelen efter att ha öppnats när det interna API:et finns, istället för att stanna på standardfliken **Element**.- **Action manager**: misslyckande **Notify** rader väljer bort den automatiska toast-to-console-spegeln när hanteraren redan avger **console.error**, så felrader förblir singel (samma krok gäller där **Action Monitor** radkopiering misslyckas efter sin egen rad **console.error**).
- **Quasar palett** (**`app.palette.scss`**): **negativ**, **positiv** och **varning** varumärkesplatser använder djupare, mindre iögonfallande hex-värden; **info** använder en fast varm **#ffebb8** så ramtonade **Notify** bakgrunder förblir sammanhängande med mörk-på-ljusbehandling. Sammantaget anpassar dessa märkesplatser **Material**-mappad krom med Fantasia-identiteten.
- **Ladda senaste projekt**: **Projekthantering**-raden för **Ladda senaste projekt** matchar **Avancerade projektverktyg** (grå sidopilstil) och förblir inaktiverad när det inte finns någon ny lista, istället för att öppna en tom undermeny; varje ny post visar projektnamnet med sökvägen **`.faproject`** på en andra rad i samma dämpade stil som menygenvägstips, utan efterföljande ikon.- **Ladda projekt**: varje **`.faproject`** lagrar nu ett beständigt **projekt** UUID i SQLite; försöker ladda samma projekt igen medan det redan är aktivt visar ett enda tydligt meddelande istället för att öppna filen igen, och **Notify** använder en **varningsstil** (inte röd) medan konsolen och **Action Monitor** fortfarande registrerar en misslyckad åtgärd.
- **Notify** — **varning**-typ rostat bröd färgavvisande och annan actionrad **QBtn** krom i samma mörka ton som meddelandetexten så att ikonerna förblir läsbara på den ljusa **varnings**-bakgrunden (samtalssidor passerar ofta fortfarande Quasar färg **vit**, avsedd för röda felfält).
- **Skrivbordssession** — ett fullständigt huvudfönster **WebContents** omladdning (till exempel **Control+F5** eller språkstavningskontrollens uppdatering) släpper huvudprocessens aktiva **`.faproject`** databashandtag så att appen matchar en ny renderare med **inget** projekt öppet tills du laddar ett igen.- **Språkkontroll**: stavningskontrollikonen bredvid språkväljaren använder klassen **`fa-icon-spellcheck-refresh`** igen så **`--fa-color-icon-spellcheck-refresh`** målar den röda accenten; den hade refererat till ett obefintligt **`fa-color-…`**-verktygsnamn och föll tillbaka till standardljusikontonen.
- **Nytt projekt** — framgång **Notify** visas bara en gång (**`faActionManager`**); dialogrutan för nytt projekt lägger inte längre upp en andra skål med överlappande kopia.
- **Tester** — end-to-end **Nytt projekt** täckningsmål **Projektledning → Nytt projekt** utan att felklicka på välkomståtgärder vars etiketter också innehåller **projekt** (**Playwright** använder ett exakt tillgängligt namn för menyknappen **Projektledning**).
- **Test** — Vitest **enhetskomponenter** fick bredare förgreningstäckning för dialoger, flytande fönster, menyer i toppfältet, **Huvudlayout**, språkväljarkanter och **anpassad app CSS**-injektion via extra fästen plus små **`skript/mallar som testades direkt** där individuella förgreningar eller mallar testades** direkt. att slå.- **Test** — end-to-end **Playwright** specifikationer **`checkAppNoteboard`** och **`checkAppFloatingWindows`** omslag **anteckningstavla** skrapa textbeständighet över omstarter och flytande **Anpassad app CSS** plus **App Noteboard** **geometrisk beständighet, flödesfasthet, flödesresistens, och co-index; en **`WindowAppNoteboard`** komponentselespecifikation täcker flytande krom och bryggtextbeständighet isolerat.
- **Nytt projekt** — fältet **Nytt projektnamn** fokuseras när dialogrutan öppnas så att du kan börja skriva utan ett extra klick.- Dialogrutan **Nytt projekt**: inte beständig längre, så Backdrop och Escape stänger den igen som andra dialoger. Etiketter använder **Skapa nytt projekt** och **Nytt projektnamn** (**en-US**, **de**, **fr**); den gamla dämpade raden **namnTips** under fältet är borta. Namnfältet rensas när arket öppnas; kortet håller snävare handlingsavstånd och fältet använder centrerad bredd och **primärt ljus** på inmatningsskalet med teman. Modalkomponentens registernamn är **`DialogNewProject`** (ersätter **`DialogNewProjectSettings`**); **`faActionManager`** använder **`openNewProjectDialog`**, och **vue-i18n**-vägar använder **`dialogs.newProject`**. **sv-US** tillgänglighetsetikett stämmer överens med **Nytt projekt**.
- **Nytt projekt** — dialogrutan **OS spara**s föreslagna **`.faproject`** filnamn behåller ditt mellanrum och versaler när det förblir säkert över vanliga filsystemregler istället för att skriva om stammen till avstavade gemener; tecken som vägar avvisar ersätts av mellanslag eller släpps för kompatibilitet (**emoji**, kontrolltecken). Den föreslagna standarden inkluderar fortfarande suffixet **`.faproject`** tillsammans med typfiltret så att den erbjudna sökvägen förblir explicit över plattformar.- **Nytt projekt** — **`.faproject`**-sökvägar accepteras när de är absoluta på värdoperativsystemet eller använder en **Windows**-enhet eller **UNC**-rot, så validering matchar riktiga spara-dialoger även när huvudkörningar på icke-Windows (till exempel automatiserad testning).
- **Nytt projekt** — namnfältet accepterar högst **120** tecken (**Quasar** **q-ingång** **maxlängd** med en levande teckenräknare). Samma tak gäller för huvudprocessvalidering, den föreslagna **OS Save** stammen och namn lagrade för **Ladda senaste projekt**; om kvarvarande data från senaste projekt inte uppfyller den regeln (till exempel alla namn som är längre än **120** tecken), rensar skrivbordsskalet den lagrade listan tills du öppnar projekt igen.
- **Dokument och innehåll** undermenylistor **Snabblägg till nytt dokument**, **Snabbsökning befintliga dokument**, **Massradera dokument**, sedan **Exportera projekt/dokument** i **en-US**, med justerade **de** / **fr** kopior; **Exportera projekt/dokument** flyttade hit från **Projektledning** tidigare; **Importera/exportera appkonfiguration** finns längst ned i **Inställningar och verktyg**. Toppmenyn **`titel`**-etiketter förblir icke-omslagsfria så långa fraser finns kvar på en rad.- **Anpassad app CSS** (**en-US**): hjälppanelens rubrik för temavariabellistan är **Fantasia Archive variabellista** igen (ersätter den förkortade etiketten **FA variabellista**).
- **Semantisk text**: **farans betoning** (**`fa-text-danger-emphasis`**, palett **`$text-danger-emphasis`**) använder en ljusare röd (**#ff4c4c**) för tydligare kontrast på mörk krom.
- **Appinställningar**: den valda fliken **kategori** till vänster använder den ljusa **primära** etikettfärgen igen. **fa-text-muted** gäller nu bara för flikar som inte är markerade, så det bekämpar inte längre **Quasar** **active-color** när båda verktygen ställer in **color** med **`!important`**.
- **Storybook**: Autodocs visar inte längre den första berättelsen två gånger när en fil definierar flera berättelser; **DialogActionMonitor** inkluderar Autodocs som andra dialogrutor; **Foundation** färgpalettverktygstips utelämna den extra **SCSS**-raden för **`$info`**; **SocialContactButtons** tar bort den överflödiga **CompositionContract**-berättelsen (föräldralös visuell baslinje har tagits bort).- **Storybook** visuella ögonblicksbilder: iframe-beredskapen väntar inspekterar **`#storybook-root`** och **`#root`** separat för elementbarn (tillsammans med synlig portalad **Quasar** dialog eller menymarkering) så **`querySelector`** på en grupperad **root-`#storybook kan inte välja en **root-`#storybook ensam **`#root`** när förhandsgranskningsträdet lever under **`#storybook-root`**.
- **Hjälp → Ändringslogg**: om en releaserad försökte visa **CSS** regeltext med **lockiga klammerparenteser** blockavgränsare, skulle **vue-i18n** kunna behandla dessa tecken som meddelandeplatshållare; dialogrutan kanske inte återges. Den berörda raden **2.4.12** är omformulerad på vanligt språk.
- **QTooltip**: popup-fönster på mörk krom använder **`$accent`** (**#f5f5f5**) och **`$dark`** (**#1b333e**), **13px** kopia med vikt **600**, **600px** maxbredd, en hög **`z-`-streck för **`z-`höga **`z-` `bokstavsmellanrum`** i **`quasarComponentsAdjustments`**, istället för **Quasar** storlek och färger på lagerverktygstips.- **Visste du?** (**info** **Notify**): avvisningsåtgärden är **`färg: mörk`**; **`_qNotify`** använder **mörk** förgrund på **`.q-notification.bg-info`**, och **`quasarComponentsAdjustments`** tillämpar den **`färgen`** med **`!important`** så den vinner över samma nods **Quasar** ****`text-white. använder **!viktigt**, som annars skulle tvinga nästan vitt på en ljus **info**-bakgrund.
- **Välkomstskärm** och **Om** sociala rader: **Reddit**-chippet använder endast ordmärkesbilden (ingen separat textetikett; **en-US**, **de**, **fr**). Välkomstkopia använder rubrikmarkering och dämpad undertextopacitet. **SocialContactButtons** häckar **row justify-center** omslag så att Patreon/Ko-fi och sekundära länkgrupper förblir horisontellt centrerade på breda layouter.
- **Om Fantasia Archive**: dialogrubriken använder en nivå fyra-rubrik så att dokumentkonturen matchar hur titeln läses visuellt.- **Välkomstskärm** — primära åtgärder använder bredare horisontell stoppning och **passningsinnehåll** bredd så att varje **knapp** anpassas till sin etikett istället för att sträcka ut hela åtgärdskolumnen. **SplashPage** och **SplashControls** läser layout från samlokaliserade **`styles/_variables.scss`** moduler **`@forward`ed** till **`quasar.variables.scss`**, matchande semantiska **SCSS** tokenregler som används på andra ställen under **`s/`rc**/.
- **Välkomstskärmen** rubrikraden och rullgardinsmenyn **språk** i övre fältet dämpar valbar text när du drar så att krometiketter inte plockar upp oavsiktlig markering.
- **Beroenden**: **dompurify** (dragen in av **Monaco**) är fäst till **3.4.2** med en **Garn** **upplösnings**-post så att redigerarens stack spårar en korrigerad **3.x** saneringsversion istället för en äldre transitiv pin.
- **Electron desktop**: **`BrowserWindow` `preload`**-sökvägen löser sig till Quasars paketerade **`preload/electron-preload.cjs`** artefakt så att **`contextBridge`** API:er (**`window.faContentBridgeAPIs kan inte initiera om filnamn) istället för att initiera förinläsning av mappen igen från paketlayouten **`dev`**.- **Action Monitor**: framgångsrika **Ladda projekt** historikrader håller köns nyttolast tom men bifogar en avslutande förhandsgranskning med den valda **`.faproject`** sökvägen och laddat projektnamn, i linje med hur **Nytt projekt** ytor detaljer efter slutförandet; när öppning misslyckas efter att du valt en fil, kan misslyckade rader inkludera meddelandet plus sökvägen när skrivbordsskalet tillhandahåller det.
- **Electron desktop**: **`faRoutingEnv`** väntar en kort stund på förladdningsläget **extra miljö** ögonblicksbilden innan man styr de första selens rutter, undviker ett startrace där bron fäster efter den första renderarens färg.
- **Playwright hjälpare**: Utnyttja koden för Electron komponent och **E2E** sviter är uppdelad under **`helpers/playwrightHelpers_universal`**, **`helpers/playwrightHelpers_e2e`** och **`helpers/playwrightHelpers_component so`ear`, with shared launchers förbli enhetlig; **`README`** och bidragsgivarens anteckningar listar de uppdaterade importsökvägarna.

## 2.4.11 - Version bump

### Nya funktioner
- **Översta fältet**: menyetiketten **Verktyg** är **Verktyg och inställningar** (och matchande **de** / **fr**-strängar) så namnet matchar inställningsrelaterade poster i menyn.
- **Lokal paritet**: **de** och **fr** följer nu samma **vue-i18n** nyckelträd som **en-US** (inklusive **errorNotFound**, **globalWindowButtons**, **fantasiaMascotImage**, **dialogs.markdownDocument** **globalFunctionality.unsortedAppTexts** och buntade **dokument**), så fönsterkrom, felsidan, tipsrubriken och maskot-alt-texten löser sig utan att ha fel eller saknade nycklar. **Appinställningar** i **de** använder fortfarande strängmodulen **en-US** tills ett fullständigt **de**-pass för appinställningar landar; **fr** behåller sin partiella **appinställningar**-fil med **en-US** reserv för resten. **Import/export** meddelandelinjer skärptes i **en-US** med matchande **de** / **fr** uppdateringar.
- **Importera/exportera appkonfiguration** — panelerna **Import**/**Export**/checklista byts ut med en horisontell bild (stepper-animering); steghuvudet förblir dold så att flödet fortfarande ser ut som en dialogruta.- **Importera/exportera appkonfiguration** — högst upp i dialogrutan innehåller ett lokaliserat **Observera**-block (export-först vägledning, överskrivningsvarning och selektiv import) i **en-US**, **de** och **fr** före väljaren **Import** / **Export**.- **Verktyg → Importera/exportera appkonfiguration** (**mdi-database-export-outline**), direkt under **Visa anteckningstavla**: öppnar en icke-beständig **Importera/Exportera appkonfiguration**-dialogruta (utanför klickar och **Escape** stäng den) med **Importera** och **Exportera** på det första steget ** i två kolumner med en hel bredd av varje knapp med 8 kolumner. px** horisontell infogning för åtgärdsraden, platta **primära** åtgärder (fet, versaler), matchar den klassiska **FA 1.0** **Infoga bild**-dialogrutan. **Export** erbjuder tre förmarkerade kryssrutor (**App-inställningar**, **App-nyckelbindningar**, **Anpassad app CSS**), en **Skapa exportfil**-kontroll (aktiverad endast om minst en del är vald), och sedan den normala **OS-spara**-dialogrutan för att skriva en **`.faconfig`** (ZIP med samma Jlectron-namn som **SON-butiken: e**SON-butiken: **`faUserSettings.json`**, **`faKeybinds.json`**, **`faAppStyling.json`**). **Import** använder dialogrutan för öppen fil endast för **`.faconfig`**; huvudprocessen upprätthåller ett **3 MB**-tak, tillåtna medlemmar, **JSON** + **Zod**-validering och iscensättning innan du bekräftar **Importera valt**; applicering ersätter de valda butikerna och renderaren laddar om appinställningar, keybinds, och app **CSS** från bron. Flödet registrerar **faActionManager**-åtgärder (**openImportExportAppConfigDialog**, **exportAppConfigPackage**, **exportAppConfigSaveResult**, **importAppConfigStageResult**, **importAppConfigApply**) för **Action Monitor** med små, monitor-safe-filer.- **Verktyg → Anpassad app CSS**: en ny post öppnar en **beständig** **Anpassad app CSS** flytande fönster (ikon **mdi-language-css3**) med en kallladdad **Monaco** redigerare konfigurerad för **CSS** språkstöd. Fönstret är **flyttbart** (dra titelraden) och **kan ändra storlek** (kant- och hörnhandtag); den kan endast stängas med **Stäng utan att spara** eller **Spara ändringar** (**Escape** och slumpmässiga klick utanför fönstret avvisar det inte). Sparad CSS lagras i **Electron** huvudprocessen via **electron-store** och tillämpas globalt som ett **`<style id="faUserCss">`**-element som injiceras i **`document.head`** vid appladdning och uppdateras när du sparar ändringar, så anpassade regler gäller för varje rutt, layout och inbäddad yta. Ramen är en inrenderare **`position: fixed`** yta teleporterad till **`document.body`**, centrerad och storleksanpassad från viewport-fraktioner och marginaler, med **Vue** **Transition** och den anpassade **fa-floatingWindowPop**-animationen (opacitet och skala; minsta skala är aldrig som **00 m**s **T** **T** **s __s; lager **q-transition--scale** **scale(0)**, vilket kan lämna en degenererad **transformation** i **Electron**). **Monaco** laddas endast när window öppnas (och endast **CSS** språkpaketet och arbetaren är paketerade) så den första appstarten påverkas inte. **Verktyg → Keybind-inställningar** listor **Öppna anpassad app CSS** med standardgenvägen **Ctrl + Alt + Shift + J** (**Cmd** ersätter **Ctrl** på **macOS**); ackordet kan omtilldelas eller raderas därifrån. Både öppning och sparande flyter genom **action manager** (**openAppStylingWindow** och **saveAppStyling**), så sparade misslyckanden har samma behandling med en enda avisering som andra inställningsytor. (Uppströms **[@quasar/quasar-ui-qwindow](https://github.com/quasarframework/quasar-ui-qwindow)**-paketet är endast inriktat på **Quasar v1 / Vue 2**; Fantasia Archive skickar en **Vuee 3.** i appen istället.- **Åtgärdshanteraren**: varje användarbetingad åtgärd (öppna dialogruta, spara inställningar, minimera / ändra storlek / stäng fönster, språkväxling, växling av utvecklarverktyg, meddelande om starttips och liknande) flödar nu genom en enda avsändare med två exekveringstyper - **asynkronisering** (avfyra och glömma) och **synkronisering** (enkel ** ** FIFO , ** ** ** ** s ** s, caps ** timeout per åtgärd, opt-in deduplicering). Fel visar ett enda **fel**meddelande med åtgärds-id och det underliggande felmeddelandet, och samma detaljer hamnar i utvecklarkonsolens rad bredvid den. Spara flöden för **Keybind-inställningar** och **App-inställningar** väg genom denna yta, så de tidigare dubbla aviseringarna om en misslyckad lagring är borta.- **Hjälp och info → Åtgärdsövervakning**: posten under **Växla utvecklarverktyg** öppnar en **Aktionsövervakare**-dialogruta med en tabell över åtgärder från den aktuella appsessionen: **Åtgärd**-id, **Starttid**, **Sluttid**, **Nyttlast** (grön bock när kötid nyttolasttext är tom**), ** SyncTyp**T** eller grått. **Async**) och **Status** (växelspinnare under löpning, grön bock vid framgång, rött kryss vid misslyckande, blå sandtimerikon i kö, var och en med ett hovra verktygstips för betydelsen). En hjälpikon förklarar att ett vänsterklick kopierar raddetaljer till urklippet. Rutnätet är en **ögonblicksbild** när dialogrutan öppnas, begränsad till **500** rader per session, inte skriven till disk. **Keybind-inställningar** listar **openActionMonitor** så att du kan tilldela en global genväg; det finns fortfarande inget standardackord.- **Verktyg → Inställningar för nyckelbindning** öppnar en dialogruta för **Inställningar för nyckelbindning** (tabell över åtgärder, standardgenvägar och anpassade genvägar, filtrera, spara till beständig lagring i **Electron** huvudprocessen). **Hjälp & info** listar inte längre ett separat fuskark för nyckelbindning; genvägar hanteras från **Verktyg**. Globala genvägar förblir aktiva medan du skriver in inmatningar där bindningen tillåter det, och skickar pauser medan den här dialogrutan eller dess infångningspopup är öppen.
- **Gränssnittsspråk**: en **språk**-kontroll i det översta fältet (bredvid fönsterknapparna) visar den aktuella lokalen som en **flagga** i en cirkulär kontroll och öppnar en kort meny för **engelska, US**, **Deutsch** och **Français**. Valet lagras som **språkkod** i beständiga **användarinställningar** (**en-US**, **de** eller **fr**, standard**en-US**) och tillämpar **vue-i18n** i hela appen. **Appinställningar** exponerar inte detta fält ännu.
- **Landsflaggor**: en komplett uppsättning **SVG**-flaggabilder ingår under **`public/countryFlags/`** för språkkontroll och andra språkmedvetna användargränssnitt.- **Tyska** **översta fältet**: **Projekt**, **Dokument**, **Verktyg** och **Hjälp och info** menyposter använder **Deutsch** språkmoduler vid sidan av **Om** och den delade **sociala** raden, som matchar menyformen **en-US**.
- **Avancerad sökguide**: standardgenväg **Ctrl + Alt + Shift + G** öppnar nedmärkningsguiden i samma dialogruta som **Hjälp & info → Avancerad sökguide**; **Verktyg → Keybind-inställningar** listar det som **Öppna Advanced Search Guide**.
- **Tips för menygenvägar**: **Verktyg** och **Hjälp & info** visar den globala genvägen live bredvid **Appinställningar**, **Keybind-inställningar**, **Advanced Search Guide** och **Toggle utvecklarverktyg**, med samma etiketter som **Verktyg → Keybind-inställningar**; ledtrådsraden visas endast efter att tangentbindningar har laddats från **Electron** och endast för dessa åtgärder.
- **Storybook**: **AppControlSingleMenu** och **AppControlMenus** berättelser inkluderar **`keybindCommandId`** fixturer (toppnivå och undermeny), en kompositionskontroll som öppnar **Hjälp & info** och förväntar sig uppmärkning av genvägstips, och en valfri **`inbädda som så produktionsdialogs`.**variant- **End-to-end**: **`checkKeybinds`** Playwright-specifikationen täcker standard **Ctrl + Alt + Shift + G** och ett beständigt anpassat ackord för **Öppna Advanced Search Guide** mot markdown-guidens dialogruta, i samma flöde som andra globala genvägar.
- **End-to-end**: **`checkAppStyling`** Playwright-specifikationen öppnas **Anpassad app CSS** från **Verktyg**, anger **CSS** i **Monaco**, sparar så att **document.body** hämtar **zoom** från det inmatade fönstret, tömmer formatmallen, tömmer stilarket, tömmer stilarket. **CSS**, och hävdar att **zoom** återgår till standardinställningen.
- **Storybook**: **`externalFileLoader`** registrerar **`floatingWindows.appStyling`** för **en-US** så **WindowAppStyling** **autodocs** och canvas-berättelser löser titlar, verktygstipskopia och åtgärdsetiketter istället för råa **nvue;1 berättelsen ger flerradsexempel **CSS** så **Monaco** syntaxmarkering är lätt att upptäcka.- **Storybook**: **DialogImportExportAppConfig** canvas och **autodocs**; **`externalFileLoader`** registrerar **dialogs.importExportAppConfig**; arbetsytans innehållsbrygga mock lägger till **faAppConfig**-stubbar så att **prepareImport** kan lyckas i arbetsytan medan **exportToFile** returnerar **cancelled**, vilket matchar det verkliga flödet tillräckligt för att granska dialogen utan **Electron**.
- **End-to-end**: **`checkImportExportAppConfigFlow`** kör konfigurationsflödet för import/export av appar (export, stegvis import, **Action Monitor**-kontroller och relaterade **Keybind**/appstilsbieffekter) med hjälp av fasta sökvägar i den isolerade **Playwright**-profilen; **E2E** kan hoppa över inbyggda fildialoger för dessa steg via en huvudprocesssöm som endast gäller när **`TEST_ENV`** är **e2e**. **Playwright** genvägssträngar använder **`helpers/playwrightHelpers_universal/faPlaywrightKeyboardChords.ts`** så standardinställningar som följer appens **primära** modifierare och ackord som använder den fysiska **Control** tangentmatchningen **macOS** och **Windows**s värd**s.
- **Komponenttest**: **`DialogImportExportAppConfig.playwright.test`** täcker close-on-**Escape** för import-/exportdialogrutan i **komponent**-selen.

### Bugfixar och optimeringar
- **Playwright** (komponent, **E2E** och **Storybook** visual) använder terminalens **`list`** reporter: varje test skrivs ut på sin egen rad medan körningen fortskrider (HTML-rapporter oförändrade).
- **Topappmenyer** (**AppControlSingleMenu**): efter en fullbreddsrad **separator** i menydata staplas den tunna avdelaren före nästa objekt inte längre på den fullständiga avgränsaren (inga dubbla horisontella regler).
- **Importera/exportera appkonfiguration** — **OS**-dialogerna för att spara och öppna som standard till din **Nedladdningar**-mapp, och export föreslår filnamnet **faConfigExport.faconfig** så den erbjudna sökvägen är konsekvent när ett blott filnamn annars skulle landa i en oväntad mapp på vissa system.
- **Anpassad app CSS** flytande fönster — ramen **Transition** använder en anpassad **fa-floatingWindowPop**-animation (omkring **0,1** till **1** skala plus opacitet, samma **300 ms** som standard **QDialog** och standard **q5__** och standard- **q)-övergången - som kan lämna - rot-övergången** i stället för en stock degenerera **förvandla** i **Electron** och bryta **Monaco** i **Playwright** synlighetskontroller.- **Anpassad app CSS** flytande fönster — ändra storlek på kant- och hörnhandtag visar en mjuk **primär** gradient mot ytterkanten endast medan du hovrar eller drar; inaktiva handtag förblir osynliga. Glödet bleknar in och ut med en kort opacitetsövergång. Handtagsfärger är temasymboler i **quasar**-variabler; reglerna finns i **`_FaFloatingWindowFrameResizeHandles`** **`styles`** så att **Vue**-filen håller sig inom radgränserna.
- **Flytande fönster**: pekarens storlek uppdaterar rambredd och höjd högst en gång per bildruta, vilket minskar redundant layoutarbete för **Monaco** och resten av ytan medan du drar i ett handtag.
- **Anpassad app CSS** flytande fönster — titeltexten tvingar inte längre fram **`marginal: 0`**, så rubriken behåller normalt avstånd bredvid dragraden och hjälpikonen.
- **Anpassad app CSS**: medan den flytande editorn är öppen gäller **Monaco** ändringar live på den globala **`#faUserCss`** stilarket så att du kan förhandsgranska regler innan du sparar. **Stäng utan att spara** laddar om **CSS** från disk (samma bryggläsning som vid uppstart) så att appen återgår till den sista bevarade filen.- **Anpassad app CSS** flytande fönster — **Layout** begränsar inte längre bredden vid **1400 px**. **CSS** använder **`max-width: stretch`** istället för **`100vw`** så att ramen förblir inuti den synliga layouten när rullningslister eller insättningar tillämpas. **`<Övergång>`** omsluter ramroten, inte **Teleport** (så kör in/lämna på ett enda element och **Vue** varnar inte för övergångsunderlag som inte är element); öppna/stäng använd **fa-floatingWindowPop** (se **Bugfixar**). **_FaFloatingWindowBodyTeleport** skickar ramen till **`document.body`** så **Storybook** och tester är inte beroende av ett **`#q-app`** monteringsmål. Titelraden använder en flex **min-höjd** så att dragremsan håller ett riktigt träffmål. Flytande ytor använder ett **z-index** band under huvudkrom. Efter stängning, kasserar **Monaco** endast efter dialogövergången; en väntande avyttring avbryts om du öppnar igen eller stänger igen innan den fördröjningen upphör.- **Playwright** **yarn** scripts: **`scripts/playwrightWithArtifactTrim.mjs`** gör **`--component=bucket/ComponentFolder`** till **`src/components/...`** när **Garn** på **Windowsnpms** inte expanderar och tar bort **s **test-resultat/dramatiker-artefakter** efter en körning så att duplicerade bifogade träd inte finns kvar på disken.
- **Anpassad app CSS** **directInput** ledningar (kablage och **Storybook**): **`directInput`** prop watcher körs **omedelbart** vid installationen så **`WindowAppStyling`** öppnas när **`directInput`** redan är **WindowAppStyling** när argan
- **End-to-end** **`checkAppStyling`**: beredskaps- och stängningskontroller använder **`windowAppStyling-frame`**-lokaliseringen istället för att förlita sig på enbart rubrikens synlighet efter att fönstret öppnats.- **Electron renderarprotokoll**: paketerade builds laddar nu renderaren genom ett anpassat privilegierat **`app://`**-schema registrerat i huvudprocessen (**standard**, **secure**, **supportFetchAPI**, **stream**, **codeCache**, **bypassCSP**) istället för **.`file://`** Hanteraren löser renderarroten bredvid det pågående huvudpaketet, vägrar vägtraversering och **`..`**-segment och faller tillbaka genom **`index.html`**. Det är detta som låter fönstret **Anpassad app CSS** snurra upp sin **Monaco**-arbetare (**css.worker** laddad med **Vite** **`?worker`**-importer) på ett tillförlitligt sätt i den paketerade appen, eftersom webbarbetare inte initierar på samma sätt under **`file://`**. Utvecklingsserverflödet är oförändrat.- **Globala nyckelbindningar**: inbyggda standardinställningar för **Windows** och **Linux** är nu **Ctrl + F12** för **Toggle utvecklarverktyg**, **Ctrl + Alt + Shift + L** för **Öppna appinställningar** för K__T5__, **__TK5**, **__TK5** **Öppna tangentbindningsinställningar**, **Ctrl + Alt + Shift + J** för **Öppna anpassad app CSS**, och **Ctrl + Alt + Shift + G** för **Öppna Advanced Search Guide** (på **macOS**K:s **kommando **beställer macOS**K **s **ordern modifierare; **Öppna Advanced Search Guide** använder alltid **Ctrl**). Befintliga sparade åsidosättningar i huvudbutiken **Electron** är oförändrade.
- **Markdown-dokument**-dialoger öppnade med **directInput** (till exempel **license** eller **changeLog** från menyer) registrerar nu samma **markdown-dialogstapelskydd** som öppnas, så **Appinställningar** och andra komponentdialoger kan inte staplas ovanpå förrän du stänger nedmärkningsarket.- **Externa länkar** kopplar upp en liten **`faExternalLinkMouseDispatch`**-hjälp så att klick- och auxclick-logiken förblir testbar i **Node** Vitest (injicerat **`getBrowserWindow`**) medan produktionen fortfarande läser **`globalThis.window`**. Hanterarens no-op-sökväg körs när **`faExternalLinksManager`** eller dess metoder saknas så att icke-**Electron**-renderingar inte kastar.
- **Globala nyckelbindningar**: om laddning av genvägar från **Electron** huvudprocessen misslyckas, visar appen en **Quasar** avisering med ett kort **laddningsfel** meddelande istället för att bara logga; samma meddelande visas om **Verktyg → Nyckelbindningsinställningar** inte kan uppdateras vid öppning.
- **Komponenttestkabelnät**: ogiltig **COMPONENT_PROPS** JSON från **`extraEnvVariables`** ignoreras (**falskt**) istället för att krascha huvudprocessens **IPC**-hanterare.
- **Storybook** visuell regression: **`garntest:storybook:visual`** misslyckas nu när en berättelses iframe aldrig visar renderat innehåll under Storybook-roten (och ingen synlig portaldialog eller meny), såvida inte den berättelsen väljer bort den med **`skip-visual-render-check`** eller dess **ED** STORY är listad i **ID** EX_ID. visuell Playwright spec.- **Medverkande dokument**: **README**, **AGENTS.md**, **CONTRIBUTING**, **testing-terminal-isolation**, **storybook-stories**, **git-conventional-commits**, **fantasia-dev-setup**, **fantasia-testing-en-sia** och **fortsätt** **Garn** **1.x** och CI **1.22.19**, **i18n** **komponenter** hinkar inklusive **dialoger**, **testbatch:verify** täckningsnivåer (inklusive **i18n**), reporoot **hjälpare** endast för selekod **, korrigera samma VYRT lin-regler ** **Humit lin --edit`** formulering och reporot-relativa länkar i ändringsloggfärdigheten.
- **Medverkande dokument**: **README**, **AGENTS.md**, **playwright-tests**, **testing-terminal-isolation**, **fantasia-testing** och **fantasia-keybinds** dokument cross-OS **`keyboard.press`** regler för apptangentkopplingar och peka på **`helpers/playwrightHelpers_universal/faPlaywrightKeyboardChords.ts`**.- **Bidragsgivares arbetsflöde**: **AGENTS.md** och markör**regler** / **färdigheter** (**changelog-en-us**, **git-conventional-commits**, **testing-terminal-isolation**, **fantasia-changelog-en-us**, **fantasia-i18n**-,booking**stfantasia**, beskriv hur man fäller **`changeLog.md`** till samma commit-batch som innehållsarbete före **push**, använder endast changelog-commits för sällsynta korrigeringar och håller Git- eller hushållsfraser borta från releasenoteringar i appen.
- **Keybind settings** capture behandlar fysiska modifieringsnycklar (**ShiftLeft**, **ControlRight**, **AltLeft** och liknande) som ofullständig inmatning istället för att para ihop dem med dubbletter av modifieringsetiketter, och accepterar genvägar byggda från bokstäver, siffror, **F1–F24**, **F1–F24**, tu fasta tangenter och pu. tangenter (komma, parenteser, snedstreck och liknande) — inte bara numeriska tangenter eller de flesta andra specialtangenter. Dessa vägledningslinjer använder fältet **tips** (informationsstil); **duplicerad genväg** på **Set** visas fortfarande som ett fältfel.- **Keybind settings** popup-fönster för infångning är i linje med **legacy Fantasia Archive (FA 1)** capture polish: **action**-namnet som titel, en **Skriv din keybind** platshållare, en **tunn prickad** understrykning under ackordraden (**röd** endast för dubblett**-konflikter **modifierad tint-int. tillämpas), och **en kompakt panel direkt under** ackordraden vars **text växlar** mellan **behövermodifieraren** / **tangenten som inte stöds** och meddelandet **duplicerat genväg** beroende på vad som just hände (**fel**ton och **varningsroll endast för dubblettfallet). Panelen **bleknar och glider** i den platsen istället för att stapla två separata meddelandeområden. Konfliktikonen **varning** stannar bredvid ackordraden när en dubblett är aktiv; den gamla fristående **info**-ikonen för tips är borta. **hjälp**-verktygstipset listar fortfarande modifieringsmönster och accepterade nycklar; **Rensa tangentbindning** och **Set** stannar på ljuspanelen. Pressar med endast fysisk modifierare undviker fortfarande att måla platshållaren röd; en ensam modifieringstryckning efter **behöver modifierare** eller knappvägledning som inte stöds kan ta bort tipset igen.- **Keybind settings** capture **Clear keybind** ställer in den åtgärdens fungerande åsidosättning till **null** så huvudtabellens kolumn **Användarnyckelbindningar** matchar omedelbart den inbyggda standarden igen (**Lägg till ny...** när den standarden är vad som är aktiv, samma regel som bevarad **null** åsidosätter), stänger raden för infångning disabled tom, och visar endast den tomma raden för insamling av plats, och genvägstext ännu).
- **Keybind-inställningar** fångar upp en dubblettgenväg så snart ackordet känns igen, med varje rads **aktiva** bindning (standardvärden plus osparade åsidosättningar). En standard som inte längre gäller på grund av att en annan åtgärd rebound blockerar inte återanvändning. Dubblettmeddelandet är tydligare på **engelska**, **Deutsch** och **Français**; en avvikande modifieringsnyckel efter en konflikt rensar inte längre varningen ensam. Medan det duplicerade felet visas, stannar fångstraden på genvägen som var synlig när du öppnade fånga (eller förblir tom när den raden inte hade någon genväg) istället för att visa det avvisade ackordet; **Set** med ett motstridigt ackord gör samma sak.- **Keybind settings** infångning öppnas med radens **aktuella** genväg redan visad (standardvärden och anpassade åsidosättanden), släpper den redundanta **Använd standard**-kontrollen från infångningsarket och kapslar infångningspopupen i huvuddialogrutan för nyckelbindning. Huvudbladet för **Keybind-inställningar** förblir **beständigt**; du kan stänga insamlingsarket med ett **utanför klick** eller **Escape** utan att stänga den överordnade dialogrutan. **Escape** på inspelningsarket hanteras i inspelningsavlyssnaren så att det alltid avvisar det bladet istället för att behandlas som en kortkommando som inte stöds.
- Dialogrutan **Keybind settings** använder en **1200 px** kortbredd på breda visningsportar (den krymper fortfarande på smala fönster), tabellrubriker **Användarnyckelbindningar** och **Standardnyckelbindningar** och filterplatshållaren **Filter nyckelbindningar...**. Kontrollen **Användartangenter** **Lägg till ny...** använder **primär-ljus** kontur medan den aktiva genvägen matchar den inbyggda standarden eller inget har tilldelats ännu, och **grå-7** kontur med det formaterade ackordet endast när en anpassad genväg skiljer sig från den standarden.- **Keybind settings** **Standard keybinds** kolumnen visar ett **em bindestreck** när det inbyggda standardackordet inte kan utökas för den aktuella plattformen, istället för vilseledande formaterad text.
- **Keybind-inställningar**: genvägstabellen använder det vertikala utrymmet mellan dialogrubriken och sidfotens åtgärdsrad, och dess rullningshöjd uppdateras när du ändrar storlek på fönstret.
- **Keybind-inställningar**: genvägstabellen listar endast riktiga genvägsåtgärder från tangentbindningslistan; Förhandsgranskningsrader för enbart utveckling tas bort.
- **Keybind settings** fånga hjälpverktygstips (**engelska**): tydligare sammanfattning av tillåtna nycklar och **din nyckel** platshållarord i modifieringskombinationsexempel.- **Keybind-inställningar**-filter utan matchningar visar samma **ErrorCard**-behandling som **Appinställningar**-sökning (maskot i **läsläge, titel och stödjande text från **vue-i18n**) via **q-table** **no-data** plats istället för en tom tabell. Tabellens nedre fält döljs bara så länge rader finns, eftersom **Quasar** målar **ingen data** inuti fältet och **dölj botten** hoppar över den helt när den alltid är på. **Virtuell rullning** i den tabellen stängs av medan den filtrerade radlistan är tom så **Quasar** monterar fortfarande **no-data**-platsen istället för att lämna en tom virtualiserad kropp. Rensning av filterfältet kastar inte längre när **Quasar** sätter **v-modell** till **null** istället för en tom sträng. Filterfältet återställs till tomt när dialogrutan döljs, inklusive efter **Spara inställningar** eller **Stäng utan att spara**.- **Electron stavningskontroll**: Chromiums stavningskontrollspråk spårar samma beständiga **språkkod** som gränssnittet (**en-US**, **fr** eller **de**), och etiketten **Lägg till i ordbok** sammanhangsmeny matchar det valet istället för operativsystemets språk. **Français** och **Deutsch** faller inte längre tillbaka till **engelska** när Chromiums tillgängliga lista utelämnar de exakta hunspell-nycklarna vi först letar efter; vi matchar **fr-*** / **de-***-taggar i listordning, prova sedan de korta **fr** / **de**-koderna vid behov. Efter ett språkbyte inaktiverar huvudsessionen stavningskontrollen, rensar den föregående ordbokslistan, ställer in det lösta språket och aktiverar stavningskontrollen igen. Titelraden **språk**-kontrollen kan visa en valfri **reload**-åtgärd efter att du valt ett annat gränssnittsspråk så att du själv kan uppdatera fönstret om stavningskontrollen i ett redan öppet fält fortfarande följer den tidigare ordboken (Chromium behåller ibland den gamla bindningen tills en navigering).
- Kontroll av **Översta fältets språk**: **Deutsch** och **Français** strängar för den valfria stavningskontrollen **reload** tips (**aria-etikett** och flerradsverktygstips) speglar **engelska** formuleringen och radbrytningarna.- **Flaggor för språkfältet** och **sociala** verktygsfältsikoner: **offentliga/** tillgångar (till exempel **countryFlags** SVGs och **images/socialContactButtons**) löses nu med samma **Vite** **BASE_URL**-regler som resten av renderaren, med hjälp av ett **tillhörigt paket** från basen**. **Electron**, så flaggor och ikoner laddas istället för att gå sönder på root-absoluta **/**-URL:er.
- **Om Fantasia Archive** dialogruta: **Tysk** kopia för titeln, versionsraden, **Stäng** och raden för sociala länkar (**Patreon**, **Ko-Fi**, **Webbplats**, **GitHub**, **Discord**, **Reddit**X** verktygstips, **Reddit**X**). **Franska** hade redan dialog- och knappsträngar; språkträdet registrerar nu också **`socialContactButtons`** under samma camelCase-nyckel som **engelska** så de etiketterna löser sig i **Français** istället för att falla tillbaka.
- **Språkmeny**: efter att ha valt ett nytt gränssnittsspråk använder **inställningarna sparade** toast det **nya** språket istället för det tidigare.
- **Språkmeny**: alternativetiketten **en-US** är **engelska, US** (var **engelska**) i varje gränssnittsspråk.- **Toppmenyer i appen** (**Projekt**, **Dokument**, **Verktyg**, **Hjälp och info**): etiketter följer nu den aktiva **vue-i18n**-lokalen när du byter språk (de löstes en gång vid start och förblev på det ursprungliga språket). **AppControlSingleMenu** läser också om **titel** och **objekt** när föräldern skickar uppdaterad menydata.
- **Fönsterkontroller**: den anpassade rubrikraden **maximera** / **återställ**-växlingen sparar fönstergränserna före **maximera** och tillämpar dem igen efter **unmaximize** så återställning från ett maximerat tillstånd återgår till den tidigare storleken på **Windows** med ett ramlöst **Electron**-fönster vid ** enbart fönstret för maximering.
- **Electron** **preload** brygga: **Window** krom, **DevTools**, den förpackade **versionen** strängen och selen **extraEnvVariables** ögonblicksbild använder **async** **IPC** via **`ipcMain.handle`** och **`ipcMain.handle`** och **`ipcMain.handle`** och **`ipc-synchronous. **renderaren** stannar inte vid dessa samtal.- **Appstart och komponenttester**: **`faRoutingEnv`** start laddar selens ögonblicksbild och **inväntar** startrouting så **`router.push`** avslutas innan skalet renderas. **`ComponentTesting`** löser den testade **SFC** från rutten reaktivt och seeds **COMPONENT_PROPS** från **`extraEnvVariables.getCachedSnapshot`** när start redan har löst **`getSnapshot`**; **`AppControlMenus`** läser **`TEST_ENV`** på samma sätt för den första färgen. **Preload** **`extraEnvVariables`** lägger till **`getCachedSnapshot`** efter den första framgångsrika **`getSnapshot`**-lösningen. **ComponentTestingLayout** laddar nu bevarade **användarinställningar** från main på mount i **Electron** och återför **vue-i18n** från **languageCode**, matchande **MainLayout** så att en fullständig fönsterladdning under isolerade komponentkörningar behåller det valda gränssnittsspråket.
- **Globala fönsterknappar**: tillgängliga namn lever endast på **`aria-label`**; **Quasar** verktygstips och attributet **`title`** togs bort från dessa titelradsknappar.- **Appinställningar** sökning utan matchningar: tomt tillstånd **ErrorCard** visar rubriken **Ingen sökmatch** och längre vägledning från **vue-i18n** som **detaljer** under maskoten (**en-US** och **fr**). **ErrorCard** visar valfria **detaljer** endast där, med **data-test-locator** krokar **errorCard-title** och **errorCard-detaljer**; **Fel inte hittat** behåller endast titel och flera rader **detaljer**.
- **Storybook** **`externalFileLoader`** låtsasregister **dialogs.markdownDocument** från **L_markdownDocument** så **DialogMarkdownDocument**-berättelser löser etiketten **Close** och relaterade strängar istället för rånycklar.
- **Storybook** **`externalFileLoader`** låtsasregister **dialogs.actionMonitor** så **DialogActionMonitor**-berättelser löser titlar, kolumnrubriker och knappetiketter istället för råa översättningsnycklar.
- **Storybook** visuell fångst anropar **page.waitForFunction** med **undefined** som parametern injected-argument före optionsobjektet så att Playwright tillämpar renderingstimeout istället för att vänta utan en inre deadline.- **Storybook** — bidragsgivare **.storybook-workspace** config inaktiverar checklistan för introduktion av sidofältet, stänger av **Vad är nytt**-meddelanden i **kärnalternativen** och **chefshuvud** döljer den gröna olästa pricken på inställningsväxeln för **garnberättelsebok:kör** och statiska byggnader.
- **Appinställningar** Playwright förväntningshjälpare importerar **dialogAppSettingsTreeOrdering** istället för **dialogAppSettingsTree** så **Node** laddar inte **app/i18n** markdown-råimporter när spec-moduldiagrammet löses.
- **FantasiaMascotImage** centrerar bild horisontellt när bitmappen är smalare än dess plats (**marginal: auto** på den inre **img**).- **Externa länkar**: **http** och **https** URL:er valideras med en riktig URL-analys och en **http** / **https** schematillståndslista innan **openExternal** körs i main eller förladdningshjälpen **checkIfExternal** körs i renderaren. Loopback och icke-routbara mål blockeras med hjälp av den analyserade värden (**localhost**, värdnamn som slutar på **.localhost**, **127.0.0.0/8**, **::1**, IPv4-mappad loopback, **0.0.0.0**), procentkodad **localhost** är fortfarande inte längre igenkänd som https://sd,** schemat **sd,** no longer **https**. Värdnamn som bara innehåller bokstäverna för **localhost** inuti en etikett (till exempel **notlocalhost.example**) förblir tillåtna när den analyserade värden inte är loopback.
- **Användarinställningar**: **Electron** huvudprocessen validerar varje spara patch med **Zod** innan den slås samman i **electron-store**, accepterar endast kända nycklar (**boolean** växlar och **språkkod** som **en-US** / **de** / **fr**) och avvisar okända fält. Lagret **S_FaUserSettings** Pinia visar det befintliga lagringsfelmeddelandet när **setSettings** misslyckas (inklusive validerings- eller bryggfel) istället för att bara logga ett obehandlat avslag.- Levererad version är nu **2.4.11** i **Om** och paketeringsmetadata.
- **Medverkande dokument**: **README**, **AGENTS.md**, **electron-preload**-regeln och **fantasia-electron-main** / **fantasia-electron-preload** färdigheter beskriver **renderar-sandlåda** (**BrowserWindow** **webbpreferenser T**), officiella **sandbox-länkar, ____0-kontextlänkar, __0. **main** **IPC** (harness **extraEnvVariables**, externa **http** / **https**-länkar), **startApp** registrerar **ipcMain**-hanterare före det första fönstret, en **async-first**-preferens för **anrop** / **handtag** framför synkron **IPC** när det finns en giltig betalningsstruktur** med en giltig betalningsstruktur** med IP **Zod** i main kontra **typeof** plus domänkontroller, och vilka hanterare som förblir skalära. Korta kommentarer om **elektron-förspänning** och **elektron-huvud** ekar samma skyddsräcken.- **Bidragsgivares arbetsflöde**: samlokaliserade **Vitest** filer, **Playwright** komponentspecifikationer och **Storybook** berättelser finns nu under **`_tests/`** kataloger över **`src`**, **`src-electron`**, **`helpers`** n`**s, **`helpers`**, **'s, extraherade skriptmoduler där de användes (det tidigare mappsegmentnamnet var **`tests`**). **Vitest** täckning utesluter, **Storybook** berättelseupptäcktsglober och bidragsgivares vägledning (**README**, **AGENTS.md**, markörregler och färdigheter) följer den nya vägen. Toppnivå **`e2e-tests`** och **`.storybook-workspace/visual-tests`** är oförändrade.
- **Appinställningar**: medan rubrikens sökfält har text, täcker den nedtonade scrim igen hela **dialogkortet**; Åtgärderna för titel, rullningsbar text, sökfält och sidfot förblir användbara där staplingsordningen håller dem ovanför överlägget.
- Implementeringen av **Appinställningar** är uppdelad i mindre **Vue** underkomponenter och samlokaliserade **skript**-moduler så att dialogrutan håller sig inom de tvingade linjegränserna utan att ändra den synliga layouten.- **Bidragsgivares underhåll**: delade **TypeScript** kontrakt för bryggor, dialogrutor, nyckelbindningsdata, användarinställningar och relaterade hjälpare är grupperade i repository-root **`typer/`** med kortfattade **JSDoc**, som ersätter många ensidiga typfiler och samlokaliserade **`*.types.ts,**rc/**` **`src-electron/`** och **`Storybook`** hånar. **Keybind-inställningar** och **Appinställningar** samlokaliserade **`scripts/`**-kod slås samman till färre moduler var och en för samma körningsbeteende och enklare navigering samtidigt som **ESLint**-linjebudgetar respekteras. **README**, **AGENTS.md**, markörregler och färdigheter beskriver att man föredrar **färre logiskt grupperade** **`scripts/`**- och **`src/scripts/`**-moduler tills radgränser tvingar fram en uppdelning och konsolidering av samlokaliserade **`scripts/_tests/`** när produktionsskript slås samman. Tomma äldre **`tests/`**-kataloger som blivit över från **`_tests/`** byte namn togs bort från trädet.- **Contributor-verktyg**: **ESLint** tillämpar gränser för antal rader på produktion **Vue**, **TypeScript** och **JavaScript** (tester, **Storybook**, konfigurationer, **i18n**, typmoduler och liknande sökvägar förblir undantagna). **README**, **AGENTS.md** och Cursor-reglerna beskriver kapslarna—**Vue** SFC **250** rader, **TypeScript** modul **200** rader, funktion **100** rader—och när de ska delas upp i **`scripts/`** eller underkomponenter. Returnerade objektliteraler i produktionskoden måste endast lista **tidigare bindningar** för egenskapsvärden (inga inline-funktioner eller ternär logik i literalen); se **`.cursor/rules/code-size-decomposition.mdc`**. Att flytta scoped **CSS** till en fristående fil och återimportera den förblir ett undantag som kräver explicit underhållsgodkännande.- **Bidragsgivares underhåll**: produktionsytor **`retur`** som använder objektliteraler (inklusive **action manager** könyttolastformning, tangentbindningsackordhjälpare, **AppControlMenus** objektbyggare, **DialogActionMonitor** / **Keybind-inställningar IP** / **Appinställningar** ** tabellhjälpmedel, **snapshotbyggare, **C0____shot **Playwright** **`recordVideo`**-alternativ) binder nu **`const`**-värden först så returnerade objektegenskaper förblir enkla identifierare eller bokstaver. **Action Manager**-implementeringar finns i **`src/scripts/actionManager/faActionDefinitionHandlers.ts`**, där **`faActionDefinitions.ts`** endast håller **registertabellen**; **AGENTS.md** och **fa-action-manager** Markörregeln/färdigheten beskriver uppdelningen.- **GitHub Actions** manual **Build App**-arbetsflöde (**.github/workflows/build.yml**): **Windows**-jobbet körs **yarn testbatch:ensure:nochange** ände till slut (samma kvalitetsport som märkt **PR** full-suite körningar, plus en produktion ****TK4__0 återanvänds för **Quasar** **Playwright** och **Storybook** kontroller), laddar sedan upp installationsprogrammet **Windows** utan en andra paketversion. **Linux** jobb startar först efter att porten har lyckats och körs som en matris så varje ben laddar upp sin egen artefaktuppsättning: **AppImage**, **Debian**-paketet och **RPM**. **macOS** kör fortfarande en produktionsversion och laddar upp **.dmg**. **Storybook** visuella baslinjer förblir **win32**-justerade, så hela porten förblir på **windows-senaste**.
- **Medverkande dokument**: **README**, **AGENTS.md**, **vue-quasar**-regeln och **fantasia-quasar-vue** skicklighetsdokument en **Vue** enfilslayout för varje **.vue**-fil: **mall** först, sedan **manusarbete**, sedan **2-stil**s**. **Appinställningar** hjälpkomponenter ordnades om i källan för att matcha; synligt beteende är oförändrat.- **Bidragsgivareunderhåll**: **Vue**-hjälparna för flera funktioner för flytande fönsterkropp **Teleport**, handtag för storleksändring av flytande fönster och den globala **`#faUserCss`**-injektoranvändningen **`_<PascalCase>`**-mappen och **`.vue`F`**-mappen och **`.vue`F`**-namnen (**loatingWele`F**) **`_FaFloatingWindowFrameResizeHandles`**, **`_FaUserCssInjector`**) så att de läser som infrastruktur bredvid **`Window*`** och **`Global*`** ytor; **AGENTS.md** begränsar prefixet till dessa tre. Tidigare anteckningar i den här loggen som namnger dessa bitar använder nu samma stavning. Appens beteende är oförändrat.- **Bidragsgivares underhåll**: renderare **TypeScript** hjälpare är omgrupperade under **`src/scripts/appInternals/`** (lokal appliceringshjälp, språkinmatning, publika tillgångssökvägar, **Storybook** canvas-detektering) och **`src/scripts/appGlobalManagement UI/`component, component wiackdown and-ring, maskot routing, dev-tools växla, start routing, trivia notifications) med tydligare exporterade namn än tidigare ingångspunkter i **leasing**stil. **Globalt tangentbord** förbearbetning och sändning live i **`src/scripts/keybinds/faKeybinds*`** moduler som **`MainLayout`** komponerar genom **`createFaKeybindKeydownHandler`**. **preload** hjälpmodulen för externa länkar är nu **`faExternalLinksManagementAPI`** (beteende oförändrat; namnet överensstämmer med andra hanterings-API:er).
- **Storybook** arbetsyta **Vite** lägger till **`server.fs.allow`** för förvarsroten så att **Keybind-inställningar** **`.vue`**-filer kan lösa syskon-**`typer/`**-kontraktsimporter under statiska builds och visuella infångningar.- **Storybook** statisk **visuell regression** (**`yarn test:storybook:visual`**): **Vite** löser **Pinia** och **Vue** från förvarsroten så att den kapslade arbetsytan inte laddar en andra **Pinia** kopia som förhindrade att dialogrutor monterades i **berättelserna. **Playwright**-selen tjänar den statiska byggnaden på en dedikerad port och återanvänder inte en befintlig server där, så en lokal **`yarn storybook:run`**-session på standardporten kan inte tillfredsställa infångningar av misstag. Misslyckade inspelningar bifogar fortfarande en helsidesskärmdump och en kort **JSON**-sond av **Storybook**-roten när navigering, renderingskontroller, typsnitt, överlagringar eller skärmdumpsteget misslyckas.
- **Underhåll av bidragsgivare**: **Vitest** specifikationer för **AppControlMenus**, **AppControlSingleMenu**, **GlobalLanguageSelector** (inklusive sökvägen för stavningskontrollens uppdateringsfördröjning), **GlobalWindowButtons**, **ErrorCard**, **Felkortsinställningar** och expanderade skärminställningar, inställningar för appar, övningsskärmar, **s top-bar selegrenar, språkval, fönsterkromtimer och **Quasar** menyklickvägar i jsdom. Specifikationer för multi-stub-dialoger använder samma **ESLint**-undantag på filnivå som redan tillämpats på stack-guard-seletest.- **Bidragsgivareunderhåll**: mer **Vitest** jsdom-täckning för **DialogActionMonitor**-dialogrutan visar uppdatering, **DialogImportExportAppConfig** öppnas och importerar mellanlagring, **DialogKeybindSettings** när nyckelbindningsdefinitionslistan är tom, **_FaFloatingResoverHFrame** och resme **WindowAppStyling** editor hjälpverktygstips innehåll plus lastnings- och laddningsfelöverlagringar och rampekarledningar, **MainLayout** appstyling uppdateras när **Electron** bryggan avslöjar det eller inte, **AppControlMenus** komponenttestning av markeringsmenyutlösare, **SowControl**-undermeny-klickning, **SowControl**-hanterare **GlobalLanguageSelector** när användarinställningsbryggan saknas och **_FaUserCssInjector** hoppar över dubbletter av stiltextskrivningar.

## 2.4.10 - Appinställningar, ErrorCard, tematokens och bidragsgivares verktyg

### Nya funktioner
- **Verktyg → Appinställningar** öppnar dialogrutan **Appinställningar** (beständig tills **Stäng**; **Spara** är reserverad för senare kabeldragning).
- **Appinställningar** listar varje kvarstående boolean som **kategori → underkategori → växla** rader med **en-US** / **fr** kopia (titel, beskrivning, valfri röd **note**). **Kategori** och **underkategori** flikar följer nyckelordning förutom **Tillgänglighet** (alltid näst sist) och **Utvecklarinställningar** (alltid sist); **växlar** inom en underkategori sortera efter översatt **titel** (band använder inställningsnyckeln). **Visa dokument-ID:n** finns under **Utvecklarinställningar → Dokumentets brödtext**. En flik **Tillgänglighet** på översta nivån grupperar bredare rullningslister, trädräkningsavdelare, genomslagsdöljning och textskugga; tema och relaterade föremål förblir under **Visual och app-omfattande funktionalitet** utan att blocket kapslas där.
- Lagt till beständiga **användarinställningar** i **Electron** huvudprocessen (**electron-store** under app **userData**), med början med en **tema**-inställning (**ljus** eller **mörk**, standard**ljus**), exponerad för renderaren som **`window.faContentBridgeAPIs.**Settings**Settings.- Lade till **`S_FaUserSettings`** Pinia-butik för hantering av användarinställningarstillstånd i renderaren: laddar de fullständiga inställningarna en gång vid appstart via IPC-bryggan, accepterar patch-baserade uppdateringar och visar en **Quasar** avisering om framgång eller misslyckande efter varje försök att spara.
- **Appinställningar** använder vertikala **kategori**-flikar (**`q-tabs`** / **`q-tab-panels`**) och en **rullningsbar** huvudkolumn (**`q-scrollarea`**): **titeln**, tablisten och **Stäng** / **Spara** förblir fixerade medan innehållet rullar. Underkategoriväxlingar använder ett responsivt rutnät (**en** / **två** / **tre** kolumner efter brytpunkt), placera **växlingsknappen** under titeln och visa beskrivningar från en hjälpikon **verktygstips**.
- **Appinställningar** laddar om beständiga växlingsvärden från **`S_FaUserSettings`** när dialogrutan öppnas med en skyddad **Pinia** löser så att miljöer utan en aktiv användarinställningar lagrar hoppa över hydrering istället för att kasta. Hjälpikonens justering bredvid titlar med flera rader knuffas för tydligare vertikal justering med den första raden.- **ErrorCard** (**`src/components/elements/ErrorCard/`**): återanvändbar **q-card** layout för feltillstånd (**titel**, valfri multi-line **detaljer**, **FantasiaMascotImage** variant, konfigurerbar **max-width**); samlokaliserad **Storybook** förhandsvisning och **Vitest**. Rutten **Fel hittades inte** använder den med den befintliga kopian **`errorNotFound`** istället för ett inline-kort och sidlokalt **SCSS**.

### Bugfixar och optimeringar
- **Medverkande dokument**: **README**, **AGENTS.md**, **fantasia-testing**, **storybook-berättelser** och **testing-terminal-isolation** beskriver **Storybook** **VRT** **toHaveScreenshot** **maxDiffPixel**skillnad ** som en hel-__-pixel-budget för K-__-pixlar**. **Windows-senaste** kan avvika något från lokala **Windows**, och när man ska uppdatera baslinjerna kontra justera locket.
- **Storybook** visuell regression: höjt **Playwright** **toHaveScreenshot** **maxDiffPixels** för statiska **Storybook** fångar så **GitHub Actions** **Windows-senaste** klarar **garntest:storybook:subpixel från committed **Windows** körs).
- **GitHub Actions** **OSV-Scanner** arbetsflöde (**.github/workflows/osv-scanner.yml**) listar nu **main** bredvid **master** på **push**, **pull_request** och **merge_group** så samma skanningar körs när standardgrenen använder något av namnen- **Storybook** visuell regression på **Windows**: uppdaterade begångna **Chromium** ögonblicksbilder under **.storybook-workspace/visual-tests/** för **ErrorCard** (felsidan och appinställningarna är tomma), **FantasiaMascotImage** läsning, **Ingen **Integrering av sidanF. default, och **IndexPage** i layout — anpassar baslinjer med nuvarande förhandsvisningar efter inbyggda **img**-maskot- och layouttesthakar.
- **Electron** säkerhet: huvud-**BrowserWindow** kör nu **Chromium**-renderaren med **OS-sandlådan** på (**`sandbox: true`**, **`nodeIntegration: false`**). Harness-värden exponerade som **`window.faContentBridgeAPIs.extraEnvVariables`** är inbyggda i **huvud**-processen och levereras över sync **IPC**; öppning av externa **http**- eller **https**-länkar i standardwebbläsaren hanteras i **main** (**`shell.openExternal`**) via asynkron **IPC** så förladdningen håller sig inom sandlådesäkra **API**-gränser. **Playwright** lanseringsvägar använder **`faPlaywrightElectronLaunchConstants`** i **`helpers/playwrightHelpers_universal/`** istället för att importera preload-bara moduler från nodtestlöparen.- **Electron** fönster-krom sync IPC: **huvudprocess** hanterare för **minimera**, **maximera**, **återställningsväxla** och **stäng** ställer nu in **`event.returnValue`** på varje väg. Utan den slutförs **`ipcRenderer.sendSync`** aldrig och **renderaren** förblir blockerad, vilket frös hela användargränssnittet efter migreringen **@electron/remote**.
- Tog bort beroendet **@electron/remote**: ramlöst **fönster** krom (**minimera**, **maximera**, **återställ**, **stäng**, **maximerat** tillstånd) och den paketerade **appversionen**-strängen som visas i **Om** använd nu synkron **huvudprocess, Ipc-register från förladdning (**Control-in) **registerFaAppDetailsIpc** i **mainScripts/ipcManagement/**), löser det ägande **BrowserWindow** från IPC-avsändaren så att beteendet förblir korrekt när inbyggda menyer stjäl OS-fokus.
- Interaktioner med **Fönster** krom och **maximera** / **återställ** kan kännas något smidigare vid daglig användning efter denna förändring: **renderaren** sitter inte längre bakom **@electron/remote** för dessa åtgärder, och korrekt **`event.returnValue`**-hantering hindrar **sendSync** från att stoppa UI-tråden. Exakt uppfattning varierar beroende på maskin och arbetsbelastning.- Bumpade **axios** till **1.15.0** till adressen **GHSA-3p68-rc4w-qgx5** (**CVE-2025-62718**): **NO_PROXY**-matchning normaliserar nu värdnamn (till exempel **localhost** med en efterföljande punkt v6-målslingor i parentes i parentes **NO_PROXY** skickas inte via en HTTP-proxy.
- **Appinställningar** kvarstår **booleska** nycklar i **elektronbutik** använder korrigerade interna identifierare (**aggressivt** relationsfilter, **kollaps** trädalternativ, **efter val** snabbsökningsstängning).
- **Bidragsgivares arbetsflöde**: **README**, **AGENTER** och markörregler och färdigheter beskriver nu när underhållare kan hoppa över **`yarn testbatch:verify`** omedelbart före en commit som endast ändras per lokal ändringslogg i appen Markdown under **`i18n/*/documents/changeLog.md`**, förutsatt att det senaste trädet inte har gått igenom hela trädet och att det inte har gått igenom den senaste grenen. andra pågående ändringar; när du är osäker, kör porten ändå.- **Contributor docs**: root **CONTRIBUTING.md** täcker gaffel **PR** etiketter, lokala **Storybook** **VRT** ögonblicksbilduppdateringar när **CI** inte kan skicka baslinjer till gafflar, skrivskyddade **`GITHUB_TOKEN`** begränsningar för offentliga gaffel **`pull_k2**s** och K__1 arbetsflödesinställningar** och K__1 för Action godkännanden; **README** lägger till ett avsnitt **Bidragande**; **pull request** malllänkarna **CONTRIBUTING.md**.
- **Repository** root **.gitignore** ignorerar nu **.playwright-mcp** (arbetsyta-lokala mappar från **Playwright MCP**-servern som används med **Cursor** och liknande klienter; separat från **Playwright** testutdata under **testresultat**).
- **Playwright** seriella sviter och **WebM** bilagor: **`closeFaElectronAppWithRecordedVideoAttachments`** accepterar en valfri andra **`TestInfo`** så skärminspelningar bifogas där **HTML**-rapporten listar dem för multi-test **`test.describe.serial`** grupper; komponent- och **E2E**-specifikationer använder en **Electron** lansering per seriell grupp med **`resetFaPlaywrightIsolatedUserData`** endast i **`test.beforeAll`** (**README** och **dramatikertester** beskriver mönstret). Vitest täcker den uppdaterade **`playwrightElectronRecordVideo`**-hjälpen.- **Playwright** komponenttäckning för **ErrorCard** utövar den levande selen (titel, detaljer, **läsning** maskot, kortets maxbredd via **data-test-error-card-width**). Bidragsgivares vägledning (**AGENTER**, **dramatikertester**, **fantasia-testning**) föredrar **data-test-layout-width**, **data-test-layout-height** och domäntak för storlekskontrakt när smala fönster skulle krympa **boundingBox**-resultat; använd **boundingBox** för renderade pixlar endast när selen garanterar tillräckligt med utrymme.- **Storybook** **Components/foundation**: design-time kataloger under **`src/components/foundation/`** — **`FoundationColorPalette`** (**QUASAR COLOURS - ALLMÄNT** från **`quasar.variables.scss`** plus standard **`**`K2__g) material **`**`bs**) **`FoundationTextList`** (**Quasar** typografi: typskala, teckensnittsvikter, texthjälpmedel per **quasar.dev/style/typography**). Endast canvas (**Dokument** inaktiverat), **`skip-visual`** så **VRT** hoppar över långa sidor; berättelse globals pin **Återställ viewport** (iframe i full bredd). **`FoundationColorPalette`** root **SCSS** förblir transparent så att **Bakgrunder** verktygsfältet (**papper** / **mörk app**) målar duken; swatch-metadataremsor använder den mörka katalogbakgrunden från **quasar.variables.scss**.
- **Storybook** viewport-verktygsfältet lägger till **Reset viewport** (100 % iframe) före telefon-till-**4K**-förinställningarna i **`viewportBreakpoints.ts`**; **`preview`** **`initialGlobals`** börjar nu på den responsiva förinställningen istället för en fast **1920px** skrivbordsram.- **Storybook** visuell regression på **Windows**: uppdaterade den engagerade **Chromium**-skärmdumpen för **Components/dialogs/DialogAppSettings** standard så att den matchar den aktuella **Reset viewport** standard och **Storybook** ram.
- **Storybook** **Appinställningar**: **`DialogAppSettings`** accepterar valfria **`directSettingsSnapshot`** (standardspegel **`FA_USER_SETTINGS_DEFAULTS`**) så **Docs** och **Canvas** visar hela rutnätet utan __1** och växlar K. en samlokaliserad dekoratör slår samman **en-US** **`appOptions.*.title`** strängar med iscensättande **TODO** streckprefix avskalade. **Vite** alias mock för **`app/i18n/externalFileLoader`** implementerar **`global.mergeLocaleMessage`** (djup sammanslagning i scenariomeddelanden) och **`global.te`**, som matchar vad **`buildAppSettingsRenderTree`** förväntar sig av produktionen ** och dekoratören** **Storybook** **`faContentBridgeAPIs`** stubben exponerar **`faUserSettings`** med samma standardkarta så **Pinia** hydrering fungerar fortfarande när en berättelse utelämnar ögonblicksbilden.- **Storybook** komponent **`meta.title`** sökvägar kapslar under **`Components`** efter repo-bucket (**dialoger**, **element**, **globals**, **other**) plus funktionsmappens namn så att sidofältet matchar **`src/components/`**. **`yarn test:components:list`**-väljaren visar **`dialogs/DialogFoo`**-stiletiketter och **`yarn test:components:single --component=`** tar samma relativa sökväg under **`src/components/`**. Uppdaterade engagerade **Storybook** visuella baslinjer och **`EXCLUDED_STORY_IDS`**-poster för den nya berättelsens **`id`**-form; höjde **`toHaveScreenshot`** **`maxDiffPixels`** till **220** i den visuella sviten så jämför körningar tolererar lite mer typsnitt eller subpixelvariationer på täta markdown-dialoger utan att maskera stor UI-drift.- **SCSS** temakatalog (**`quasar.variables.scss`**): Quasar palett, globaler, **`q-*` justeringstoken**, sedan sektioner per funktion; **`htmlAdjustments`**, **`scrollbar`**, **`quasarComponentsAdjustments`** och matchande **Vue** **`<style lang="scss">`** block använder variabler istället för spridda bokstaver. Tema **`$`** identifierare använder bara bindestreck-separerade segment (se **`project-scss.mdc`**). Döpte om **qMarkdown** kodbakgrundstoken till **`backgroundColor`** (skrivs fel).
- **FantasiaMascotImage** återger maskotkonst med en vanlig **HTML** **img** (**object-fit: contain**, **display: block**) istället för **Quasar** **q-img**, undviker **q-img** omslag och laddningslayouthopp som störde **höjden******, ** **`max-width: 100%`**, och **data-test** hakar på bilden.
- In-app **changelog** och bidragsgivare: omformulerad en **Playwright** punkt och utökad **vue-i18n** vägledning så att prosa inte tolkas som ogiltig platshållarsyntax (curly-brace-formade API-snuttar och liknande).- **Appinställningar** sökning: **vue-i18n** platshållare (**en-US** / **fr**). Med en beskuren fråga som inte är tom, förblir de vertikala **kategoriflikarna** synliga men dämpas bakom en kort **Animate.css**-tonning och kan inte klickas på; den högra kolumnen staplar **matchande**-inställningar i en rullning (skiftlägesokänslig delsträng på **titel**, **taggar** och verktygstips **beskrivning**; icke-matchande rader och tomma sektioner utelämnas; filtrering i **`scripts/dialogAppSettingsSearch.ts`** med Vitests separata sektionsrader under uppsättningen, med underkategorisektionens linjer under uppsättningen. När inget stämmer överens använder ett centrerat kort samma krämfärgade **Error Not Found**-stil kantlinje och fyll som **`ErrorNotFound`**, med **FantasiaMascotImage** i **läsläge och en dedikerad **searchNoResultsMessage**-sträng. Medan sökningen är aktiv stannar kortet utan resultat och kolumnen för matchande resultat i dokumentet med synlighet inkopplad (**v-show**) så att **läsmaskoten** kan laddas innan det tomma tillståndet visas. Sökfältet behåller en fast bredd när **rensbar** visar sin ikon, förblir användbar ovanför det dunkla lagret och rensningen kastar inte längre när kontrollen ställer in modellen på tom. Subtila blekningar mjukar upp toppen and längst ned i de staplade resultaten och längs sökremsan medan sökningen är aktiv; rullningsområden använder en stabil rullningslist, så att visa eller dölja en vertikal rullningslist inte förskjuter layouten i sidled. **SCSS** **BEM** namn för sökomslaget, klibbiga titlar och sidfotsåtgärder matchar mallen så regler gäller.
- **`rgbToHex`** och **`hexToRgb`** i **`src/scripts/_utilities/colorFormatConvertors.ts`**: **`rgbToHex`** mappar nu endast de tre första numeriska kanalerna så att strängar i **rgba()**-stil (till exempel från **Ställa till appen inte en alpha) hex-resultat, kräver minst tre matchningar innan konvertering, och analyserar kanalheltal med en explicit radix; **`hexToRgb`** trimmar inmatningen och strippar ett ledande **`#`** före analys. Colocated **Vitest** täckning utövar de uppdaterade banorna.
- **`toggleDevTools`** (**`src/scripts/appGlobalManagementUI/toggleDevTools.ts`**) valfria-kedjor genom metoden **`toggleDevTools`** på **`window.faContentBridgeAPIs.faDevToolsControlbridge`** saknar till exempel inte en del av testet (exempelvis saknas testbryggan**) eller **Storybook**).- Utökade **Vitest**-sviter över **Electron** (**contentBridgeAPIs**, **preload**, **mainScripts** inklusive **userSettings** rensning för nullish eller partiell bevarade ögonblicksbilder), **renderer** (**boot**, **butiker**, **dialoger**globals, **scenarios**globals, **s **sidor**), reporoot **i18n**-tester och **hjälpare/dramatikerHjälpare** så att skiktade täckningströsklar förblir i linje med dessa korrigeringar.
- **SocialContactButtons** **webbsida**-stil: **quasar.variables.scss** ställer in webbsidans chipbakgrund till **$mörk** istället för **$qCard-bakgrund** så att den läses tydligt på det mörka verktygsfältet.
- **Appinställningar**: underkategori-rad och staplad sökning på toppnivåavgränsare använder **`showNonLastSeparator`** och **`showNonLastTopCategorySeparator`** från **`scripts/dialogAppSettingsSearch.ts`** med Vitest-täckning; layoutbeteendet matchar den tidigare inline-logiken.- Flyttade hela **vue-i18n** språkträdet från **`src/i18n/`** till reporot **`i18n/`** (vid sidan av **`src/`**); importer använder **`app/i18n`** och **`app/i18n/...`**. **Vitest** **`unit-i18n`** (**`yarn test:coverage:i18n`**) upprätthåller **100 %** täckning på omfångsrika **`i18n/`**-poster **`.ts`**-filer; **README**, **AGENTER**, markörregler/färdigheter, **Quasar** (**`@intlify/unplugin-vue-i18n`** inkluderar sökväg) och **Storybook** alias uppdaterades.
- **vue-i18n** lokala meddelandefiler använder ett **`L_`** filnamnsprefix (till exempel **`dialogs/L_appSettings.ts`**) så att **`T_`** förblir reserverad för **TypeScript** typalias; **Storybook** hånar, **Electron** importer, tester och bidragsgivare använder de nya sökvägarna.- Lokala verifieringsstandarder är tystare: **Vitest** täckning använder terminalrapportören **agent**; **Playwright** komponent och **E2E** använder reportern **list**; **Storybook** statisk konstruktion och rökanvändning **tyst** CLI-flaggor och lugnare **Vite** loggning av produktionsförhandsvisning; **Storybook** visuell **Playwright** hoppar över per berättelse **konsol** framsteg om inte **FA_STORYBOOK_VISUAL_VERBOSE** är inställd; **`yarn testbatch:ensure:nochange`** och **`yarn testbatch:ensure:change`** anropar **`yarn quasar:build:electron:summarized`** så hela **Quasar** / **electron-builder**-loggen registreras under **när ett testresultat misslyckats.** **README**, **AGENTER** och Cursor regler och färdigheter beskriver skripten och opt-in verbose flaggor.- **Playwright** körs (**`TEST_ENV`** **`components`** / **`e2e`**) använd **`%APPDATA%/<paketnamn>/playwright-user-data`** (t.ex. **`fantasia-archive\playwright-user-data`**sia-**), inte **`fantasia-archive\playwright-user-data`**sia-**) (**`electron-store`**) förbli separat från **`quasar dev`** och paketerade profiler. Varje komponent och **E2E** spec anropar **`resetFaPlaywrightIsolatedUserData()`** i **`test.beforeEach`** så att mappen tas bort före varje **`electron.launch`** och tester startar från standardinställningarna. Namnet på den isolerade profilmappen delas via **`src-electron/mainScripts/appIdentity/playwrightIsolatedUserDataDirName.ts`** (ingen **`electron`**-import) så nodsidans återställning och Vitest förblir säkra under testinsamlingen.- Fast paketerad **Electron** **preload** sökväg i **`mainScripts/windowManagement/mainWindowCreation`**: huvudprocesspaketet löser **`import.meta.url`** till den delade huvudkatalogen (inte källmappen **`windowManagement/`**). Att lösa **preload** och **ikon** från den katalogen laddar igen **`elektronförladdning`** korrekt så **`window.faContentBridgeAPIs`** (inklusive **`extraEnvVariables`** för **`TEST_ENV`** / **`COMPONENT_NAME`**) är tillgängligt; komponent **Playwright** kör vägen till **`/componentTesting/...`** istället för att falla tillbaka till standardsidan för **Index**.
- Dela upp **`src-electron/mainScripts/`** i funktionsmappar - **`appIdentity`**, **`chromiumFixes`**, **`ipcManagement`** (**`register*Ipc`**-hanterare), **`nativeShell`**, **`userSettings`**, **`userSettings`**, **`user-window ** och **`us-registrering, **` inställningar**, skapande av fönster och **Chromium**-intilliggande korrigeringar har stabila importvägar; **Vitest** och **Playwright** importer, **README**, **AGENTER** och markörregler/färdigheter följer den nya layouten.- Uppföljningsbidragsdokument: **README** **Arkitektur**-anteckningar paketerade **`elektronförladdning`** upplösning under **`windowManagement/`**; **AGENTS** och **`electron-preload`** pekar **`faUserSettings`** **`electron-store`** till **`mainScripts/userSettings/`**; **`vitest-tests`** listar varje **`mainScripts/<area>/_tests/`** hink; **`fantasia-electron-main`** varnar för en extra **`..`** när man löser förladdning från **`import.meta.url`** i paketerade versioner.
- Tvärsnittsmoduler för **Playwright** (**`playwrightElectronRecordVideo`**, **`playwrightUserDataReset`** och deras Vitest-sviter) live under **`helpers/playwrightHelpers/`**; specifikationer import **`app/helpers/playwrightHelpers/...`**. **Vitest** **`unit-helpers`** kör **`helpers/**/*.vitest.test.ts`** via **`vitest/vitest.helpers.config.mts`**; delade Vitest-arbetsutrymmeskonfigurationer live under reporoot **`vitest/`** så den strikta **`helpers/**/*.ts`** täckningsporten gäller endast för riktiga hjälparpaket. Bidragsdokument (**README**, **AGENTER**, Markörregler/-färdigheter) beskriver **`helpers/`** för framtida selepaket så att arkivroten stannar för config, **`README`**, låsfiler och **`scripts/`**.- **Vitest** täckningsnivåer är dokumenterade och kopplade i **`vitest/`**: **100%** v8 trösklar på **src-electron** och **helpers**-paket; **100 %** på alla fyra mätvärdena för scoped reporot **`i18n/`** post **.ts** filer i **unit-i18n** (**`specialCharactersFixer`**, register **`index.ts`**, **`externalFileLoader.en-locale`**,** och`per-locale`**,** **`fr`**, **`de`** indexmoduler — se **`vitest/vitest.i18n.config.mts`**); **100 %** på alla fyra mätvärdena för **src**-renderare **.ts** i **unit-src-renderer** (**src/boot**, **src/scripts**, **src/stores** endast — **`specialCharactersFixer`** flyttade ut med **`**i18); **100 %** på alla fyra mätvärdena för sammanslagna samlokaliserade **.ts**-filer under **src/components**, **src/layouts** och **src/pages** i **unit-components** (varje område behåller sin egen inkluderar globs; **Storybook-endast exkluderar/** **foundation). **Vue** SFC:er har ingen bristande tröskel; rapporter använder **60%** vattenstämplar så svaga summor föranleder granskning. **Appinställningar** render-tree **.ts** bygger valfria **note**blad med en explicit tilldelning (inte en villkorlig objektspridning); tester täcker tomma ögonblicksbilder och återanvändning när två inställningar delar en kattegori och underkategori; Vitest importerar sorteringshjälpare från **dialogAppSettingsTree** direkt. **README**, **AGENTER**, **.github/workflows/verify.yml** kommentarer, **eslint-typescript**, **vitest-test**, **testing-terminal-isolation**, **git-conventional-commits**, **playwright-tests**, **vue-a,quasar,** **fantasia-dev-setup**, **fantasia-changelog-en-us**, **fantasia-electron-preload**, **fantasia-quasar-vue**, **git-conventional-commits**) beskriver samma policy.
- **Root** **vitest.config.mts** registrerar **fem** projekt med **extends** till **vitest/** så att arbetsytan **root** förblir arkivet och globs som **src-electron/** löser sig korrekt. **package.json** **test:coverage:electron**, **test:coverage:helpers**, **test:coverage:i18n** och **test:coverage:src** laddar dessa filer; **tsconfig.json** exkluderar **vitest/vitest.components.config.mts** från appens typkontroll. Tidigare **vitest.*.config.mts** / **vitest.setup.ts**-fragment på rotnivå flyttade till **vitest/**.- **Vitest** **`unit-components`** inkluderar nu **`src/layouts/**/*.vitest.test.ts`** och **`src/pages/**/*.vitest.test.ts`** vid sidan av **`src/components/**`**, med en **`vitest.test.ts** .>-baslinje för`test.**s.** . varje **`MainLayout`**, **`ComponentTestingLayout`**, **`IndexPage`**, **`ErrorNotFound`** och **`ComponentTesting`** SFC. **Appinställningar**-trädet Vitest täcker **`toSortedRecord`**, **`buildAppSettingsRenderTree`**, kategoriordning, **`showDocumentID`**-mappning och hantering av lokala inställningar. **Electron** main Vitest lägger till täckning för **`suppressChromiumDevtoolsAutofillStderrNoise`**, **`registerFaDevToolsIpc`**, **`PLAYWRIGHT_ISOLATED_USER_DATA_DIR_NAME`**, **`fixAppName`** är tom, **`fixAppName`** när produktnamnet är tom och nu visas **spellChecker** beteende och **tweaks**; **`mainWindowCreation`** avslöjar **`assignAppWindowRefForTesting`** endast för enhetstester.- **Renderer** och sele **Vitest** expansioner: **boot** (**qmarkdown**, bredare **externalLinkManagement** fall), **butiker** indexmodul, **scripts** verktygstester (**colorFormatConvertors**, **mdListArrayConverter**), **AppControlMenus**; **Playwright** hjälppaket fick djupare **`playwrightElectronRecordVideo`** och **`playwrightUserDataReset`** enhetstester. **eslint.config.mjs** inkluderar **vitest/** i lint-omfattningen; **yarn.lock** återspeglar beroendeuppdateringar från denna arbetscykel.
- **Playwright** Electron **WebM** bilagor kan visa en **syntetisk röd markörprick** som följer skriptade pekare (valfritt **`FA_PLAYWRIGHT_CURSOR_MARKER=0`** för att inaktivera); **Appinställningar** komponentspecifikationer igen **Quasar** live **verktygstips** sväva med **`expect().toPass`**, **`locator.hover`** med alternativet **`force`** aktiverat och en något längre flikpanelsfördröjning för att minska flagning under **Electron**.- **Playwright** **Appinställningar** komponentspecifikation lägger till serietest för rubriken **sök**-fältet efter fliken **en-US**: en matchning vardera via **titel**, **taggar** och inställning av **beskrivnings**-understrängar, sedan resultatlösa **ErrorCard** med **reading no-result,Card på **result,C och ett **650** pixelkorts maxbredd via **data-test-error-card-width**.
- **Appinställningar** hjälpverktygstips för den uttalade räkneavdelaren visar nu den vertikala stapeln i meningen: **vue-i18n** behandlar en blank **pipe** som meddelandeformatsyntax, så en-US-strängen använder det dokumenterade snedstrecket escape så att tecknet renderas bokstavligt.
- **Quasar** **rippel** är på som standard igen; **fönster** kontroller (**GlobalWindowButtons**) och **AppControlSingleMenu** verktygsfältet **`q-btn`** inaktiverar **rippel** så att krom och menyraden förblir visuellt stabila.
- **DialogMarkdownDocument** ökar innehållet **max-höjd** offset så höga dialogrutor lämnar fortfarande utrymme för sidhuvud och sidfotskromet när du rullar.- Omorganiserad renderare **Vue** komponenter under **`src/components/`** hinkar — **`dialogs/`** (modala **`Dialog*`** SFCs), **`globals/`** (**`GlobalWindowButtons`**, **`AppControlMenus`**), **`AppControlMenus`**,` **`elements/`** (**`FantasiaMascotImage`**, **`SocialContactSingleButton`**) och **`other/`** (**`SocialContactButtons`**); speglade språkmoduler under **`i18n/*/components/<bucket>/...`**, uppdaterade importer och **Storybook** hån, fixade **`SocialContactSingleButton`** berättelseimporter efter flytten, uppdaterad **Storybook** VRT-baslinje för **App-inställningar **tor**, och reviderade ADME-filer (reviderade appinställningar** och ADME) Markörregler/färdigheter).
- Stoppade valfria **appinställningar** **note**-strängar från att utlösa **vue-i18n** varningar för saknad nyckel när ingen **note**-nyckel existerar för en inställning (lös anteckningar endast efter en nyckel-existenskontroll).
- Lade till ett startrensningspass för de kvarstående **användarinställningarna** som lagrar så att äldre eller okända nycklar som inte längre finns i **`src-electron/mainScripts/userSettings/faUserSettingsDefaults.ts`** tas bort och den sanerade konfigurationen sparas automatiskt.- Extraherade alla hårdkodade **en-US** språksträngar från **`i18n/en-US/index.ts`** till dedikerade språkmoduler per funktion per komponent, dialogruta, sida och global funktion; normaliserade alla i18n-nycklar på toppnivå och undernivå till **camelCase** med en liten första bokstav i appen.
- Justerade **Storybook**s **`externalFileLoader`** mock (**`.storybook-workspace/.storybook/mocks/externalFileLoader.ts`**) med det **camelCase** meddelandeträdet (inklusive **`globalFunctionality`**) och kopiera komponenten via appen lokalt och nedåt, så att komponenten använder samma lokala och com-märke. layoutförhandsvisningar – och lokalt **`garntest:storybook:visual`** – återger riktiga strängar istället för råa översättningsnyckelvägar.
- Centraliserad **huvud ↔ förladdning** **IPC** kanalnamn i **`src-electron/electron-ipc-bridge.ts`** (inklusive **DevTools** synkroniseringskanaler) så att **Electron** hanterare och **preload** hjälpare inte kan driva till felaktiga strängar; tog bort den äldre **DevTools**-endast kanalmodulen.- Dokumenterade **`electron-ipc-bridge.ts`**, **`mainScripts/ipcManagement/register*Ipc.ts`** och **IPC**-registrering under **`startApp()`** (**`mainScripts/appManagement.ts`**) i **README**, **AGENTS**``** och /lectron-sor-preload` **`fantasia-electron-preload`** / **`fantasia-electron-main`** / **`fantasia-sqlite-main`** vägledning så att bidragsgivare och agenter följer ett heltäckande mönster.
- Lade till **`src/vue-i18n-shim.d.ts`** så att **vue-i18n** injicerade globaler (**`$t`**, **`$d`**, **`$n`** och relaterade **`$*`**-hjälpare) deklareras på **`@vue/Kuntime-core **Typing **__` **`tsc`** och rensar falska missing-**`$t`** diagnostik i redigerare som löser **`ComponentCustomProperties`** genom runtime-core-sökvägen.
- Ersatte huvudprocessens **`sqlite3`**-beroende med **`bättre-sqlite3`** (synkrona **SQLite**-bindningar för det planerade databaslagret; fortfarande ett inbyggt tillägg – återuppbyggt efter **Electron**/**Node** bumps som dokumenterats i **README**). **AGENTER**, **`fantasia-sqlite-main`** och **`electron-main`** stubbkommentaren hänvisar nu till **`bättre-sqlite3`**.- Omdöpta Storybook-relaterade **Yarn**-skript under **`test:storybook:*`**: **`yarn test:storybook:visual`** (med **`:headed`**, **`:update`**, **`:update:headed`**) ersätter **`yarn visual`:storybook; **`garntest:sagobok:rök`** ersätter **`garnsagobok:rök`**. **`.storybook-workspace`** skriptnycklar matchar. Uppdaterade **README**, **AGENTER**, markörregler/färdigheter och plananteckningar.
- Dela hela projektets port: **`yarn testbatch:ensure:nochange`** körs **`testbatch:verify`**, produktion **Electron** build, **Playwright** komponent + **E2E**, **`yarn test:storybook:smoke`** och **`storyarn:visual. **`yarn testbatch:ensure:change`** är densamma genom rök, sedan **`yarn test:storybook:visual:update`** för avsiktlig uppdatering av baslinjen. Tog bort **`garn testbatch:ensure`**. Uppdaterade **README**, **AGENTER**, **testing-terminal-isolation**, **fantasia-testing**, **fantasia-dev-setup**, **fantasia-release-build** och **changelog-en-us** vägledning.- Tog bort **GitHub Actions** Storybook visual-regression-jobbet från **`.github/workflows/verify.yml`** och bytte namn på arbetsflödet till **Verify** (push/PR kör endast **`yarn testbatch:verify`**; **`.storybook-workspace`** installationen behölls för ESL`**). Storybook VRT förblir **lokal** eller via **`yarn testbatch:ensure:nochange`**. Uppdaterade **README**, **AGENTER**, **testing-terminal-isolation**, **fantasia-testing**, **fantasia-dev-setup** och **fantasia-changelog-en-us** (utelämna-listans formulering).- **GitHub Åtgärder** arbetsflöden för pull-begäran **pr-full-suite-nochange** och **pr-full-suite-change** kräver exakt en lagringsetikett på PR, **novisualchange** eller **visualchange** (se **`.github/PULL_REQUEST_TEMPLATE.md`**). **novisualchange** kör **`yarn testbatch:ensure:nochange`** på **Windows** så att **Storybook** skärmdumpsnamn förblir i linje med **chromium-win32** baslinjer; **visualchange** kör **`yarn testbatch:ensure:change`** och kan pusha uppdaterade filer under **`.storybook-workspace/visual-tests/`** när PR-grenen bor i detta arkiv (fork PRs kör fortfarande tester men kan inte ta emot den pushen). PRs som har båda etiketterna eller inte misslyckas med **PR full svit (säker inte ändra)** bevakar tills de är fixade; **se till att byta**-vakten lämnar framgången i så fall så samma misstag ger inte två identiska röda checkar. En enda idempotent botkommentar förklarar regeln (PRs förblir öppna så att en etikett kan läggas till efter öppnandet). **`.github/scripts/pr-label-guard.sh`** centraliserar etikettkontrollen för båda arbetsflödena.- **Contributor setup**: **`commitlint`** och **`husky`** validerar **`git commit`** meddelanden lokalt (**`commitlint.config.mjs`**, **`.husky/commit-msg`**, **`yarn install`** körs **`prepare`**). Tillåtna typer matchar **AGENTER** (**feat**, **fix**, **test**, **syssla**, **refaktor**, **stil**, **dokument**); **`Sammanfoga …`** och **`Återställ …`** rader ignoreras. **README** och **AGENTER** beskriver krokar; **`.cursor/rules/git-conventional-commits.mdc`** dokumenterar samma policy.
- **GitHub Actions** **OSV-Scanner** arbetsflöde (**`.github/workflows/osv-scanner.yml`**) anropar nu **`google/osv-scanner-action`** återanvändbara arbetsflöden vid **v2.3.5** så **`scan-pr`** drar inte längre ut v**-`artaction utskriven**-`upp` (GitHub misslyckades med dessa körningar på **Setup job**).
- Ställ in **`actions/upload-artifact@v5`** i det manuella **Build**-arbetsflödet (**`.github/workflows/build.yml`**) för **Linux**, **macOS** och **Windows** utgivningsartefakter, tillsammans med **`actions/checkout@v5`s** och` artefaktuppladdningar stannar på **GitHub Actions**-körtider som stöds.- Bumpade **Vue** till **`^3.5.32`** (root och **`.storybook-workspace`**), **`@types/node`** till **`^22.19.17`**, uppdaterade **root** och **`.storybook-workspace`**-filer med __`, RT och uppgradering **T__** **`toHaveScreenshot`** **`maxDiffPixels: 150`** så jämför körningar förblir stabila under ombyggnader. Uppdaterade en **Win32** ögonblicksbild av krom som påverkades av verktygskedjan.
- Döpte om markdown-punktlisthjälpen till **`mdListArrayConverter`** (standardstavning) och normaliserade loopvariabelnamn i **`specialCharacterFixer`**; **Tips, tricks och trivia**-aviseringar och släckning av markdown-dokument är oförändrade i beteende.
- Polerad **en-US** kopia i **Advanced Search Guide**, **Tips, tricks & trivia** och **en-US** appnamnsträngen för grammatik, stavning och tydlighet; fixade en saknad artikel i rubriken **README** Quasar CLI.- Flyttade alla **`*.stories.ts`**-filer under matchningsfunktionen **`tests/`**-trädet (**`src/components/**/tests`**, **`src/layouts/**/tests`**, **`src/pages/**/tests`**) och pekade **`.ma.story-book` i glosboken. stigar så att **Storybook** ligger bredvid **Vitest** och **Playwright**; uppdaterade **Storybook** VRT-ögonblicksbilder (inklusive **DialogAppSettings** standard och **DialogMarkdownDocument** förhandsvisningar).
- Påtvingade utökade flerradiga objektliteraler i hela projektet via **ESLint** (**`object-curly-newline`**, **`object-property-newline`**) för **JavaScript**, **TypeScript**, **Vue** och **JSON**, med **`tråden`s repetition .**o repetition:es.**
- Extraherade små fillokala gränssnitt och typalias från källorna **Vue** och **TypeScript** till samlokaliserade **`<filnamn>.types.ts`**-moduler där tillämpligt (renderarstart, Pinia-typningar, **Electron**-tester, **Storybook**-tester, **Storybook** AD-konventioner** och registrerade hån** **AGENTER** och markörregler/färdigheter.- Utökad **Stylelint** med **`stylelint-order`** så att **CSS anpassade egenskaper** och vanliga **deklarationer** förblir i **alfabetisk** ordning (autofix via **`yarn lint:stylelint:fix`**); breddad **`yarn lint:stylelint`** för att skanna **`vue`**, **`css`**, **`scss`** och **`sass`** under **`src/`** och under **`.storybook-workspace/.storybook/`** (bygg utdata under **`storybook**s ignored-static/).`
- **Electron Playwright** kör bifoga **WebM** skärminspelningar per test; öppna **`test-results/playwright-report/index.html`** efter **`yarn test:components`** eller **`yarn test:e2e`** (eller **`:single`**-skripten) för att granska videor under varje tests **Bilagor** (filer under **`test-right-**data)/w. Varje Playwright körning **ersätter** den HTML-rapporten; **`scripts/playwrightWithArtifactTrim.mjs`** tar bort **`test-results/playwright-artifacts`** efter dessa garnkommandon så att repor inte behålls mellan körningarna. Dokumenterat i **README**, **AGENTER** och Cursor **fantasi-test** / **dramatiker-test** vägledning; valfritt **`FA_PLAYWRIGHT_NO_VIDEO`** hoppar över inspelning.
- Tog bort ett bortsprunget **HTML** **textområde** från sidmallen **Index**.

## 2.4.9 - Contributor-verktyg, Pinia-dialoger och CI-härdning

### Bugfixar och optimeringar
- Tog bort **`yarn visual:storybook:test:local`** och **`yarn visual:storybook:update:local`** (och matchande **`.storybook-workspace`** **`storybook:visual:*:local`**-skript). För build + headed compare eller uppdatering av ögonblicksbild, använd **`yarn test:storybook:visual:headed`** / **`yarn test:storybook:visual:update:headed`**; för att köra Playwright endast när **`storybook-static/`** redan är aktuell, använd **`yarn --cwd .storybook-workspace test:storybook:visual`** eller **`test:storybook:visual:headed`** / **`test:storybook:visual:update`** / **`:update` Uppdaterad **README**.
- Fixat **Pinia 3** dialogflöde: **`S_DialogMarkdown`** och **`S_DialogComponent`** är nu standardinstallationsbutiker (ring **`S_DialogMarkdown()`** / **`S_DialogComponent()`** på användningsplatser). **`openDialogMarkdownDocument`**, **`DialogMarkdownDocument`** och **`DialogAboutFantasiaArchive`** använder bevakade övervakare när en butik inte är aktiv (t.ex. **Storybook**). **`ComponentTesting`** hanterar saknade **`componentName`**-rutter på ett säkert sätt och renderar selen först efter att en matchande SFC har lösts.- Förenklade förhandsgranskningssidan för **Index** genom att ta bort det extra **Fantasia Mascot**-blocket från standardrutten **`IndexPage`** (behåller rutten som ett lätt skal för lokala länkar/testning).
- Omdöpta och grupperade **Garn**-bidragsskript för tydlighetens skull: kvalitetsportar **`yarn testbatch:verify`** / **`yarn testbatch:ensure`**; lint **`yarn lint:eslint`**, **`yarn lint:typescript`**, **`yarn lint:stylelint`**; Quasar Electron **`yarn quasar:build:electron`** / **`yarn quasar:dev:electron`**; Playwright **`yarn test:components`** och kolon-suffixed **`test:components:**`**, **`test:e2e:**`**; Storybook dev **`yarn storybook:run`** och statisk **`yarn storybook:build`** (tidigare **`build-storybook`**). Ta bort föråldrat **`garntest:full`** och duplicera **`yarn visual:storybook:ci`**; **GitHub Actions** ring **`yarn visual:storybook:test`** direkt. Uppdaterade **README**, **AGENTER**, markörregler/färdigheter, **`package.json`**, **`testRunner_*.mjs`** och arbetsflöden.- Samlokaliserad Storybook **statisk utdata** (**`.storybook-workspace/storybook-static/`**), **Playwright** visual-regression config, **`visual-tests/`** (inklusive engagerade ögonblicksbilder), arbetsyta **`storybook:visual:*`encies** scripts,` depends,`-** **`@playwright/test`**) under **`.storybook-workspace/`**; root **`yarn visual:storybook:*`** delegater med **`yarn --cwd .storybook-workspace`**. Uppdaterade sökvägar **`.gitignore`**, **`tsconfig.json`** och **`lint:eslint`**.
- Ställ in explicita minst-privilegier **`behörigheter`** (`content: read`) på **`verify`** och manuella **`build`** **GitHub Actions** arbetsflöden; lagt till **Dependency Review**, **OSV Scanner** (återanvändbara arbetsflöden + veckoschema på **`master`**), och **Gitleaks** för PR/push-hygien.

## 2.4.8 - Pinia 3 och Vue Router 5

### Bugfixar och optimeringar
- Uppdaterade **Sass** till **1.99.x** under **`^1.78.0`** i **root** och **`.storybook-workspace`** låsfiler så att skrivbordsappen och **Storybook** använder samma lösta Dart Sass-linje för stilar.
- Uppgraderad **Pinia** till **v3** och **Vue Router** till **v5** för skrivbordsappen och **`.storybook-workspace`** (explicit **`vue-router`** där för **`StoryRouterShell`**); lade inte till valfritt **`@pinia/colada`**. Uppdaterade **root** och **`.storybook-workspace`** låsfiler med **`yarn upgrade`** inom befintliga semver-intervall (**Storybook** förblir fäst vid **10.3.4** på den raden).
- Uppgraderat **`uuid`** till **v13** för markdown- och komponentdialog-UUID (**`S_Dialog`**) och **`@quasar/cli`** till **v3** för **`quasar`** CLI som används med **`@quasar/app-vite`** v2.
- Uppgraderad **Electron** till **41.x** (från **33.x**); **`electron-builder`** körs fortfarande **`@electron/rebuild`** så **`sqlite3`** matchar den medföljande körtiden. **`@electron/remote`** **2.1.x** och den kända peer-varningen för **`electron`** är oförändrade.- Standardiserad import av endast typ över kodbasen TypeScript och Vue ("importtyp") för att anpassa sig till strikt linting och hålla runtime-paket fria från importbrus av endast typ.
- Lade till ett nytt "yarn secure"-kommando för validering av hela projektet (`verify` + produktionsuppbyggnad + Playwright-komponent + Playwright E2E) och anpassad vägledning för bidragsgivare i **README**, **AGENTS** och markörregler/färdigheter för att tydligt dokumentera arbetsflödet för snabbporten kontra fullgrind.
- Reducerat icke-åtgärdsbart Vue-varningsljud i komponent Vitest-utgång genom att filtrera kända Quasar-upplösningar och varningar för insprutning endast för test i delad testkonfiguration, vilket håller signalen fokuserad på åtgärdsbara fel.
- Fast CI TypeScript-upplösning för Vue SFC-importer genom att ersätta den genererade `.quasar` shim-referensen i `src/env.d.ts` med en in-repo `declare module '*.vue'`-deklaration, så att `yarn verify` lyckas på rena löpare/__TK0-ringar utan re.__ filer.

## 2.4.7 - Storybook 10 och Vite 8 justering

### Bugfixar och optimeringar
- Uppgraderat **`.storybook-workspace`** till **Storybook 10** och **Vite 8** så att komponentförhandsgranskningar körs på samma stora paketeringslinje som skrivbordsappen (**`@quasar/app-vite`**), uppdaterade den kapslade **Yarn**-låsfilen för reproducerbara installationer (inklusive frysta **T** och frysta 0-filer) / **`eslint-plugin-storybook`** versioner med den utgivningsfamiljen.
- Uppdaterade **root** och **`.storybook-workspace`** låsfiler med **`yarn upgrade`** inom befintliga semver-intervall (särskilt **Quasar**, **Vue**, **vue-i18n**, **Playwright**, **Sass** och relaterade verktyg); behöll **`@types/node`** på **22.x** typings-raden för **Node 22**-motorn. Uppdaterade **README** och **AGENTER** så att bidragsgivare se **Storybook 10** på **Vite 8** istället för den gamla split-Vite-formuleringen.
- Lade till **`debug-storybook.log*`** till **`.gitignore`** så att lokala Storybook diagnostiska loggar inte plockas upp som ospårade filer.

## 2.4.6 - Fönster krom, Storybook, Stylelint och DevTools

### Bugfixar och optimeringar
- Tillämpade **`getCurrentWindow()`** från **`@electron/remote`** till **window chrome** brohjälpare (**minimera**, **maximera**, **ändra storlek**, **stäng**, **maximerat tillstånd**) så kontrollerna förblir knutna till appfönstret när OS-fokus flyttas till menyer eller andra ytor.
- Justerad **`.storybook-workspace`** med huvudappen på **Vue 3.5**, **vue-i18n 11**, **Quasar 2.16+**, **Pinia 2.3+** och **@quasar/extras 1.17**; tog bort stray **`package-lock.json`** där så endast **Yarn** lockfiler används.
- Uppdaterade **root** och **`.storybook-workspace`** låsfiler med **`yarn upgrade`** inom befintliga semver-intervall; fäste rot **Storybook** tillägg till **8.6.18** så att de matchar **`@storybook/vue3-vite`** och den kapslade arbetsytan (särskilt nyare **Playwright**, **Sass** och andra kompatibla verktyg).
- Migrerade **`src/css/app.scss`** och **`globals/*.scss`** till Sass **`@use`** med explicita **`quasar.variables.scss`**-importer i partiella delar, och ersatte utfasade **`@import`**-kedjor för 3 appägda globala stilar av Darts.- Storybooks Vite **`additionalData`** föregår **`quasar.variables.scss`** för projekt **`.vue` / `.scss`** (via **`file://`** URL-adresser så att Windows-sökvägar med blanksteg löser sig). För **`quasar/src/css/index.sass`** använder den **`@import`** (inte **`@use`**) före Quasars egna importer – samma effektiva ordning som **`@quasar/vite-plugin`s `sassVariables`** transformera – så **`veefed theme-default your palettes and framework to entries CSS variabler (**`--q-dark`**, **`--q-primary`** och relaterade tokens) matchar skrivbordsappen; andra **`node_modules`** Sass förblir orörda.
- Utökade root-**Stylelint**-inställningen med **`@stylistic/stylelint-config`** så stilistiska regler försvann från **Stylelint 17**s kärna och fortsätter att tillämpa välbekant CSS/SCSS-formatering; uppdaterade **`.vscode`** rekommendationer och inställningar så att Stylelint-tillägget kan använda projektkonfigurationen, **fixa vid spara** och formatera fristående **`.scss`**-filer konsekvent.- Dirigerad **DevTools** kontroll (**växla**, **öppen**, **stäng** och **status**) genom små **huvudprocess IPC**-hanterare istället för **`@electron/remote`** **`getCurrentWindow()`**, så **Hjälp → Växla utvecklarverktyg**** förblir tillförlitlig i T__d-paketet**** förblir tillförlitlig; **E2E**-kontrollen väntar nu tills DevTools avslutar bifogningen istället för att hävda direkt efter att menyn stängs.
- Uppdaterade **GitHub Actions** på **`verify`** och manuella **build** arbetsflöden (**`actions/checkout`**, **`actions/setup-node`**, **`actions/upload-artifact`**) till aktuella huvudversioner.
- Noterade i **`quasar.config.ts`** att **`@electron/remote`**s **`electron`** peer förväntas visas som ouppfyllt under UnPackaged **`yarn installation --production`**; paketeringen lyckas fortfarande eftersom appen körs på den medföljande körtiden Electron.- Storybook förhandsgranskning registrerar Quasars **`Dark`** plugin, **`@quasar/quasar-ui-qmarkdown`** appplugin (efter Quasar, matchande startordning), **Roboto (latin-ext)** och **`preview-body.D`ark)`** / **`tru **`body--dark`** förblir i linje med appen; Sass **`additionalData`** hoppar över **`app.scss`** för att undvika dubbelladdning av variabler. **Dialog** och **toppmeny** komponent Dokument använder **`parameters.docs.story.inline: false`** igen så Autodocs monterar inte varje berättelse i ett dokument (varje **`q-dialog`** öppnas automatiskt vid montering, vilket staplade modaler när **`inline: true`**); varje kapslad Dokument-iframe laddar fortfarande samma förhandsgranskningsstapel CSS.

## 2.4.5 - Bidragsgivares arbetsflöde och CI

### Bugfixar och optimeringar
- Lade till **`yarn verify`** för att köra ESLint, TypeScript (`tsc`), Stylelint och enhetstester i ett kommando (som heter **`verify`** eftersom Yarn 1 reserverar **`yarn check`** för beroendeverifiering); dokumenterade det i README och dev-inställningsguiden, anpassade markörregler/färdigheter och körde det i manualen GitHub Actions build workflow före produktionen Electron build.
- Fixade den kombinerade enhet-sedan-Playwright-hjälparen så att den kör enhetstester och sedan Playwright över komponent- och E2E-vägar (tog bort den trasiga "samtidigt"-anropet och släppte det oanvända **`samtidigt`**-beroendet).
- Rensade den franska ändringsloggen för språk, så release notes bibehålls inte per språk förrän översättningarna kommer tillbaka.
- Uppgraderat **`@intlify/unplugin-vue-i18n`** till v11 för Quasar/Vite locale pipeline, **`uuid`** till v11 (inbyggda TypeScript-typer; borttagna **`@types/uuid`**), och **`sqlite3`-processen ** till v11 för huvudlinjen __process.
- Lade till ett **GitHub Actions**-arbetsflöde som kör **`yarn verify`** på **push** och **pull request** till **`master`** / **`main`** (inklusive installation av Storybook-arbetsytan så att ESLint kan lösa `.storybook-workspace/.storybook`).- Uppdaterade **root** och **`.storybook-workspace`** låsfiler med **`yarn upgrade`** inom befintliga semver-intervall (särskilt nyare **Playwright**, **Quasar**, **Vue**, **vue-i18n**, **Sass** och relaterade verktyg).
- Gjorde **DevTools** brohjälpare att använda **`getCurrentWindow()`** istället för **`BrowserWindow.getFocusedWindow()`** så att **växla / status** förbli knuten till appfönstret när menyer eller DevTools ändrar OS-fokus, vilket stabiliserar DevTools kontrollen.**E2

## 2.4.4 - Beroenden och utvecklingsverktyg

### Bugfixar och optimeringar
- Uppdaterade beroendelåsfilen med `yarn upgrade` under de befintliga semver-intervallen så att installationer löser sig till de senaste kompatibla utgåvorna (inklusive Quasar, Vue, `vue-i18n`, Sass, Playwright och Vite-verktygen som dras in av Quasar).
- Uppgraderat `jsdom` till v29 för Vitest DOM-miljön, `eslint-plugin-n` till v17, och uppdaterade `@types/node` på Node 22 typings-raden.
- Migrerade lint och typer verktyg till **ESLint 9** flat config (**neostandard**, **eslint-plugin-vue** v10, **typescript-eslint**), **TypeScript** 6 och **Stylelint** 17 med uppdaterade delbara konfigurationer; små källjusteringar matchar strängare standardvärden (importattribut, unionsavstånd, webbadressskydd för utvecklarfönster, QMarkdown CSS shim).
- Justerade **AGENTER**, markörregler och färdigheter så att standardarbetsflödet för pre-commit och pre-changelog alltid körs **ESLint**, **TypeScript (`tsc`)**, **Stylelint** och **enhetstester** i den ordningen; **kvalitetsporten** kan köras som ett kedjat terminalkommando för effektivitet, med individuella kommandon reserverade för felsökningsfel. **Bygg** och **Playwright** kontroller förblir ett kommando per terminal.

## 2.4.3 - Ytterligare beroendeuppdateringar och förbättrat testarbetsflöde

### Bugfixar och optimeringar
- Uppgraderade "vitest" till v4 och justerade Electron huvudprocessenhetstest-mockar till konstruktorkompatibla implementeringar så att "BrowserWindow", "Menu" och "MenuItem"-mocking förblir stabila under den nyare löparen.
- Uppdaterade ytterligare lågriskberoenden (`sqlite3`, `lodash-es`, `stylelint`, `stylelint-config-standard-scss` och `eslint-plugin-promise`) och omvaliderade hela lint/test/build pipeline efter uppdateringarna.
- Lagt till beständig agent/testvägledning för att köra varje valideringskommando i en isolerad terminalanrop för att hålla långa testutgångar läsbara och lättare att granska.
- Justerade ändringsloggblocket `2.4.3` med den kanoniska versionen av `package.json` och stärkte vägledning för bidragsgivare så att ändringar i ändringsloggar alltid läser om live `package.json` först, för att undvika sektions-/versionsdrift.

## 2.4.2 - Uppdatering av kärnberoende och Quasar/Vite-arbetsflödesvalidering

### Bugfixar och optimeringar
- Uppgraderade `vue-i18n` till v11 och tog bort äldre TypeScript-undertryckning i i18n-bootstrapping nu när nuvarande skrivningar kompileras rent.
- Uppdaterade kärnapp- och verktygsberoenden (inklusive Quasar extras/qmarkdown, Axios, Pinia, Vue Router, Vitest och utvalda ESLint-plugin-program) för att minska underhållsrisken samtidigt som man håller sig inom den nuvarande projektarkitekturen.
- Tillämpade en uppföljningsverktygsuppdatering för Quasar/Vite-arbetsflödet (`@quasar/app-vite`, `@intlify/unplugin-vue-i18n`, `vite-plugin-checker`, `eslint-plugin-vue` och `@types/node`) med full lint, component build, E, component build, validation.

## 2.4.1 - Ändringsloggpolicylås och komponentteststabilitet

### Bugfixar och optimeringar
- Fixat Playwright komponenttestupptäckt för `DialogAboutFantasiaArchive` genom att undvika fullständig import av i18n-lastare som drar nedmarkeringsdokument till nodsidans testladdning.
- Uppdaterade dialogens sociala knapppåståenden för att jämföra renderade etiketter, matcha aktuell UI-utgång och hålla komponenttester stabila.
- Låst projektvägledning för att **aldrig auto-bump-versioner**; ändringsloggsektioner följer nu strikt den befintliga `package.json`-versionen om inte en manuell ändring uttryckligen begärs.

## 2.4.0 - Quasar CLI Vite v2 och Node 22 verktygskedja

### Bugfixar och optimeringar
- Uppgraderat verktygskedjan för skrivbordsappar till `@quasar/app-vite` v2 (Vite 8), ESM `quasar.config.ts`, TypeScript 5.6 och ESLint-feedback via `vite-plugin-checker` under utveckling/build.
- Standardiserad bidragsgivare och CI Node.js på **22.22.0 eller nyare** (krävs av nuvarande Quasar CLI), uppdaterade GitHub Actions `setup-node` och dokumenterade förväntningar på inbyggd modul ombyggnad för `sqlite3` efter Electron./Node bumps.
- Ersatte QMarkdown-apptillägget med direkt `@quasar/quasar-ui-qmarkdown`-registrering, Vite-native `*.md?raw`-lokalimporter och Vue mall `isPreTag`-hantering så att markdown-dialoger förblir stabila utan tilläggets Vite-plugin.
- Migrerade vue-i18n-buntning till `@intlify/unplugin-vue-i18n`, stötte Electron och elektronbyggare för ESM-huvudprocessutdata och anpassade Sass till Storybook-delprojektet; Storybook finns kvar på Vite 6 tills dess stödda peer-sortiment inkluderar Vite 8. *(Ersatt i **2.4.7**: Storybook körs nu på **Vite 8** med **`@quasar/app-vite`**.)*
- Stöt **`@electron/remote`** till 2.1.x så att Electron 33 behåller en fungerande förbelastning (undviker borttagna Chromium-funktionssonder som tidigare bröt förbelastningsbelastningen och brygg-API:erna).- Fixad **Playwright komponent-testning** routing: `ComponentTesting` globs nu `../components/**/*.vue` från `src/pages`, så produktion Electron körs montera den begärda SFC istället för en tom sele.
- Höjde **`@typescript-eslint`** till v8 för TypeScript 5.6, lade till **`yarn lint:types`** (fullprojektet `tsc`), och utökad AGENTS/Cursor-vägledning för ESLint och typkontrollportar (TSLint används inte).
- Minskad bullriga **DevTools Autofyll** CDP stderr-linjer under utveckling genom att filtrera de specifika meddelandena i Electron huvudprocessen (endast ofarliga protokollfelmatchningar).
- Härdade renderarkodsökvägar som anropar **`window.faContentBridgeAPIs`** när förladdningsbryggan saknas, och justerad Vitest **`vi.stubEnv`** användning med Vitest 3 booleska regler för `DEV` / `PROD` / `SSR`.
- Avstämd **Quasar/elektronbyggare**-konfiguration för striktare skrivningar och paketering: PWA `workboxMode`-hölje (`GenerateSW`), tomt **`bex`** blockmatchande ström Quasar-typer, Linux **`desktop.entry`** för elektronbyggare __TK1, plus ____ (`env.d.ts` Vue shim referens, vue-i18n augmentation hantering) så `lint:types` förblir tillförlitlig.- Adresserade **Dart Sass 2** utfasningsbrus i delad SCSS via `sass:color` i Quasar variabler och rullningsliststilar (ersätter äldre färghjälpmedel).
- **Installationspåminnelse:** **`electron`**-paketet behöver sin **efterinstallation** nedladdning; att hoppa över livscykelskript (till exempel `npm install --ignore-scripts`) kan lämna en trasig binär tills du installerar om eller kör `node node_modules/electron/install.js`.

## 2.3.2 - Storybook layouter, sidor och bidragsgivares verktyg

### Nya funktioner
- Lade till enbart Storybook-förhandsvisningar för huvudlayouter och representativa appsidor med en delad router i minnet (`StoryRouterShell`), en enda `STORYBOOK_APP_ROUTES`-tabell och samlokaliserade `src/layouts/**`- och `src/pages/**`-berättelser.
- Kopplade en Storybook-endast Vite-transform som skriver om `import.meta.globEager` till ivrig `import.meta.glob` så att komponenttestsidan laddas under Storybook-arbetsytans verktygskedja.
- Utökad Storybook i18n hånar så att sidan som inte hittades visar riktig kopia istället för råa översättningsnycklar; förenklade index- och enkomponentberättelseetiketter (borttog den redundanta förhandsgranskningen av endast index).
- Dokumenterad layout och förväntningar på sidan Storybook i `AGENTS.md` och markörregler/färdigheter, inklusive förhandsgranskningar av enbart canvas och ändringsloggordning tillsammans med komponentberättelser.

### Bugfixar och optimeringar
- Spetsad rot ESLint vid avsiktliga käll- och konfigurationsvägar istället för hela förvaret; lagt till `yarn lint:style` med Stylelint för `src` Vue och SCSS plus en root Stylelint-konfiguration.
- Aktiverade TypeScript `skipLibCheck` och exkluderade byggartefakter, Storybook statisk utdata, Vitest-komponentkonfigurationen och samlokaliserade `*.stories.ts` från rotprojektet `tsconfig` för renare kontroller.
- Tog bort ett tomt stilblock med omfattning från "SocialContactButtons" som kom till ytan genom stillinting.
- Uppdaterad Vitest och Playwright täckning (extern länkhanterare, maskotbild, fönsterkontroller) för att matcha aktuellt beteende och väljare.

## 2.3.1 - Electron förpackningsikon och favicon täckning

### Bugfixar och optimeringar
- Tillagd explicit Electron Builder-ikonkonfiguration för Windows, macOS och Linux, inklusive Linux metadata för skrivbordsinmatning och Linux målinställning.
- Länkade alla genererade favicon-storlekar i `index.html` så renderarikondeklarationer täcker hela `public/icons/`-uppsättningen.
- Uppdaterad ändringslogg-markdown-formulering för att undvika analysfel vid byggtid i qmarkdown/Vite-pipelinen under Electron-produktionsbyggen.

## 2.3.0 - Storybook arbetsyta och skrivbordspolering

### Nya funktioner
- Flyttade Storybook till ett kapslat `.storybook-workspace`-paket med sin egen verktygskedja, tog bort rotträdet `.storybook` och uppdaterade samlokaliserade berättelser (menyer, dialogrutor, maskot, fönsterkontroller och sociala knappar) för stabila tillägg, `public/`-tillgångar och författararbetsflöden.
- Avstämda globala Quasar variabla tokens och delade komponent SCSS justeringar för mer konsekvent fönsterkrom, kontroller och markdown/dialogytor.
- Lade till Fantasia maskotbildetikettsträngar för en-USA och tyska språk för att stödja tillgänglig namngivning.
- Dokumenterade Storybook, Electron `file://` och Playwright återskapar förväntningar för bidragsgivare i `AGENTS.md`, `README.md` och Cursor färdigheter/regler (inklusive en dedikerad Storybook berättelseregel).

### Bugfixar och optimeringar
- Fixade ikoner för sociala kontaktknappar som inte laddas i paketerade Electron-byggen genom att lösa offentliga bildwebbadresser med en relativ bas när appbasens URL är "/" eller tom, istället för rotrelativa URL:er under file://.
- Uppdaterad Playwright komponenttäckning för fönsterkontroller för att matcha aktuella markeringar och tillgänglighetsetiketter.

## 2.2.1 - Typnamn och konsistenssvep

### Nya funktioner
- Tillagd explicit komponentteststyrningsvägledning över AGENTER/regler/färdigheter: varje `src/components/**` Vue-komponent har en samlokaliserad `*.vitest.test.ts`-svit, med paritet som behandlas som täckningsnärvaro (inte uttömmande gren-/linjeprocentsatser).
- Standardiserad fixturplaceringspolicy för framtida testförfattande: Vitest och Playwright fixturer förblir inline i sina respektive testfiler, medan `_data/` förblir enbart för produktion.
- Omstrukturerade delade meny- och dialog-/dokumenttypdeklarationer till tydligare singular- och samlingsnamn samtidigt som prefixkonventionerna `I_` / `T_` bevaras.
- Lade till återanvändbara menyalternativ och byggstenar av undermenytyp för att göra menykonfigurationen lättare att underhålla.
- Utökad Vitest-täckning på renderersidan under `src/` för skript och lagringstillståndsövergångar med deterministiska hån.
- Lade till en spårad renderingsteststrategiinventering under `.cursor/plans/` för att kartlägga `src/` TS/Vue-mål över Vitest och Playwright ansvarsområden.
- Dela upp Vitest i en nod/kärnkonfiguration och en jsdom-komponentkonfiguration med delad inställning så att `yarn test:unit` täcker boot, i18n, scripts, stores och colocated smoke-tests för varje `src/components/*.vue`.- Samlokaliserade "AppControlSingleMenu" och "SocialContactSingleButton" som komponenter på toppnivå (sökvägar och importer uppdaterade).
- Dirigerad initial navigering genom en dedikerad `appStartupRouting`-hjälpare som konsumeras från `App.vue`.

### Bugfixar och optimeringar
- Åtgärdade trigger callback-typfel i hjälpmenydata genom att linda in typade hanterare i noll-argument callbacks kompatibla med generiska menytriggersignaturer.
- Normaliserade kommentarer och formuleringar som är benägna att skriva fel i komponenttester, E2E-tester och utvalda källfiler för konsekvent namngivning och grammatik.
- Uppdaterade exempel på README-testkommandon för att matcha faktiska skriptnamn och användningsmönster i `package.json`.
- Separerade Playwright artefaktutdata från HTML-rapportmappen i `playwright.config.ts` för att ta bort varningar för reporterns utdatakatalog.
- Omjusterade testvägledning för att behålla "garntest:enhet" som baslinjens enhetstestarbetsflöde i projektdokument och markörvägledningsfiler.
- Förtydligade AGENTER/regler/färdigheter vägledning så Vitest behandlar uttryckligen både `src/` och `src-electron/` som förstklassiga enhetstestningsytor samtidigt som Playwright behålls som integrations-/körtidsskikt.
- Förtydligade testdataregler: automatiserade testfixturer förblir inline i varje `*.vitest.test.ts` / `*.playwright.test.ts`, komponenttestande nyttolaster använder `COMPONENT_PROPS` där det är möjligt, och inbäddad komponentlägesmenydata förblir inline i `AppControl-triggermenus`.- Lösta ESLint-fynd i boot extern-länk Vitest hånar ('Event' lyssnare typning och vadderade block).

## 2.2.0 - Testning och agentverktyg

### Nya funktioner
- Utökad Vitest-täckning för Electron förinlästa bryggmoduler och huvudprocessskript (fönsterkontroller, utvecklingsverktyg, externa länkar, apphantering, stavningskontroll och skapande av huvudfönster) med deterministiska hån.
- Playwright komponenttester för DialogAboutFantasiaArchive (ersätter den tidigare TODO-platshållarfilen).
- Markörregel och färdighet för lokala plandokument i `.cursor/plans/` (gitignored), plus ändringsloggguide för att läsa versionsmatchande planfiler för releasesammanhang.

### Bugfixar och optimeringar
- Ersatte återstående TypeScript `alla`-anteckningar i delade typdeklarationer med `okänd` för att hålla menyutlösare och Vue-komponentskrivning striktare.
- Lagt till explicit anti-`any`-vägledning till Vue, TypeScript skript, Vitest och Playwright regler/färdigheter för att hålla framtida kod starkt skriven.

## 2.1.0 - Verktygs- och AI-stödd utveckling

### Nya funktioner
- Markörprojektregler och färdigheter för Vue/Quasar, BEM och scoped SCSS, Playwright och Vitest, Electron preload, global SCSS, konventionella Git-commits och en-US changelog underhåll kopplat till appversionen.
- AGENTS.md som projektstartpunkt för AI-assisterad utveckling.

## 2.0.0 - The Big Rewrite

### Nya funktioner
- En hel uppsjö eftersom det här är en fullständig omskrivning!

### Kända problem
- Inga problem vid releasedatumet.

### Bugfixar och optimeringar
- För många optimeringar att lista eftersom detta är en omskrivning.
- Inga buggar vid releasedatumet.